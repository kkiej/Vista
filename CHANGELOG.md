# Changelog

本文件同时充当技术 Breakdown 的时间线：每个模块落地时记录**参考来源**、**做的取舍**、
**踩的坑**、**性能数据**。作品集评审看的是这些，不是 feature 列表。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added

- package 骨架：`Vista.Runtime` / `Vista.Editor` asmdef，
  基于 `IRenderPipelineResources` 的 shader 资源容器（零手动赋值）。
- **大气 Transmittance LUT**（256×64，R16G16B16A16_SFloat，静态）。
  - 参考：Hillaire, *A Scalable and Production Ready Sky and Atmosphere Rendering Technique*,
    EGSR 2020；参数化沿用 Bruneton & Neyret 2008 / Bruneton 2017。
  - 介质模型：Rayleigh 指数剖面（标高 8 km，无吸收）+ Mie 指数剖面（标高 1.2 km，HG g=0.8）
    + 臭氧三角帐篷剖面（中心 25 km，半宽 15 km）。
  - Editor 自检工具 `Window/Vista/Validate Atmosphere LUTs`：读回三个闭式解已知的纹素比对。
    地面·天顶 (0.93799, 0.86523, 0.75928) vs 闭式解 (0.94040, 0.86766, 0.76235)，
    最大误差 0.00307；大气顶·天顶 = (1,1,1)；地面·切线 = (0.10681, 0.00961, 0.00005)。
- **共享 raymarch 积分器** `ShaderLibrary/AtmosphereScattering.hlsl`。
  MS LUT / SkyView LUT / AerialPerspective LUT / 体积雾 共用一个
  `VistaIntegrateScatteredLuminance`，差异全部收进 `VistaRaymarchSettings`。
  动机：单次散射的能量计算若在多处各写一遍，天空与雾会在地平线交界处出现一条缝，
  而这类不一致极难定位。
- **大气 Multi-Scattering LUT**（32×32，R16G16B16A16_SFloat，静态）。
  - 每个纹素一个线程组、组内 64 线程各跑一个球面方向（Marsaglia 均匀球面采样），
    groupshared 归约后用等比级数 `Σ = L₂ / (1 − f_ms)` 一次求出无限阶。
  - 自检：全表有限非负（最大 0.06525，量级符合各向同性相位 1/4π ≈ 0.0796）、
    沿 muSun 严格单调递增（回退 0）、太阳当顶·地面 (0.05191, 0.05573, 0.06464) 偏蓝、
    太阳在地平线下归零。
- **大气 Sky-View LUT**（192×108，R16G16B16A16_SFloat，逐帧）+ **物理天空盒**。
  - 参数化：uv.y ↔ viewZenithCos 以**地平线为分界**双段平方 warp（张角随相机高度变化，
    不是常量 π/2）；uv.x ↔ lightViewCos 平方 warp，uv.x=0 正对太阳。
  - 自检新增 15 项：正反映射 round-trip 最大 uv 误差 3.76e-4（阈值 = 半个纹素 2.6e-3）、
    两个方向的 warp 严格单调（回升 0）、四个端点 +1/−1/+1/−1。
  - 绝对亮度校验：正午天顶 (943.5, 1893, 4184) cd/m² 偏蓝、地平线比天顶亮
    12053 vs 1857、地平线被 Mie 洗白（B/R 1.19 vs 天顶 4.43）；
    **天底辐射对闭式解 `albedo·E☉·sin(elev)·T^(1/sin elev)/π` 偏差 0.4%**
    —— 这一项同时验证了绝对光度单位与曝光链路。
  - 日落（太阳 −2°）：日侧地平线 (52800, 14800, 1875) 红移 R>2B、
    Mie 前向峰日侧/背侧 21946 vs 2240、背侧中高空转蓝紫 (1031, 1259, 1692)、
    天顶比正午暗 5.5 倍。全表峰值 54304 cd/m²，未逼近 fp16 上限 65504。
    （以上数值是修完 `VistaEarthShadow` 偏置方向后的基线，比修之前整体高 6~9%，
    见「坑」里那一条；天顶值不变，因为天顶视线不经过贴地那 10 m。）
- **`VistaAtmospherePass` / `VistaAtmosphereFeature`**：RenderGraph-only 的逐帧调度，
  三张表拆成三个 compute pass（原因见「坑」）。静态表只在参数变化的帧排入。
- **`Window/Vista/Setup Sky (Material + Renderer Feature)`**：一键生成天空盒材质、
  挂到 `RenderSettings.skybox`、并把 feature 装进当前生效管线的所有 Renderer。
- **Aerial-Perspective froxel LUT**（32×32×32，R16G16B16A16_SFloat ×2，逐帧）。
  - 散射表存**累积散射亮度**、透射表存**彩色透射率**，两张分开而不是打包进一张：
    合成时是 `color * T + S`，两者的数值范围与插值行为都不同。
    散射表的 alpha 通道另存 `mean(T.rgb)` 的灰度透射率，供移动端一次采样路径用。
  - 深度切片支持 Log 与 Power(k) 两种分布，运行时可切（Task #6 的 A-B 输入）。
  - 一次行进、逐切片输出：每个线程负责一根 froxel 柱，沿视线只走一遍，
    在每个切片边界把当前累积量写出。步长 `clamp(ceil(segLen/0.25), 2, 16)`。
  - 自检新增 22 项。有限性/单调性/打包一致性：散射沿切片非减（回退 0）、
    透射率沿切片非增（回升 0）、`scatter.a` vs `mean(T.rgb)` 最大偏差 4.07e-4、
    峰值 8816 cd/m² 未逼近 fp16 上限。
  - **参数化 round-trip**：`w → 距离 → w` 最大 |Δw| 1.49e-7（Log）/ 1.19e-7（Power），
    `w = i/(depth−1)` 最大误差 4.57e-4，两端点与 `nearDistanceKm` / `maxDistanceKm` 一致，
    `texW` 半片对齐 0.01563 / 0.98438。
  - **对 256 步参考解的两项误差**，分别测两件不同的事（见下方「取舍」）：
    | 分布 | errCenter（行进循环） | errMid（切片分布） |
    | --- | --- | --- |
    | Log(near 20 m) | 1.19% @ 0.020 km | 0.31% @ 13.9 km |
    | Power(k=2) | 0.30% @ 0.133 km | 0.15% @ 23.4 km |
  - **区间/连续性回归探针**（不判定，只报数）：`tBottom`、`tTop`、相机海拔、`up·ray`、
    `earthShadow` 在 3 m 与 1 km 处的取值、以及线性探针 `ref(0.04)/ref(0.02) = 2.022`。
    这三组数是定位下方那个 24% 偏差的工具，留在自检里当回归网。
  - 灰度透射率近似的代价（移动端分级用）：中心柱最大通道偏差
    **3.8 km 6.1%，12.4 km 22.1%，32 km 71.2%**。
- **天空环境光 SH**（L2 / SH9，`StructuredBuffer<float4>` × 9，逐帧）。
  接管 Unity 的环境光链路，取代 `AmbientMode.Skybox`（后者会把太阳圆盘一起卷进去，
  见「坑」里那条 8 倍偏亮）。
  - 投影：从 **SkyView LUT** 采 **1024 个 Fibonacci 均匀球面方向**，
    一个线程组 64 线程跨步取样 + groupshared 树形归约，权重 `4π/N`。
    存的是**原始辐射亮度矩** `L_i`，余弦卷积留给消费端（理由见「取舍」）。
  - 两个出口，一份系数：**GPU** 侧 `_VistaSkyAmbientSh` 全局 buffer（零延迟，
    供 Step 3 雾与 Step 4 PRT relight 用）；**CPU** 侧 `AsyncGPUReadback` 27 个 float
    → `SphericalHarmonicsL2` + `AmbientMode.Custom`（容忍 2~3 帧延迟）。
  - `Runtime/Core/VistaSphericalHarmonics.cs`：基函数常数、Unity 槽位缩放表
    `k_RadianceToUnitySh`、以及带拒绝条件（长度不足 / DC 全非正 / 非有限值）的
    `TryConvertMomentsToProbe`。运行时与自检共用同一份，不许各抄一遍。
  - Editor 自检 `Window/Vista/Validate Ambient SH`，两节：
    - **Unity `SphericalHarmonicsL2` 约定标定**（3 项）。逐槽位置 1 实测 `Evaluate`
      的权重 `k_i` = 3.54491, 2.04665×3, 0.91529, 0.91529, 3.17066, 0.91529, 1.83058，
      `|k_i·Ŷ_i − 1|` 最大 **6.32e-8** → `Evaluate` 用的是**未归一化多项式基**
      `{1, y, z, x, xy, yz, 3z²−1, xz, x²−y²}`；`AddAmbientLight(white)` 的
      `c_0 = 1.000000` 且各向偏离 1 为 **0** → `Evaluate` 的语义是 albedo=1 的出射亮度
      （辐照度/π）；均匀天空 `L=1` 过缩放表回到 `c_0 = 1.000000`。
    - **SH 投影 vs 参考解**（4096 样本数值积分，8 个测试法线 + 2 个全天球均值组）。
      | | 正午 60° | 日落 3° |
      | --- | --- | --- |
      | `L_00` | (20247, 21893, 25385) | (2681.3, 2165.8, 2187.9) |
      | `c_0` | (5711.6, 6175.8, 7161.0) | (756.38, 610.95, 617.20) |
      | 均值恒等式偏差（GPU / CPU） | 1.36e-7 / 1.36e-7 | 8.29e-8 / 8.29e-8 |
      | GPU 重建 vs CPU 重建 | 8.15e-8 | 1.23e-7 |
      | 逐法线最大偏差（= L2 截断） | **2.17%**（天顶） | **31.25%**（朝下） |
      | 求积收敛 1024 vs 4096 | 0.01% | 2.75% |
      判定只用前三行（与截断无关），第四行在正午档判 5%、日落档只记录 ——
      理由展开在「取舍」里，那是这一节真正的设计内容。
  - Editor 诊断 `Window/Vista/Log Ambient Probe State`：不算任何东西，只报运行期
    实际状态（`ambientMode`、`c_0`、上/下/水平三向重建、场景脏标记）。
    与上面那个自检互补 —— 自检走立即模式验数学，这个验"RenderGraph 那条 pass 真的跑了、
    读回真的把系数灌进了 `RenderSettings`"。两者可以一绿一红，而只看自检报告看不出来。
    Demo 场景实测：`Custom`、`c_0 = (5120, 5551, 6458)`、
    上 (1444, 2721, 5400) / 下 (8163, 7383, 6234) / 水平 (5153, 5626, 6513)。

- **天空镜面反射 cubemap（#5b）**：64²、7 级 mip 的 Cube RenderTexture，
  挂到 `RenderSettings.customReflectionTexture`，于是 URP 的
  `GlossyEnvironmentReflection` 把它当 `unity_SpecCube0` 采 —— **不改任何材质 shader**。
  这是选"产出一张真 cubemap"而不是"塞一个自定义全局 + 改 shader"的全部理由。
  - `SkyReflection.compute` 的 `SkyReflectionFilter`：**每级 mip 直接从 SkyView LUT
    做 GGX 预积分**（Karis 2013 的 `PrefilterEnvMap` 累加，N = V = R，权重 NdotL）。
    七级各一趟 dispatch，级间零依赖。
  - 移动端同一个核，辐射来源换成 #5a 的那份 SH（`VISTA_SKY_REFLECTION_SRC_SH`），
    采样数降到 16。**仍然产出一张真 cubemap**，所以 `unity_SpecCube0` 这条路
    在两个平台上完全一致 —— 消费端不需要知道自己采的是哪一条路径来的图。
  - `mip → 感知粗糙度` 用 URP `PerceptualRoughnessToMipmapLevel` 的**解析反函数**
    `pr = (1.7 − sqrt(2.89 − 2.8·m/6)) / 1.4`，不是线性映射。
    实测七级：0 / 0.1024 / 0.2151 / **0.3424** / 0.4917 / 0.6814 / 1.0 ——
    mip3 是 0.342 而不是朴素线性的 0.5。
  - Editor 自检 `Window/Vista/Validate Sky Reflection`，四条判据全在 GPU 上算
    （C# 侧刻意不重算任何一条：重算就得再抄一遍 mip 映射与面方向约定，
    抄错的那一份与 shader 那份走歧时，报出来的偏差既不是 0 也不是明显错误）。
    Demo 场景实测：

      | | 正午 60° | 日落 3° |
      | --- | --- | --- |
      | ① 逐面 mip0 vs LUT（六面最大） | **0.00%** | **0.04%** |
      | ① 参与比较的取样点 | 64/面，空面 0/6 | 64/面，空面 0/6 |
      | ② cube 整球均值 | (5709.6, 6172.4, 7155.4) | (723.7, 602.5, 615.9) |
      | ② LUT 同方向均值 | (5711.6, 6175.8, 7161.0) | (756.4, 611.0, 617.2) |
      | ② SH 的 `L_00·Y00` | (5711.6, 6175.8, 7161.0) | (756.4, 611.0, 617.2) |
      | ② cube 离散化偏差 | **0.078%**（阈 0.5%） | **4.315%**（阈 6%） |
      | ② 跨模块偏差（LUT vs SH） | **1.36e-7** | **1.61e-7** |
      | ③ mip round-trip 最大 | 3.58e-7 | — |
      | ③ HLSL 常量 SIZE/MIPS/LOD_STEPS | 64 / 7 / 6 | — |
      | ④a wide 归约 vs 顺序（逐 mip 最大） | **6.8e-7**（mip4） | **7.2e-7**（mip6） |
      | ④a 权重列最大 / K↔分界对账 | 5.4e-7 / 一致 | 5.1e-7 / 一致 |
      | ④b wide mip2 硬件 round-trip | **0.09%**（阈 1%） | **0.08%** |
      | ④b 参与比较的内部纹素 | 96/96 | 96/96 |

    判据 ② 的第三列与 #5a 的 `c_0` 逐位一致（正午 (5711.6, 6175.8, 7161.0)），
    这不是巧合而是设计：两边用**同一个** 1024 点 Fibonacci 方向集
    （`VISTA_SKY_SH_SAMPLES` == `VISTA_SKY_REFL_VERIFY_MEAN_SAMPLES`），
    所以均值恒等式在有限样本下也精确成立 —— 1.36e-7 的偏差只来自两边归约顺序不同。
  - Editor 诊断 `Window/Vista/Log Sky Reflection State`：报运行期链路的实际状态。
    Demo 场景实测 `defaultReflectionMode = Custom`、
    `VistaSkyReflectionCube_64x64_Mips_R16G16B16A16_SFloat_Cube`、64²、7 级 mip、
    `filter = Trilinear`、**`unity_SpecCube0_HDR = (1, 1, 0, 0)`**、`isDirty = False`。
  - `SkyReflectionFilterWide`：第二个滤波核，64 线程协作算**一个**纹素
    （一条 lane 领若干样本 → 6 步树形归约 → lane 0 写出）。粗糙那几级每组只有一条
    线程跑几百次取样，是占用率灾难；这个核把并行度从"纹素数"换成"纹素数 × K"。
    形状分界 `K ≥ 64`（= wide 核线程数，每条 lane 至少一个样本），
    在 LUT 模式下即 mip ≥ 2；mip1（K=32）与 SH 模式（K=16 恒定）留在 narrow。
    归一化必须在归约**之后**做，不是逐 lane 做。
    `Hammersley2d(i, K)` 只依赖 `i` 与 `K`，所以按 lane 跨步取 `i` 得到的是
    **完全相同的样本集** —— 这是"改形状不改质量"这件事可判的前提。

- **LUT 链路耗时计时器（模型 B）**：`Window/Vista/Profile Atmosphere LUTs`。
  Edit 模式立即提交、N=200 次背靠背摊销、M=5 轮取**最小值**，报逐 pass / 整链 /
  逐 mip 归因 / 反射的「只绑不派」CPU 对照 / 三条计算式证据。
  RTX 3060 + D3D11 实测（固定开销 0.161 ms 已从每项扣除）：

    | | 改形状之前 | 现在 |
    | --- | --- | --- |
    | 稳态五 pass 整链 | 0.494 ms（±3%） | **0.170~0.198 ms（±2~4%）** ／阈 0.300 |
    | 全七 pass 整链 | — | 0.213 ms（±4%） |
    | 其中 Sky Reflection | 0.391 ms（79%） | 0.076~0.101 ms ⚠不可引用 |
    | 反射「只绑不派」 | 0.000 ms | 0.001 ms → CPU 占 ~1% |
    | 静态两表的净代价 | — | 0.044 ms |

  逐 mip 归因（`mipMask` 旋钮，只派某一级）是这套工具的主要产出：
  改形状**之前** mip3~6 合计吃掉 85% 的时间却只产出 510/32766 个纹素（1.6%），
  吞吐从 mip0 的 2.96 掉到 mip6 的 0.02 G样本/s，每次循环迭代暴露 295 ns ——
  正好是一次纹理取样往返，**延迟完全没被隐藏**。病因由此定为占用率而非取样总量，
  wide 核是照这个结论开的方。改完之后同一份报告给出 32 ns/迭代（延迟已藏住）、
  per-dispatch 地板占 59%（瓶颈换人了）。

- **帧内延迟交叉验证（模型 A）**：`Window/Vista/Cross-Check LUT Timing (Play Mode)`。
  自动进 play 模式、丢前 45 帧、对七个 pass 名各起一对 `ProfilerRecorder`
  （GPU 一份 + CPU 一份）采 300 帧、逐帧取最小值，然后自动退出并打报告。
  刻意**不放 MonoBehaviour 进场景**：`EditorApplication.update` 在 play 模式里照样每
  tick 触发，读 recorder 不需要在渲染线程上 —— 量性能的工具改被量的对象是这类 harness
  最容易犯的错。跨域重载的"这次是为了采样"用 `SessionState` 存（静态字段会被清）。
  RTX 3060 + D3D11 实测（Scene View + Game View 各渲染一次，下表已按出现次数 2 折算成单次）：

    | pass | 模型 A 单次 | 模型 B | 备注 |
    | --- | --- | --- | --- |
    | Sky-View | 0.036 | 0.059 ±18% | |
    | Sky Ambient SH | 0.010 | 0.016 ±24% | |
    | Sky Reflection | **0.093（±9%）** | 0.076 ⚠±132% | 模型 A 反而可引用 |
    | Sky Reflection Copy | 0.031 | 0.030 ±17% | |
    | Aerial Perspective | 0.036 | 0.035 ±18% | |
    | **稳态五 pass** | **0.206（中位 0.212）** | 0.170~0.198 | 阈 0.300 → 达标 |
    | Transmittance / Multi-Scattering | 300 帧 **0 样本** | 0.044（净） | 脏标记生效 |

  只判**方向**不判差值：两个模型量的不是同一件事，差多少没有先验；能判的是
  "A 不应该比 B 小"（帧内延迟 ≥ 允许重叠的吞吐下界）。实测 A/B = 1.21，方向对，
  差额就是 pass 边界 / barrier / 无重叠的代价。若某项 A < B，报告把它判成
  **工具错而不是性能事实**（marker 被合并 / 被剪 / 相机数不同）——
  这个方向单独判，是因为它指向的修法完全不同。
  **必须一起给出去的限制**：RenderGraph 的逐 pass marker 被
  `#if DEVELOPMENT_BUILD || UNITY_EDITOR` 包着（core `RenderGraph.cs:2868-2884`），
  Release 构建里这些 marker 不存在 —— 这套数字是 Editor 口径，不是发行版性能。
  没有绕过的办法，所以写在报告正文里，而不是留给引用的人自己发现。

- **`VistaSkyBandingSelfTest`（Editor，`Window/Vista/Validate Sky-View Banding`）**：
  Sky-View 表的带状（banding）数值判定。配套 `AtmosphereLut.compute` 的
  `SkyViewBandingSignature` 核。

  **测的是"采样之后"，不是表里的值。** 核只调 `VistaSampleSkyViewLut` ——
  天空盒运行时用的那一个入口，同一个 `sampler_LinearClamp`、同一套 `ParamsToUv`。
  这一条是整套判据成立的前提：表里的数值可以完全单调、完全平滑，而
  「硬件双线性 + 两段分支的参数化 warp + fp16 存储」这三样叠起来仍然能在画面上
  产出可见台阶。若改成在核里自己解析积分一遍，被测对象整个跑掉，而且报告会
  一路全绿（解析积分当然平滑）。所以这个核绑 `_VistaSkyViewLut`（SRV），
  **不绑 RW 名字**。

  **方向固定在世界空间。** 太阳仰角是被扫的自变量；如果观察方向也随太阳转，
  逐仰角的差分里就同时混进了"表变了"与"我在看别的地方"两种贡献，无法分离。
  固定之后每个方向的亮度是仰角的一元函数，差分才有意义。
  64 个方向 = 4 个仰角环（2°/10°/30°/70°）× 16 个方位，方位 0 正对太阳。

  **判据不拍阈值，测二阶差分的收敛阶。** 拍阈值等于把结论写进判据。
  归一化二阶差分 `S₂(Δ) = max_j |L(j+1) − 2L(j) + L(j−1)| / max(L(j), 下限)`
  随采样步长 Δ 的阶 `p = log₂(S₂(2Δ)/S₂(Δ))` 把三种机制自己分开：

  | p | 机制 | 要不要修 |
  |---|---|---|
  | ≈2 | 被采函数在这个轴上光滑，S₂ 就是真实物理曲率 | 不用，分辨率不是瓶颈 |
  | ≈1 | 双线性重建在**纹素边界**的斜率跳变，`S₂ ≈ f''·h·Δ` | 不用，这是纹理过滤的固有行为；幅度随 Δ 线性缩小，可外推 |
  | ≈0 | 真的 C⁰ 跳变（参数化分支 / 量化） | 看幅度是否高于地板 |

  中间那一档（p≈1）是这次设计里最容易漏掉的：只分"光滑 vs 台阶"两态的话，
  **每一次普通的过滤纹理采样都会被判成缺陷**。

  三条判据回答三个不同的问题：

  - **判据 A（时间轴）**：太阳 0°→90°、步长 0.5°，**181 次重烘**，每次采那 64 个方向。
    一次扫描按 stride 1/2/4 抽稀就得到 Δ = 0.5°/1°/2° 三条曲线 —— 少烘三分之二的表，
    而且三条严格同源，排除了"两次烘焙参数不一致"这种伪信号。
  - **判据 B（空间轴）**：96×54 / 192×108 / 384×216 各烘一次，同一个太阳（3° 日落档）、
    同一组方向。双线性插值误差是 O(h²)，所以
    `e_hi = |L(h) − L(h/2)| = C·h²·(1 − 1/4) = 0.75·E(h)` ⇒ **E(192×108) ≈ 1.333·e_hi**
    （Richardson 外推）。`e_lo/e_hi ≈ 4` 是这个外推能不能用的**门槛**，不是附赠信息：
    比值明显不到 4 就说明 O(h²) 前提不成立，那个系数随之失效，报告要判**不可引用**。
  - **判据 C（地板）**：在纹素内部走弧，步长逐档减半 0.005°→0.000625°。
    二阶差分里能出现的只有三类项：边界斜率跳变 ∝Δ¹、函数自身曲率 ∝Δ²、算术噪声 ∝Δ⁰。
    前两类都随 Δ 单调缩小，所以一直缩，**幅度停止下降的那个平台就是地板**。

  实测（RTX 3060 / D3D11，相机 (0,2,0)，192×108）：

  | 判据 | 关键数 | 阶 | 结论 |
  |---|---|---|---|
  | C 地板 | 1.241e-3 → 8.577e-4 → 1.068e-3 → **1.067e-3** | 0.53 / −0.32 / **0.00** | 平台确认；理论最坏 fp16 地板 4·2⁻¹¹ = 1.950e-3，实测/理论 = 0.55 |
  | A 时间轴 | 最坏 S₂(0.5°) = 3.563e-2 @ 仰角 2°/方位 180°（反日点，G 通道）；外推到 0.01°/帧 → **1.124e-3** | 0.88（64 方向中位 1.70） | 达标（阈 1e-2） |
  | B 空间轴 | e_lo 4.574e-3、e_hi 1.414e-3、比值 3.24 ⇒ **E(192×108) ≈ 1.885e-3** | 1.69（中位 2.05） | 达标，误差/阈 = 0.19 |

  **可见阈取 Weber 对比 1%**（大面积平滑渐变上的经典可觉差）。这是整套判据里
  **唯一**一个外部常数，取自感知阈，不是取自"跑出来的数字附近"。

  **A 为什么能用 0.5° 量却按 0.01° 判。** 生产环境的时间轴步长是 20 分钟一个昼夜循环、
  60 fps → 每帧 0.005°，取 0.01° 留一倍余量。直接在 0.01° 上量的话信号整个埋进 fp16
  地板里、什么都测不出来；所以用 0.5° 把信号顶出来，再按**实测**阶外推
  `ampProd = amp·(0.01/0.5)^p`。这一步是判据 A 真正的落地点 —— 阶不是学术装饰，
  它是外推系数本身。

  **实测暴露的两件事，都写进报告而不是藏起来：**
  1. A 的 Δ=2° 档幅度（6.057e-2）**反而小于** Δ=1°（6.573e-2）—— 粗档只有 46 个点，
     把极值整个跨过去了。所以阶只取 1°→0.5°，2° 档降级为旁证，报告打 ⚠ 说明
     "三点幂律不成立不代表信号异常"。
  2. A 外推后的 1.124e-3 与 C 的地板 1.067e-3 **基本重合**。这比"低于可见阈"是更强的
     结论：在生产步长下，时间轴上的台阶已经和 fp16 量化噪声不可区分，
     这条轴上**没有可再优化的余量**。反过来说，当年截图上看到的那圈极淡等值线，
     数值上确实是量化 —— 但 B 那一项（1.885e-3）也只在地板的 1.8 倍上，
     两者同一量级，所以"是量化不是分辨率"这个说法其实分不开；能说的是两项都看不见。

- **两条引擎出口的曝光级 + 场景全局状态的保存/还原/复位**
  （`VistaSkyAmbientProbe.Publish`、`VistaAtmospherePass.BindReflectionToRenderSettings`、
  `IVistaRenderSettingsClient` + `VistaRenderSettingsGuard`、
  `Window/Vista/Reset Scene Ambient & Reflection`）。
  起因是一句用户报告：「我的场景里的物体怎么没有阴影了」。阴影其实一直是对的
  —— 详细归因见「坑」的第一条。
  - 曝光在**交接点**乘，不进 SH 投影核、也不烘进 cubemap：GPU 侧
    `_VistaSkyAmbientSh` 与反射 cubemap 一律留在**绝对光度量**（cd/m²），
    因为自检的参考解、Step 3 的雾、Step 4 的 PRT relight 全部按绝对量对账；
    折进核里会让那些判据跟着 EV100 漂。
  - SH 那条按系数逐个乘（`SphericalHarmonicsL2` 反射查不到 `op_Multiply`，
    只有索引器，所以是 `[c, i]` 双层循环）；反射那条挂在
    `RenderSettings.reflectionIntensity` —— 那是引擎给 custom reflection 留的唯一乘子。
  - 三层生命周期：`Dispose`/`Teardown` 还原（挡"关掉 feature"）、
    `EditorSceneManager.sceneSaving` 前还原（挡"运行中 Ctrl+S 把 Custom 落盘"）、
    显式复位菜单（挡"基线本身就是脏的"，见「坑」）。每层挡的是不同的洞，
    少任何一层都会留下可落盘的路径。
  - 还原按**场景**校验（存 `Scene` 结构而不是 `scene.handle` —— Unity 6000.4 把
    handle 换成了 `SceneHandle`，到 int 的隐式转换已废弃）。拿 A 场景的原值去还原
    B 场景比不还原更糟：那是静默的数据破坏，而不还原至少留着一份可辨认的值。
  - 验收：Play 模式实测 `ambientMode = Custom`、
    `c_0 = (0.1302, 0.1412, 0.1643)` = `5119.606 × 2.5431e-5`（EV100=15），
    与绝对量口径精确对上；平行光 3.14159 对环境光 ~0.13，直接光是环境光的约 24 倍。
    守卫的端到端验证：实时状态 `defaultReflectionMode = Custom` +
    `reflectionIntensity = 2.54313e-5` + cubemap 已绑定时执行一次真实
    `SaveScene`，磁盘仍为 `m_AmbientMode: 0` / `m_DefaultReflectionMode: 0`。

- **AP 切片数 / 分布定档：误差-距离曲线 + 实测毫秒双侧判定**（Task #7）。
  自检里新增"切片预算扫描"：5 档切片数（16/24/32/48/64）× 4 种分布
  （Log near 20 m、Log near 100 m、Power k=2、Power k=3）共 20 组，
  每组在**两个视角**（贴地/太阳 60°，海拔 1 km/太阳 10° 落日）上各测一遍。
  - 三条判据，各自回答一个不同的问题：
    - `errCenter`（切片中心 vs 256 步参考解）—— **前置门**，阈 5%。它测的是行进循环
      本身准不准，与切片布在哪无关。它红了上面两条都没有意义，所以不是同级判据。
    - `errMid`（切片**中点**处 LUT 插值值 vs 参考解，相对该柱最远片的累积量）—— 阈 2%。
      中点是双线性插值误差最大的位置；分母取柱子总量而不是本片值，是因为本片值在近处
      趋于 0，相对误差会爆掉一个看不见的量。
    - `errMidT`（同上但相对**透射率自身**）—— 阈 1%。必须单列：最终像素是
      `几何·T + 入散射`，近处画面几乎全由 T 决定，而 T 的误差在"相对柱子总量"
      的口径下会被入散射的量级淹掉。
  - **不取聚合最大值，按距离分段取 max**：①脚下 <50 m ②近景 .05–.5 km
    ③中景 .5–4 km ④远景 4–32 km。分段的理由是全局 max 可被刷 —— 把切片全堆到远处，
    远景误差降下去，聚合 max 就"过了"，而脚下已经糊掉。段界按画面内容定，不按等距分。
  - **空段按绝对可见性豁免，不直接判失败**。某段一片都没有时（Log near 100 m 的①段），
    先查参考解在该段远端的累积入散射相对"日照下 albedo 0.3 漫反射面"
    （`0.3·120000/π ≈ 1.1e4 cd/m²`）以及遮挡 `1−T`，两者都 < 1% 才免检。
    分母取这个参考白而不是柱子总量：后者本身随切片布局变，那样又是一个能被布局刷的判据。
    实测①段只有 0.26~0.31% / 0.24~0.29%，全部免检 —— 即**晴空脚下本来就没有雾**。
  - **余量档要求两个视角同时满足阈值的一半**（`k_ApSelectMargin = 0.5`）。
    余量存在的唯一依据是视角敏感性，所以只在一个视角上核它是自相矛盾的：
    实测同一档（d=16 Log 20 m）在两个视角上 errMid④ 是 0.77% 与 0.91%、
    T④ 是 0.49% 与 0.46%，摆动 0.14 个百分点，与阈值本身同量级。
  - 成本侧（`Profile Atmosphere LUTs` 新增一节，RTX 3060 / D3D11 / Editor）：
    | 配置 | AP 单测 | 稳态整链 | 显存 | 最差段 errMid/errMidT（视角2） |
    |---|---|---|---|---|
    | d=16 Log 20 m | 0.027 ms | 0.183 ms | 256 KB | 0.91% / 0.46% |
    | d=16 Pow k=3  | 0.033 ms | 0.192 ms | 256 KB | 0.19% / 0.10% |
    | d=24 Log 20 m | 0.036 ms | 0.192 ms | 384 KB | 0.37% / 0.23% |
    | **d=32 Log 20 m（出厂）** | 0.040 ms | 0.195 ms | 512 KB | **0.21% / 0.14%** |
    | d=32 Pow k=3  | 0.041 ms | 0.196 ms | 512 KB | 0.04% / 0.05% |
    | d=64 Log 20 m | 0.063 ms | 0.218 ms | 1024 KB | 0.05% / 0.08% |
    切片数翻倍的单测比是 16→32 ×1.50、32→64 ×1.58（**亚线性**：柱内步数受
    `VISTA_AP_STEP_MAX_KM` 与 `VISTA_AP_STEPS_MAX` 双向夹，切片数不是步数的自变量）。
    同深度换分布 d=16 Log→Pow k=3 是 ×1.25，显存与切片数都不变。
  - **判定挂在"出厂档"上，不挂在"扫描里有没有配置能过"上**。后者永远会绿
    （总有一档够精细），改坏默认值也照样绿 —— 那是伪通过。现在出厂档在扫描行里标 ★，
    并单独判"合格 + 含余量"，且不在网格里就直接失败。
    同一批把原来的"定档 →"改名为"最省候选 →"：那一行算的是"合格线能压到多低"，
    不是"出厂发什么"，用旧名字等于把一条藏在代码里的偏好印成结论。
- **太阳位置** `VistaSolarPosition`（低精度算法，适用区间 1950–2050）。
  给定年月日 + 当地时钟 + 经纬度 + 时区，输出高度角/方位角/赤纬/时角/世界光向。
  - 参考：Meeus, *Astronomical Algorithms* 的低精度太阳位置；儒略日 → 平黄经 →
    真黄经 → 赤纬/赤经 → 时角 → 地平坐标。均时差走完整的中心差项，不是查表。
  - Editor 自检 `Window/Vista/Validate Solar Position`，六条判据都对**闭式已知量**：
    A 夏至赤纬 23.4351°（真值 23.44，误差 0.0049°）；
    B 北纬 23.44° 夏至正午过天顶得 90.0000°，且春分那天算出均时差 7.46 分钟；
    C 北极圈内极昼时最低高度角 −0.0014°（应 ≥ 0，误差在阈值内）；
    D 子午线对称性 —— 太阳时正午前后等时距的高度角应相等，一般日（8/18）残差
    0.0094→0.1198°，至日对照组残差 0.0002→0.0007°（低两个数量级，正是赤纬漂移的签名）；
    E 高度角/方位角 → 方向矢量的两条路径互校，432 组最大偏差 3.129e-7；
    F 参考海拔包线（见下）。
- **CPU 侧太阳透射率** `VistaSunTransmittance`：`AtmosphereDef.hlsl` 的
  `VistaSampleMedium` + `VistaComputeOpticalDepthToTopAtmosphereBoundary` 的逐行镜像
  （同为 40 段梯形积分），外加"绝对光度量 → URP `color × intensity`"的换算。
  - 对账自检 `Window/Vista/Validate Sun Transmittance`：CPU 走解析积分、GPU 走
    LUT + Bruneton 参数化 + 双线性插值，两条**完全不同**的路径逐通道对上才算两边都对。
  - fp16 地板实测 **5.047e-4**，与理论相对精度 2^-11 = 4.88e-4 同量级 ——
    这条把"LUT 插值误差"与"存储精度地板"分开，否则低太阳角的偏差会全被归给插值。
- **物理光单位接缝的闭式解 + 端到端实测。** URP 的直接光漫反射是
  `albedo · (color.linear · intensity) · NdotL`，**没有 1/π**（`LightingLambert`
  只有 `lightColor·NdotL`，`brdfData.diffuse` 是裸 albedo）。要与绝对光度量对齐就得
  `color · intensity = lux · T · exposure / π`。EV100=15、T=1 时闭式解 **0.97140**，
  `Window/Vista/Validate Light Seam` 渲一帧读回像素得 **0.97119**（偏差 0.0219%）。
  这条自检**驱动的是产品代码本身**（`ApplyLightParams` 因此从私有改成公开），
  不是自己抄一份写灯逻辑。
- **`VistaTimeOfDay`**（`[ExecuteAlways]`）：按时间与地点驱动平行光的朝向与光色。
  - 灯的回落链：`Sun` 字段 → 本对象上的 `Light` → `RenderSettings.sun`，
    每一级都要求 `type == Directional`。朝向与光色可分别关闭（UE 的
    Sun Position Calculator 也是可分离的）。
  - 大气参数从 `VistaAtmosphereFeature.current` 读，组件**不存副本**；取不到时
    仍驱动朝向、不动光色，并在面板上报警。
  - 写入门：朝向 0.01°（= 太阳视角直径 0.545° 的 1/50）、光色/强度相对 1e-4
    （= Weber 1% 的 1/100）。门存在的理由是编辑器下每帧写属性会把场景标脏。
- **`VistaTimeOfDayEditor`：报警 + 诊断面板。** 组件注释里承诺过"面板上明确报警"，
  但在此之前 `sunMissing` / `atmosphereMissing` 只是两个 public 属性 ——
  **一个只有代码能读到的报警等于没有报警**。面板把三层不可见换算（曝光、1/π 接缝、
  gamma 往返）的中间量全部摊开：透射率 T → 曝光后照度 → 求得的 color/intensity →
  **灯上实际的** color/intensity。最后两行与前面分开显示是刻意的：不一致就说明
  有别的东西在改这盏灯（另一个组件、Animator、Timeline 轨道）。
  另加"距太阳时正午 N 分钟"一行 —— 经度/时区填错是最常见的配置错误，
  面板直接给分钟数比让人自己按 15°/h 去算有用。
- **参考海拔的有效包线** `SolveReferenceAltitudeEnvelopeMeters`：
  "参考海拔填错多少米，画面上还看不出来"。判据是逐通道 Weber 1% **且**
  同通道 |ΔT| ≥ 1e-3（后者约等于"错了参考白的 0.1%"）。实测（Validate Solar Position 的 F2）：

  | 太阳仰角 | 参考 T (R,G,B) | 包线 Δh | 包线处最大相对 |
  |---|---|---|---|
  | 1° | (0.22636, 0.03929, 0.00110) | 41 m | 3.122% |
  | 3° | (0.41427, 0.13841, 0.01807) | 46 m | 2.053% |
  | 5° | (0.53938, 0.24726, 0.06280) | 51 m | 1.587% |
  | 15° | (0.78741, 0.58055, 0.35262) | 77 m | 0.998% |
  | 45° | (0.91429, 0.81559, 0.67827) | 193 m | 0.998% |
  | 90° | (0.93848, 0.86551, 0.75965) | 271 m | 0.998% |

  这张表同时是 Task #12（逐像素大气透射率）的立项依据，见「取舍」。
  求解器与面板读数**同一份实现**，F1 另有一条单调性判据（T 随海拔严格不降，
  6 仰角 × 100 步 × 3 通道）。
- **`Window/Vista/Validate TimeOfDay Diagnostics`**：面板那几条报警的可达性验收。
  报警是保护性代码，而它最常见的失效方式不是"报错"而是"**永远不报**" ——
  一条永不触发的 HelpBox 与一行注释等价。四组的详情见「坑」里"每条报警都要双向验"。
- **AP 合成：共享合成层 + 全屏合成 pass（变体 A）**。AP LUT 在此之前没有消费者，
  Step 1 的验收标准③（远山洗白）一直悬着。
  - `ShaderLibrary/AerialPerspectiveComposite.hlsl` 是**全项目唯一**一份合成实现：
    距离 `VistaApDistanceKm`、两个乘子 `VistaGetAerialPerspectiveTerms`、
    应用 `VistaApplyAerialPerspective`（= `color·T + S·exposure`）各只有一份。
    变体 A 与变体 B 调用的是同一个函数 —— 这样"A 与 B 必须逐像素一致"才是个有意义的判据，
    而不是两份实现互相担保。
  - `VistaAerialPerspectiveCompositePass` 排在 `AfterRenderingSkybox`：
    天空盒之后（它在不透明之后画且**不写深度**，更早合成既白做一遍、又没法用深度剔天空）、
    半透明之前（AP 用的是不透明深度）。这两个约束之间只剩这一个位置。
  - 深度**一定拿得到，没有兜底分支**：`ConfigureInput(Depth)` 把
    `earliestDepthReadEvent` 拉到 AfterRenderingSkybox，
    `CalculateDepthCopySchedule` 于是选 `DepthCopySchedule.AfterSkybox`，
    `CalculateSplitEventRange` 把"事件 ≥ earliestDepthReadEvent"的自定义 pass 排到拷贝之后。
    这条链与用户在 renderer 上选的 `CopyDepthMode` 无关，所以不写"深度无效就跳过"——
    那种分支只会把真正的失效藏起来。
  - `Window/Vista/Validate AP Composite Wiring`：**接线**自检，刻意不验任何数值
    （在 C# 里重算合成式就是造第二份真相，反射自检那里已经付过这个学费）。
    三条判据全绿：shader 无编译错误、Pass 序号 ↔ Pass 名逐字相符（乘/加互换不报任何错，
    只让远景雾偏暗）、`_VistaApSize = (32,32,32,0)` 且 `_VistaApConsumer.x = 0.0`（预期 0.0）。
  - 已知不覆盖：XR 单趟立体（`UNITY_MATRIX_I_VP` 不逐眼索引，全屏三角形也没 instancing）；
    `#pragma only_renderers` 不含 GLES，所以变体 A 的移动端目前只覆盖 Vulkan/Metal。
    两条都是明确记为**不支持**，不是假装支持。
- **`Window/Vista/Validate AP Composite (Numeric)`**：变体 A 的**数值**验收（Task #15 的①③）。
  在一个 GPU 上现搭的自带布景里量出 `out = in·T + S` 这条公式本身，而不是去量美术场景 ——
  美术场景一改，判据的读数就跟着漂，回归价值归零；而当前 Demo 场景的 Terrain 是禁用的，
  只有一块 10 m 的 Plane，"远山洗白"根本没有可观测的对象。
  - 布景：64×64 ARGBHalf、FOV 2°、相机海拔 200 m、远裁 45 km（**大于** `maxDistanceKm`，
    为的是把"钉在最后一片"也量进来）。同一距离上并排四片 Unlit 四边形：
    黑 / 白 / 中灰 / 黑，96 个距离按 5 m → 40 km 等比排布。
    用 Unlit 而不是 Lit：布景要的是一个**已知的入射值**，直接光一进来，
    被测量就变成"着色 × 合成"的乘积，解不出 T 和 S。
  - 黑白两列给两个方程解出逐通道 T 与 S（`T = (white_on−black_on)/(white_off−black_off)`，
    `S = black_on − black_off·T`），**分母用实测基线而不是假设白=1 黑=0** ——
    链路上若有别的增益，假设值会把它悄悄记到 T 的账上。第三列中灰是**独立**的预测靶：
    它没参与解方程，所以"用 T/S 预测中灰"是一条真判据（合成对底色必须是仿射的）。
    第四列复用第 0 列的材质、同距离，它与第 0 列的差值就是**这把尺子自己的串扰地板**。
  - 八条判据全绿。地板 = `max(串扰, 2×量化) = 9.760E-004`（串扰实测 2.441E-004，
    量化实测 0.000E+000，fp16 理论 4.880E-004；乘 2 是因为解 T 要做一次减法）——
    **阈值是量出来的，不是拍的**。
    ① 基线常数：白 (1,1,1) / 中灰 (0.1799,…) / 黑 (0,0,0)，随距离最大漂移 0。
    ② 信号非空：近端 5 m `T=(0.9995,0.9995,0.9994)`、`S≈0`；
    远端 40 km `T=(0.7433,0.5837,0.3181)`、`S=(0.0712,0.1301,0.2375)` ——
    最大消光 68.2%、蓝通道散射 0.2375，波长依赖的方向也对（蓝散得多、透得少）。
    ③ T ∈ [0,1] 且单调不增：越界 0/96、回弹 0/95。
    ④ S 单调不减：回退 0/95。
    ⑤ 仿射（预测中灰）：最大误差 1.555E-004 @ 11.694 km，超阈 0/96。
    ⑥ 切片数对折 32 → 256：最大偏差 1.343E-003（相对 0.15% @ 8.804 km），超阈 0/96 ——
    判的是"采样坐标与切片数解耦"，即距离→UVW 的映射与深度分布同源。
    ⑦ 天空像素**逐位**未被触碰：Off↔Fullscreen 整屏最大差 0.000E+000。
  - 报警是**注入故障验过的**，不是写上去的（详见「坑」的两条）：调换两趟混合顺序 →
    判据 ④ 报警 4/95、最大 3.418E-003 @ 30.1 km；去掉天空 `clip` → 判据 ⑦ 报警
    整屏最大差 7.385E-002。同一次注入还量出判据 ⑤ 的**盲区**：`(dst+S)·T = dst·T + S·T`
    仍然是 dst 的仿射函数，所以仿射判据抓不到顺序调换 ——
    这条盲区原先只是注释里的一句断言，现在是量出来的，也正是判据 ④ 不能删的理由。
  - 仍然欠着：判据②（变体 A 与 B 逐像素一致）要等 #14。性能项已完成，见下一节 ——
    并且**去掉了**原计划的"双源混合带宽 A/B"：Unity 里没有双源混合，那个对照组不存在。

- **`VistaGpuTimer`：Edit 模式 GPU 计时核，从 `VistaAtmosphereLutProfiler` 里提出来。**
  不是新写的，是把那份 profiler 里已经存在的计时纪律搬到一个能被复用的地方 ——
  AP 合成的性能项要用**同一把尺子**量一个光栅 pass。
  提取的理由是具体的：fence 能力位不可信（`supportsGraphicsFence` 返回 true 而
  `fence.passed` 永不到）、等待不能带超时（探测与等待必须分开）、必须取多轮最小值、
  负值不能 clamp —— 这四条每一条都是事故换来的，复制一份会让两份纪律走歧，
  而走歧的表现是"两份报告的数字对不上"，没人看得出是哪份错。
  提取后重跑 LUT profiler：稳态五 pass **0.180 ms（max 0.185，±3%）**，
  落在提取前记录的 0.170~0.198 带内 —— 提取没有移动数字。
  - 顺手把 fence 的同步阶段从 `ComputeProcessing` 改成 `AllGPUOperations`
    （只等计算阶段会让 draw 还没跑完就判 passed）。**这一行在本机上未被执行过** ——
    RTX 3060 / D3D11 上 `ProbeFence` 一直不通过、整场退到 readback，
    而 `GetData` 本身就等全部 GPU 工作。所以这个改动目前只有推理支撑、没有实测支撑，
    已经写在那一行的注释里，免得下一个人以为它验过了。

- **`VistaApCompositePerf`：AP 全屏合成的性能测量（Task #15 验收项 ④）。**
  菜单 `Window/Vista/Profile AP Composite (Perf)`。
  - **量的不是一个 ms，是一条线 `ms = a + b·覆盖面积`。**
    a 是每个像素都付的（全屏三角形、采样深度、判天空、被 clip 的像素到此为止），
    b 是只有非天空像素才付的（两趟混合对颜色附件的读-改-写）。
    报一个 ms 等于把某一次构图的天空占比偷偷编进结论里，换个镜头就不成立；
    有了 a/b，任意构图的开销可以直接换算，不必再测一遍。
    这条线还把 shader 文件头那句"天空占屏比例高的构图省掉的是真带宽"变成**可判定的**：
    省下的正是 b·天空面积，而 b 是否超出噪声就是判据 ③ —— 若落在噪声里，那句注释该删。
  - 覆盖率是通过**被测 shader 自己的 `VISTA_AP_IS_SKY_DEPTH` 分支**起作用的：
    手填的 R32 深度里底部若干行是几何、其余是远平面。若哪天那个宏的方向改了，
    这里会立刻表现为覆盖率与耗时反相关，而不是悄悄测了个别的东西。
    几何深度按**对数**扫过 20 m → 30 km，因为 froxel 的切片分布也是对数的 ——
    线性扫描会让绝大多数行挤在最后几片上，测不到 3D 表的真实局部性。
    原始深度由 `GL.GetGPUProjectionMatrix` 反解，不手写 near/far 公式：
    反向 Z、平台差异、无穷远平面这三件事都已经折在那个矩阵里，手写就是第二份实现。
  - **四条判据全部由测量自身给出，没有一条是拍的。** 刻意不设"1080p 必须低于 X ms"
    这类门 —— 给一条产出数据设通过门，门自己就会变成结论。
    ① 线性（残差 ≤ 观测离散度）；② 斜率 b 的分辨率折叠；③ 覆盖项超出噪声；
    ④ 除以 N 之后与 N 无关。
  - **实测（RTX 3060 / D3D11 / readback / AP 表 32³ Log / RGBA16F）：**

    | 档位 | a (ms) | b (ms/Mpx) | a/全屏 (ms/Mpx) | 天空构图省下 |
    |---|---|---|---|---|
    |  720p | 0.0243 | 0.0247 | 0.0264 | 48.4% |
    | 1080p | 0.0556 | 0.0249 | 0.0268 | 48.2% |
    | 1440p | 0.1019 | 0.0231 | 0.0276 | 45.5% |
    | 2160p | 0.2209 | 0.0254 | 0.0266 | 48.8% |

    像素数跨 8 倍，b 的极差只有 9.6%、a/全屏 的极差 4.5%（判据 ② 的上界是入选档位
    最差可用点的离散度 16.0%）。线性残差 0.0008~0.0086 ms，全都在观测噪声的 1/5 以下。
    这两个系数同时折叠，是整套测量最强的一条自检 —— 布景、深度填法、基线扣除里
    任何一处出错，都很难同时在四个分辨率上错成同一对系数。
  - **结论：1080p 满覆盖 0.106~0.109 ms，天空占屏一半的实际构图约 0.08 ms。**
    天空 clip 省下的是 45~49%，也就是说 shader 头上那句话成立（判据 ③ 四档全过，
    覆盖项 0.023~0.211 ms vs 噪声 0.002~0.046 ms）。
  - 两趟拆分：仅乘 + 仅加 与两趟合测的差在 −0.003~+0.005 ms —— 两趟没有被串行化到
    出现可测的额外开销。深度归因（对数扫描 vs 常量深度）比值 0.99~1.02，
    也就是说 **≥720p 上 froxel 3D 表的缓存局部性量不到**。
    这一行的初版判定文本写着"> 1 说明缓存局部性是真实成本项"，那是预先注册结论 ——
    已改成只报比值。
  - 带宽地板（360 GB/s：覆盖像素 40 B/px → 0.1111 ms/Mpx，clip 像素 8 B/px →
    0.0222 ms/Mpx）**只报告、不判定**。实测 b 是覆盖地板的 0.21~0.23×，
    低于地板是合法的 —— 3060 有 3 MB L2，深度在两趟之间可能还在缓存里。
    把它当判据会把缓存命中报成"尺子坏了"，正是本项目记过的那个反模式。
  - 未覆盖并已声明：MSAA（改混合样本数）、XR 单趟立体、移动端 tile 架构；
    口径是吞吐不是帧内延迟，深度拷贝→合成这条边上的 barrier 要由模型 A 给。

- **模型 A（`VistaLutGpuRecorderCrossCheck`）接上 AP 合成，把上面那句"barrier 要由模型 A 给"落实。**
  菜单项改名 `Window/Vista/Cross-Check Atmosphere Timing (Play Mode)`（原来只说 LUT，
  现在还量一个光栅 pass）。
  - **合成必须自己一档（`k_CompositeIdx`），不能混进稳态五 pass。** 两条理由都是硬的：
    ① **不能除以出现次数。** 那一步的前提是"LUT 尺寸与相机分辨率无关，所以两次渲染
    做的是同样的工作量"。全屏光栅恰好违反它 —— Scene View 与 Game View 分辨率不同，
    和除以 2 得到的数**不对应任何一次真实渲染**。所以这一行只报原始和 + 出现次数，
    **没有「单次」**。
    ② **不能进那个和。** 那个和是拿去和模型 B 的 0.170~0.198 ms 对账的，
    而那是五个 compute 的口径。掺进一个随分辨率变化的项，和会随 Game View 大小漂移，
    对账区间不会 —— 对账从此失效。
  - **对账用包线，不用一个数。** 模型 B 给的是一条线 `ms = a + b·覆盖面积`，
    而覆盖面积（非天空像素占比）由**构图**决定 —— 模型 A 跑在真实场景上，
    那个占比不由测量方控制，也无法从 marker 里反推。能对的只有两端：
    全天空 = `a`、满覆盖 = `a + b·全屏面积`。
    每帧出现多次时**不做除法**，改成把每次渲染的面积逐个点名加起来再套包线 ——
    模型对面积是**线性**的，所以「两次渲染之和」的包线就是两个包线之和，
    不需要任何等工作量假设。这是替掉那个不合法除法的正解。
  - 系数 `k_ClippedMsPerMpx = 0.0266` / `k_CoveredMsPerMpx = 0.0245` 与
    `Bracket(megapixels, out floor, out ceil)` 放在 `VistaApCompositePerf` 上**共用**，
    没有抄进模型 A：同一个量有两份字面值的话，改了这边忘了那边，
    表现是"两份报告的结论互相矛盾"而没人知道该信哪份。
    `Bracket` 收**面积**而不是宽高，就是为了让那两个乘法只存在一处。
    换 GPU 必须重跑模型 B 重填这两个数 —— 只有 `ms = a + b·面积` 这个**形式**与机器无关。
  - 点名面积时 `renderScale` **只乘 Game View**，且与 1 相差 < 0.05 时吸成 1 ——
    照着 URP `UniversalRenderPipeline.cs:1453-1460` 的 `kRenderScaleThreshold` /
    `isScenePreviewOrReflectionCamera` 两条写的，不是推的（Scene View / Preview /
    Reflection 一律 1.0）。凭印象写会把缩放算两次。
  - **点名数与 marker 出现次数对不上就认输，不猜。** 少了 = 有视图没在渲染（被 tab 遮住），
    多了 = 还有别的相机（第二个 game 相机 / 预览窗口）。两种都会让面积和错，
    而错的面积和会给出一个**看起来很像结论**的数。
  - **归因是单向的。** 超出上界的部分可以归因 —— 覆盖率只能把值往下拉，
    所以超出量是 barrier / pass 边界 / 无重叠三样代价之和的**下界**。
    落**在**包线内**不能**拆成「天空占比」与「barrier 加价」两项：
    两者符号相反且都未知，报告里明写了这一点而不是给个像结论的分解。
  - 两端的前提**分别**判、分别说，不合成一个「前提不成立」一票否决：
    HDR 关 → 下界不成立（上界仍成立）；MSAA n× → 上界失效（下界仍成立，只会更宽松）。
    合成一个的话会把唯一还成立的那一端也挡掉。
  - **实测（RTX 3060 / D3D11 / 场景 Demo / 45 帧预热 + 300 帧采样，判定「交叉验证完成」）：**

    | pass | GPU min | 中位 | max | 每帧出现 | 单次 |
    |---|---|---|---|---|---|
    | Transmittance LUT | — | — | — | 0 样本 | 静态表缓存生效（期望） |
    | Multi-Scattering LUT | — | — | — | 0 样本 | 同上 |
    | Sky-View LUT | 0.083 | 0.086 | 0.121 | 2 | 0.041 |
    | Sky Ambient SH | 0.020 | 0.023 | 0.032 | 2 | 0.010 |
    | Sky Reflection | 0.177 | 0.205 | 0.280 | 2 | 0.089 |
    | Sky Reflection Copy | 0.066 | 0.067 | 0.092 | 2 | 0.033 |
    | Aerial Perspective LUT | 0.049 | 0.052 | 0.073 | 2 | 0.025 |
    | **AP 合成** | **0.043** | 0.047 | 0.069 | 2 | **不除**（全屏光栅） |

    稳态五 pass 之和：原始 0.395 ms（2 次渲染）→ 单次渲染 min 0.198 / 中位 0.216 ms，
    A/B = 1.16，**A ≥ B 方向符合预期**。
    合成包线：`Game 1100×592 + Scene 1100×602 = 1.313 Mpx` ⇒ **[0.035, 0.067] ms**，
    A = 0.043 ms ≥ 全天空下界 0.035（方向符合），落在上界的 64%。
  - 四次完整运行的跨轮稳定性：Sky-View 0.084/0.082/0.084/0.083；
    Reflection 0.200/0.176/0.201/0.177；Copy 0.066 平；合成 0.043/0.042/0.046/0.043；
    稳态单次渲染 0.210/0.198/0.198/0.198。
    一个不解释只记下来的离群点：AP LUT 在其中一轮读到 0.025 而另三轮约 0.050。

- **AP 合成：`Vista/Lit` 着色内合成（变体 B）**。变体 A 的全屏 pass 只能拿到深度重建的
  `positionWS`，且在延迟下才是唯一可行的路；变体 B 拿的是插值出来的 `positionWS`，
  并且是 Task #12（逐像素大气透射率）**唯一**可能的挂载点 —— 那一项要乘在直接光上，
  而全屏 pass 手上只有一个已经合成完的颜色。两条路都得存在，理由各自独立。
  - `ShaderLibrary/VistaLighting.hlsl`：`VistaComputeLighting` 是
    `UniversalFragmentPBR(InputData, SurfaceData)` 的**逐行拷贝**
    （对照 `Lighting.hlsl:282-373` @13e5115b98bf），停在累加，交出 `LightingData`。
    拷贝的理由不是"想改"而是"拿不到"：URP 内部已经把光照拆成
    `giColor / mainLightColor / additionalLightsColor / vertexLightingColor`
    ——**正是 #12 需要的那个拆分**—— 但它不把这个结构返回出来。
    拷贝的**边界**同样是明确的：末尾的合成仍然调 URP 的 `CalculateFinalColor`
    （`VistaResolveLighting`），所以本文件里没有一行是"URP 已经有、我又写了一遍"的**算术**，
    抄的全部是控制流（哪些分支在什么宏下成立、以什么顺序调）。
  - `surfaceData` 收 `inout` 而不是按值：17.4 的 `InitializeBRDFData` 实测没改它，
    两种收法今天等价；取 `inout` 是为了**将来它真改了的时候仍然等价** ——
    差异只会出现在开了 `_ALPHAPREMULTIPLY_ON` 的透明材质上，那正是最不容易注意到的地方。
  - `Shaders/Vista/VistaLit.shader`：Properties 与 ShadowCaster / DepthOnly /
    DepthNormals / Meta / MotionVectors 五个 pass 是 URP `Lit.shader` 原样拷贝，
    只有 ForwardLit 的片元不同。**少掉的 pass 各有理由，都写在文件头**：
    · GBuffer 不提供 —— 延迟的着色发生在 lighting pass 里，材质 shader 根本没有
    "着色结束"这个位置可以挂 AP。这不是"B 还没做完"，而是变体 A 必须存在的独立理由。
    · `UniversalForwardOnly` 不提供 —— `UniversalRenderer.cs:323` 明确写着与
    `UniversalForward` 同时声明是 ERROR，而 `DrawObjectsPass.cs:80` 的 ShaderTagId
    集合里两者都在（会画两遍）。
    · Universal2D / XRMotionVectors 记为**未覆盖**，不是默默省掉。
  - **去掉了 `Fog.hlsl` 的 `include_with_pragmas`**，省下 4× 变体。那个文件只做一件事：
    声明 `multi_compile_fog`。Vista/Lit **永不调 `MixFog`** —— URP 的雾与 AP 描述的是
    同一件事（视线上的大气），两者都乘一遍透射率、都加一遍散射，同时上就是远处过暗
    且颜色偏向 fog color。而且刻意**不**做"AP 关掉时退回 URP 雾"：那会让"关掉 AP"
    变成"换一种雾"，A/B 与反例测量全部失去意义。代价只有一个没人读的 fogCoord 插值器。
  - 透明材质**不合成 AP**（`VistaApplyApTail` 里 `#if !defined(_SURFACE_TYPE_TRANSPARENT)`）。
    不是偷懒：变体 A 挂在 `AfterRenderingSkybox`，半透明在它之后画，A 覆盖不到；
    B 若在这里给透明物上了 AP，"A 与 B 逐像素一致"这条验收标准会在每一个透明像素上
    **必然**失败 —— 而那不是 bug，是两条路的覆盖范围本来就不同。
  - **`Window/Vista/Validate Vista Lit Accumulation`：拷贝的存在前提。**
    没有这条自检就不该抄 —— URP 升级后函数体会变，人眼 diff 不可靠。
    做法是 `VISTA_LIT_DIFF_DEBUG` 变体在**同一次片元调用**里既算我的累加、
    又算 `UniversalFragmentPBR`，输出逐通道相对误差。同一次调用而不是画两遍：
    两遍画的话，两次的关键字集合 / 渲染状态 / 插值结果都得靠"应该一样"来保证，
    而这三样恰恰是最容易不一样的；同一次调用里两者共享同一份
    `inputData` / `surfaceData` / 寄存器，差异只可能来自累加本身。
  - 六个输出档（`_VistaDiffCtrl`）与哨兵：0 档相对误差（判定用）、1/2 档两侧原值、
    3 档写出**之前**做减法的分子、4 档分母、5 档已知常量。
    1/2 档不够、必须有 3 档：1/2 档把两个值**各自**量化成 half 再写出，
    小于一个 half ulp 的差异读回来必然是 0 —— 相减得到的是量化噪声，不是差异。
    3/4 档取的是那次除法的**两个操作数本身**，于是 CPU 侧能验 `0 档 == 3 档 / 4 档`
    这条恒等式，归因不成立时当场暴露。哨兵 30000 同时是 RT 的清屏色 ——
    **没被画到的像素同样是哨兵**，任何未覆盖的像素都不可能被读成"误差 0，一致"。
  - **实测（RTX 3060 / D3D11 / 128×128 ARGBHalf / 8 个关键字配置）：**
    · 判据 1：八个配置（不透明·无阴影 / +主光软阴影 / +两盏带阴影点光 /
    贴图特性全开 / 高光与反射关 / Specular 工作流 / AlphaTest / 透明+预乘 alpha）
    全部 `≤ 9.766E-006`，比对像素 16384/16384，哨兵 0。这个数是**尺子的地板**，
    报成上界而不是值。
    · 判据 1b（最坏像素归因，六档同组渲染）：1/2 档读回**逐位相同** `8.422852E-001`；
    3 档分子 `≤ 9.766E-010`、商 `≤ 1.159E-009` —— 比 Weber 1% 低七个数量级。
    结论口径是"两侧的差异小于可测下限"，不是"差异为 0"。
    · 判据 2（逐项故障注入 +2%）：`mainLightColor` 报出 1.895E-002、
    `additionalLightsColor` 1.982E-002、`giColor` 1.255E-002 —— 三项**有分辨力**；
    `vertexLightingColor` 报出地板值，判成**未覆盖**（本帧为 0 或占比 < 1%），
    并明写"它在判据 1 里的通过不可采信"。
  - 故障注入走 `_VistaDiffInject` 这个 **uniform 四元组**而不是四个关键字：
    正常输出恒 0，而"恒 0"有三种解释 —— 累加确实等价 / 被编译器折叠 /
    **根本没渲到被测像素**。只有注入已知量级的偏差并要求自检报出来，才能排除第三种。
    必须**逐项**注入，因为四项各由不同关键字组合决定是否活着；uniform 四元组
    一个变体就能分四次探到底。（顺带一条反直觉但正确的性质：若编译器把两条表达式
    折叠成一条、输出恒 0，那不是自检失效而是**最强的通过** —— 编译器只有在
    证明两者等价时才敢折叠。）
    声明进**具名** `CBUFFER_START(VistaDiffDebug)`：裸写在全局作用域会落进隐式
    `$Globals`，那会让整个 pass 被判成 SRP Batcher 不兼容；具名 cbuffer 里的全局量
    不影响兼容性，而 `Shader.SetGlobalVector` 照样写得进去。
  - 未覆盖清单（自检自己印出来，不当成通过）：UniversalGBuffer / 延迟（B 在延迟下
    不存在挂载点，那条路由 A 负责）；`USE_CLUSTER_LIGHT_LOOP`（要把 Rendering Path
    切到 Forward+ 再跑一遍）；`LIGHTMAP_ON` / `DIRLIGHTMAP_COMBINED` /
    `DYNAMICLIGHTMAP_ON` / `SHADOWS_SHADOWMASK`（布景是运行时搭的，拿不到烘焙数据，
    于是 `MixRealtimeAndBakedGI` 的 subtractive 分支未覆盖 ——
    **而 #12 要动的正是它前后的位置，那一步必须补一个带烘焙数据的场景**）；
    `_ADDITIONAL_LIGHTS_VERTEX` / `EVALUATE_SH_VERTEX` / 探针体 / `_LIGHT_COOKIES` /
    `_LIGHT_LAYERS` / `_DBUFFER` / `_SCREEN_SPACE_OCCLUSION`（由 URP asset 与其他
    RendererFeature 决定，自检不去改全局资产，改了会落盘）；`DEBUG_DISPLAY` 接管路径
    （两边都输出替身颜色，比较无意义，shader 里直接写哨兵）。

### 取舍

- **AP 出厂档 = d=32 / Logarithmic / near 20 m（PC）；移动档 = d=16 / Power k=3。**
  两档都由上面那两张表定，而不是由"看起来够了"定。
  - **为什么 PC 不省到 d=16。** 判据（1% 的 Weber 可见阈）只能判**合格**，不能判
    **该选谁** —— 20 组配置全部合格。所以选择必须落到成本上：16→32 在稳态五 pass
    整链上只多 **0.012 ms**（链的 6.6%、0.300 ms pass 预算的 4%、16.67 ms 一帧的
    0.07%）、显存多 256 KB，换来最差段误差约 **4 倍**余量
    （0.77%/T0.49% → 0.18%/T0.16%，视角 2 是 0.91%/T0.46% → 0.21%/T0.14%）
    和 2.5 倍的近场切片密度（①2 ②5 → ①4 ②10）。
    0.012 ms 小于这次测量自身的离散度（整链 ±3~7%），也就是说 d=16 省下来的东西
    **测不太准**，而被换掉的是四倍余量。这个方向的取舍在成本可忽略时是单向的。
  - **为什么 d=32 上选 Log 而不是 Power。** 纯晴空下 Power k=3 数值更好
    （最差段 0.04% vs 0.18%），但两者都远在阈值内，这个差额买不到画面。
    真正的依据是 Step 3 的雾**要合成进这张 AP LUT**（已定的 UE5 路线），
    而高度雾的密度梯度恰好落在晴空几乎空着的近场段：实测①段累积入散射只有参考白的
    0.26~0.31% —— 晴空近场切片"看起来免费"正是因为那里本来没有东西。
    Log 在 d=32 上给近两段 4/10 片，Power k=2 只给 1/3 片（同一档实测的分段片数）。
    ⚠ 这条是**前瞻性理由，尚未实测**：Step 3 接上后要用带雾的场景重新跑这套扫描，
    结论可以被推翻。写在这里是为了让它到时候被复核，而不是被当成已验证的事实。
  - **为什么移动档反过来选 Power k=3。** 它在 d=16 上给出与 PC 出厂档同级的晴空精度
    （最差段 0.18%/T0.12%，视角 2 是 0.19%/T0.10%），显存只有一半（256 KB），
    单测比 d=32 Log 便宜 18% —— 靠**重分布**切片而不是**增加**切片拿到的。
    d=16 上 Log 与 Power 的差距是 4~5 倍（0.77% vs 0.18%），代价是行进步数
    ×1.25~1.47，这是移动端唯一值得付的那一笔。
    它与上一条不矛盾：移动端的雾走**解析高度雾**那条实现，不骑 AP LUT，
    所以移动端不需要近场 AP 切片密度。
  - 诚实的保留：d=16 上数值最好的分布是 Power，且判据认为 16 片对**晴空**在两个
    测试视角上已经够。余量档没能区分出优劣，选择来自成本 + 分段内容需求，不来自门。

- **单位用 km 而不是 m**。地球半径 6360 km 写成 6.36e6 后，froxel raymarch 里的 `r*r`
  会到 4e13，逼近 fp32 有效位上限（~1.7e7 整数精度），远处步进会出现明显抖动。
  世界空间(m) 只在边界处乘 `_VistaGround.w = 0.001` 转换。
- **积分采样数 40 而非 Bruneton 的 500**。这张表只在参数变化时重算，采样数不进帧开销；
  40 段梯形法的误差已经压到 3e-3，再加采样是浪费烘焙时间。
- **曝光只在"交出引擎"的那一层乘，GPU 侧资源一律留绝对光度量。**
  另一种写法是让 SH 投影核与反射滤波核直接输出已曝光的值，出口就不用管了 ——
  少一处容易漏的乘法（而这次正是漏在那里）。不选它的理由是判据会全部失效：
  自检的参考解、Step 3 的雾、Step 4 的 PRT relight 都按绝对量对账，
  折进核里之后每一条阈值都会跟着 EV100 漂，改一次曝光就要重标一遍全部自检。
  代价是"出口"成了必须逐个点清的清单（当前两个：`ambientProbe`、
  `reflectionIntensity`），所以把它写进了「坑」里当成纪律，而不是靠记性。
- **接受"Vista 用绝对光度量、宿主工程用 Unity 常规单位"两制共存，不去改宿主的灯。**
  Project-ARPG 里现成的灯和材质都是常规单位。理论上更干净的做法是把整个工程
  换成物理单位（灯改 lux、相机走物理曝光），但那会动到所有已调好的场景，
  而收益只在"数字更自洽"。既然曝光已经在交接点补上，两制在**画面**上是一致的
  （实测环境光 ~0.13 对平行光 3.14，比例正常），就把边界留在交接点。
  这条待与 Task #8（TimeOfDay 平行光接入）一起复核 —— 那时大气的太阳要真的去驱动
  场景里的平行光，两制的接缝会正式落到一个具体的转换系数上。
- **Transmittance 用 fp16**。值域 [0,1]，且下游只做乘法不做累加，没有精度累积。
  256×64×8B = 128 KB。
- **多次散射用各向同性近似 + 等比级数，不做完整 Monte Carlo**。完整解要在每一阶重解
  辐射传输方程，烘焙从毫秒级涨到分钟级，而差别在 1% 量级、过 tonemap 后不可见。
  UE5 的 SkyAtmosphere 与 Unity HDRP 的 PhysicallyBasedSky 用的都是这套近似——
  这是"业内主流"而非省事。代价是二阶以上散射丢失方向性，逆光时太阳附近的多次散射
  略偏暗，但被一阶的 Mie 前向峰完全盖住。
- **MS LUT 尺寸 32×32 定为算法常量，不做质量分级项**。它对 (muSun, 高度) 是极平滑的
  二维函数，128×128 与 32×32 肉眼无差别。分级项留给真正吃性能的 SkyView / AP。
- **步段内用解析积分 `S·(1−e^(−σ·dt))/σ` 而非矩形法 `S·dt`**。低步数下矩形法在光学厚的
  步段会显著高估，是带状的主因之一。步段内取样位置取 0.3 而非中点 0.5，因为密度沿
  步段指数衰减、能量重心偏近端。
- **天空走 Skybox 材质，不自己画全屏 pass**。Unity 的环境光（`AmbientMode.Skybox`）
  与反射探针都是从 skybox 材质渲一张 cubemap 卷积出来的，走材质等于免费接上这两条链路；
  自己画全屏 pass 就要把这两套都重写。代价是天空只能在 `DrawSkyboxPass` 的时机画
  （不透明之后、透明之前）—— 对我们无影响，aerial perspective 是独立 pass。
- **太阳圆盘不烘进 SkyView LUT，单独解析画**。192×108 的表上圆盘（张角 0.53°）
  占不到一个纹素，烘进去只会得到一个被双线性糊开的方块。更硬的理由是**动态范围**：
  圆盘亮度在 1e9 量级，烘进去这张表必须从 fp16 升到 fp32，显存与带宽翻倍；
  排除圆盘后全表峰值 54304，安全落在 fp16 的 65504 以内（实测值见上）。
- **SkyView 尺寸 192×108 定为质量分级项**（与 MS LUT 的 32×32 相反）。
  它逐帧重算，且地平线附近的纹素密度直接决定日落时会不会看到横向台阶。
  移动端分级降到 128×72 + SPP 8/16（Task #7 落地）。
  **判据 B 给出了这条分级的量化依据**：192×108 自身的双线性插值误差
  E ≈ 1.885e-3，可见阈 1e-2，余量 5.3 倍。按 O(h²)（实测阶 1.69，中位 2.05），
  边长减半误差涨 4 倍 → 96×54 的 E ≈ 7.5e-3，仍在阈下但只剩 1.3 倍余量。
  所以 128×72 是有依据的下限，96×54 是极限而不是选项。
- **banding 判据不设阈值，改测收敛阶**。原计划是"扫一遍、看二阶差分有没有超过某个数"。
  问题是那个数只能从跑出来的结果附近挑，等于把结论写进判据 —— 面试时这是最先被问倒的地方。
  换成测阶之后，判定依据变成"幅度随步长怎么变"，这是被测对象的**性质**而不是我的选择：
  p≈2 是物理曲率、p≈1 是双线性在纹素边界的斜率跳变、p≈0 是真 C⁰ 跳变。
  代价是要多烘两档步长（靠对同一次扫描抽稀，实际零成本）和多写一条判据 C。
  换来的是唯一一个外部常数只剩感知阈（Weber 1%），其余全部实测。
- **判据 C（地板）不是可选项**。p≈0 有两种含义：真台阶、或者信号已经掉进 fp16 量化噪声里。
  一个要修一个不用修，而只看阶数分不开 —— 必须先实测地板。
  而且**污染方向是危险方向**：地板若被高估（窗口跨了纹素边界，量到的是斜率跳变而非噪声），
  判据 A 的"低于地板 → 通过"会**更容易**成立。所以 C 的实现不去赌窗口够不够小
  （v 轴被 sqrt warp 扭过，平均纹素 1.67° 根本不代表 8° 处的局部值），
  而是步长逐档减半到幅度自己停下来；未到平台时报告把 A 里所有"低于地板"的结论
  **降级为无判定**，不当成通过。
- **一次扫描抽稀出三个步长，而不是烘三遍**。判据 A 要 Δ = 0.5°/1°/2° 三条曲线定阶。
  按 0.5° 烘 181 次再按 stride 1/2/4 抽稀，比烘三遍省三分之二的时间，
  更重要的是三条曲线**严格同源** —— 分开烘的话，任何一处参数不一致都会伪装成
  "阶不对"，而那是这套判据唯一的输出。
- **判据 B 用分辨率对折 + Richardson 外推，而不是"跨纹素走一步看跳多少"**。
  后者量到的是"某一步的跳变"，回答不了实际要问的那个问题（192×108 够不够）；
  前者直接给出**这个分辨率自身的插值误差**，并且自带前提检验：`e_lo/e_hi ≈ 4`
  不成立就说明 O(h²) 不成立，1.333 这个外推系数随之失效，报告判不可引用。
  代价是要多烘两个分辨率档（各 1 次，可忽略）和一次 `SetSkyViewResolution` 的
  分配抖动（跑完在 `finally` 里恢复 192×108）。
- **`RenderPassEvent.BeforeRenderingPrePasses`**。LUT 不依赖任何屏幕空间资源，
  而下游消费者（天空盒、雾、不透明物的 aerial perspective、SH 投影）分布在整条管线上。
  排在最早处，所有下游都能无条件拿到当帧的表。
- **一个 RendererFeature 管整个大气模块，不是每个效果一个**。LUT 是共享资源，
  拆成多个 feature 会让用户配得出"开了雾但没开大气"这种必然黑屏的组合。
- **`IVistaLutDispatcher` 抹平两条录制路径，而不是把 dispatch 写两遍**。
  RenderGraph 的 `ComputeCommandBuffer.SetComputeTextureParam` 六个重载**全部只收
  `TextureHandle`**，而 Editor 自检/预览只有原生 `CommandBuffer`（只收 `RTHandle`），
  两者之间没有转换。写两遍的后果是：哪天只改了运行时那份采样数，自检还在验旧参数，
  而它照样全绿 —— 自检就从资产变成了负债。实现是 `readonly struct` + 泛型约束，
  JIT 去虚化，无装箱。
- **不走 `AddUnsafePass` + `GetNativeCommandBuffer` 这条逃生通道**（`GetNativeCommandBuffer`
  只接受 `UnsafeCommandBuffer`，所以想拿原生 cb 就必须放弃 `AddComputePass`）。
  放弃它会同时失去 `EnableAsyncCompute` 与图的依赖校验，而换来的"省一层抽象"并无收益：
  LUT 排在所有光栅工作之前，native render pass 合并对它没意义。
  这三张表本就是持久资源，所以保持 `ImportTexture` + 让图只负责 barrier / 性能剖析 / 异步调度。
- **AP 用 32×32×32 的 froxel 表，而不是在合成 shader 里逐像素 raymarch**。
  逐像素 raymarch 的画面上限更高（能吃阴影贴图、能做逐像素的深度截断），但代价是
  雾的开销随分辨率线性涨；froxel 表把它固定成 32768 个柱子，与屏幕分辨率解耦。
  这是 UE5 SkyAtmosphere 与 HDRP PhysicallyBasedSky 都采用的形态。
  代价是深度方向只有 32 片，靠非线性分布 + 三线性插值补 —— 补得够不够由 errMid 判定。
- **AP 的散射与透射分两张表，不打包成一张 RGBA**。合成式是 `color·T + S`，
  S 在 1e4 量级、T 在 [0,1]，塞进一张 RGBA 就必须牺牲其中一个的精度或色彩
  （灰度透射率）。分两张的实测代价是灰度近似在 32 km 处偏差 71.2%（见上）——
  这个数字本身就是"为什么 PC 档必须存彩色透射率"的论据。
  移动端档位仍可只采一张：散射表的 alpha 里备了灰度透射率。
- **errCenter 与 errMid 测的是两件不同的事，所以用两套不同的分母**。
  这一条是 AP 自检里最容易被写成"随便定个阈值"的地方，展开记一下：
  - errCenter = `|LUT[i] − truth(D(w_i))| / truth(D(w_i))`，**本地**相对误差。
    它比较的是"同一个位置上，AP 的一次行进循环"与"共享积分器跑 256 步"，
    测的是**代码等价性**（步数够不够、重写的循环有没有写歧）。
    代码等价性必须处处成立，所以分母用本地真值、阈值定在 5%。
  - errMid = `|(LUT[i]+LUT[i+1])/2 − truth(D(w_mid))| / LUT[depth−1]`。
    分子是三线性插值在两片中点给出的值与真值之差，测的是**切片分布够不够密**。
    分母**不能**用本地真值：Power(k=2) 的 `d ∝ w²` 在 w=0 处导数为 0，
    第 0 片在 d=0、第 1 片在 d=33 m，而中点 `w=0.5/31` 对应 d=8.3 m，
    插值给 `0.5·L(33m)`、真值约 `0.25·L(33m)` —— 本地误差恒为 **100%**，
    加多少切片都不会变，这是参数化**算术上强制**的，不是欠采样。
    而那里的绝对量只有 1.35 cd/m²，整根柱子要累积到几千，画面上并不存在这个误差。
    改用**柱子在最远片上的累积总量**作分母后，这个数直接读作
    "贴到画面上的雾量错了百分之几"，阈值也就能从"平滑渐变上多少对比度会看出带状"
    （约 1%）定成 2%，而不是拍一个比值。换分母后 argmax 从退化的第一对切片
    移到了 13.9 km / 23.4 km —— 也就是雾真正有量级的地方，这正是该被监控的位置。
  - 顺带得到 Task #6 的结论输入：等切片数下 Power(k=2) 的插值保真度优于 Log
    （0.15% vs 0.31%），但 Log 的第 0 片能落在 `nearDistanceKm`（20 m）而非 0，
    对"相机贴着地形时近处不要有雾"更友好。两者都远在阈值内，最终取哪个按画面定。
- **环境光用 L2（SH9）而不是 L1（SH4）**。判据是日落：那时天空是"天顶暗 + 地平线一圈
  橙红 + 背侧蓝紫"，L1 只有一个方向矢量，会把这一圈塌成单侧色偏 —— 朝太阳与背对太阳
  的物体拿到的环境色几乎一样，日落的方向感全丢。L2 至少能表达"水平方向亮、上下暗"
  这个二阶形状。另外 `RenderSettings.ambientProbe` 本身就是 `SphericalHarmonicsL2`，
  用 L1 是主动丢信息而不省任何东西；UE 的 SkyLight 与 HDRP 的 ambient probe 也都是 L2。
- **buffer 里存原始辐射亮度矩 `L_i`，不存已与余弦瓣卷积过的辐照度 SH**。
  消费端不止一个：漫反射要 `Â_l` 卷积（`VistaShIrradiance`），Step 3 的体积雾要的是
  **各向的入射亮度**（`VistaShRadiance`，不该乘余弦），Step 4 的 PRT relight 要与
  烘出来的可见性 SH 做逐系数点乘（也不该预卷积）。存卷积后的版本等于把三个消费者中的
  两个逼去反解 `Â_l`，而 `Â_2 = π/4` 一旦被漏乘就是 4 倍误差、且只在二阶上体现 ——
  症状是"环境光的方向对比过强"，几乎不可能被认成 bug。
- **太阳圆盘排除在 SH 之外**。这也是不用 `AmbientMode.Skybox` 的**唯一**动机
  （见「坑」：圆盘会让环境光偏亮约 8 倍）。排除是能量守恒的：圆盘的直射由 URP 的
  平行光负责，卷进环境光就是把同一份能量记两遍。
- **投影时采均匀 Fibonacci 方向，不遍历 SkyView LUT 的纹素**。逐纹素累加看着更"省"
  （表就在那儿），但 SkyView 的 uv 参数化在地平线附近是**故意加密**的（双段平方 warp），
  按纹素等权累加等于给地平线一圈几倍的权重 —— 日落时那一圈正是最亮的地方，
  结果是环境光整体偏橙，且**偏多少取决于 LUT 分辨率**：Task #7 把移动端降到 128×72
  时环境色会跟着变，而这种耦合在改分辨率的那一刻不会有任何报错。
  均匀采样把"表的存储密度"与"积分的测度"解耦。UE5 SkyAtmosphere 的 SkyLight capture
  同样是独立方向集，不复用 SkyView 的参数化。
  实现上 z 在 (−1,1) 等距（Archimedes 投影保证面积均匀）+ 黄金角 `π(3−√5)`，
  方位角先 `frac` 再乘 2π，否则高 index 处 fp32 的有效位全被整圈数吃掉。
- **一份系数、两个出口；GPU 消费者绝不走 CPU 那条路**。雾与 PRT relight 如果读
  CPU 读回的结果，环境光就会比天空慢 2~3 帧 —— 快速过渡的日落里表现为"雾的颜色追不上
  天空"，而这种延迟错位在静态截图里完全看不出来，只在动起来时暴露。
  所以 GPU 侧直接绑 buffer（同帧可用），CPU 侧那条只服务 Unity 内建的漫反射环境光
  （`RenderSettings.ambientProbe`），那里本来就有一帧以上的滞后，无所谓。
- **读回请求在记录期发起，不在 SH pass 的 execute 里**。`AsyncGPUReadback.Request`
  是 CPU 侧 API，与图无关；放在记录期最前面，它拿到的必然是"上一帧已经写完"的内容，
  时序不依赖图什么时候提交。放进 execute 反而要处理"同一帧内请求自己刚写的 buffer"，
  多一次隐式同步。代价是延迟从 2 帧变 3 帧 —— 对漫反射环境光无感。
- **`VistaLutBufferSlot` 让 dispatcher 也抹平 buffer 绑定，尽管这次两条路径的类型是兼容的**。
  与纹理那条的处境不同，值得写下来：纹理侧 `SetComputeTextureParam` 的六个重载**只收
  `TextureHandle`**，原生 `CommandBuffer` 只收 `RTHandle`，不抽象就编译不过 —— 抽象是被迫的。
  buffer 侧不是：`ComputeCommandBuffer.SetComputeBufferParam` 有直收 `GraphicsBuffer` 的
  重载，而 `BufferHandle` 又有到 `GraphicsBuffer` 的隐式转换，所以"直接把持久
  `GraphicsBuffer` 递给 dispatch"**两条路径都能编译、都能跑出正确画面**。
  它悄悄丢掉的是：图不知道这个 pass 碰了这块 buffer，于是不插 barrier、不做依赖排序、
  也不会在剖析里显示这条边。这类错误没有任何编译期或运行期信号，只在换厂商驱动或
  开异步 compute 时才炸 —— 正是必须靠约定拦住的那一类。所以 buffer 也走
  `ImportBuffer` + `UseBuffer` + slot 枚举，与纹理保持同一种写法。
- **自检的判据设计：截断误差不能当判据，均值恒等式才能**。
  这是 #5a 里唯一真正需要想的东西，展开记：
  - 表面上最自然的判据是"SH 重建 vs 高精度参考解，逐法线比"。它不行 ——
    SH9 是二阶带限的，日落天空含大量高阶成分，**必然**对不上。实测正午 ≤ 2.17%，
    日落最暗那个法线（朝下、只吃地面反弹）达 **31.25%**。把阈值定在能容下 31% 的地方，
    等于放过任何小于 31% 的缩放错误；定紧了则"日落必红"，而必红的自检会被习惯性忽略，
    等于没有。**这一项测的是 L2 的表达力，不是代码的正确性，两者不该共用一个阈值。**
  - 真正判定的三条，都与截断无关：
    ① **均值恒等式**（唯一的*精确*判据）：
       `mean_n[(1/π)∫L·max(0,n·ω)dω] = (1/π)∫L·mean_n[max(0,n·ω)]dω = (1/4π)∫L dω`，
       而 SH 侧同一个量恰好是 `L_00·Y00`（一阶以上在整球上均值为 0）。
       于是它与"L2 够不够"完全无关，只取决于 `4π/N` 权重、`Ŷ_0`、`Â_0` ——
       也就是唯一会真出 bug 的那部分。实测 **1.36e-7 / 8.29e-8**。
    ② **GPU 重建 == CPU 重建**：两条"矩 → 辐照度"的实现互不相干（GPU 走 `Â_l` 与
       归一化基；CPU 走 `k_RadianceToUnitySh` + Unity 的未归一化多项式基），
       失败模式不重叠。实测 **8.15e-8 / 1.23e-7**。
    ③ 正午档的逐法线偏差判 5%：正午天空足够平滑，截断本就只有 2.17%，
       这一档的紧阈值能抓"某一阶的 `Â_l` 缩放错了"（至少 30% 量级），
       而那种错误会**同时**污染两个太阳高度，截断误差却只在日落放大 —— 可区分。
  - 顺带给出一个设计结论的实测依据：**31% 的截断误差就是镜面反射必须另走 cubemap 的
    理由**（#5b）。这个量级在漫反射上过 tonemap 后可接受，在镜面上就是把高光糊成一团。
- **`VISTA_SKY_SH_SAMPLES` 取 1024**，依据是自检里那条"1024 vs 4096 均值偏差"：
  正午 0.01%、日落 **2.75%**。日落那 2.75% 落在 DC 项上（≈0.04 EV），且方向集是**固定的**
  （不做逐帧抖动），所以误差是太阳角度的平滑函数 —— 不会有时域闪烁，只是一个恒定的
  微小偏色。用不着为它加采样。这个数留在自检里打印，Task #7 分级时直接读。
  当前整趟只 dispatch 一个线程组（64 线程 × 16 次采样），加到 4096 就是 64 次 ——
  单 SM 上的纹理延迟会真实体现在耗时里，而收益是看不见的 0.04 EV。

- **反射走 cubemap 而不是直接用 #5a 的那份 SH**。依据是 #5a 自检里那个 31.25% ——
  L2 在朝下法线上的截断误差。这个量级在**漫反射**上过完 tonemap 可以接受
  （地面反弹本来就是低频的），在**镜面**上就是把日落地平线那圈橙红糊成一团均匀的橙，
  而反射恰恰是观众唯一能逐像素对照"这个方向的天空到底什么颜色"的地方。
  移动端也还是 cubemap（只是内容由 SH 重建），理由见下一条。
- **反射不做「渐进预滤波」（读 mip N−1 写 mip N），每级都直接从 SkyView LUT 积分。**
  渐进预滤波是这件事的标准做法（HDRP 的 `IBLFilterGGX`、Karis 2013 的实现都是），
  但它要求同一张资源在同一趟里既当 SRV 又当 UAV，而 RenderGraph 跟踪的是**整张资源**
  的状态 —— 与三张静态大气表必须拆 pass 是同一类 UB（见「坑」）。绕开只有三条路：
  ① 源 mip 也按 UAV 读（丢掉硬件双线性与跨面无缝滤波，cube 接缝处出硬边）；
  ② 两张纹理 ping-pong（HDRP 的形态，正确但显存翻倍且要在图里 `GenerateMips`）；
  ③ 拆 7 个 pass 串行（正确，但每个 pass 只有几百纹素的工作量，全是调度开销）。
  直接从 LUT 积分把这一整类问题消掉：全程纯写、只有 UAV 一个状态、零 barrier、
  七趟 dispatch 挤在一个 pass 里。而且**质量严格更好** —— 没有 box filter 的近似、
  没有跨级累积误差、mip0 是精确镜面，接缝在结构上不可能出现（每个纹素积的都是
  真实世界方向）。
  能这么做的前提是开销够低：需要滤波的纹素只有 mip1~6 共
  `6 × (1024+256+64+16+4+1) = 8190` 个，配上随 mip 上升的采样数总计约 376k 次
  LUT 双线性**取样**（不是 raymarch），加上 mip0 的 24.6k 次约 0.4M。
  （这个估算是纠正过一次的：第一版把 mip0 也算进滤波、还用了一个平的采样数，
  得出 2.1M，据此差点选了渐进预滤波 —— 改对之后架构结论就翻了。）
- **不做逐帧摊销（HDRP 会把面/mip 分帧）。** HDRP 摊销是因为它在 256² 上滤波、
  且走 42 趟光栅 pass；那个约束不迁移过来 —— 这里 mip0 只有 24576 个纹素，
  全部七级一趟做完。摊销这件事留给 Task #7 当分级旋钮（与采样数、辐射来源一起）。
- **cubemap 尺寸 64² 是被 URP 定死的，不是拍的。** `ImageBasedLighting.hlsl:16` 的
  `UNITY_SPECCUBE_LOD_STEPS = 6`，而 URP 的 `GlobalIllumination.hlsl` 用的是
  **单参**重载 `PerceptualRoughnessToMipmapLevel(pr)` —— 也就是它假设 maxMip 恰好是 6，
  即 mip 0..6 共 7 级，即边长 `1 << 6 = 64`。所以这两个常量都从
  `UNITY_SPECCUBE_LOD_STEPS` 推导（`SkyReflection.hlsl:41-42`），
  而不是各写一个字面量：URP 哪天改了那个 6，我们跟着变，而不是安静地错位一档。
  自检判据 ③ 把 HLSL 侧的实际取值报回 C# 比对，所以"两边不一致"是一条会红的断言。
- **判据 ② 的阈值按太阳高度分两档（正午 0.5% / 日落 6%），不是统一放宽到 6%。**
  实测日落 4.315%、正午 0.078%，差了 55 倍，而这个差是**物理**：
  cube 三通道分别偏暗 4.3% / 1.4% / 0.2% 且一律偏暗 —— 线性插值在凸的亮带上
  系统性下冲的签名。日落地平线那圈橙红是全天角频率最高的结构（64² 下每纹素约 1.4°），
  几乎全在红通道；蓝通道在日落时整片天空近乎平坦，所以几乎不错。
  统一取 6% 的代价是正午档从"回归探测器"退化成"形式上绿着"：真出现 1% 级别的整体
  缩放错（比如曝光被乘了两次），只有正午那一档能抓住它，日落档会被离散化误差淹掉。
  这与 #5a 日落档取 `PositiveInfinity` 的做法**故意不同** —— 那边的截断误差在特定
  法线上无界，给不出有意义的上限；这边的离散化误差是有界的，留一个真阈值它才还是断言。
  另外这 4.3% 只影响 mip0：任何有粗糙度的表面读 mip≥1，那里 GGX lobe 的张角远大于
  1.4°，离散化被滤波宽度盖掉了。
- **太阳圆盘不进反射图。** 两个理由，任一个单独就够：① URP 的平行光已经给了解析
  GGX 高光，圆盘再进反射就是重复计一次；② 圆盘亮度在 1e9 量级而 fp16 上限 65504，
  进来直接饱和成 Inf，而 Inf 会经预滤波的归一化污染整片纹素（不是一个点，是一片）。
- **GGX 预积分用各向同性近似（N = V = R），不做"更物理"的 NdotV 相关 lobe。**
  真实 GGX lobe 掠射时会拉长成各向异性，但采样端（URP 的
  `GlossyEnvironmentReflection`）用的是不带 NdotR 的单参重载 —— 也就是说
  **它本来就假设这张图是各向同性预滤波的**。跟着采样端的假设走，
  而不是把写入端做得更物理然后与它错开；后者的症状是掠射角反射整体偏暗，
  而且查起来会怀疑到积分本身。
- **wide 核的形状分界定在 `K ≥ 64`，而且这个 64 是量出来的、不是推出来的。**
  第一版写 `mip > 0`（"除了 mip0 全走 wide"），结果 mip1 从 0.021 **退化**到 0.041 ms，
  反而成了单项最大 —— 它的 K=32 只喂饱一半 lane，另一半空转，还白付一轮 6 步 barrier
  归约；而 mip1 在 narrow 下本来就是全场吞吐最高的一级（96 组 × 64 线程 × 每条 32 深，
  延迟早被别的 warp 藏住了）。同一次测量里 mip2 从 0.032 改善到 0.017，
  分水岭正好落在 K=32 与 K=64 之间。门槛的物理含义因此是"每条 lane 至少领一个样本"。
  也没有为 K<64 再切一层「一组多纹素 + 组内分段归约」：按 mip2 达到的 5.89 G样本/s
  折算，mip1 那 196.6k 个样本要 0.033 ms，比 narrow 的 0.021 还慢 —— 多一套索引换更差
  的结果。**SH 模式（K 恒为 16）完全没有实测**，它走 narrow 只靠这条占用率论证支撑。
- **分界写成 mip 序号（`!useSh && mip >= 2`），不在 C# 里重算一遍 K。**
  K 的定义在 HLSL 的 `VistaSkyReflectionSampleCount`，运行时抄第二份就多一处会走歧的
  真源。代价是这个 `2` 与那个函数隐式绑着，所以自检判据 ④a 把 HLSL 侧的 K **逐 mip
  导出**、C# 拿它跟门槛和 `mip >= 2` 对账 —— 必须让它会红，因为 K 改了而分界没跟上的
  症状是某一级性能悄悄退化，不是错图，画面上永远看不出来。
- **合并粗糙几级的 dispatch：已识别，主动搁置。** 改完形状之后瓶颈换成了 per-dispatch
  的固定开销（证据 3 给出地板占反射 pass 的 59%），下一个杠杆是把 mip3~6 那 510 个纹素
  合进一趟。但那需要同时绑 4 个 `RWTexture2DArray` UAV 加一个级别 switch，
  换回约 0.03 ms —— 而整链已经是 0.170 / 0.300 ms。**在已达标的链路上为 0.03 ms 引入
  一处形状复杂度不划算**，把它记在这里比做掉它更有价值：留着当"下次预算收紧时先动谁"的
  已定位杠杆。
- **计时用「模型 B 为主 + 模型 A 交叉验证」；逐 pass 绝对值只从模型 A 引，模型 B 只读占比。**
  模型 B = Edit 模式立即提交、N 次背靠背摊销；它测的是**吞吐**，相邻 dispatch 允许重叠，
  所以逐 pass 值是下界。模型 A（Play 模式 `ProfilerRecorder`）测**帧内延迟**，跑在真正的
  RenderGraph 图上，是唯一把 barrier / pass 边界 / 资源状态转换算进去的口径。两个模型都不是
  "随手加个 Stopwatch"：立即模式的状态转换由图形层插，**不是** RenderGraph 那批 barrier。
  这条分工原本写成"只对外引用整链数字"，跑完模型 A 之后改了 —— 理由见下一条。
- **同一个 pass，两套口径的可引用性可以反过来。** 反射 pass 在模型 B 里离散度 ±5%~±132%
  （0.076 / 0.095 / 0.101 三次复测），判成不可引用；在模型 A 里是 0.093 ms，
  min 0.186 / max 0.202（±9%），完全可引用。原因不是"模型 A 更准"，而是这个量级上
  **两个模型的误差来源不同**：模型 B 要从摊销值里扣一个 0.161 ms 的固定开销，被测量
  （0.09 ms）比扣减项还小，扣减自身的误差就主导了结果；模型 A 直接读 300 个真实帧的
  marker，不做任何扣减。所以"哪个数字能引用"是**按口径和量级判的**，不是按工具判的。
  推论：模型 B 的逐 pass 数在 0.01~0.02 ms 档一律只读占比，绝对值去模型 A 拿。
- **静态表的判定方向要掉过来：稳态里"没有样本"才是对的。** 第一版把七个 pass 一视同仁，
  凡是取不到样本就报"pass 名走歧 / pass 被剪"，于是两张静态表（Transmittance /
  Multi-Scattering）在 300 帧里 0 样本被判成失败 —— 而那恰恰是脏标记正常工作的证据。
  真正该报警的是反向：这两行**有**样本 = 静态表每帧重算，白烧约 0.044 ms/帧，
  **画面完全正常，只有计时器能看见**。这类"期望缺席"必须显式建模成正判据；
  否则工具每次都对着正确行为报警，人很快就学会忽略它的警告 —— 那时它就不再是判据了。
- **多相机求和不能悄悄除掉。** Editor 里 Scene View 与 Game View 各渲染一次，
  开了 `SumAllSamplesInFrame` 之后一行的值是**两次之和**。除以出现次数是合法的，
  但合法性有前提：LUT 尺寸全是定值（256×64 / 32×32 / 192×108 / 64²×7 / 32³）、
  与相机分辨率无关，每帧只有一份大气参数，所以两次渲染做同样的工作量。
  报告把出现次数（min~max）、原始和、单次值、以及这条前提**一起打出来**：
  出现次数在帧间变动（min≠max）时连"稳态"都不成立，那时除法直接不做。
  先试过用 `ExecuteMenuItem("Window/General/Game")` 把 Game 置前来消掉第二个相机，
  这台机器的停靠布局下无效（Scene 与 Game 不在同一区域），于是保留除法分支 —— 
  能消掉假设当然更好，消不掉就把假设写在数字旁边，而不是让它隐含在结论里。
- **每项取 M 轮的最小值，不取平均。** 噪声在这里是单向的 —— Editor 重绘、驱动争用只会
  **加**时间，不会减。平均会把偶发的重绘平摊进结果，min 是对"无争用真实耗时"的一致估计。
  min 与 max 同时上报，离散度 > 25% 的行标 ⚠ 并**不参与判定、不对外引用**：
  这台机器上单个 pass 到 0.01~0.02 ms 档就已经在噪声地板下（复测见过 ±132%）。
  正因为有这条纪律，才能诚实地说出"mip1 那一级确实快了 2×，而 pass 总数在噪声内没变"。
- **CPU 上重算一份 T，而不是异步读回 GPU 的 Transmittance LUT。** 读回是"零物理重复"的
  写法，但异步读回有 1~2 帧延迟，而延迟的那一项是**直射光色** —— 拖时间滑竿时天空已经
  红了、物体还是白的，是全场最显眼的滞后，而文档 Step 2 的验收标准明写"无一项滞后"；
  同步读回则 stall。另有三个消费者根本不在渲染上下文里：Step 4 的 PRTGI 烘焙（要对每个
  时刻 relight）、自检、Editor 时间轴预览，它们连"上一帧的读回结果"都没有。
  代价是第二份实现，靠 CPU/GPU 逐通道对账自检控住 —— 那条自检是**双向**的，
  两条完全不同的路径对上才说明两边都对。多一把独立的尺子是资产不是负债（#7 里两次
  "尺子自己坏了"都是因为只有一把尺子）。
  也没有用 Chapman 函数那类解析近似：它只覆盖**指数**密度剖面，而臭氧是帐篷剖面，
  正是黄昏蓝紫对侧色的来源，这个项目里不能丢。
- **由场景组件写灯，不由 RenderPass 写。** 从渲染代码里改场景状态本身是反模式（标脏、
  与 Undo 打架、多相机时后渲染的覆盖前一个），但更要紧的是**光色会变成相机相关的**：
  同一盏灯在主相机与反射探针相机下拿到不同的 T，画面与反射对不上。太阳照的是**几何**
  而不是相机，所以求值点必须是场景里一个与相机无关的固定参考点。
- **色度放 `color`、幅度放 `intensity`，不把整个矢量塞进 `color`。**
  `Light.color` 在 Inspector 上是非 HDR 的颜色选择器：塞 >1 的值代码里能生效，
  但一被面板碰到就夹掉，症状是"手点一下灯颜色，画面突然变暗"。UE 与 HDRP 同样是
  色度/幅度分离。归一化取**最大通道**而不是亮度 —— 取亮度会让日落 T=(0.3,0.1,0.03)
  归一出一个 >1 的红通道，同样被夹掉。
- **单值参考海拔的近似**保留，但把它的失效范围**算出来放到面板上**。
  用一个高度的 T 照亮整个场景在正午是够的（包线 271 m），问题是包线随太阳角剧烈收窄
  （5° 只剩 51 m），而魂系/开放世界外景的地形起伏是数百到数千米量级 ——
  也就是说**这个近似恰好在日出日落这段最关键的时间失效**。
  三条路：① 就这么用并在文档里写一句"注意高差"（等于没写）；② 每个高度层各摆一盏灯
  （灯数爆炸，且过渡处有硬边）；③ 逐像素透射率。选 ③，但它是独立一步（Task #12），
  因为它要改**不透明着色**而不只是写灯 —— 对应 UE5 平行光上的
  Per Pixel Atmosphere Transmittance 开关，也就是说这是有生产先例的分级项，
  而不是我发明的东西。当下先把包线做成面板读数：美术能当场看到"我这个场景高差 800 m、
  现在太阳 5°、包线只有 51 m"，这比一句文档提示有用得多，也是 #12 的立项依据。
- **不做大气折射。** 真实地平线附近折射把太阳抬高约 0.57°（约等于一个日面直径），
  所以"日出时刻"会差一两分钟。不做的理由不是难，是**它会污染判据**：
  加了折射之后高度角就不再是纯天文量，A/B/C/D 四条对闭式解的判据全部要跟着改口径，
  而画面上的收益是日出日落各早/晚一两分钟。真需要时它是一个后处理式的单调映射，
  可以在不动天文核心的前提下叠上去。
- **`k_MinMuSun` 取 0，不加人为的地平线淡出。** 太阳在地平线以下时直射光物理上就是 0，
  而 Transmittance LUT 的参数化在 mu < 0 且视线穿地时给的是"穿过地心"的无意义值。
  取 0 而不是某个负的软化值：地平线附近的软化应该由**大气自己**给出（T 在那里已经掉到
  1e-2 量级，是连续的）；人为再叠一层淡出会让"日落时刻的光色"变成一个凑出来的东西，
  那就不能拿物理去辩护了 —— 而这份作品集里每个选择都要能被追问。
- **朝向用 `Quaternion.Euler(alt, az+180, 0)`，不用 `LookRotation(-dir, up)`。**
  太阳升到天顶时 forward 与 up 共线，`LookRotation` 退化（结果未定义），
  症状是赤道地区正午光向突然乱跳。Euler 形式没有这个退化，附带好处是 Inspector 上的
  Rotation.X 可以直接读作"太阳仰角"。
- **一个量只允许有一份实现。** 这一步为此做了三次提取：包线求解器从自检搬进
  `VistaSunTransmittance`（面板与 F2 项共用）、灯值一致性判据 `LightMatchesComputed`
  留在面板里但改成 public static（诊断自检调它）、`ApplyLightParams` 从私有改成 public
  （接缝自检驱动它渲一帧）。理由都一样：**若自检自己抄一遍被测逻辑，改一边忘另一边时
  自检会替错的那份背书**，等于自检失效。这类"两份真相"在 #7 里吃过教训。
  代价是三个本可以是私有的 API 变成了公开面，接受。
- **诊断面板 `RequiresConstantRepaint() => true`。** 时间滑竿可能被 Timeline / 演示脚本 /
  别的面板驱动，不重绘的话面板会显示一个过期状态，而**过期的诊断比没有诊断更坏**
  （它让人去查一个不存在的问题）。代价量化过：包线求解是 40 次二分 × 41 段积分，
  几十微秒，且只在面板可见时发生。
- **面板绝不显示过期的 `lastLightParams`。** 三个前提（有灯 / 开着光色驱动 / 有 feature）
  任一不满足，那个结构体就是上一次成功求值留下的旧值 —— 尤其无灯这一支，`Apply` 提前
  返回，连大气那段都没跑。所以改成显式给出**不是活值的原因**那一行，而不是照着旧值画。
  这个洞是我重读自己刚写的面板代码时发现的，不是测出来的：第一版只判了缺 feature 与
  关驱动两支。

- **全屏合成不采样颜色目标，把两个乘子交给混合器，代价是两趟。**
  直觉写法是"采 cameraColor → 算 → 写回"，但同一张 RT 不能同时采样和写入：
  要么申请临时 RT 再拷回（双倍带宽），要么把 `cameraColor` 换成新贴图
  （那会强制 `requiresIntermediateTexture = true`，多一次最终 blit）。
  两条都不走，因为 `dst·T + S` 的两个乘子混合器都能做：
  `Blend Zero SrcColor` 得 `dst·T`，再 `Blend One One` 得 `dst·T + S`。
  换来的不只是省一次拷贝：不读颜色目标意味着**直接渲后台缓冲也能用**、
  **MSAA 下逐样本混合天然正确**、移动端两趟画在同一个 RenderPass 里不打断 tile。
  两趟的**顺序是公式的一部分**：反过来先加后乘是 `(dst + S)·T`，散射项被多衰减一次，
  症状是远景雾偏暗且越远越暗（T 越小错得越多）。
- **`AccessFlags.Write` 而不是 `ReadWrite`，尽管这个 pass 在做混合。**
  RenderGraph 把"Write 且未附加 Discard"当作 partial write，load action 仍然是
  `Load`（core 包 `NativePassCompiler.cs` 的 `partialWrite` 那一段），
  混合要的正是这个。URP 自己的 `DrawSkyboxPass` / `DrawObjectsPass` 也是这么声明的
  （全仓只有 `StopNanPostProcessPass` 用 `ReadWrite`）。
  改成 `ReadWrite` 不会让画面变对，只会多一条读依赖 —— 所以代码里写了注释挡住这个"修复"。
- **`_VistaApConsumer` 由 Sky-View pass 下发，不跟着 AP pass 走。**
  变体 B 的开关必须**每帧无条件写**，包括 AP 整个关掉的那一帧 —— 那时它得是 0。
  跟着 AP pass 走的话，AP 关掉的帧里那个 pass 根本不排入（记录期就 return），
  uniform 留着上一帧的 1，材质就会去采一张已经释放的 3D 表。
  「关掉某功能后画面才坏」是最难反查的一类失效，所以它挂在唯一一个"一定存在"的
  逐帧 pass 上。同理它不塞进 `_VistaApFlags` 的空位：那一组只在 AP 启用时才被写。
  也没有给 `VistaAtmosphereViewData.Bind` 加参数 —— 那会波及约十个不关心它的 dispatch 调用点。
- **排入与否在 `AddRenderPasses` 里判完，不在 `RecordRenderGraph` 里 return。**
  这个 pass 声明了 `ConfigureInput(Depth)`，**光是排入**就会让 URP 安排一次全屏深度拷贝。
  为一个什么都不做的 pass 拷一张深度是实打实的浪费。
  三个条件（`compositeMode == Fullscreen`、`m_Luts.isAerialPerspectiveValid`、材质有效）
  与大气 pass 里的 `apEnabled` 必须同真同假；这条耦合写成了
  `PrepareAerialPerspective` 上的**契约注释**，而不是在运行时再复制一份 guard ——
  复制出来的 guard 会变成"永不触发的保护性代码"，这个项目已经把那条教训记在上面了。

### 坑

- **「不可判定」被判定行读成了「通过」。**
  模型 A 接上合成后的第一版：合成那一行印着「不可判定：出现次数与点名对不上」，
  而最后的判定行照样打「模型 A 交叉验证完成」。
  成因是那道门只看 `compositeMissing / compositeCpuOnly / compositeOccUnstable`，
  而 `compositeOccUnstable` 只数「帧间在变」—— 一个稳定的 2 不算缺口，
  于是"一次判定都没跑"这件事没有任何变量记录。
  修法是加一个 `compositeJudged` 进那道门。
  **未覆盖的路径必须是缺口，不能是通过**，这是本项目已经记过的反模式又犯一次
  （上一次是自检里的"没进这个 renderer"分支）。

- **下界判定失败之后还在描述落点，同一份报告里出现两句互相矛盾的话。**
  故障注入（把 `floorMs *= 10`）时方向告警正确响了 ——
  「**低于全天空下界 0.349 ms**」、判定行「合成包线**方向反了**」——
  但紧接着的下一行照样打「落在包线内（占上界 66%）」。
  成因是落点段落只由 `minMs > ceilMs` 选择，没有挂在下界判定上。
  真出故障时这句话会**替故障背书**：读的人看到"落在包线内"就不会再往上看。
  修法是把 `bool aboveFloor` 提到前面，没过就印
  「下界未过，落点位置不再解读：包线本身已经不可信」，一个位置数字都不给。
  这条是"故障注入不只是验告警响不响，还会顺手挖出没人想过的自相矛盾"的实例 ——
  两次注入（点名数 +1、下界 ×10）都是这么用的，第二次的收获比第一次大。

- **`runInBackground` 关着 + Editor 失焦 = 静默挂死，而且连挂在哪都看不见。**
  Project-ARPG 的 `ProjectSettings.asset:86` 是 `runInBackground: 0`。
  Editor 一失焦 play 循环就停走，`Time.frameCount` 冻住，
  于是按帧数推进的采样永远等不到 —— 表现是**菜单执行完什么都不打印，一直挂着**，
  而控制台还被 Clear on Play 清空了，连"武装成功"那条都看不见。
  静默挂死是最坏的失败模式：它和"还在跑"、"pass 名走歧"长得一模一样。
  实测那次挂了 50 s 才靠 `manage_editor(stop)` 逼出
  「交叉验证被手动中断，采样帧数不足，未出报告」，才确认 `s_Running` 是设上了的。
  两条修法：
  - 进 play 时打开**运行期的** `Application.runInBackground`、退出还原，
    **不是** `PlayerSettings.runInBackground` —— 后者会写进 `ProjectSettings.asset`，
    那就成了"量性能的工具改了工程配置"。运行期属性退出即还原，磁盘上不留痕
    （已核对：跑完后那一行仍是 0，`git status` 干净）。报告头上把这件事声明出来。
  - 15 s 无**帧号**进展就中止并说出成因。看帧号而不是看墙钟，
    否则"机器慢"会被误判成"卡死"。

- **双源混合在 Unity 里不存在 —— 试过了，记下来免得再试一遍。**
  硬件本来有一条一趟写法：双源混合（`SV_Target0 = S`、`SV_Target1 = T`，
  `dst = S·One + dst·Src1Color`）。但 `UnityEngine.Rendering.BlendMode` 里
  **根本没有** `Src1Color` / `Src1Alpha`（反射实测只有 `Zero/One` 与 `Src|Dst` 的
  `Color|Alpha` 那十一个），ShaderLab 的 `Blend` 解析器也直接拒收：
  `Parse error: syntax error, unexpected TVAL_ID, expecting TVAL_VARREF or TVAL_BMODE`。
  发现路径值得记：这个 Pass 是**先写进 shader 的**，是刚写好的接线自检在第一次运行时
  报出编译错误才查清的 —— 而它同时暴露了一个更坏的现象：整个 shader 编译失败时，
  `Material.passCount` 仍然返回 3，pass 名却是替身 shader 的
  `<Unnamed Pass 0>` / `DepthNormalsOnly` / `DepthOnly`。
  也就是说**只判 passCount 不判 pass 名的自检会在这种情况下通过**。
  其余混合方程也表达不出 `dst·T + S`：`Blend One SrcAlpha` 只能给出灰度 T，
  `Blend One OneMinusSrcColor` 的 dst 系数是 `1−S` 而不是 T。
  所以两趟不是"先图省事"，而是 Unity 里唯一可表达的形式。
  **这条同时推翻了我上一轮自己的计划**：原本打算保留双源那一趟做带宽 A/B，
  #15 的性能项里那个对比必须删掉。

- **绝对光度量的管线里，"少乘一次曝光"的症状是"阴影不见了"，而不是"画面太亮"。**
  `VISTA_EXPOSURE` 全工程只在 `VistaSky.shader` 里乘了一处；两条**引擎出口**
  （SH → `RenderSettings.ambientProbe`、cubemap → `customReflectionTexture`）
  发的是裸的绝对量。它们的消费者是 URP Lit 的 `unity_SHAr` 与
  `GlossyEnvironmentReflection` —— 不是 Vista 的 shader，身上没有曝光级。
  于是环境光重建值 1.4e3~8.2e3，而工程里的平行光是 Unity 常规单位
  （`m_Intensity: 3.14159`），比值 1600~2600 倍：直接光只占最终亮度的约 0.04%，
  低于 1% 的 Weber 可见阈（判据来自 Task #6）。shadowmap 全程正确、
  Frame Debugger 里一切正常，但阴影**在数值上被淹没了**。
  这类 bug 的排查代价来自它的伪装：报出来的现象（"没有阴影"）与病因
  （"环境光单位不对"）在概念上毫无关联，很容易直接去查阴影距离、级联、
  `m_Shadows.m_Type`。教训：**混合单位制的边界必须显式列出来**，
  凡是把值交给引擎/第三方 shader 的地方都是一个边界，一个都不能漏。
  同理，只在自家 shader 里乘曝光的做法只有在"所有消费者都是自家 shader"时才成立 ——
  一旦要接引擎的现成路径，这个前提就不再成立。
- **"实测这个属性不会置脏"这类断言，会在代码里放很久然后被发现是错的。**
  `BindReflectionToRenderSettings` 原先带一句注释："实测环境光那几个属性不会置脏"。
  实测（`Log Ambient Probe State` 报 `isDirty`）是 **True**。这是本项目记过的
  *stale-assertion* 模式的又一个实例（上一个是计时器里硬编码的判定文本）。
  处置不是把注释改对就完了 —— 是让代码**不再依赖**任何"不会置脏"的前提，
  一律走保存/还原。凡是靠"某个引擎行为恰好如此"来省掉清理逻辑的设计，
  都欠着一次这样的返工。
- **`RenderSettings` 是"逐场景序列化"+"域重载不丢"的组合，这两条加起来会伪造出
  "改动没生效"。** 修完曝光后重编译，`Log Ambient Probe State` 仍印 5119 ——
  看起来像改动没编进去。两个原因叠在一起：(a) `read_console` 的 `count`
  返回的是**最旧**的匹配（已记过的坑，这次又踩了），读到的是上一次的报告；
  (b) `RenderSettings` 是原生状态，域重载不会清，而 Edit 模式下 Game view
  不重绘就没有新的回读完成，于是内存里那份陈旧的绝对量探针一直挂着。
  确认手段：先 grep 场景 YAML —— `ambientProbe` **不在**序列化字段里
  （只有 `m_UseRadianceAmbientProbe: 0`），所以磁盘上找不到它；
  然后进 Play 模式强制出真帧才拿到 0.1302。
  顺带一条：Edit 模式下**反射出口是同步写、环境光出口是异步回读**，
  完全可能出现"`defaultReflectionMode` 已 Custom 而 `ambientMode` 还是 Skybox"
  的中间态；诊断只报环境光那一条会把这种偏态误判成"链路没通"。
  所以 `Log Ambient Probe State` 现在两条出口一起报。
  进 Play 模式后也要**等几帧**：回读本身 2~3 帧，刚进去就读会拿到全零。
- **"保存前还原"挡不住"基线本身是脏的"。** 守卫还原的是"本次运行首次写入前扣下的
  原值"。如果某个场景在守卫存在之前就已经把 `m_AmbientMode: 4` /
  `m_DefaultReflectionMode: 1` 写进了磁盘，那么这次打开场景扣下来的"原值"
  就是被污染的那一份 —— 守卫会忠实地把 Custom 还原回去，**越保存越干净不会发生**。
  这种状态**无法自动分辨**（Custom 也可能是用户有意设的），只能由人显式复位。
  于是加了 `ForgetBaselines()`（只丢基线、不写回）与
  `Window/Vista/Reset Scene Ambient & Reflection`。顺序是硬性的：
  丢基线必须在保存**之前**，否则守卫会在写盘前把脏基线还原回去，
  表现为"复位了也保存了，磁盘上还是 Custom"。
- **验证一次"保存不会写入 Custom"，先得确认那次保存真的写盘了。**
  第一版验证是点 `File/Save` 然后 grep 磁盘 —— 看到 `0` 就以为守卫生效。
  但当时 `isDirty = False`（前一步的复位刚保存过），`File/Save` 是空操作，
  这个"通过"什么都没证明。这是本项目一直在追的 *false-pass* 模式。
  改成显式 `EditorSceneManager.SaveScene`（无条件写盘），并在保存前先把
  实时状态打出来确认它确实是 `Custom` + `2.54e-5` + cubemap 已绑定，
  测试才有内容。**判据的前提条件本身也需要被测量**，不能假定它成立。
- **改了口径就要回头查所有按旧口径标定过的阈值。** 探针改成"交接时乘曝光"之后，
  `Log Ambient Probe State` 里那句"链路已接通"的门限 `c_0 > 1e-3` 是照**绝对量**
  定的，直接沿用会在低太阳角/夜间把正常链路判成未接通
  （1 cd/m² × 2.54e-5 = 2.5e-5 < 1e-3）—— 一个**自己造出来的假失败**。
  修法是把门限按曝光归一，且曝光从 `_VistaSunDirection.w` 读（与运行时同一个来源），
  不在自检里重算一遍 EV100：重算就是第二份真值，迟早走歧。
- **`Scene.handle` 在 Unity 6000.4 已换成 `SceneHandle`**，到 `int` 的隐式转换
  标记为废弃（CS0618）。存 `Scene` 结构本身即可 —— 它自带 `==` 且比的就是句柄，
  既躲开废弃 API，也不用跟着 `SceneHandle` 的表示形式改。
- **`MenuItem` 路径里的 `&` 要写成 `&&`**，而通过 MCP 的 `execute_menu_item`
  调用时传的也必须是带双写的那份（`Reset Scene Ambient && Reflection`），
  传单个 `&` 会报"菜单项无效"。
- **本次的既定残留：装了大气模块的场景在 Editor 里会长期挂 `*`。**
  写 `ambientProbe` 会置脏，太阳一动就再脏一次。**不**用
  `EditorSceneManager.ClearSceneDirtiness` 去消它 —— 那会连带清掉用户自己未保存
  改动的脏标记，关闭时不再提示保存，是拿数据丢失换一个星号。
  受影响面已实测：扫过的约 29 个场景里只有 `Assets/Scenes/Demo.unity` 被污染过
  （已复位为 `0 | 0`）；`World_02` 的 Trilight、
  `PolygonFantasyHeroCharacters/Demo_RandomCharacter` 的 Flat 都是原有设置，与 Vista 无关。
- `Categorization.CategoryInfo`（Graphics Settings 面板分组用的特性）是**引擎内部 API**，
  只对 Unity 官方 SRP 包开放（`InternalsVisibleTo` 白名单）。第三方 package 实现
  `IRenderPipelineResources` 时不能加这个特性，否则 CS0246。纯显示层，去掉无影响。
- LUT 的单位区间端点必须落在**纹素中心**（Bruneton 的 `0.5/size + x*(1-1/size)` 内缩），
  不做这层映射时双线性采样会把区间外的值混进来，症状是地平线附近出现台阶状色带。
  这是整套 LUT 最容易漏的精度坑，且症状延后到最终天空颜色才暴露 —— 所以先写自检工具
  比先写 RenderGraph pass 更划算。
- **HG 相位函数的 cosθ 符号**。约定 `cosTheta = dot(rayDir, sunDir)`（看向太阳为 +1）时，
  分母必须是 `1 + g² − 2g·cosθ`。写成 `+2g·cosθ` 会把前向散射峰搬到太阳的**反侧**，
  症状是光晕出现在背光方向。推导写在 `AtmosphereScattering.hlsl` 的函数注释里。
- **朝太阳的透射率不能只靠 Transmittance LUT**。太阳在地平线以下时，Bruneton 参数化会
  给出一条穿过星球本体的无意义路径，必须另做 ray-sphere 判定（`VistaEarthShadow`）归零。
- 建 MS LUT 时积分器必须关掉 `useMultiScattering`，否则自引用（读一张正在写的表）。
- `execute_code` 在本工程不可用：Roslyn 未安装，CodeDom 因引用程序集过多导致
  mono 命令行超长。验证逻辑要做成 `[MenuItem]` + `Debug.Log` 才能自动化调用。
  且多行日志经 MCP 转发只保留首行，报告需压成单行。
- **RenderGraph 只在 pass 边界插入资源状态转换**，一个 pass 内同一资源只有一个状态。
  Transmittance 以 UAV 写出 → MultiScattering 当 SRV 读 → SkyView 读前两张，
  因此**必须拆三个 pass**。挤在一个 pass 里在 D3D12 / Vulkan 上就是缺 barrier 的 UB，
  在 NV 驱动上经常"看起来是对的"，换台 AMD 机器才炸。
  立即模式（Editor 预览）没有这个问题：原生 `CommandBuffer` 的状态转换由图形层自动插入
  —— 这也是为什么先写好的立即模式自检是可信的。
- **每个 Vista pass 都要 `AllowPassCulling(false)`**。URP 自带的 `DrawSkyboxPass` 无法声明
  对我们 LUT 的读取（它不认识这些资源），图里于是没有任何消费者，整个大气模块被静默剪掉。
  症状极具误导性：天空一片黑，而 Frame Debugger 里连 pass 都找不到。
- **全局纹理统一在 SkyView pass 里发布**，不是各自在产出 pass 里发布。静态表的 pass 在
  参数不变的帧里根本不存在，那些帧也必须有全局绑定；而 `SetGlobalTextureAfterPass`
  允许一个 pass 为它**读**的资源设全局，正好用上。
- **`SetGlobalTextureAfterPass` 的第一个参数是 `in` 而不是 `ref`**。反射把 `in` 显示成
  `ref`，照着反射结果加 `ref` 会 CS1615。
- **`view.Bind` 在 execute 里写全局 cbuffer，所以 SkyView pass 必须
  `AllowGlobalStateModification(true)`**。
- **太阳方向取 `visibleLights[i].localToWorldMatrix.GetColumn(2)` 而不是 `light.transform`**：
  `VisibleLight.light` 在某些剔除路径下为 null。
- **环境光/反射若设为 `AmbientMode.Skybox`，会把太阳圆盘一起卷积进去**。
  EV100=15 下圆盘约 41000 渲染单位、占球面 1.8e-5，平均贡献 ~0.74，而天空平均只有 ~0.1
  —— 环境光会偏亮约 8 倍。所以 `VistaSkySetup` **故意不动** `ambientMode`，
  只在日志里警告。漫反射那条链路已由 Task #5a 的 SH 投影（不含圆盘）接管
  （`AmbientMode.Custom` + `RenderSettings.ambientProbe`）；反射链路见 #5b。
- **`RTHandles.Alloc` 的 3D 重载必须显式给 `dimension: TextureDimension.Tex3D`**。
  少写这个参数会命中 2D 的便捷重载，静默分配出一张 Tex2D，于是 `RWTexture3D` 绑定失败，
  而**唯一的提示是 Editor.log 里一行 warning**，Console 里什么都没有。
  症状是 AP 表读回来全 0 —— 看起来像 kernel 没跑，其实是绑定就没成。
- **3D 纹理不能用 `RenderTexture.active` + `ReadPixels` 读回**：那套 API 是 2D-only，
  在 3D RT 上**静默只读到 slice 0**，不报错。自检里所有 3D 读回都走
  `Graphics.CopyTexture(src, z, 0, tmp2D, 0, 0)` 逐片拷。
  这个坑的恶劣之处在于"读到的数据看着完全合理"，只是每一片都一样。
- **往 fp16 表里塞诊断数值要先换单位**。相机海拔以 km 存是 9.766e-6，落进 fp16 的
  次正规区，读回来是 0，与"真的为 0"分不开。诊断通道统一换成 m 再写。
- 装 RendererFeature 要同步 `m_RendererFeatureMap`（URP 用其中的 `localFileId` 在引用丢失后
  重建），少写它的症状是重启 Editor 后 feature 变 `None`。`localId` 在
  `AssetDatabase.AddObjectToAsset` 之后立刻可取，不用等 `SaveAssets`。
- **`VistaEarthShadow` 的偏置方向反了，代价是全场亮度低 6~9%，而症状伪装成"求积不准"。**
  这是这个模块最贵的一个坑，完整记一下，因为它示范了"看起来像精度问题的东西其实是
  参数化问题"，以及为什么该停止推理、去加观测。
  - 症状：AP 的一次行进循环与共享积分器的 256 步参考解，在最近的切片上差 **24%**。
    远处切片全部对得上（< 1%）。
  - 三个**猜错**的方向（都很像，都不是）：① AP 的行进循环与共享积分器不等价 ——
    实测差 0.30%，等价；② `c = dot(oc,oc) − Rb²` 在量级 4e7 上算，fp32 那里的间隔是 4，
    所以 c 的符号是噪声、`VistaRaySphereIntersectNearest` 返回一个 ~0.01 km 的伪根
    把 `tMax` 钳掉 —— 实测 `tBottom = −1`，没有伪根；③ `(S − S·T)/σ` 在极小 dt 下
    灾难性抵消 —— 量级上界约 1%，远不够。
  - 真正解决问题的一步不是继续推理，而是注意到**参考解自己就不是线性的**：
    `3.553/0.020 = 177.6` 而 `7.234/0.0333 = 217`。在 20~33 m 的路径上
    `VistaEvaluateScatterSample` 只随 r、muSun 变化，实质是常数，被积函数**必须**线性。
    这把问题从"哪个积分器错了"改写成"为什么被积函数不是常数"，于是加了三个观测：
    区间（`tBottom` / `tTop` / 海拔 / `up·ray`）、`earthShadow` 在阶跃两侧的取值、
    以及线性探针 `ref(0.04)/ref(0.02)`。
  - 读数直接指出原因：相机海拔 **9.766 m**、`up·ray 0.01802`、
    **`earthShadow @3 m = 0` 而 `@1 km = 1`**、线性比值 2.553。
  - 根因：`VistaEarthShadow` 把遮挡球的球心沿 `+up` 推了 10 m，即**朝采样点推近**，
    于是 `c = (r − offset)² − Rb² < 0` —— 海拔不足 10 m 的采样点一律被判成
    "在星球内部"，直射项被整体归零。UE 的 SkyAtmosphere 就是这么写的，在那里不显眼
    （近处切片本就贡献极小），但本项目的自检把它照出来了：相机在 9.766 m、
    中心视线每 km 爬 18 m，阶跃落在 `t = (10 − 9.766)/18.02 = 0.0130 km`，
    而 AP 循环的第 0 片节点正好在 `t = 0.003` 和 `t = 0.013`。
    **两套求积在阶跃两侧取节点，差出几十个百分点是必然的 —— 两个积分器都没错。**
  - 修法：球心改推 `−up`，即**推远**，于是 `c = (r + offset)² − Rb² > 0` 恒成立，
    判定退化成物理上正确的"太阳是否在当地地平线以下"。10 m 的容差同时还兜住 fp32：
    r 在 6360 km 上的 ulp 是 0.49 m，海拔算出负值也不会误判
    （顺便解释了为什么实测海拔读到 9.766 而不是 10 —— 差 20 个 ulp）。
  - 修完每一项预测都对上了：`earthShadow @3 m` 0→1、线性比值 2.553→2.022、
    Log errCenter 23.99%→1.19%、Log errMid 19.96%→1.13%、Power errCenter 5.89%→0.30%。
    天空基线整体上移（正午地平线 11316→12053，日落峰值 49728→54304），
    而**天顶值一个都没动** —— 天顶视线不经过贴地那 10 m，正是该有的签名。
- **共享积分器的均匀分支只覆盖了 `tMax·(N−0.7)/N` 的路径**。
  `tNew = tMax·(i + 0.3)/N` 配 `dt = tNew − t`，最后一段止于 `tMax·(N−0.7)/N`。
  N=256 时短 0.27%（无所谓），N=2 时只覆盖 65%（严重）。
  目前所有走这条分支的调用都用高步数，所以没有实际影响，
  但**低步数分级（Task #7 的移动端档）不能直接复用这个分支**，需要改成
  末段补齐或换成 variableSampleCount 那条（它的 t1 有 `> 1.0 → tMax` 的兜底）。
- **`SphericalHarmonicsL2` 的归一化约定文档没写全，必须实测。**
  两个未知：这个类型存的是"辐射亮度 SH"还是"已与余弦瓣卷积过的辐照度 SH"
  （相差逐阶的 `Â_l` = π、2π/3、π/4），以及基函数常数 `Ŷ_i`（`Y00 = 0.2820948` 等）
  有没有折进去。猜错的症状是环境光整体亮/暗约 3 倍 —— **在任何单一场景里都像
  "美术没调好"**，不会被当成 bug。
  - 第一次尝试是从 `AddDirectionalLight` 的打印结果反解，错了：那样有两个未知量、
    一个方程，而且我当时以为 +y 方向光的 L2 项为 0（并不是）。
    **正确做法是停止推理、去构造观测**：一次只把一个系数置 1，在一个 9 个基全非零的
    通用方向上求值，权重就直接量出来了 —— 完全不需要知道 Unity 内部折了哪些常数。
  - 实测：`k_i` 恰好等于 `1/Ŷ_i`，即 `Evaluate` 用的是**未归一化多项式基**
    `{1, y, z, x, xy, yz, 3z²−1, xz, x²−y²}`（就是 `ShadeSH9` / `unity_SHAr` 那套形态）；
    且 `AddAmbientLight(white)` 后 `Evaluate` 各向恒为 1，说明它返回的是
    **albedo=1 的出射亮度**（辐照度/π）。
  - 于是写入公式是 `c_i = (Â_l/π)·Ŷ_i·L_i`。这三项实测都写成了断言而不是打印：
    Unity 哪天改约定必须在自检里炸，而不是等到看图。
- **RenderGraph 里没有 `SetGlobalBufferAfterPass`。** 纹理有 `SetGlobalTextureAfterPass`，
  buffer 没有对应物（`Runtime/RenderGraph` 整个目录里 "SetGlobalBuffer" 零命中，
  它只存在于 CommandBuffer 包装层：`ComputeCommandBuffer` 上四个重载）。
  所以 buffer 全局只能在 render func 里手动绑，且该 pass 必须
  `AllowGlobalStateModification(true)` 才能过 `ThrowIfGlobalStateNotAllowed()`。
  `BufferHandle → GraphicsBuffer` 的隐式转换要查 `RenderGraphResourceRegistry.current`，
  只有在 execute 阶段才有效 —— 正好也只能在那里绑。
- **compute pass 里绑 `RWStructuredBuffer` 用 `UseBuffer`，不是 `UseBufferRandomAccess`。**
  读实现才分得清：`UseBuffer` 就是 `UseResource(handle, flags)`；
  `UseBufferRandomAccess` 在此之上多调一次 `SetRandomWriteResourceRaw(h, index, ...)`，
  那是给"**光栅** pass 里用 u# 寄存器写 UAV"准备的（对应旧的
  `Graphics.SetRandomWriteTarget`）。compute kernel 是按名字绑的，多绑一次随机写目标
  只会白占一个 UAV 槽。两个都能跑，所以只能靠读源码区分。
- **`RequestAsyncReadback` 不在 `ComputeCommandBuffer` 上**，只声明在
  `IUnsafeCommandBuffer`（四个重载）。想在 compute pass 里发读回是走不通的 ——
  这正好推向"读回在记录期用 `AsyncGPUReadback.Request` 发"那条更简单的路（见「取舍」）。
- **均值恒等式只在两侧用*同一个方向集*时才精确成立。**
  第一版参考解用 4096 样本、投影用 1024，恒等式在日落红通道差 **2.75%** ——
  那不是 bug，是 1024 与 4096 在"地平线几度宽的高对比亮带"上的求积差异。
  一个只在 N→∞ 才成立的等式没法卡 1e-3 的阈值。
  修法是给参考核加**第二个**均值组，用与投影完全相同的 1024 方向集：
  于是两边逐项对应、只差归约顺序，实测降到 1e-7。
  被丢掉的那 2.75% 反而成了有用的东西 —— 它就是采样数的收敛性度量（见「取舍」末条）。
  参考核的输出布局做成**自描述**的（法线也一并写出、均值组的法线写零向量做标记），
  C# 侧就不必镜像一份法线定义；镜像的那份迟早与 shader 走歧，届时自检会拿错法线去比，
  报出的偏差既不是 0 也不是明显错误 —— 最难查的那种失败。
- **Dispose 顺序：先清读回、再放 buffer。** `VistaSkyAmbientProbe.Dispose` 里会
  `WaitForCompletion`，反过来就是让在飞的读回请求从已释放的显存里搬数据。
- **白天环境光的 `c_1`（y 的系数）是**负**的，这不是符号错。** 第一眼一定会怀疑
  ——"天上亮、地上暗，y 矩怎么会是负的"。实测 Demo 场景 `c_1 = (−3359, −2331, −417)`，
  而按法线重建出来是 上 (1444, 2721, 5400) / 下 (8163, 7383, 6234)：
  **朝下确实比朝上亮 5.7 倍（红通道）**。原因是天顶恰好是全天最暗的地方
  （正午 R 仅 ~943 cd/m²），而天底吃的是被太阳直射的地面反弹
  （≈ `albedo·E☉·sin(elev)·T/π`，正午在数千量级）。
  通道比例同时印证：`|R| > |G| >> |B|` —— 蓝天（上）与暖地面（下）在 y 矩上几乎抵消，
  所以蓝通道的 y 矩只剩 −417。把这三向的重建值固定打进诊断里，
  就是为了不必每次重新推一遍。
- **逐帧写 `RenderSettings.ambientProbe` / `ambientMode` 不会把场景标脏**（实测 Demo
  场景连续渲染后 `isDirty` 仍为 `False`）。所以不需要给导出加"仅在值变化时写"的门控。
  这条是写之前担心过、实测证伪的 —— 记下来免得以后又去加没用的门控。

- **Unity 不允许把 Cube RT 绑到 compute 的 `RWTexture2DArray`。** 报错：
  `Property (_VistaSkyReflectionRW) at kernel index (0) has mismatching output texture
  dimension (expected 5, got 4)`（5 = Tex2DArray = HLSL 声明，4 = Cube = 绑上来的 RT）。
  硬件层面 cube 的 UAV view **就是** 2D array view，所以这纯粹是引擎侧的校验，
  不是能力限制。落地方案：compute 写一张 6 层 × 7 级 mip 的 `Tex2DArray` 中转纹理，
  dispatch 完逐面 `CopyTexture` 搬进 cube，代价 6×64²×7级 fp16 ≈ **0.4 MB**。
  另一条路是照 HDRP `IBLFilterGGX` 改成光栅（`SetRenderAttachment` 带
  `mipLevel`/`depthSlice`），那要重写 GGX 积分核；中转纹理让 GGX 积分、
  mip↔粗糙度反函数、逐面方向约定这三处最贵最容易错的东西一个字都不用动。
  顺带**加强**了自检：判据 ① 现在同时验证 `element → CubemapFace` 的映射，
  搬错面的症状是那一面单独炸红，而不是"六面都对但整体转了 90°"这种要看图才发现的错。
  - 先前我写下过一条"core / URP 里没有 compute 写 cubemap 的先例"，
    当时被自己纠正为"分配有先例、绑定没有" —— 这次的报错正好证实了那个拆分：
    `Runtime/PathTracing/Environment/CubemapRender.cs:152-160` 确实
    `dimension = Cube` + `enableRandomWrite = true`，但它自己是用
    `SetRenderTarget(new RenderTargetIdentifier(tex, 0, (CubemapFace)i))` 逐面**光栅**写的，
    从没把它当 compute UAV 绑过。**`enableRandomWrite` 分配得下来 ≠ 绑得上去。**
- **`CopyTexture` 只存在于 `IUnsafeCommandBuffer`**（`IUnsafeCommandBuffer.cs:430-464`），
  `ComputeCommandBuffer` / `RasterCommandBuffer` 上都没有。所以 RenderGraph 侧的
  拷贝必须是 `AddUnsafePass` + `CommandBufferHelpers.GetNativeCommandBuffer(ctx.cmd)`。
  这与 `RequestAsyncReadback`、`SetGlobalBuffer` 是同一类不对称 —— 判断一个 API
  在 RenderGraph 里能不能用，看的是它挂在哪个 command buffer 接口上，不是它在
  原生 `CommandBuffer` 上存不存在。
  - 要用的重载是 `CopyTexture(src, srcElement, dst, dstElement)`（:440），
    它一次搬一个 element 的**全部 mip** —— 所以是 **6 次调用，不是 42 次**。
  - 立即模式（Editor 自检 / 预览）不需要拆：原生 `CommandBuffer` 的状态转换由图形层
    自动插，dispatch 与 copy 录在同一条 cmd 里是安全的。所以一个
    `CopySkyReflectionToCube(CommandBuffer)` 同时服务两条路径。
- **`RTHandles.Alloc` 的 `slices` 对 Cube 必须是 1，对 Tex2DArray 才是层数。**
  `RTHandleSystem.cs:881` 把 `slices` 直接转成 `RenderTextureDescriptor.volumeDepth`；
  cube 的六面来自 `dimension`，再给 `slices: 6` 就是要一个 6×6 面的东西。
  这次一张 cube（`slices: 1`）+ 一张 array（`slices: 6`）并排放着，正好是对照。
- **FXC：`GroupMemoryBarrierWithGroupSync` 不能位于任何线程相关 `return` 的下游 ——
  即使那个条件事实上是组内常量。** 自检核最自然的写法是
  `if (group == MEAN_GROUP) { ...; return; }` 三段并列，FXC 直接拒绝：
  `thread sync operation must be in non-varying flow control`。`SV_GroupID` 在组内明明
  是常量，但 FXC 的判据是**语法上**的。而且这不算保守过度：DXIL 层面 barrier 要求
  整组到齐，提前退出的线程永远到不了。
  修法是各段判据顺序排开、barrier 一律留在顶层无条件流里，让**九个组**全部走完全部
  barrier —— 出结果的组由 `if (group == ...) && tid == 0u` 决定**写不写**，
  而不是决定**走不走**。代价是每组多跑两趟归约，自检核不值一提。
  （备选是拆三个 kernel，但那样 C# 侧要维护三次 dispatch 与三套绑定，
  而这三条判据是互相印证的，拆开反而弱化了它。）
  这条规则在 wide 滤波核上有个反过来的推论：**它的 dispatch 必须精确覆盖纹素数**，
  所以核内一个越界检查都不能有 —— 有 `if (id >= count) return;` 的话下游的归约 barrier
  就非法了。也就是说"派发规模刚好等于工作量"在这里不是省一点开销的优化，
  而是 barrier 合法性的前提。反过来，barrier 出现在**字面常量**上界的循环里是合法的，
  出现在线程相关的 `if`（不带 `return`）之后也是合法的 —— FXC 只卡 `return`。
  另外两个核的线程数用 `#error` 绑在一起：改了 wide 核却没改自检，
  判据 ④a 会退化成"用 64 段去验 32 段"，仍然全绿，但验的不是同一件事。
- **HLSL 的 `out` 数组 copy-out 语义会打乱 FXC 在 `if` 里的初始化跟踪。**
  共享积分函数第一版用 `out float3 sum[N]`，在条件分支里赋值后 FXC 报
  "use of potentially uninitialized variable"。换成返回一个 `struct` 就干净了。
  （`SkyReflection.compute:157` 那条同名警告是**另一回事**、且是刻意留的：
  `VistaSkyReflectionRadiance` 的两个来源分支 FXC 判不出必有其一，两个滤波核各报一次；
  自检核不报，因为它的来源是字面常量。）
- **`#pragma only_renderers` 没有 `d3d12` 这个 token。** D3D12 走的就是 `d3d11` 的
  编译目标，写上只换来一条 `Unrecognized renderer` 警告。三个文件都清掉了
  （`SkyReflection.compute`、`AtmosphereLut.compute:23`、`VistaSky.shader:40`）。
- **自检输出缓冲的「行基址」不要用组号做算术。** 第一版写的是 `MEAN_GROUP + i` /
  `MIP_GROUP + 3 + m`，看着简洁，实际在第 9 行留了个洞，而 C# 侧按"紧凑排列"去读
  就整段错位。**行基址与组号是两件不同的事，别复用同一个数** ——
  现在 HLSL 与 C# 两侧各有一份显式的 `ROW_*` / `k_ReflVerifyRow*` 常量。
- **`unity_SpecCube0_HDR` 的残余风险已实测排除：`(1, 1, 0, 0)`**，
  也就是 `DecodeHDREnvironment` 是恒等 —— float Cube RenderTexture 经
  `customReflectionTexture` 挂上去时引擎不会塞一个解码系数。
  这条原本列为"待 Task #6 用一个 roughness-0 的球对着背后天空验"的开放风险
  （症状是"反射整体亮度差一个常数倍"），现在由 `Log Sky Reflection State` 直接读出
  全局量解决了 —— 比看图判断可靠，也省掉了 Task #6 的一项。
- **`filterMode` 给中转纹理留 `Trilinear` 而不是 `Point`。** 它不会被采样，
  滤波模式在功能上无关紧要；但它是 RenderDoc / Frame Debugger 里排查反射问题的第一站，
  预览图跟着 cube 的滤波模式走比较不容易看错。
- **cube 上取掉了 `enableRandomWrite`。** 它现在只是 `CopyTexture` 的目标 + SRV，
  开着也能跑。取掉是为了不让"这张图是 compute 直接写的"这个**已经被验伪**的假设
  留在代码里 —— 下一个人（包括三个月后的我）会照它去 debug。
- **在 `file:` 引用的本地 package 里新建文件，Unity 不会自动发现。**
  `refresh_unity(mode: if_dirty)` 返回 `refresh_triggered: false` 就过去了，
  编译看着成功、控制台干净，但新的 `[MenuItem]` 根本没注册（菜单执行报
  "might be invalid, disabled, or context-dependent"）。要 `mode: force` + `scope: all`。
  这个失败形态很坑，因为它**看起来像代码写错了**而不是像资产没导入。
- **`GraphicsFence` 在 Edit 模式 + D3D11 下不可用，而能力位说它可用。**
  `SystemInfo.supportsGraphicsFence` 返回 `True`，但 `fence.passed` 永远不翻 ——
  CPU 侧轮询 fence 要靠 Editor 推动提交，自旋把主线程占满就互相锁死。
  计时器因此改成开跑前**探测**（≤250 ms），探测不过就退回 `GraphicsBuffer.GetData()`
  的硬同步，并把这件事印在报告头上。教训是一句可以直接复用的话：
  **能力查询 ≠ 该原语在当前线程模型下可用。**
- **`ProfilerRecorder` 抓 `ProfilerMarker` 必须给 `SumAllSamplesInFrame`，
  而"逐样本"这个默认口径根本不支持。** 不给的话 `StartNew` 直接抛
  `NotSupportedException: ... only with SumAllSamplesInFrame or CollectOnlyOnCurrentThread`。
  另一个选项在这里没用：RenderGraph 的 pass marker 记在**渲染线程**上，
  从主线程按"当前线程"收集会是空的。于是口径被 API 强制成"一个样本 = 一帧的总和"——
  这不是我选的，是唯一能跑的那条。附带后果是多相机会被求和（见「取舍」）。
  两个次级坑：① 我凭印象写的 `WrapAroundBuffer` 不存在，真名是
  `WrapAroundWhenCapacityReached`（枚举成员表用 `unity_reflect` 查出来的，
  不查就是三处 CS0117）；② 第一版没 fail-fast，异常在 `s_Started = true` 之前抛，
  于是每个 Editor tick 重试一次，控制台被同一条填满，真正有用的信息
  （第一条的类型与位置）被埋在几十条重复里 —— 起 recorder 失败是**配置错**，
  重试不会好，必须立刻收摊并退出 play。
- **`ProfilerRecorderSample` 除了 `Value` 还有 `Count`。** 我原来为了知道"这一帧同名
  marker 出现了几次"，打算再起一组不带 `SumAllSamplesInFrame` 的 recorder 去做
  sum/single 的比值 —— 而 `Count` 就是被求和的样本个数，一个字段解决的事不该用两倍
  采样开销去换。查一遍结构体成员比设计一个绕路的机制便宜得多。
- **"期望缺席"被判成失败，比漏判更糟。** 第一版报告对七个 pass 一视同仁，
  两张静态表在 300 帧里 0 样本被打成"pass 名走歧 / pass 被剪"。工具对着正确行为报警，
  代价是人学会忽略它的警告；判据的价值全在"红了一定有事"。修法见「取舍」里
  静态表那条：方向掉过来判，缺席=通过，有样本=报警。
- **"改了一条没有任何判据覆盖的路径，而自检仍然全绿"——最贵的一种假通过。**
  mip1~6 从 narrow 核换到 wide 核之后，自检四条判据（当时是三条）全绿、
  而且数字**逐位不变**。原因是判据 ① 与 ② 都采 `LOD 0.0`，也就是全落在 narrow 核上；
  我当时以为"判据 ② 会立刻抓住归约写错"，错了 —— 整球均值也只采 LOD 0。
  补的两条刻意分工：④a 纯算术（同方向同 K，分摊切法 vs 顺序算完，不碰纹理，
  红了锅一定在求和）、④b 纯布线（wide 产出的 mip2 走硬件采样 round-trip，
  覆盖 groupId → (纹素, 面) 映射、UAV 写入、逐面 `CopyTexture`）。
  一条判据只该有一个失败方向，否则红了还要再查一轮"是算错还是写错"。
- **判定文本不能硬编在证据行里 —— 修法生效之后它会继续断言旧病因。**
  计时器的证据 2 原先无条件打印"一次纹理取样的延迟，完全没被隐藏"。归因阶段这句是对的
  （295 ns/迭代），wide 核落地之后是 32 ns/迭代，这句就成了**假断言**，而报告照旧绿着。
  三条证据全部改成从实测值算三分判定，并留一条显式的**"这一条不做判定"**带 ——
  没有那条中间带，阈值附近的抖动会被强行归到某一侧，比不判定更糟。
  同一批还清掉了一个更早的同类问题：预先注册分类（"若 A 主导则修 X，若 B 主导则修 Y"）
  然后把它当结论打印，等于让工具去确认作者的先验。
- **`read_console` 的 `count: 1` 返回的是**最旧**的匹配项，不是最新的。**
  只要一条报告的时候会拿到上一次运行的结果，而两次报告长得几乎一样，
  非常容易当成"改动没生效"。规矩：一律多要几条，并核对匹配数量。
- **收敛阶只分"光滑 vs 台阶"两态，会把每一次普通的过滤纹理采样判成缺陷。**
  banding 判据最初的设计是两态：p≈2 = 光滑（通过）、p≈0 = 台阶（失败）。
  漏掉的是中间那一档 —— 双线性重建在**纹素边界**上斜率不连续，跨界的三点二阶差分
  ≈ |Δf'|·Δ，阶恰好是 1。这是硬件双线性的固有行为，任何过滤纹理都有；
  两态判据会把它归到"台阶"那一侧。实测最坏方向的阶是 0.88，正落在这一档，
  两态版会把一个不需要修的东西报成失败。三态之后它的处置是**外推**（幅度 ∝Δ，
  按生产步长缩小后再比可见阈），而不是修。
- **两点算不出"平台"。** 判据 C 第一版只跑两个步长（0.005° / 0.0025°），
  得到 1.241e-3 / 8.577e-4、阶 0.53 —— 卡在 0 和 1 正中间，两种解释都说得通，
  下不了结论。原因是二阶差分里同时有 ∝Δ⁰（噪声）和 ∝Δ²（函数曲率）两项，
  两点只能读出**混合物的表观指数**。改成四档逐级减半后
  （1.241e-3 → 8.577e-4 → 1.068e-3 → 1.067e-3，末两档阶 0.00）平台自己出来了。
  教训：要判"某一项是否已经消失"，就得缩到它消失、看剩下的那个值停住，
  而不是拿两点去拟合一个自己假设的模型。
- **粗档抽稀会把极值整个跨过去，表现为"幅度反而变小"。** 判据 A 的 Δ=2° 档
  幅度 6.057e-2 < Δ=1° 的 6.573e-2 —— 粗档只有 46 个采样点，最坏那个太阳角
  根本没被采到。于是三点不成幂律。处置不是换数据，而是**说出来**：
  阶只取 1°→0.5°，2° 档降级为旁证，报告打 ⚠ 注明"三点幂律不成立不代表信号异常"。
  若不注明，下一个读报告的人（包括三个月后的我）会以为信号有问题。
- **步段解析积分照公式直写，在短步段上会灾难性相消 —— 而"短步段"是 AP 的常态。**
  `∫₀^dt S·e^{-σt} dt = S·(1-e^{-σ·dt})/σ`。σ·dt 掉到 1e-7 量级时，
  `exp(-σ·dt)` 与 1.0 之间只剩一两个 fp32 台阶，相减出来的几乎全是舍入噪声。
  地表 σ ≈ 1e-2 /km，步长短于 ~1 cm 就会踩到 —— 而 AP 首片天生只有几米长，
  Step 1 把 `tMax` 设成近处几何深度时（1 m 外的墙）也正是这个量级。
  #7 的 256 步参考解跑 1 m 路径时 σ·dt ≈ 4e-8，实测把 `errCenter` 抬到 **126%**。
  当时差点被判成"切片布得太近导致行进不准" —— 其实是**尺子自己坏了**。
  修法：σ·dt < 1e-4 时改用 `1 - x/2 + x²/6` 的截断展开（误差 < 4e-14，且整个
  不做除法），逐通道 `step` 选择（RGB 三向 σ 差 6 倍，同一步长可能一个通道相消
  一个不相消）。
- **同类的第二个"尺子偏置"：均匀步长分支把段长写成"相邻取样点之差"。**
  取样点在段内 30% 处（`VISTA_SAMPLE_SEGMENT_T`），段边界应当是 `[i·dt, (i+1)·dt]`。
  写成相邻取样点之差后，第一段只剩 0.3·dt、末段整个丢掉，总覆盖变成
  `tMax·(N−0.7)/N` —— N=256 时少积 0.27%。症状是 `errCenter` 在**全部 20 组**
  配置上都读到 0.25~0.29%，一个几乎不随配置变化的常数偏置，
  把切片布局本该显出的差别整个盖住。
  两次都是同一个反面模式：**把参考解自身的偏置当成被测对象的缺陷**。
  规矩：一个误差指标在所有配置上都给出同一个非零值时，先怀疑参考解。
- **AP LUT 的 pass 漏了 `view.Bind()`，只在换视角时才显形。**
  核读的是全局 view 常量，而上一个 pass 恰好绑的是同一组值，所以单视角自检永远绿。
  #7 加了第二个视角（海拔 1 km / 太阳 10°）之后立刻暴露：AP 用的是**第一个视角**的
  参数。这类 bug 的可发现性完全取决于自检有没有第二个状态 —— 单状态测不出
  "状态没被写进去"。
- **判定行的口径里藏着一条没有依据的偏好。** `ValidateApSliceBudget` 原先打印
  `定档 → d=16 Log 20m`，取的是"通过的候选里最省的那个"。但那套判据
  （1% Weber 可见阈）只能判**合格**；20 组全过时，"取最省的"就是一条藏在代码里的
  偏好，而当时并没有成本数字支撑它 —— 补测之后恰恰相反（省 0.012 ms 换掉 4 倍余量）。
  改成 `最省候选 →`（它回答的是"合格线能压到多低"），并另起一行判**出厂档**。
  同一批修掉的伪通过：判定原先挂在"扫描里有没有配置能过"上，那永远会绿
  （总有一档够精细），改坏默认值也照样绿。现在出厂档在扫描行里标 ★、单独判
  "合格 + 含余量"，且**不在网格里就直接失败**（改了默认值不同步网格会响）。
  具体毫秒数**不在自检代码里抄一份**：抄了就会在核变化后静默过期，
  自检只留口径与理由，数字留在成本侧的报告里。

- **URP 完全不消费 `Light.lightUnit`，物理灯单位是 HDRP 独有的。** URP 包里搜不到任何
  `LightUnit` 引用，所以 lux → `color × intensity` 的那一步只能由我们自己乘。
  这条决定了整个接缝的存在必要性；先假设"填 lux 就行"会得到一个暗 π 倍的画面。
- **URP 无条件打开 `lightsUseColorTemperature`，代价是 85.22% 的光色偏移。**
  `UniversalRenderPipeline.cs:449-450` 里 `lightsUseLinearIntensity` 还看色彩空间，
  但 `lightsUseColorTemperature = true` 是**无条件**的。于是引擎算
  `VisibleLight.finalColor` 时会在我们已经算好的物理色度上再乘一遍
  `CorrelatedColorTemperatureToRGB(colorTemperature)`。反例实测：手动开 3000K，
  端到端像素偏离 **85.22%** —— 而面板上完全看不出是这个开关干的。
  所以 `ApplyLightParams` 每次写灯都强制 `useColorTemperature = false`。
- **`Light.color` 按 Gamma 语义存，写线性值进去会暗 89.04%。** 引擎在
  `lightsUseLinearIntensity`（URP 在线性色彩空间下会打开）时算的是
  `finalColor = Light.color.linear × intensity`。我们手里的 `lp.color` 已经是线性色度，
  所以写进去之前必须先 `.gamma`，否则被 `.linear` 多转一次。反例实测偏离 **89.04%**。
  这两条（色温 + gamma）都是**先写了保护性代码、再各配一条反例测量**才敢留的：
  一行防御若在反例下也通过，说明它没有实质后果，应当删掉。
- **C# 的 `Color.gamma` 与引擎的 `.linear` 是精确互逆，不是 pow(2.2) 近似。**
  这一条是我**预先登记为风险**然后被实测推翻的：担心 C# 用分段 sRGB 而引擎用
  幂近似，往返会留下几个百分点的残差。实测往返误差 0.03%，两边用的是同一条分段曲线。
  把被推翻的假设也记下来，因为下一个读代码的人会有同样的担心。
- **逐通道判据必须是"同通道的相对 AND 绝对"，跨通道 OR 是一个假通过的洞。**
  低太阳时三通道的 T 差两三个数量级（0.5° 实测 0.168 / 0.022 / 0.0003）。
  第一版判据写成"最大相对误差 或 最大绝对误差"，于是出现**红通道的绝对量小去替蓝通道的
  相对量大担保** —— 两个数根本不来自同一个通道。这个坑在透射率对账那条自检里踩过一次，
  现在判据收进 `IsTransmittanceDifferenceVisible`，逐通道循环、同通道 AND。
- **尺子的条件数：同一个物理量，"求极值"和"求根"差三个数量级。**
  子午线对称性判据（D）要先定位太阳时正午。第一版用"最大化高度角"去找 ——
  那是个**平坦的二次极值**：float 的高度角分辨率 7.6e-6°、正午附近曲率 k ≈ 3.8 °/h²，
  代入 `Δt = sqrt(eps/k)` 只有约 5 s 精度；而方位角在正午附近以 ~38°/h 变化，
  5 s 的轴偏移直接变成 0.05° 的残差 —— 与判据阈值同量级。改成对时角 H = 0 二分：
  H 是单调的、15°/h，一次二分就到 float 底。**归因验证**：故意保留峰值搜索版本当尺子，
  它给出的轴偏差 8.6 s，按 15°/h 换算应引入 0.1808° 残差，实测残差 0.1811° —— 对上了。
  这是本项目第三次踩「把尺子自己的偏置当成被测对象的缺陷」，所以这次先量尺子。
- **"参考海拔差几百米应该没事"是错的，而且错在最关键的时段。**
  这是动手前的直觉，被 F2 那张表推翻：正午 271 m 尚可，太阳 5° 时只剩 51 m、1° 时 41 m。
  也就是说单值参考海拔**恰好在日出日落失效**，而那正是这套大气最想展示的时段。
  没有这条测量，Task #12 会被排在很后面甚至不做。
- **每条报警都要双向验：既要有让它亮的场景，也要有让它灭的场景。**
  只验亮会漏掉误报（在正常场景里常亮，用户学会忽略所有报警 —— 那时它就不再是判据了），
  只验灭会漏掉漏报（永不触发的 HelpBox 等于一行注释）。
  `Window/Vista/Validate TimeOfDay Diagnostics` 四组：A `sunMissing`、
  B `atmosphereMissing`、C 灯值一致性、D `useColorTemperature`。
  C 组的三步是"写灯后一致 → 只污染 intensity → 只污染 color"，
  两个通道**分开**污染是为了验出 AND 没被写成"一项通过就整体通过"；
  实测两次都在"抓到不一致"的同时确认另一项仍判一致。
- **覆盖不到的路径要说是覆盖不到，不能让它读起来像通过了一条强判据。**
  上面四组里有两组给不出反例：B 组的 `VistaAtmosphereFeature.current` 是 feature 在
  `Create()` / `Dispose()` 里设的静态量，Editor 侧没有合法途径把它翻成 null 再翻回来；
  A 组的正向格更是**物理上不可达**（下一条）。两组的标题和报告行都明写了这一点。
  这是 #7 记下的「未覆盖路径的假通过」的反面 —— 那次是自检全绿而一整条路径没有判据。
- **`RenderSettings.sun` 的 getter 有回落语义：字段为空时它返回场景里最亮的平行光。**
  A 组本来打算"清掉 `RenderSettings.sun` → `sunMissing` 应为 true"，稳定失败。
  第一反应是竞态 —— 场景那盏灯上也挂着 `VistaTimeOfDay` 且开着
  `AssignRenderSettingsSun`，而 `new GameObject` / `AddComponent` 会触发编辑器同步重绘、
  跑一遍 `ExecuteAlways` 的 `Update()`，把自己写回这个全局。于是把清空挪到 `Apply` 的
  **前一行**，中间不留任何会触发重绘的调用 —— **仍然失败**。中间只剩一次 `Apply`，
  不可能有别人插进来，所以竞态假设被自己的修法排除了。
  真正的原因是 getter 本身：写 null 后**立刻**读回，拿到的仍是场景那盏灯
  （procedural skybox 就是这样取太阳的）。产品结论：`ResolveSun` 的第三级在任何有平行光
  的场景里恒非空，`sunMissing == true` 只在"一盏平行光都没有"时出现 —— 而那也正是它该
  报警的时候。所以 A 组改成把 getter 的回落**测成观测值**，作为"正向格为什么不可达"的
  依据，而不是留一句断言；也没有为凑一格通过去硬造条件（禁用场景里的灯、开临时场景），
  那些手段本身就是风险源。
  两次失败都是靠**先加归因行再理论**定位的：第一版报告只说"期望 true 实得 false"，
  下一步只能靠猜；加上"测量后 `RenderSettings.sun` = ? / 自身 Light = ? / m_Sun = ?"
  之后，一次运行就把原因缩到一个具体对象上。
- **诊断旗子的时序错误比功能 bug 更坏。** `m_AtmosphereMissing` 原先算在
  `sun == null` 的提前返回**之后**，于是"既没灯又没 feature"时它会停在上一帧的值，
  面板照着过期状态报警 —— **它让人去查一个不存在的问题**。改成在任何提前返回之前算。
  这条修法给不出反例测量（理由同 B 组），所以自检里明写它只是一致性核对；
  能挡住的唯一失败模式是"走过无灯路径后旗子仍与 `current` 一致"。
- **fp16 的相对精度是尺度无关的，所以接缝自检不需要绝对可见性豁免，而透射率自检需要。**
  fp16 相对精度 2^-11 = 4.88e-4 不随数值大小变（实测地板 5.047e-4）。
  接缝测的是一个 O(1) 的比值，量级固定，纯相对判据就够；透射率在低太阳角下 T → 0，
  分母自己都快没了，纯相对判据会在那里永远判"不一致"，所以必须配一条绝对门 ——
  且必须是**同通道**的绝对门（见前面那条跨通道 OR 的坑）。
  同一个 Weber 1% 阈在两条自检里用法不同，不是不一致，是量级条件不同。

- **往 shader 里声明为 `Color` 的属性写值，`SetVector` 和 `SetColor` 一样会做 gamma → linear。**
  我在 AP 数值验收里写了注释断言反面（"`SetColor` 会转换，`SetVector` 原样写入"），
  第一次运行就被打脸：写 0.18 渲出来是 **0.0272 = `sRGBToLinear(0.18)`**。
  URP `Unlit.shader` 的 `_BaseColor` 带 `[MainColor]`、类型是 Color，转换发生在属性层，
  与用哪个 setter 无关。修法是显式写 `new Color(v,v,v,1f).gamma`。
  值得记的是**为什么能被发现**：黑和白是这个变换的两个不动点，
  只有中灰会暴露它 —— 而中灰那一列本来是为"独立预测靶"加的，抓到这个坑是白赚的。
  两列黑白的方案会一路全绿地把错误带下去。
- **一个错的注释可以造出一个"通过一切"的地板。** 上一条那个 0.849 的中灰偏差被喂进
  `measuredRel`，算出 `floor = 1.698E+000` —— 比后面每一个被判量都大，
  于是判据③④⑤⑥**全部空过**，报告还是绿的。
  修法是让 `measuredRel` 只由白和黑（sRGB 的两个不动点）算出，中灰那一列改成
  `greyUsable ∈ (0.02, 0.98)` 的合理性**报告**而不是断言 —— 它是被预测的量，
  给一条产出数据设通过门会让判据自我实现。修完地板落到 **9.760E-004**。
  这是「把尺子自己的偏置当成被测对象的缺陷」的镜像：这次是**尺子自己的偏置把被测对象洗白了**。
- **上游判据 `return false` 会把唯一能抓某个故障的下游判据挡在门外。**（新的反模式）
  "信号非空"那条原先写 `20 × k_RelTol = 0.2`（一个发明出来的倍数）然后 `return false`。
  注入"两趟混合顺序调换"时远端散射从 0.2375 掉到 0.0756，于是这条把
  **"有信号但被算错"误判成"没有信号"**并提前返回，而判据④存在的**唯一理由**就是抓这个故障。
  两处都改了：门槛回到全项目那把尺子（Weber 1%），提前返回换成一个 `vacuous` 旗子 +
  一行横幅 —— 信号太弱确实会让下游的**通过**没有意义，但下游的**失败**仍然是真信息，
  所以判据照跑完，只声明其中的 "OK" 不可采信。
- **URP 的 Unlit 也调 `MixFog`。** 布景想要一个"已知入射值"，就必须在测量期间强制
  `RenderSettings.fog = false` 并在 `finally` 里还原 —— 否则场景里随手开的雾会直接
  记进 T 的账上，而 Unlit 材质看起来"不该受雾影响"。
- **探针自带一盏平行光、挂在探针专属 layer 上，就不必去动全局 `RenderSettings.sun`。**
  `GetSunDirection` 优先取 URP 的主平行光；探针相机的 `cullingMask` 只留自己那一层，
  场景那盏灯于是被剔掉，探针这盏自动成为主光。
  这比"临时改全局再还原"稳得多 —— 上面那条 `RenderSettings.sun` getter 回落语义
  已经证明了那条路会失败。layer 是运行时扫全场 `Renderer` / `Light` 的占用位、
  从 31 往下找空位拿到的，不写死。
- **`ViewportPointToRay` 的 origin 在近裁剪面上，不在相机位置上。**
  按半径摆四边形时用它会在近端引入系统误差：近裁 0.1 m、目标距离 5 m 时是 2%，
  刚好落在 Weber 1% 阈的两倍上，看起来像"近端合成有偏差"。
  改成 `ViewportToWorldPoint(z=1)` 减相机位置再归一化。
- **前两个自检的隔离手法在这里都不能复用。** `VistaLightSeamSelfTest` 用 0.09 单位的
  薄近远裁剪面夹出一片平板，装不下 5 m → 45 km 的视锥；把布景挪到 `y = 1e5` 会让探针
  跑到 100 km 海拔、直接出大气层顶。所以这次用的是**层剔除 + 专属光**这条隔离，
  代价是要自己扫空闲 layer。手法不通用这件事本身写进了工具的类注释，
  免得下一个自检照抄一个装不下的方案。
- **拿坏点自己的离散度当判据的宽容度上界，判据就永远通过。** 反模式家族又添一例。
  性能 harness 第一版里，判据 ②（斜率折叠）的上界取的是**全部**点里最差的相对离散度。
  而 720p 那一档有个 ±520% 的坏点，于是上界被撑到 519.8% —— 任何极差都能过。
  一条不可能失败的判据不是判据。
  修法是先按 `k_MaxSpread = 0.25`（与 LUT profiler 那条"±25% 以上不引用"同一把尺子）
  把坏点判成不可用，再让上界只从**入选档位的可用点**里取。
  改完上界落到 16.0%，实测极差 9.6% —— 这才是一次真的通过。
  推论也一并落实了：被排除的档位既不进折叠、**也不给上界**，否则判据又松一层。
- **全局预热不能靠每项测量自己那 20 次。** 同一版里，720p 是全场第一次测量，
  于是它付了两个 pass 的 PSO 创建、RT 首次分配、以及 GPU 从低功耗档爬频。
  表现是它的「覆盖 0%」测出 **0.181 ms**（另外三档同位置折算是 0.026 ms/Mpx 量级），
  把最小二乘拽成 **−0.11 ms/Mpx 的负斜率**、"天空省下的比例"打印成 −601.5%。
  `VistaGpuTimer` 里每项各自的 20 次预热只够把**这一项**的命令流跑热，付不掉
  整场一次性成本。加了 1000 次的全局预热，并且把"第一趟 vs 第二趟"两个数都印出来 ——
  实测 **265.9 ms vs 112.6 ms（2.36×）**，那个比值就是这一层存在的实测理由；
  不印出来它看起来像多余的防御。预热之后 720p 的「覆盖 0%」降到 0.025 ms、
  斜率 0.0247，与另外三档折叠。
- **测量窗口不等长，相对离散度就不可横向比较 —— 该加长窗口，不是放宽容差。**
  预热修好之后 720p 仍被判不可用：它的「覆盖 25%」离散度 36% > 25%。
  但那一档的五个点本身是漂亮的直线（斜率 0.0249，与另外三档折叠得很好）——
  被判掉的不是被测对象，是**测量窗口太短**：720p 用 200 次时总窗口只有 ~6 ms，
  而另外三档是 21~26 ms，于是 Editor 自己的重绘抖动在它身上占比大得多。
  修法是把 720p 的 reps 提到 800（窗口 ~24 ms，与其余档位拉齐），
  而**不是**放宽 `k_MaxSpread` —— 放宽容差去让一档通过，就是上面那条反模式的翻版。
  同一条道理让判据 ④ 的两个 N 从 200/50 改成 400/100：本判据必须让 N 变，
  没法把窗口拉齐，但可以让两个窗口都够长。200/50 那一版的小 N 只有 ~5 ms 窗口，
  相对差 15.2% 擦着 15.7% 的上界过 —— 擦线通过等于没判；改成 400/100 后是 3.1% vs 8.4%。
- **渲染 → 读回路径有一条幅度 ±1/1024 的**加性**扰动场。**（本项目最贵的一次测量）
  `Vista/Lit` 累加自检连续报出最大相对误差 `6.595E-004`，八个关键字配置一模一样。
  那个量级与 fp16 相对精度（`2^-11 = 4.88E-004`）几乎相等，于是我把它当成
  "求值次序不同导致的 fp16 舍入"找了很久 —— 而它根本不在 shader 里。
  抓出来的办法是加一个 5 档：往 RT 里写死 `(0.25, 0.5, 0.75)` 这三个**精确的 half**，
  读回来逐像素低 1~2 个 ulp，最大偏离恰好 `9.766E-004 = 1/1024`，且**随像素位置变化**
  （`(0,0)` 与 `(64,64)` 不同）。写死的常量不可能"算错"，所以偏差只能来自路径。
  证明它是**加性**而不是相对的：把 0 档的 relError 在 shader 里 ×100 之后重跑，
  报数变成 `6.595E-006` —— 恰好是原来的 1/100。若那 6.6e-4 是被测对象的性质，
  它会随载荷一起放大；只有加性污染才会这样被除掉。
  **教训：尺子的地板与被测量的期望值同量级时，尺子会自己伪造一个结论。**
  而 relError 的期望值恰恰是 0，正踩在这条地板上。
  修法**不是**给判据放宽容差到地板（那会把 1e-3 量级的真实偏差一起放过），
  而是在**写出之前**把被测量抬到地板之上：×100 后地板折算回相对误差是 1e-5，
  比判定门（Weber 1%）与故障注入的 2% 都低三个数量级。代价只有一次乘法。
  机制未定（`FinalBlit` / dither / 格式转换 / `ReadPixels` 路径都有可能），
  但地板的**幅度**已经被量出来、并且每轮都被交叉验证，所以修法与机制无关。
- **拿两个上界做相对比较，判据必然报"尺子坏了"。**（新的反模式，这一轮踩了两次）
  地板量出来之后，归因链上那条恒等式判据
  `|relCpu − rel0| > 5%·rel0` 立刻开始报警：relCpu = `2.899E-010`、rel0 = `6.595E-006`,
  两个数**都是上界**而不是测量值，它们的差必然接近地板本身 —— 于是一条本该
  验证"3/4 档量的是不是同一件事"的判据，把"两侧完全一致"印成了"归因链断裂"。
  修法三条，缺一不可：① 在所有"拿 rel0 去比"的判据**之前**插入
  "两侧同时落在各自地板以下"这条**结论**分支（那是这台尺子能给出的最强结果，
  不是故障）；② 紧跟一条"只有一侧落地板"的**矛盾**分支（用 `&&` 与 `||` 把两者分开）；
  ③ 剩下那条相对容差里把两条地板作为**加性**项带上
  （`5%·rel0 + relFloor + NumFloor/den`）。
  一般规则：**地板一旦量出来，就必须作为一个具名量（`relFloor` / `NumFloor`）
  穿过每一条比较；任何两个操作数都可能落在地板以下的比较，必须先门控、不能靠放容差。**
  同一轮里还顺手把分母判据（`den ≥ 分母下限`）**上移**到所有数值解释之前 ——
  否则下游会去解释一个分母本身就错的商。
- **容差取 0，隐含了一条已被实测推翻的前提。**（同一族的第三例）
  5 档那条"读回值必须逐位等于 `(0.25,0.5,0.75)`"的守卫，前提是写→读回无损。
  地板量出来之后那个前提没了，于是它报 16384/16384 偏离 —— 一条**恒报**的守卫
  与一条恒不报的守卫一样，都不是守卫。
  但修法**也不能**顺手把容差放宽到实测地板 —— 那是拿被测现象给自己的门限背书。
  修法是**把一条守卫拆成硬判据 + 测量**：
  ① `misclassified` —— 最近邻分类，读数归到 `{0.25, 0.5, 0.75, 预清屏值, 0}`
  里最近的那个候选，门限由**候选之间的间距**（0.125，即地板的 128 倍）决定，
  与地板幅度无关，没有任何发明出来的容差；
  ② `maxDev` —— 一个**测量**，它反过来交叉验证 `k_ReadbackFloor` 这个常量。
  失败条件是"地板**变大**了"（变小不算失败，上界仍然成立）。
  于是 `k_ReadbackFloor` 从一个假设的常量变成一个每轮被验证的常量，
  判据 1 那个上界也因此站得住。实测：误判 0/16384、地板 1.00×。
- **读数接近 0 的档位，无法自证自己执行过。** 上一版 3 档写 `num×1e6`、5 档写死 0，
  两者在真实情形下都接近 0 —— 于是它们读回一个与 0 档相同的小数时，
  "else 分支没进"和"值确实很小"在数值上**完全不可分辨**，归因链在这里断掉，
  而我却先去怀疑读回路径。修法是给每一档加上"我是几档"的印记：3 档加偏置 0.25
  （读回 < 偏置一半 → 这一档根本没执行，别去解释那个数），
  5 档输出**三个互异**的常量（顺带还能查出通道错位）。
  1/2/4 档不加印记：它们的量级本身与 0 档差三个数量级，已经是印记了。
- **印记的偏置取大了会把分辨力吃掉。** 3 档偏置第一版取 8，`ulp(8) = 7.8e-3`，
  分子只能分辨到 `7.8e-9`；而读回路径那 2 个 ulp 的亏损换算成分子是 `−1.6e-8`,
  于是解码出一个**负的绝对差** —— 一个不可能的数，它本身就是分辨力不足的证据。
  改成 0.25（`ulp = 2.44e-4`，分子分辨到 `2.4e-10`），同时 0.25 仍远高于那条
  1e-3 的加性地板，"有没有执行"这个判断不受影响。
- **哨兵值与放大后的被测量撞车，方向恰好是最危险的那一侧。** 哨兵原取 100，
  是在 relError 未放大时定的；×100 之后 100 只相当于"100% 的偏差"，
  于是一次真实的严重不一致会被读成"这个像素没参与比对"——
  假通过，而不是假报警。改成 30000（远低于 half 上限 65504），
  RT 清屏色同步改掉。判定用 `!(m < gate)` 而不是 `m >= gate`，`NaN`/`inf` 同样落进哨兵一侧。
- **本轮无法失败的守卫，要在报告里点名说它无法失败。** `worstAll` 被钉在地板上之后，
  "两次渲染是不是同一幅画"那条守卫（`rel0 <= worstAll * 0.5`）本轮不可能触发 ——
  rel0 只能等于地板，不可能低于地板的一半。报告现在明写它是**惰性判据**、本轮
  不提供保障，并点明真正排除"换了画面"的是 1/2 档读回的 `8.422852E-001`：
  那是一个与地板无关、有结构的量，换了画面它会跟着变。
  一条读起来像保障、实际不可能失败的判据，比没有这条判据更坏。

### 待办（Task #6 验收时补）

- ~~LUT 七个 pass 的实测耗时（目标合计 < 0.3 ms）~~ —— **已完成**，数据见 Added 的
  计时器一节。当时对反射的估算是"0.4M 次取样 ≈ 0.03 ms"，实测第一版是 0.391 ms，
  差一个数量级：估算按取样率算，忽略了粗糙那几级每组只有一条线程、延迟完全裸露。
  拷贝那一条的担心（多一个 pass 边界的 barrier）没有成立，实测 0.030 ms。
- **模型 A 交叉验证：Play 模式 `ProfilerRecorder`（`ProfilerCategory.Render` +
  `GpuRecorder`）对七个 pass 名逐个取数** —— **已完成**，见 Added 的
  `VistaLutGpuRecorderCrossCheck` 一节。结论与预期一致（A/B = 1.21），
  并且意外地把"反射单 pass 不可引用"这条限制**解除**了。
- 合并 mip3~6 的 dispatch（per-dispatch 地板已占反射 pass 的 59%）。
  已定位、已估价（约 0.03 ms）、主动搁置，理由见「取舍」。
- SH 模式（移动端路径）的反射耗时**完全没测**。计时器目前只跑 LUT 模式，
  形状分界里 `!useSh` 那一半只有占用率论证、没有实测支撑。
- 太阳 0°→90° 扫描的 banding 数值判定 —— **已完成**，见 Added 的
  `VistaSkyBandingSelfTest` 一节。当时的截图判断（"是量化而非分辨率不足"）
  被数值证实了，但理由和当时想的不一样：分辨率那一项（判据 B，1.885e-3）本身也
  只在地板的 1.8 倍上，两者是同一个量级，说"是量化不是分辨率"其实分不开 ——
  真正的结论是**两项都已经小到看不见**（各差可见阈 5 倍以上）。
- ~~AP 的分辨率与切片分布定档~~ —— **已完成**（Task #7），依据见 Added 与「取舍」。
  当时以为"32³ 够不够、Power 还是 Log"必须等 Step 1 的合成才能判，这一点只对了一半：
  **合格与否**不用等 —— 把"看得见吗"换成 Weber 1% 可见阈、分母取日照下 albedo 0.3
  漫反射面（`0.3·120000/π ≈ 1.1e4 cd/m²`）之后，"远山有没有可见雾量台阶"
  就是一个可以现在算的数（分段 errMid / errMidT），不需要眼睛。
  真正等不了的是**该选谁**：判据只能判合格，20 组全过，所以选择落到了实测毫秒 +
  显存上。仍然挂着的两件事：
  - Log 优于 Power 的理由是"雾的密度梯度落在近场"，这条是前瞻性的，**未实测**。
    Step 3 接上后要用带雾场景重跑这套扫描，结论可以被推翻。
  - 移动档（d=16 Power k=3）的精度只在**晴空**下核过；它配的是解析高度雾那条实现，
    等那条落地后要单独核一遍。
- **逐像素大气透射率**（Task #12），依据是上面 F2 那张包线表：太阳 5° 时单值参考海拔的
  有效范围只有 51 m，而外景高差是数百到数千米。做法上对应 UE5 平行光的
  Per Pixel Atmosphere Transmittance：在不透明着色里把直射光乘上
  `T(着色点) / T(参考点)`。三条约束已经定了 ——
  ① 复用 GPU 侧那张 Transmittance LUT，只走一个 `VistaSampleTransmittanceToSun`，
  不新增第三份 T 实现；② 参考侧的 T 从 `VistaTimeOfDay.lastLightParams.transmittance`
  上传成全局，**不动 `Light.color` 那条接缝**（否则 0.97119 那条端到端验收就失效了）；
  ③ 与 Step 1「AP 折进不透明着色」那条实现放同一个 pass，避免为同一件事走两遍几何。
  验收方式是把 F2 那张表在开启后**重测一遍** —— 包线应当整体撑开到搜索上界。

## [0.1.0] - 2026-08-14

初始化。目标平台 PC（D3D12 / Vulkan），Unity 6000.4 + URP 17.4 RenderGraph。
