# Changelog

本文件同时充当技术 Breakdown 的时间线：每个模块落地时记录**参考来源**、**做的取舍**、
**踩的坑**、**性能数据**。作品集评审看的是这些，不是 feature 列表。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added

- package 骨架：`Vista.Runtime` / `Vista.Editor` asmdef，
  基于 `IRenderPipelineResources` 的 shader 资源容器（零手动赋值）。
- **大气 Transmittance LUT**（256×64，R16G16B16A16_SFloat，静态）。
  - 参考：Hillaire, *A Scalable and Production Ready Sky and Atmosphere Rendering Technique*,
    EGSR 2020；参数化沿用 Bruneton & Neyret 2008 / Bruneton 2017。
  - 介质模型：Rayleigh 指数剖面（标高 8 km，无吸收）+ Mie 指数剖面（标高 1.2 km，HG g=0.8）
    + 臭氧三角帐篷剖面（中心 25 km，半宽 15 km）。
  - Editor 自检工具 `Window/Vista/Validate Atmosphere LUTs`：读回三个闭式解已知的纹素比对。
    地面·天顶 (0.93799, 0.86523, 0.75928) vs 闭式解 (0.94040, 0.86766, 0.76235)，
    最大误差 0.00307；大气顶·天顶 = (1,1,1)；地面·切线 = (0.10681, 0.00961, 0.00005)。
- **共享 raymarch 积分器** `ShaderLibrary/AtmosphereScattering.hlsl`。
  MS LUT / SkyView LUT / AerialPerspective LUT / 体积雾 共用一个
  `VistaIntegrateScatteredLuminance`，差异全部收进 `VistaRaymarchSettings`。
  动机：单次散射的能量计算若在多处各写一遍，天空与雾会在地平线交界处出现一条缝，
  而这类不一致极难定位。
- **大气 Multi-Scattering LUT**（32×32，R16G16B16A16_SFloat，静态）。
  - 每个纹素一个线程组、组内 64 线程各跑一个球面方向（Marsaglia 均匀球面采样），
    groupshared 归约后用等比级数 `Σ = L₂ / (1 − f_ms)` 一次求出无限阶。
  - 自检：全表有限非负（最大 0.06525，量级符合各向同性相位 1/4π ≈ 0.0796）、
    沿 muSun 严格单调递增（回退 0）、太阳当顶·地面 (0.05191, 0.05573, 0.06464) 偏蓝、
    太阳在地平线下归零。
- **大气 Sky-View LUT**（192×108，R16G16B16A16_SFloat，逐帧）+ **物理天空盒**。
  - 参数化：uv.y ↔ viewZenithCos 以**地平线为分界**双段平方 warp（张角随相机高度变化，
    不是常量 π/2）；uv.x ↔ lightViewCos 平方 warp，uv.x=0 正对太阳。
  - 自检新增 15 项：正反映射 round-trip 最大 uv 误差 3.76e-4（阈值 = 半个纹素 2.6e-3）、
    两个方向的 warp 严格单调（回升 0）、四个端点 +1/−1/+1/−1。
  - 绝对亮度校验：正午天顶 (943.5, 1893, 4184) cd/m² 偏蓝、地平线比天顶亮
    12053 vs 1857、地平线被 Mie 洗白（B/R 1.19 vs 天顶 4.43）；
    **天底辐射对闭式解 `albedo·E☉·sin(elev)·T^(1/sin elev)/π` 偏差 0.4%**
    —— 这一项同时验证了绝对光度单位与曝光链路。
  - 日落（太阳 −2°）：日侧地平线 (52800, 14800, 1875) 红移 R>2B、
    Mie 前向峰日侧/背侧 21946 vs 2240、背侧中高空转蓝紫 (1031, 1259, 1692)、
    天顶比正午暗 5.5 倍。全表峰值 54304 cd/m²，未逼近 fp16 上限 65504。
    （以上数值是修完 `VistaEarthShadow` 偏置方向后的基线，比修之前整体高 6~9%，
    见「坑」里那一条；天顶值不变，因为天顶视线不经过贴地那 10 m。）
- **`VistaAtmospherePass` / `VistaAtmosphereFeature`**：RenderGraph-only 的逐帧调度，
  三张表拆成三个 compute pass（原因见「坑」）。静态表只在参数变化的帧排入。
- **`Window/Vista/Setup Sky (Material + Renderer Feature)`**：一键生成天空盒材质、
  挂到 `RenderSettings.skybox`、并把 feature 装进当前生效管线的所有 Renderer。
- **Aerial-Perspective froxel LUT**（32×32×32，R16G16B16A16_SFloat ×2，逐帧）。
  - 散射表存**累积散射亮度**、透射表存**彩色透射率**，两张分开而不是打包进一张：
    合成时是 `color * T + S`，两者的数值范围与插值行为都不同。
    散射表的 alpha 通道另存 `mean(T.rgb)` 的灰度透射率，供移动端一次采样路径用。
  - 深度切片支持 Log 与 Power(k) 两种分布，运行时可切（Task #6 的 A-B 输入）。
  - 一次行进、逐切片输出：每个线程负责一根 froxel 柱，沿视线只走一遍，
    在每个切片边界把当前累积量写出。步长 `clamp(ceil(segLen/0.25), 2, 16)`。
  - 自检新增 22 项。有限性/单调性/打包一致性：散射沿切片非减（回退 0）、
    透射率沿切片非增（回升 0）、`scatter.a` vs `mean(T.rgb)` 最大偏差 4.07e-4、
    峰值 8816 cd/m² 未逼近 fp16 上限。
  - **参数化 round-trip**：`w → 距离 → w` 最大 |Δw| 1.49e-7（Log）/ 1.19e-7（Power），
    `w = i/(depth−1)` 最大误差 4.57e-4，两端点与 `nearDistanceKm` / `maxDistanceKm` 一致，
    `texW` 半片对齐 0.01563 / 0.98438。
  - **对 256 步参考解的两项误差**，分别测两件不同的事（见下方「取舍」）：
    | 分布 | errCenter（行进循环） | errMid（切片分布） |
    | --- | --- | --- |
    | Log(near 20 m) | 1.19% @ 0.020 km | 0.31% @ 13.9 km |
    | Power(k=2) | 0.30% @ 0.133 km | 0.15% @ 23.4 km |
  - **区间/连续性回归探针**（不判定，只报数）：`tBottom`、`tTop`、相机海拔、`up·ray`、
    `earthShadow` 在 3 m 与 1 km 处的取值、以及线性探针 `ref(0.04)/ref(0.02) = 2.022`。
    这三组数是定位下方那个 24% 偏差的工具，留在自检里当回归网。
  - 灰度透射率近似的代价（移动端分级用）：中心柱最大通道偏差
    **3.8 km 6.1%，12.4 km 22.1%，32 km 71.2%**。
- **天空环境光 SH**（L2 / SH9，`StructuredBuffer<float4>` × 9，逐帧）。
  接管 Unity 的环境光链路，取代 `AmbientMode.Skybox`（后者会把太阳圆盘一起卷进去，
  见「坑」里那条 8 倍偏亮）。
  - 投影：从 **SkyView LUT** 采 **1024 个 Fibonacci 均匀球面方向**，
    一个线程组 64 线程跨步取样 + groupshared 树形归约，权重 `4π/N`。
    存的是**原始辐射亮度矩** `L_i`，余弦卷积留给消费端（理由见「取舍」）。
  - 两个出口，一份系数：**GPU** 侧 `_VistaSkyAmbientSh` 全局 buffer（零延迟，
    供 Step 3 雾与 Step 4 PRT relight 用）；**CPU** 侧 `AsyncGPUReadback` 27 个 float
    → `SphericalHarmonicsL2` + `AmbientMode.Custom`（容忍 2~3 帧延迟）。
  - `Runtime/Core/VistaSphericalHarmonics.cs`：基函数常数、Unity 槽位缩放表
    `k_RadianceToUnitySh`、以及带拒绝条件（长度不足 / DC 全非正 / 非有限值）的
    `TryConvertMomentsToProbe`。运行时与自检共用同一份，不许各抄一遍。
  - Editor 自检 `Window/Vista/Validate Ambient SH`，两节：
    - **Unity `SphericalHarmonicsL2` 约定标定**（3 项）。逐槽位置 1 实测 `Evaluate`
      的权重 `k_i` = 3.54491, 2.04665×3, 0.91529, 0.91529, 3.17066, 0.91529, 1.83058，
      `|k_i·Ŷ_i − 1|` 最大 **6.32e-8** → `Evaluate` 用的是**未归一化多项式基**
      `{1, y, z, x, xy, yz, 3z²−1, xz, x²−y²}`；`AddAmbientLight(white)` 的
      `c_0 = 1.000000` 且各向偏离 1 为 **0** → `Evaluate` 的语义是 albedo=1 的出射亮度
      （辐照度/π）；均匀天空 `L=1` 过缩放表回到 `c_0 = 1.000000`。
    - **SH 投影 vs 参考解**（4096 样本数值积分，8 个测试法线 + 2 个全天球均值组）。
      | | 正午 60° | 日落 3° |
      | --- | --- | --- |
      | `L_00` | (20247, 21893, 25385) | (2681.3, 2165.8, 2187.9) |
      | `c_0` | (5711.6, 6175.8, 7161.0) | (756.38, 610.95, 617.20) |
      | 均值恒等式偏差（GPU / CPU） | 1.36e-7 / 1.36e-7 | 8.29e-8 / 8.29e-8 |
      | GPU 重建 vs CPU 重建 | 8.15e-8 | 1.23e-7 |
      | 逐法线最大偏差（= L2 截断） | **2.17%**（天顶） | **31.25%**（朝下） |
      | 求积收敛 1024 vs 4096 | 0.01% | 2.75% |
      判定只用前三行（与截断无关），第四行在正午档判 5%、日落档只记录 ——
      理由展开在「取舍」里，那是这一节真正的设计内容。
  - Editor 诊断 `Window/Vista/Log Ambient Probe State`：不算任何东西，只报运行期
    实际状态（`ambientMode`、`c_0`、上/下/水平三向重建、场景脏标记）。
    与上面那个自检互补 —— 自检走立即模式验数学，这个验"RenderGraph 那条 pass 真的跑了、
    读回真的把系数灌进了 `RenderSettings`"。两者可以一绿一红，而只看自检报告看不出来。
    Demo 场景实测：`Custom`、`c_0 = (5120, 5551, 6458)`、
    上 (1444, 2721, 5400) / 下 (8163, 7383, 6234) / 水平 (5153, 5626, 6513)。

- **天空镜面反射 cubemap（#5b）**：64²、7 级 mip 的 Cube RenderTexture，
  挂到 `RenderSettings.customReflectionTexture`，于是 URP 的
  `GlossyEnvironmentReflection` 把它当 `unity_SpecCube0` 采 —— **不改任何材质 shader**。
  这是选"产出一张真 cubemap"而不是"塞一个自定义全局 + 改 shader"的全部理由。
  - `SkyReflection.compute` 的 `SkyReflectionFilter`：**每级 mip 直接从 SkyView LUT
    做 GGX 预积分**（Karis 2013 的 `PrefilterEnvMap` 累加，N = V = R，权重 NdotL）。
    七级各一趟 dispatch，级间零依赖。
  - 移动端同一个核，辐射来源换成 #5a 的那份 SH（`VISTA_SKY_REFLECTION_SRC_SH`），
    采样数降到 16。**仍然产出一张真 cubemap**，所以 `unity_SpecCube0` 这条路
    在两个平台上完全一致 —— 消费端不需要知道自己采的是哪一条路径来的图。
  - `mip → 感知粗糙度` 用 URP `PerceptualRoughnessToMipmapLevel` 的**解析反函数**
    `pr = (1.7 − sqrt(2.89 − 2.8·m/6)) / 1.4`，不是线性映射。
    实测七级：0 / 0.1024 / 0.2151 / **0.3424** / 0.4917 / 0.6814 / 1.0 ——
    mip3 是 0.342 而不是朴素线性的 0.5。
  - Editor 自检 `Window/Vista/Validate Sky Reflection`，四条判据全在 GPU 上算
    （C# 侧刻意不重算任何一条：重算就得再抄一遍 mip 映射与面方向约定，
    抄错的那一份与 shader 那份走歧时，报出来的偏差既不是 0 也不是明显错误）。
    Demo 场景实测：

      | | 正午 60° | 日落 3° |
      | --- | --- | --- |
      | ① 逐面 mip0 vs LUT（六面最大） | **0.00%** | **0.04%** |
      | ① 参与比较的取样点 | 64/面，空面 0/6 | 64/面，空面 0/6 |
      | ② cube 整球均值 | (5709.6, 6172.4, 7155.4) | (723.7, 602.5, 615.9) |
      | ② LUT 同方向均值 | (5711.6, 6175.8, 7161.0) | (756.4, 611.0, 617.2) |
      | ② SH 的 `L_00·Y00` | (5711.6, 6175.8, 7161.0) | (756.4, 611.0, 617.2) |
      | ② cube 离散化偏差 | **0.078%**（阈 0.5%） | **4.315%**（阈 6%） |
      | ② 跨模块偏差（LUT vs SH） | **1.36e-7** | **1.61e-7** |
      | ③ mip round-trip 最大 | 3.58e-7 | — |
      | ③ HLSL 常量 SIZE/MIPS/LOD_STEPS | 64 / 7 / 6 | — |
      | ④a wide 归约 vs 顺序（逐 mip 最大） | **6.8e-7**（mip4） | **7.2e-7**（mip6） |
      | ④a 权重列最大 / K↔分界对账 | 5.4e-7 / 一致 | 5.1e-7 / 一致 |
      | ④b wide mip2 硬件 round-trip | **0.09%**（阈 1%） | **0.08%** |
      | ④b 参与比较的内部纹素 | 96/96 | 96/96 |

    判据 ② 的第三列与 #5a 的 `c_0` 逐位一致（正午 (5711.6, 6175.8, 7161.0)），
    这不是巧合而是设计：两边用**同一个** 1024 点 Fibonacci 方向集
    （`VISTA_SKY_SH_SAMPLES` == `VISTA_SKY_REFL_VERIFY_MEAN_SAMPLES`），
    所以均值恒等式在有限样本下也精确成立 —— 1.36e-7 的偏差只来自两边归约顺序不同。
  - Editor 诊断 `Window/Vista/Log Sky Reflection State`：报运行期链路的实际状态。
    Demo 场景实测 `defaultReflectionMode = Custom`、
    `VistaSkyReflectionCube_64x64_Mips_R16G16B16A16_SFloat_Cube`、64²、7 级 mip、
    `filter = Trilinear`、**`unity_SpecCube0_HDR = (1, 1, 0, 0)`**、`isDirty = False`。
  - `SkyReflectionFilterWide`：第二个滤波核，64 线程协作算**一个**纹素
    （一条 lane 领若干样本 → 6 步树形归约 → lane 0 写出）。粗糙那几级每组只有一条
    线程跑几百次取样，是占用率灾难；这个核把并行度从"纹素数"换成"纹素数 × K"。
    形状分界 `K ≥ 64`（= wide 核线程数，每条 lane 至少一个样本），
    在 LUT 模式下即 mip ≥ 2；mip1（K=32）与 SH 模式（K=16 恒定）留在 narrow。
    归一化必须在归约**之后**做，不是逐 lane 做。
    `Hammersley2d(i, K)` 只依赖 `i` 与 `K`，所以按 lane 跨步取 `i` 得到的是
    **完全相同的样本集** —— 这是"改形状不改质量"这件事可判的前提。

- **LUT 链路耗时计时器（模型 B）**：`Window/Vista/Profile Atmosphere LUTs`。
  Edit 模式立即提交、N=200 次背靠背摊销、M=5 轮取**最小值**，报逐 pass / 整链 /
  逐 mip 归因 / 反射的「只绑不派」CPU 对照 / 三条计算式证据。
  RTX 3060 + D3D11 实测（固定开销 0.161 ms 已从每项扣除）：

    | | 改形状之前 | 现在 |
    | --- | --- | --- |
    | 稳态五 pass 整链 | 0.494 ms（±3%） | **0.170~0.198 ms（±2~4%）** ／阈 0.300 |
    | 全七 pass 整链 | — | 0.213 ms（±4%） |
    | 其中 Sky Reflection | 0.391 ms（79%） | 0.076~0.101 ms ⚠不可引用 |
    | 反射「只绑不派」 | 0.000 ms | 0.001 ms → CPU 占 ~1% |
    | 静态两表的净代价 | — | 0.044 ms |

  逐 mip 归因（`mipMask` 旋钮，只派某一级）是这套工具的主要产出：
  改形状**之前** mip3~6 合计吃掉 85% 的时间却只产出 510/32766 个纹素（1.6%），
  吞吐从 mip0 的 2.96 掉到 mip6 的 0.02 G样本/s，每次循环迭代暴露 295 ns ——
  正好是一次纹理取样往返，**延迟完全没被隐藏**。病因由此定为占用率而非取样总量，
  wide 核是照这个结论开的方。改完之后同一份报告给出 32 ns/迭代（延迟已藏住）、
  per-dispatch 地板占 59%（瓶颈换人了）。

- **帧内延迟交叉验证（模型 A）**：`Window/Vista/Cross-Check LUT Timing (Play Mode)`。
  自动进 play 模式、丢前 45 帧、对七个 pass 名各起一对 `ProfilerRecorder`
  （GPU 一份 + CPU 一份）采 300 帧、逐帧取最小值，然后自动退出并打报告。
  刻意**不放 MonoBehaviour 进场景**：`EditorApplication.update` 在 play 模式里照样每
  tick 触发，读 recorder 不需要在渲染线程上 —— 量性能的工具改被量的对象是这类 harness
  最容易犯的错。跨域重载的"这次是为了采样"用 `SessionState` 存（静态字段会被清）。
  RTX 3060 + D3D11 实测（Scene View + Game View 各渲染一次，下表已按出现次数 2 折算成单次）：

    | pass | 模型 A 单次 | 模型 B | 备注 |
    | --- | --- | --- | --- |
    | Sky-View | 0.036 | 0.059 ±18% | |
    | Sky Ambient SH | 0.010 | 0.016 ±24% | |
    | Sky Reflection | **0.093（±9%）** | 0.076 ⚠±132% | 模型 A 反而可引用 |
    | Sky Reflection Copy | 0.031 | 0.030 ±17% | |
    | Aerial Perspective | 0.036 | 0.035 ±18% | |
    | **稳态五 pass** | **0.206（中位 0.212）** | 0.170~0.198 | 阈 0.300 → 达标 |
    | Transmittance / Multi-Scattering | 300 帧 **0 样本** | 0.044（净） | 脏标记生效 |

  只判**方向**不判差值：两个模型量的不是同一件事，差多少没有先验；能判的是
  "A 不应该比 B 小"（帧内延迟 ≥ 允许重叠的吞吐下界）。实测 A/B = 1.21，方向对，
  差额就是 pass 边界 / barrier / 无重叠的代价。若某项 A < B，报告把它判成
  **工具错而不是性能事实**（marker 被合并 / 被剪 / 相机数不同）——
  这个方向单独判，是因为它指向的修法完全不同。
  **必须一起给出去的限制**：RenderGraph 的逐 pass marker 被
  `#if DEVELOPMENT_BUILD || UNITY_EDITOR` 包着（core `RenderGraph.cs:2868-2884`），
  Release 构建里这些 marker 不存在 —— 这套数字是 Editor 口径，不是发行版性能。
  没有绕过的办法，所以写在报告正文里，而不是留给引用的人自己发现。

- **`VistaSkyBandingSelfTest`（Editor，`Window/Vista/Validate Sky-View Banding`）**：
  Sky-View 表的带状（banding）数值判定。配套 `AtmosphereLut.compute` 的
  `SkyViewBandingSignature` 核。

  **测的是"采样之后"，不是表里的值。** 核只调 `VistaSampleSkyViewLut` ——
  天空盒运行时用的那一个入口，同一个 `sampler_LinearClamp`、同一套 `ParamsToUv`。
  这一条是整套判据成立的前提：表里的数值可以完全单调、完全平滑，而
  「硬件双线性 + 两段分支的参数化 warp + fp16 存储」这三样叠起来仍然能在画面上
  产出可见台阶。若改成在核里自己解析积分一遍，被测对象整个跑掉，而且报告会
  一路全绿（解析积分当然平滑）。所以这个核绑 `_VistaSkyViewLut`（SRV），
  **不绑 RW 名字**。

  **方向固定在世界空间。** 太阳仰角是被扫的自变量；如果观察方向也随太阳转，
  逐仰角的差分里就同时混进了"表变了"与"我在看别的地方"两种贡献，无法分离。
  固定之后每个方向的亮度是仰角的一元函数，差分才有意义。
  64 个方向 = 4 个仰角环（2°/10°/30°/70°）× 16 个方位，方位 0 正对太阳。

  **判据不拍阈值，测二阶差分的收敛阶。** 拍阈值等于把结论写进判据。
  归一化二阶差分 `S₂(Δ) = max_j |L(j+1) − 2L(j) + L(j−1)| / max(L(j), 下限)`
  随采样步长 Δ 的阶 `p = log₂(S₂(2Δ)/S₂(Δ))` 把三种机制自己分开：

  | p | 机制 | 要不要修 |
  |---|---|---|
  | ≈2 | 被采函数在这个轴上光滑，S₂ 就是真实物理曲率 | 不用，分辨率不是瓶颈 |
  | ≈1 | 双线性重建在**纹素边界**的斜率跳变，`S₂ ≈ f''·h·Δ` | 不用，这是纹理过滤的固有行为；幅度随 Δ 线性缩小，可外推 |
  | ≈0 | 真的 C⁰ 跳变（参数化分支 / 量化） | 看幅度是否高于地板 |

  中间那一档（p≈1）是这次设计里最容易漏掉的：只分"光滑 vs 台阶"两态的话，
  **每一次普通的过滤纹理采样都会被判成缺陷**。

  三条判据回答三个不同的问题：

  - **判据 A（时间轴）**：太阳 0°→90°、步长 0.5°，**181 次重烘**，每次采那 64 个方向。
    一次扫描按 stride 1/2/4 抽稀就得到 Δ = 0.5°/1°/2° 三条曲线 —— 少烘三分之二的表，
    而且三条严格同源，排除了"两次烘焙参数不一致"这种伪信号。
  - **判据 B（空间轴）**：96×54 / 192×108 / 384×216 各烘一次，同一个太阳（3° 日落档）、
    同一组方向。双线性插值误差是 O(h²)，所以
    `e_hi = |L(h) − L(h/2)| = C·h²·(1 − 1/4) = 0.75·E(h)` ⇒ **E(192×108) ≈ 1.333·e_hi**
    （Richardson 外推）。`e_lo/e_hi ≈ 4` 是这个外推能不能用的**门槛**，不是附赠信息：
    比值明显不到 4 就说明 O(h²) 前提不成立，那个系数随之失效，报告要判**不可引用**。
  - **判据 C（地板）**：在纹素内部走弧，步长逐档减半 0.005°→0.000625°。
    二阶差分里能出现的只有三类项：边界斜率跳变 ∝Δ¹、函数自身曲率 ∝Δ²、算术噪声 ∝Δ⁰。
    前两类都随 Δ 单调缩小，所以一直缩，**幅度停止下降的那个平台就是地板**。

  实测（RTX 3060 / D3D11，相机 (0,2,0)，192×108）：

  | 判据 | 关键数 | 阶 | 结论 |
  |---|---|---|---|
  | C 地板 | 1.241e-3 → 8.577e-4 → 1.068e-3 → **1.067e-3** | 0.53 / −0.32 / **0.00** | 平台确认；理论最坏 fp16 地板 4·2⁻¹¹ = 1.950e-3，实测/理论 = 0.55 |
  | A 时间轴 | 最坏 S₂(0.5°) = 3.563e-2 @ 仰角 2°/方位 180°（反日点，G 通道）；外推到 0.01°/帧 → **1.124e-3** | 0.88（64 方向中位 1.70） | 达标（阈 1e-2） |
  | B 空间轴 | e_lo 4.574e-3、e_hi 1.414e-3、比值 3.24 ⇒ **E(192×108) ≈ 1.885e-3** | 1.69（中位 2.05） | 达标，误差/阈 = 0.19 |

  **可见阈取 Weber 对比 1%**（大面积平滑渐变上的经典可觉差）。这是整套判据里
  **唯一**一个外部常数，取自感知阈，不是取自"跑出来的数字附近"。

  **A 为什么能用 0.5° 量却按 0.01° 判。** 生产环境的时间轴步长是 20 分钟一个昼夜循环、
  60 fps → 每帧 0.005°，取 0.01° 留一倍余量。直接在 0.01° 上量的话信号整个埋进 fp16
  地板里、什么都测不出来；所以用 0.5° 把信号顶出来，再按**实测**阶外推
  `ampProd = amp·(0.01/0.5)^p`。这一步是判据 A 真正的落地点 —— 阶不是学术装饰，
  它是外推系数本身。

  **实测暴露的两件事，都写进报告而不是藏起来：**
  1. A 的 Δ=2° 档幅度（6.057e-2）**反而小于** Δ=1°（6.573e-2）—— 粗档只有 46 个点，
     把极值整个跨过去了。所以阶只取 1°→0.5°，2° 档降级为旁证，报告打 ⚠ 说明
     "三点幂律不成立不代表信号异常"。
  2. A 外推后的 1.124e-3 与 C 的地板 1.067e-3 **基本重合**。这比"低于可见阈"是更强的
     结论：在生产步长下，时间轴上的台阶已经和 fp16 量化噪声不可区分，
     这条轴上**没有可再优化的余量**。反过来说，当年截图上看到的那圈极淡等值线，
     数值上确实是量化 —— 但 B 那一项（1.885e-3）也只在地板的 1.8 倍上，
     两者同一量级，所以"是量化不是分辨率"这个说法其实分不开；能说的是两项都看不见。

- **两条引擎出口的曝光级 + 场景全局状态的保存/还原/复位**
  （`VistaSkyAmbientProbe.Publish`、`VistaAtmospherePass.BindReflectionToRenderSettings`、
  `IVistaRenderSettingsClient` + `VistaRenderSettingsGuard`、
  `Window/Vista/Reset Scene Ambient & Reflection`）。
  起因是一句用户报告：「我的场景里的物体怎么没有阴影了」。阴影其实一直是对的
  —— 详细归因见「坑」的第一条。
  - 曝光在**交接点**乘，不进 SH 投影核、也不烘进 cubemap：GPU 侧
    `_VistaSkyAmbientSh` 与反射 cubemap 一律留在**绝对光度量**（cd/m²），
    因为自检的参考解、Step 3 的雾、Step 4 的 PRT relight 全部按绝对量对账；
    折进核里会让那些判据跟着 EV100 漂。
  - SH 那条按系数逐个乘（`SphericalHarmonicsL2` 反射查不到 `op_Multiply`，
    只有索引器，所以是 `[c, i]` 双层循环）；反射那条挂在
    `RenderSettings.reflectionIntensity` —— 那是引擎给 custom reflection 留的唯一乘子。
  - 三层生命周期：`Dispose`/`Teardown` 还原（挡"关掉 feature"）、
    `EditorSceneManager.sceneSaving` 前还原（挡"运行中 Ctrl+S 把 Custom 落盘"）、
    显式复位菜单（挡"基线本身就是脏的"，见「坑」）。每层挡的是不同的洞，
    少任何一层都会留下可落盘的路径。
  - 还原按**场景**校验（存 `Scene` 结构而不是 `scene.handle` —— Unity 6000.4 把
    handle 换成了 `SceneHandle`，到 int 的隐式转换已废弃）。拿 A 场景的原值去还原
    B 场景比不还原更糟：那是静默的数据破坏，而不还原至少留着一份可辨认的值。
  - 验收：Play 模式实测 `ambientMode = Custom`、
    `c_0 = (0.1302, 0.1412, 0.1643)` = `5119.606 × 2.5431e-5`（EV100=15），
    与绝对量口径精确对上；平行光 3.14159 对环境光 ~0.13，直接光是环境光的约 24 倍。
    守卫的端到端验证：实时状态 `defaultReflectionMode = Custom` +
    `reflectionIntensity = 2.54313e-5` + cubemap 已绑定时执行一次真实
    `SaveScene`，磁盘仍为 `m_AmbientMode: 0` / `m_DefaultReflectionMode: 0`。

- **AP 切片数 / 分布定档：误差-距离曲线 + 实测毫秒双侧判定**（Task #7）。
  自检里新增"切片预算扫描"：5 档切片数（16/24/32/48/64）× 4 种分布
  （Log near 20 m、Log near 100 m、Power k=2、Power k=3）共 20 组，
  每组在**两个视角**（贴地/太阳 60°，海拔 1 km/太阳 10° 落日）上各测一遍。
  - 三条判据，各自回答一个不同的问题：
    - `errCenter`（切片中心 vs 256 步参考解）—— **前置门**，阈 5%。它测的是行进循环
      本身准不准，与切片布在哪无关。它红了上面两条都没有意义，所以不是同级判据。
    - `errMid`（切片**中点**处 LUT 插值值 vs 参考解，相对该柱最远片的累积量）—— 阈 2%。
      中点是双线性插值误差最大的位置；分母取柱子总量而不是本片值，是因为本片值在近处
      趋于 0，相对误差会爆掉一个看不见的量。
    - `errMidT`（同上但相对**透射率自身**）—— 阈 1%。必须单列：最终像素是
      `几何·T + 入散射`，近处画面几乎全由 T 决定，而 T 的误差在"相对柱子总量"
      的口径下会被入散射的量级淹掉。
  - **不取聚合最大值，按距离分段取 max**：①脚下 <50 m ②近景 .05–.5 km
    ③中景 .5–4 km ④远景 4–32 km。分段的理由是全局 max 可被刷 —— 把切片全堆到远处，
    远景误差降下去，聚合 max 就"过了"，而脚下已经糊掉。段界按画面内容定，不按等距分。
  - **空段按绝对可见性豁免，不直接判失败**。某段一片都没有时（Log near 100 m 的①段），
    先查参考解在该段远端的累积入散射相对"日照下 albedo 0.3 漫反射面"
    （`0.3·120000/π ≈ 1.1e4 cd/m²`）以及遮挡 `1−T`，两者都 < 1% 才免检。
    分母取这个参考白而不是柱子总量：后者本身随切片布局变，那样又是一个能被布局刷的判据。
    实测①段只有 0.26~0.31% / 0.24~0.29%，全部免检 —— 即**晴空脚下本来就没有雾**。
  - **余量档要求两个视角同时满足阈值的一半**（`k_ApSelectMargin = 0.5`）。
    余量存在的唯一依据是视角敏感性，所以只在一个视角上核它是自相矛盾的：
    实测同一档（d=16 Log 20 m）在两个视角上 errMid④ 是 0.77% 与 0.91%、
    T④ 是 0.49% 与 0.46%，摆动 0.14 个百分点，与阈值本身同量级。
  - 成本侧（`Profile Atmosphere LUTs` 新增一节，RTX 3060 / D3D11 / Editor）：
    | 配置 | AP 单测 | 稳态整链 | 显存 | 最差段 errMid/errMidT（视角2） |
    |---|---|---|---|---|
    | d=16 Log 20 m | 0.027 ms | 0.183 ms | 256 KB | 0.91% / 0.46% |
    | d=16 Pow k=3  | 0.033 ms | 0.192 ms | 256 KB | 0.19% / 0.10% |
    | d=24 Log 20 m | 0.036 ms | 0.192 ms | 384 KB | 0.37% / 0.23% |
    | **d=32 Log 20 m（出厂）** | 0.040 ms | 0.195 ms | 512 KB | **0.21% / 0.14%** |
    | d=32 Pow k=3  | 0.041 ms | 0.196 ms | 512 KB | 0.04% / 0.05% |
    | d=64 Log 20 m | 0.063 ms | 0.218 ms | 1024 KB | 0.05% / 0.08% |
    切片数翻倍的单测比是 16→32 ×1.50、32→64 ×1.58（**亚线性**：柱内步数受
    `VISTA_AP_STEP_MAX_KM` 与 `VISTA_AP_STEPS_MAX` 双向夹，切片数不是步数的自变量）。
    同深度换分布 d=16 Log→Pow k=3 是 ×1.25，显存与切片数都不变。
  - **判定挂在"出厂档"上，不挂在"扫描里有没有配置能过"上**。后者永远会绿
    （总有一档够精细），改坏默认值也照样绿 —— 那是伪通过。现在出厂档在扫描行里标 ★，
    并单独判"合格 + 含余量"，且不在网格里就直接失败。
    同一批把原来的"定档 →"改名为"最省候选 →"：那一行算的是"合格线能压到多低"，
    不是"出厂发什么"，用旧名字等于把一条藏在代码里的偏好印成结论。
- **太阳位置** `VistaSolarPosition`（低精度算法，适用区间 1950–2050）。
  给定年月日 + 当地时钟 + 经纬度 + 时区，输出高度角/方位角/赤纬/时角/世界光向。
  - 参考：Meeus, *Astronomical Algorithms* 的低精度太阳位置；儒略日 → 平黄经 →
    真黄经 → 赤纬/赤经 → 时角 → 地平坐标。均时差走完整的中心差项，不是查表。
  - Editor 自检 `Window/Vista/Validate Solar Position`，六条判据都对**闭式已知量**：
    A 夏至赤纬 23.4351°（真值 23.44，误差 0.0049°）；
    B 北纬 23.44° 夏至正午过天顶得 90.0000°，且春分那天算出均时差 7.46 分钟；
    C 北极圈内极昼时最低高度角 −0.0014°（应 ≥ 0，误差在阈值内）；
    D 子午线对称性 —— 太阳时正午前后等时距的高度角应相等，一般日（8/18）残差
    0.0094→0.1198°，至日对照组残差 0.0002→0.0007°（低两个数量级，正是赤纬漂移的签名）；
    E 高度角/方位角 → 方向矢量的两条路径互校，432 组最大偏差 3.129e-7；
    F 参考海拔包线（见下）。
- **CPU 侧太阳透射率** `VistaSunTransmittance`：`AtmosphereDef.hlsl` 的
  `VistaSampleMedium` + `VistaComputeOpticalDepthToTopAtmosphereBoundary` 的逐行镜像
  （同为 40 段梯形积分），外加"绝对光度量 → URP `color × intensity`"的换算。
  - 对账自检 `Window/Vista/Validate Sun Transmittance`：CPU 走解析积分、GPU 走
    LUT + Bruneton 参数化 + 双线性插值，两条**完全不同**的路径逐通道对上才算两边都对。
  - fp16 地板实测 **5.047e-4**，与理论相对精度 2^-11 = 4.88e-4 同量级 ——
    这条把"LUT 插值误差"与"存储精度地板"分开，否则低太阳角的偏差会全被归给插值。
- **物理光单位接缝的闭式解 + 端到端实测。** URP 的直接光漫反射是
  `albedo · (color.linear · intensity) · NdotL`，**没有 1/π**（`LightingLambert`
  只有 `lightColor·NdotL`，`brdfData.diffuse` 是裸 albedo）。要与绝对光度量对齐就得
  `color · intensity = lux · T · exposure / π`。EV100=15、T=1 时闭式解 **0.97140**，
  `Window/Vista/Validate Light Seam` 渲一帧读回像素得 **0.97119**（偏差 0.0219%）。
  这条自检**驱动的是产品代码本身**（`ApplyLightParams` 因此从私有改成公开），
  不是自己抄一份写灯逻辑。
- **`VistaTimeOfDay`**（`[ExecuteAlways]`）：按时间与地点驱动平行光的朝向与光色。
  - 灯的回落链：`Sun` 字段 → 本对象上的 `Light` → `RenderSettings.sun`，
    每一级都要求 `type == Directional`。朝向与光色可分别关闭（UE 的
    Sun Position Calculator 也是可分离的）。
  - 大气参数从 `VistaAtmosphereFeature.current` 读，组件**不存副本**；取不到时
    仍驱动朝向、不动光色，并在面板上报警。
  - 写入门：朝向 0.01°（= 太阳视角直径 0.545° 的 1/50）、光色/强度相对 1e-4
    （= Weber 1% 的 1/100）。门存在的理由是编辑器下每帧写属性会把场景标脏。
- **`VistaTimeOfDayEditor`：报警 + 诊断面板。** 组件注释里承诺过"面板上明确报警"，
  但在此之前 `sunMissing` / `atmosphereMissing` 只是两个 public 属性 ——
  **一个只有代码能读到的报警等于没有报警**。面板把三层不可见换算（曝光、1/π 接缝、
  gamma 往返）的中间量全部摊开：透射率 T → 曝光后照度 → 求得的 color/intensity →
  **灯上实际的** color/intensity。最后两行与前面分开显示是刻意的：不一致就说明
  有别的东西在改这盏灯（另一个组件、Animator、Timeline 轨道）。
  另加"距太阳时正午 N 分钟"一行 —— 经度/时区填错是最常见的配置错误，
  面板直接给分钟数比让人自己按 15°/h 去算有用。
- **参考海拔的有效包线** `SolveReferenceAltitudeEnvelopeMeters`：
  "参考海拔填错多少米，画面上还看不出来"。判据是逐通道 Weber 1% **且**
  同通道 |ΔT| ≥ 1e-3（后者约等于"错了参考白的 0.1%"）。实测（Validate Solar Position 的 F2）：

  | 太阳仰角 | 参考 T (R,G,B) | 包线 Δh | 包线处最大相对 |
  |---|---|---|---|
  | 1° | (0.22636, 0.03929, 0.00110) | 41 m | 3.122% |
  | 3° | (0.41427, 0.13841, 0.01807) | 46 m | 2.053% |
  | 5° | (0.53938, 0.24726, 0.06280) | 51 m | 1.587% |
  | 15° | (0.78741, 0.58055, 0.35262) | 77 m | 0.998% |
  | 45° | (0.91429, 0.81559, 0.67827) | 193 m | 0.998% |
  | 90° | (0.93848, 0.86551, 0.75965) | 271 m | 0.998% |

  这张表同时是 Task #12（逐像素大气透射率）的立项依据，见「取舍」。
  求解器与面板读数**同一份实现**，F1 另有一条单调性判据（T 随海拔严格不降，
  6 仰角 × 100 步 × 3 通道）。
- **`Window/Vista/Validate TimeOfDay Diagnostics`**：面板那几条报警的可达性验收。
  报警是保护性代码，而它最常见的失效方式不是"报错"而是"**永远不报**" ——
  一条永不触发的 HelpBox 与一行注释等价。四组的详情见「坑」里"每条报警都要双向验"。
- **AP 合成：共享合成层 + 全屏合成 pass（变体 A）**。AP LUT 在此之前没有消费者，
  Step 1 的验收标准③（远山洗白）一直悬着。
  - `ShaderLibrary/AerialPerspectiveComposite.hlsl` 是**全项目唯一**一份合成实现：
    距离 `VistaApDistanceKm`、两个乘子 `VistaGetAerialPerspectiveTerms`、
    应用 `VistaApplyAerialPerspective`（= `color·T + S·exposure`）各只有一份。
    变体 A 与变体 B 调用的是同一个函数 —— 这样"A 与 B 必须逐像素一致"才是个有意义的判据，
    而不是两份实现互相担保。
  - `VistaAerialPerspectiveCompositePass` 排在 `AfterRenderingSkybox`：
    天空盒之后（它在不透明之后画且**不写深度**，更早合成既白做一遍、又没法用深度剔天空）、
    半透明之前（AP 用的是不透明深度）。这两个约束之间只剩这一个位置。
  - 深度**一定拿得到，没有兜底分支**：`ConfigureInput(Depth)` 把
    `earliestDepthReadEvent` 拉到 AfterRenderingSkybox，
    `CalculateDepthCopySchedule` 于是选 `DepthCopySchedule.AfterSkybox`，
    `CalculateSplitEventRange` 把"事件 ≥ earliestDepthReadEvent"的自定义 pass 排到拷贝之后。
    这条链与用户在 renderer 上选的 `CopyDepthMode` 无关，所以不写"深度无效就跳过"——
    那种分支只会把真正的失效藏起来。
  - `Window/Vista/Validate AP Composite Wiring`：**接线**自检，刻意不验任何数值
    （在 C# 里重算合成式就是造第二份真相，反射自检那里已经付过这个学费）。
    三条判据全绿：shader 无编译错误、Pass 序号 ↔ Pass 名逐字相符（乘/加互换不报任何错，
    只让远景雾偏暗）、`_VistaApSize = (32,32,32,0)` 且 `_VistaApConsumer.x = 0.0`（预期 0.0）。
  - 已知不覆盖：XR 单趟立体（`UNITY_MATRIX_I_VP` 不逐眼索引，全屏三角形也没 instancing）；
    `#pragma only_renderers` 不含 GLES，所以变体 A 的移动端目前只覆盖 Vulkan/Metal。
    两条都是明确记为**不支持**，不是假装支持。
- **`Window/Vista/Validate AP Composite (Numeric)`**：变体 A 的**数值**验收（Task #15 的①③）。
  在一个 GPU 上现搭的自带布景里量出 `out = in·T + S` 这条公式本身，而不是去量美术场景 ——
  美术场景一改，判据的读数就跟着漂，回归价值归零；而当前 Demo 场景的 Terrain 是禁用的，
  只有一块 10 m 的 Plane，"远山洗白"根本没有可观测的对象。
  - 布景：64×64 ARGBHalf、FOV 2°、相机海拔 200 m、远裁 45 km（**大于** `maxDistanceKm`，
    为的是把"钉在最后一片"也量进来）。同一距离上并排四片 Unlit 四边形：
    黑 / 白 / 中灰 / 黑，96 个距离按 5 m → 40 km 等比排布。
    用 Unlit 而不是 Lit：布景要的是一个**已知的入射值**，直接光一进来，
    被测量就变成"着色 × 合成"的乘积，解不出 T 和 S。
  - 黑白两列给两个方程解出逐通道 T 与 S（`T = (white_on−black_on)/(white_off−black_off)`，
    `S = black_on − black_off·T`），**分母用实测基线而不是假设白=1 黑=0** ——
    链路上若有别的增益，假设值会把它悄悄记到 T 的账上。第三列中灰是**独立**的预测靶：
    它没参与解方程，所以"用 T/S 预测中灰"是一条真判据（合成对底色必须是仿射的）。
    第四列复用第 0 列的材质、同距离，它与第 0 列的差值就是**这把尺子自己的串扰地板**。
  - 八条判据全绿。地板 = `max(串扰, 2×量化) = 9.760E-004`（串扰实测 2.441E-004，
    量化实测 0.000E+000，fp16 理论 4.880E-004；乘 2 是因为解 T 要做一次减法）——
    **阈值是量出来的，不是拍的**。
    ① 基线常数：白 (1,1,1) / 中灰 (0.1799,…) / 黑 (0,0,0)，随距离最大漂移 0。
    ② 信号非空：近端 5 m `T=(0.9995,0.9995,0.9994)`、`S≈0`；
    远端 40 km `T=(0.7433,0.5837,0.3181)`、`S=(0.0712,0.1301,0.2375)` ——
    最大消光 68.2%、蓝通道散射 0.2375，波长依赖的方向也对（蓝散得多、透得少）。
    ③ T ∈ [0,1] 且单调不增：越界 0/96、回弹 0/95。
    ④ S 单调不减：回退 0/95。
    ⑤ 仿射（预测中灰）：最大误差 1.555E-004 @ 11.694 km，超阈 0/96。
    ⑥ 切片数对折 32 → 256：最大偏差 1.343E-003（相对 0.15% @ 8.804 km），超阈 0/96 ——
    判的是"采样坐标与切片数解耦"，即距离→UVW 的映射与深度分布同源。
    ⑦ 天空像素**逐位**未被触碰：Off↔Fullscreen 整屏最大差 0.000E+000。
  - 报警是**注入故障验过的**，不是写上去的（详见「坑」的两条）：调换两趟混合顺序 →
    判据 ④ 报警 4/95、最大 3.418E-003 @ 30.1 km；去掉天空 `clip` → 判据 ⑦ 报警
    整屏最大差 7.385E-002。同一次注入还量出判据 ⑤ 的**盲区**：`(dst+S)·T = dst·T + S·T`
    仍然是 dst 的仿射函数，所以仿射判据抓不到顺序调换 ——
    这条盲区原先只是注释里的一句断言，现在是量出来的，也正是判据 ④ 不能删的理由。
  - 判据②（变体 A 与 B 逐像素一致）不在这条自检里，见下面单独一条 ——
    它要六次渲染和一套 Lit 布景，与"Unlit 基线是精确常数"这条前提互斥。
    性能项已完成，见下一节 —— 并且**去掉了**原计划的"双源混合带宽 A/B"：
    Unity 里没有双源混合，那个对照组不存在。

- **`Window/Vista/Validate AP Variant A-B Agreement`**：变体 A（全屏 pass）与
  变体 B（`Vista/Lit` 着色内合成）**逐像素一致**（Task #15 的判据②）。
  这条判据是"为什么两个变体都留着"的唯一凭据：如果两条路给出的画面不一样，
  那么"运行时可切"就不是一个选项而是两个不同的产品。
  - **为什么不能"一次调用里比两条路"**：变体 A 是一个独立的全屏 pass，
    它与 B 的开关是同一个 `compositeMode` 的两个互斥取值。所以只能渲两次、
    在 CPU 侧逐像素比 —— 而两次渲染之间隔着整条渲染→读回路径。
    那条路径实测带一个 **±1/1024 的加性扰动场**（见「坑」里累加自检那条），
    量级 9.766E-004，与被测的 fp16 量化项几乎相同。
    于是布景里加了**第六次渲染**：变体 A 连渲两遍、一行状态都不改，当**对照**。
    实测对照的最大逐像素差 = **0.000E+000** —— 这是这条自检最要紧的一个读数：
    它第一次**量到**（而不是论证）那个扰动场随**像素位置**变化、
    因而在同像素两次渲染之差里一阶抵消。整套两次渲染的比较就站在这个读数上。
  - **差异被分解成两个操作数分别判**，而不是只比最终颜色。A 与 B 的差只有两个来源：
    ① A 从深度图反投影出 `positionWS`，B 直接插值；
    ② 给定同一距离，A 是往一张**已经量化成 fp16** 的颜色上混合（3 次舍入），
    B 在寄存器里算完只量化一次。只比颜色，这两项混成一个数，红了定不了位。
    为此两侧各开一个**距离出口**：变体 A 的合成 shader 加 Pass 2
    （`Blend Off`，输出 `VistaApDistanceKm`），变体 B 加
    `VISTA_AP_DEBUG_DISTANCE` 关键字（`shader_feature_local_fragment`，
    不是 uniform —— 那是一次性测量出口，用 uniform 要让每个出货片元永久多付一次分支）。
    两侧调的是**同一个** `VistaApDistanceKm`，所以量到的差只可能来自 `positionWS`。
  - 布景：128×128、8 列按 500 m → 34 km 等比排布的正对相机四边形，
    加一块**贴地大平面**当强斜面（法线 +Y、相机下方 30 m、延伸到远裁面），
    因为正对相机的板子上"深度反投影"与"插值"几乎不会分歧 ——
    分歧要在深度梯度大的地方才显形。远裁 45 km > `maxDistanceKm = 32`。
    所有几何**共用一个材质**：两个材质会给"板子 vs 斜面"的归因多引入一个变量。
    镜面高光 / 环境反射 / 阴影全关（关键字与 uniform 一起关 —— `SetFloat`
    只改 uniform，分支由关键字决定，这一条踩过）。
  - 六次渲染：`Off` 基线 → `InShader` → `Fullscreen` → **对照**（Fullscreen 再来一遍）
    → B 的距离出口 → A 的距离出口。前四次写 `ARGBHalf`，后两次写
    **`ARGBFloat`** —— half 在 40 附近的一个 ulp 是 3.125E-002 km = 31 m，
    会把要量的 Δd 整个吞掉。
  - 覆盖判定靠**哨兵**，不靠算边界：清屏色写成 30000（颜色）/ 1000（距离），
    远高于任何真实读数。两侧同时被覆盖的像素 11368/16384，
    `onlyA = 0`、`onlyB = 0`。空白带取的是一条**保守**的窄带（两侧都留余量），
    2688 px 全部未覆盖、哨兵读回恒为 **3.981E+006 = 1000^2.2** ——
    顺手确认了 `Color` 类型的 gamma→linear 转换**对 `Camera.backgroundColor` 也生效**，
    所以自检是去**量**这个哨兵而不是预测它。
  - 五条判据 + 一条非空性守卫，全绿：
    - **②-0（非空性）**：`|B − 未合成基线|` 最大 2.378E-001，最大
      差/容差比 **100.0×** @ 11582 m（要求 ≥ 10×）—— 证明②b 的"通过"有内容，
      不是在比两张几乎相同的图。
    - **②a-0（读出路径标定）**：拿 **B** 当尺子去量距离通道的地板，
      因为 B 的 `positionWS` 是插值来的、**不经过深度缓冲** ——
      它对已知距离的相对误差就是"读出路径 + 插值"的地板，与深度无关。
      八列实测 2.10E-007 ~ 2.81E-006，最大 2.806E-006 对门限 1.952E-003（700× 余量）；
      地板 = **7.813E-003 m**。
    - **②a（测量，非判据）**：`|d_A − d_B|` 按距离分档全部 ≤ 0.008 m
      （<1 km / 1–5 km / 5–15 km / 15–32 km），>32 km 档 0.013 m，
      最大 Δd/d ≈ 4.1E-007。最坏板子 0.013 m @ d=34002 m、最坏斜面 0.009 m @ d=44014 m。
      **深度格式因此是量出来的而不是假设的**：`ndc ≈ 0.1/d − 2.2e-6`，
      24 位定点在 34 km 处预测 Δd ≈ **689 m** = 读出地板的 **88195 倍**，
      实测排除；读数与 32 位浮点深度相容，但 D32 自己的预测（6.57E-004 m）
      **在地板之下**，所以这只能报成**上界**，不是等式（见「坑」）。
    - **②b（硬判据）**：`max|A − B|` = **2.441E-004**，超容差像素 **0/11368**。
      容差 = `max(1% 相对, 1e-3 绝对) + 对照`，对照那一项是 0。
    - **②c（测量）**：换算成 fp16 ulp 最大 **2.00**，≤ 上界 3（一次混合 3 次舍入），
      即两个变体只差在**表示精度**上，算术是**同一份实现**。
      这比 Weber 1% 严 **6.8 倍**。
  - 明确**不覆盖**：开了镜面高光/环境反射之后的一致性（材质上关掉的，没有担保）；
    透明材质（两条路都**故意**不合成 AP，理由见 `VistaLighting.hlsl` 里
    `VistaApplyApTail` 的注释）；延迟渲染（变体 B 在延迟里概念上不存在，
    那正是变体 A 必须存在的独立理由）。

- **`VistaGpuTimer`：Edit 模式 GPU 计时核，从 `VistaAtmosphereLutProfiler` 里提出来。**
  不是新写的，是把那份 profiler 里已经存在的计时纪律搬到一个能被复用的地方 ——
  AP 合成的性能项要用**同一把尺子**量一个光栅 pass。
  提取的理由是具体的：fence 能力位不可信（`supportsGraphicsFence` 返回 true 而
  `fence.passed` 永不到）、等待不能带超时（探测与等待必须分开）、必须取多轮最小值、
  负值不能 clamp —— 这四条每一条都是事故换来的，复制一份会让两份纪律走歧，
  而走歧的表现是"两份报告的数字对不上"，没人看得出是哪份错。
  提取后重跑 LUT profiler：稳态五 pass **0.180 ms（max 0.185，±3%）**，
  落在提取前记录的 0.170~0.198 带内 —— 提取没有移动数字。
  - 顺手把 fence 的同步阶段从 `ComputeProcessing` 改成 `AllGPUOperations`
    （只等计算阶段会让 draw 还没跑完就判 passed）。**这一行在本机上未被执行过** ——
    RTX 3060 / D3D11 上 `ProbeFence` 一直不通过、整场退到 readback，
    而 `GetData` 本身就等全部 GPU 工作。所以这个改动目前只有推理支撑、没有实测支撑，
    已经写在那一行的注释里，免得下一个人以为它验过了。

- **`VistaApCompositePerf`：AP 全屏合成的性能测量（Task #15 验收项 ④）。**
  菜单 `Window/Vista/Profile AP Composite (Perf)`。
  - **量的不是一个 ms，是一条线 `ms = a + b·覆盖面积`。**
    a 是每个像素都付的（全屏三角形、采样深度、判天空、被 clip 的像素到此为止），
    b 是只有非天空像素才付的（两趟混合对颜色附件的读-改-写）。
    报一个 ms 等于把某一次构图的天空占比偷偷编进结论里，换个镜头就不成立；
    有了 a/b，任意构图的开销可以直接换算，不必再测一遍。
    这条线还把 shader 文件头那句"天空占屏比例高的构图省掉的是真带宽"变成**可判定的**：
    省下的正是 b·天空面积，而 b 是否超出噪声就是判据 ③ —— 若落在噪声里，那句注释该删。
  - 覆盖率是通过**被测 shader 自己的 `VISTA_AP_IS_SKY_DEPTH` 分支**起作用的：
    手填的 R32 深度里底部若干行是几何、其余是远平面。若哪天那个宏的方向改了，
    这里会立刻表现为覆盖率与耗时反相关，而不是悄悄测了个别的东西。
    几何深度按**对数**扫过 20 m → 30 km，因为 froxel 的切片分布也是对数的 ——
    线性扫描会让绝大多数行挤在最后几片上，测不到 3D 表的真实局部性。
    原始深度由 `GL.GetGPUProjectionMatrix` 反解，不手写 near/far 公式：
    反向 Z、平台差异、无穷远平面这三件事都已经折在那个矩阵里，手写就是第二份实现。
  - **四条判据全部由测量自身给出，没有一条是拍的。** 刻意不设"1080p 必须低于 X ms"
    这类门 —— 给一条产出数据设通过门，门自己就会变成结论。
    ① 线性（残差 ≤ 观测离散度）；② 斜率 b 的分辨率折叠；③ 覆盖项超出噪声；
    ④ 除以 N 之后与 N 无关。
  - **实测（RTX 3060 / D3D11 / readback / AP 表 32³ Log / RGBA16F）：**

    | 档位 | a (ms) | b (ms/Mpx) | a/全屏 (ms/Mpx) | 天空构图省下 |
    |---|---|---|---|---|
    |  720p | 0.0243 | 0.0247 | 0.0264 | 48.4% |
    | 1080p | 0.0556 | 0.0249 | 0.0268 | 48.2% |
    | 1440p | 0.1019 | 0.0231 | 0.0276 | 45.5% |
    | 2160p | 0.2209 | 0.0254 | 0.0266 | 48.8% |

    像素数跨 8 倍，b 的极差只有 9.6%、a/全屏 的极差 4.5%（判据 ② 的上界是入选档位
    最差可用点的离散度 16.0%）。线性残差 0.0008~0.0086 ms，全都在观测噪声的 1/5 以下。
    这两个系数同时折叠，是整套测量最强的一条自检 —— 布景、深度填法、基线扣除里
    任何一处出错，都很难同时在四个分辨率上错成同一对系数。
  - **结论：1080p 满覆盖 0.106~0.109 ms，天空占屏一半的实际构图约 0.08 ms。**
    天空 clip 省下的是 45~49%，也就是说 shader 头上那句话成立（判据 ③ 四档全过，
    覆盖项 0.023~0.211 ms vs 噪声 0.002~0.046 ms）。
  - 两趟拆分：仅乘 + 仅加 与两趟合测的差在 −0.003~+0.005 ms —— 两趟没有被串行化到
    出现可测的额外开销。深度归因（对数扫描 vs 常量深度）比值 0.99~1.02，
    也就是说 **≥720p 上 froxel 3D 表的缓存局部性量不到**。
    这一行的初版判定文本写着"> 1 说明缓存局部性是真实成本项"，那是预先注册结论 ——
    已改成只报比值。
  - 带宽地板（360 GB/s：覆盖像素 40 B/px → 0.1111 ms/Mpx，clip 像素 8 B/px →
    0.0222 ms/Mpx）**只报告、不判定**。实测 b 是覆盖地板的 0.21~0.23×，
    低于地板是合法的 —— 3060 有 3 MB L2，深度在两趟之间可能还在缓存里。
    把它当判据会把缓存命中报成"尺子坏了"，正是本项目记过的那个反模式。
  - 未覆盖并已声明：MSAA（改混合样本数）、XR 单趟立体、移动端 tile 架构；
    口径是吞吐不是帧内延迟，深度拷贝→合成这条边上的 barrier 要由模型 A 给。

- **模型 A（`VistaLutGpuRecorderCrossCheck`）接上 AP 合成，把上面那句"barrier 要由模型 A 给"落实。**
  菜单项改名 `Window/Vista/Cross-Check Atmosphere Timing (Play Mode)`（原来只说 LUT，
  现在还量一个光栅 pass）。
  - **合成必须自己一档（`k_CompositeIdx`），不能混进稳态五 pass。** 两条理由都是硬的：
    ① **不能除以出现次数。** 那一步的前提是"LUT 尺寸与相机分辨率无关，所以两次渲染
    做的是同样的工作量"。全屏光栅恰好违反它 —— Scene View 与 Game View 分辨率不同，
    和除以 2 得到的数**不对应任何一次真实渲染**。所以这一行只报原始和 + 出现次数，
    **没有「单次」**。
    ② **不能进那个和。** 那个和是拿去和模型 B 的 0.170~0.198 ms 对账的，
    而那是五个 compute 的口径。掺进一个随分辨率变化的项，和会随 Game View 大小漂移，
    对账区间不会 —— 对账从此失效。
  - **对账用包线，不用一个数。** 模型 B 给的是一条线 `ms = a + b·覆盖面积`，
    而覆盖面积（非天空像素占比）由**构图**决定 —— 模型 A 跑在真实场景上，
    那个占比不由测量方控制，也无法从 marker 里反推。能对的只有两端：
    全天空 = `a`、满覆盖 = `a + b·全屏面积`。
    每帧出现多次时**不做除法**，改成把每次渲染的面积逐个点名加起来再套包线 ——
    模型对面积是**线性**的，所以「两次渲染之和」的包线就是两个包线之和，
    不需要任何等工作量假设。这是替掉那个不合法除法的正解。
  - 系数 `k_ClippedMsPerMpx = 0.0266` / `k_CoveredMsPerMpx = 0.0245` 与
    `Bracket(megapixels, out floor, out ceil)` 放在 `VistaApCompositePerf` 上**共用**，
    没有抄进模型 A：同一个量有两份字面值的话，改了这边忘了那边，
    表现是"两份报告的结论互相矛盾"而没人知道该信哪份。
    `Bracket` 收**面积**而不是宽高，就是为了让那两个乘法只存在一处。
    换 GPU 必须重跑模型 B 重填这两个数 —— 只有 `ms = a + b·面积` 这个**形式**与机器无关。
  - 点名面积时 `renderScale` **只乘 Game View**，且与 1 相差 < 0.05 时吸成 1 ——
    照着 URP `UniversalRenderPipeline.cs:1453-1460` 的 `kRenderScaleThreshold` /
    `isScenePreviewOrReflectionCamera` 两条写的，不是推的（Scene View / Preview /
    Reflection 一律 1.0）。凭印象写会把缩放算两次。
  - **点名数与 marker 出现次数对不上就认输，不猜。** 少了 = 有视图没在渲染（被 tab 遮住），
    多了 = 还有别的相机（第二个 game 相机 / 预览窗口）。两种都会让面积和错，
    而错的面积和会给出一个**看起来很像结论**的数。
  - **归因是单向的。** 超出上界的部分可以归因 —— 覆盖率只能把值往下拉，
    所以超出量是 barrier / pass 边界 / 无重叠三样代价之和的**下界**。
    落**在**包线内**不能**拆成「天空占比」与「barrier 加价」两项：
    两者符号相反且都未知，报告里明写了这一点而不是给个像结论的分解。
  - 两端的前提**分别**判、分别说，不合成一个「前提不成立」一票否决：
    HDR 关 → 下界不成立（上界仍成立）；MSAA n× → 上界失效（下界仍成立，只会更宽松）。
    合成一个的话会把唯一还成立的那一端也挡掉。
  - **实测（RTX 3060 / D3D11 / 场景 Demo / 45 帧预热 + 300 帧采样，判定「交叉验证完成」）：**

    | pass | GPU min | 中位 | max | 每帧出现 | 单次 |
    |---|---|---|---|---|---|
    | Transmittance LUT | — | — | — | 0 样本 | 静态表缓存生效（期望） |
    | Multi-Scattering LUT | — | — | — | 0 样本 | 同上 |
    | Sky-View LUT | 0.083 | 0.086 | 0.121 | 2 | 0.041 |
    | Sky Ambient SH | 0.020 | 0.023 | 0.032 | 2 | 0.010 |
    | Sky Reflection | 0.177 | 0.205 | 0.280 | 2 | 0.089 |
    | Sky Reflection Copy | 0.066 | 0.067 | 0.092 | 2 | 0.033 |
    | Aerial Perspective LUT | 0.049 | 0.052 | 0.073 | 2 | 0.025 |
    | **AP 合成** | **0.043** | 0.047 | 0.069 | 2 | **不除**（全屏光栅） |

    稳态五 pass 之和：原始 0.395 ms（2 次渲染）→ 单次渲染 min 0.198 / 中位 0.216 ms，
    A/B = 1.16，**A ≥ B 方向符合预期**。
    合成包线：`Game 1100×592 + Scene 1100×602 = 1.313 Mpx` ⇒ **[0.035, 0.067] ms**，
    A = 0.043 ms ≥ 全天空下界 0.035（方向符合），落在上界的 64%。
  - 四次完整运行的跨轮稳定性：Sky-View 0.084/0.082/0.084/0.083；
    Reflection 0.200/0.176/0.201/0.177；Copy 0.066 平；合成 0.043/0.042/0.046/0.043；
    稳态单次渲染 0.210/0.198/0.198/0.198。
    一个不解释只记下来的离群点：AP LUT 在其中一轮读到 0.025 而另三轮约 0.050。

- **AP 合成：`Vista/Lit` 着色内合成（变体 B）**。变体 A 的全屏 pass 只能拿到深度重建的
  `positionWS`，且在延迟下才是唯一可行的路；变体 B 拿的是插值出来的 `positionWS`，
  并且是 Task #12（逐像素大气透射率）**唯一**可能的挂载点 —— 那一项要乘在直接光上，
  而全屏 pass 手上只有一个已经合成完的颜色。两条路都得存在，理由各自独立。
  - `ShaderLibrary/VistaLighting.hlsl`：`VistaComputeLighting` 是
    `UniversalFragmentPBR(InputData, SurfaceData)` 的**逐行拷贝**
    （对照 `Lighting.hlsl:282-373` @13e5115b98bf），停在累加，交出 `LightingData`。
    拷贝的理由不是"想改"而是"拿不到"：URP 内部已经把光照拆成
    `giColor / mainLightColor / additionalLightsColor / vertexLightingColor`
    ——**正是 #12 需要的那个拆分**—— 但它不把这个结构返回出来。
    拷贝的**边界**同样是明确的：末尾的合成仍然调 URP 的 `CalculateFinalColor`
    （`VistaResolveLighting`），所以本文件里没有一行是"URP 已经有、我又写了一遍"的**算术**，
    抄的全部是控制流（哪些分支在什么宏下成立、以什么顺序调）。
  - `surfaceData` 收 `inout` 而不是按值：17.4 的 `InitializeBRDFData` 实测没改它，
    两种收法今天等价；取 `inout` 是为了**将来它真改了的时候仍然等价** ——
    差异只会出现在开了 `_ALPHAPREMULTIPLY_ON` 的透明材质上，那正是最不容易注意到的地方。
  - `Shaders/Vista/VistaLit.shader`：Properties 与 ShadowCaster / DepthOnly /
    DepthNormals / Meta / MotionVectors 五个 pass 是 URP `Lit.shader` 原样拷贝，
    只有 ForwardLit 的片元不同。**少掉的 pass 各有理由，都写在文件头**：
    · GBuffer 不提供 —— 延迟的着色发生在 lighting pass 里，材质 shader 根本没有
    "着色结束"这个位置可以挂 AP。这不是"B 还没做完"，而是变体 A 必须存在的独立理由。
    · `UniversalForwardOnly` 不提供 —— `UniversalRenderer.cs:323` 明确写着与
    `UniversalForward` 同时声明是 ERROR，而 `DrawObjectsPass.cs:80` 的 ShaderTagId
    集合里两者都在（会画两遍）。
    · Universal2D / XRMotionVectors 记为**未覆盖**，不是默默省掉。
  - **去掉了 `Fog.hlsl` 的 `include_with_pragmas`**，省下 4× 变体。那个文件只做一件事：
    声明 `multi_compile_fog`。Vista/Lit **永不调 `MixFog`** —— URP 的雾与 AP 描述的是
    同一件事（视线上的大气），两者都乘一遍透射率、都加一遍散射，同时上就是远处过暗
    且颜色偏向 fog color。而且刻意**不**做"AP 关掉时退回 URP 雾"：那会让"关掉 AP"
    变成"换一种雾"，A/B 与反例测量全部失去意义。代价只有一个没人读的 fogCoord 插值器。
  - 透明材质**不合成 AP**（`VistaApplyApTail` 里 `#if !defined(_SURFACE_TYPE_TRANSPARENT)`）。
    不是偷懒：变体 A 挂在 `AfterRenderingSkybox`，半透明在它之后画，A 覆盖不到；
    B 若在这里给透明物上了 AP，"A 与 B 逐像素一致"这条验收标准会在每一个透明像素上
    **必然**失败 —— 而那不是 bug，是两条路的覆盖范围本来就不同。
  - **`Window/Vista/Validate Vista Lit Accumulation`：拷贝的存在前提。**
    没有这条自检就不该抄 —— URP 升级后函数体会变，人眼 diff 不可靠。
    做法是 `VISTA_LIT_DIFF_DEBUG` 变体在**同一次片元调用**里既算我的累加、
    又算 `UniversalFragmentPBR`，输出逐通道相对误差。同一次调用而不是画两遍：
    两遍画的话，两次的关键字集合 / 渲染状态 / 插值结果都得靠"应该一样"来保证，
    而这三样恰恰是最容易不一样的；同一次调用里两者共享同一份
    `inputData` / `surfaceData` / 寄存器，差异只可能来自累加本身。
  - 六个输出档（`_VistaDiffCtrl`）与哨兵：0 档相对误差（判定用）、1/2 档两侧原值、
    3 档写出**之前**做减法的分子、4 档分母、5 档已知常量。
    1/2 档不够、必须有 3 档：1/2 档把两个值**各自**量化成 half 再写出，
    小于一个 half ulp 的差异读回来必然是 0 —— 相减得到的是量化噪声，不是差异。
    3/4 档取的是那次除法的**两个操作数本身**，于是 CPU 侧能验 `0 档 == 3 档 / 4 档`
    这条恒等式，归因不成立时当场暴露。哨兵 30000 同时是 RT 的清屏色 ——
    **没被画到的像素同样是哨兵**，任何未覆盖的像素都不可能被读成"误差 0，一致"。
  - **实测（RTX 3060 / D3D11 / 128×128 ARGBHalf / 8 个关键字配置）：**
    · 判据 1：八个配置（不透明·无阴影 / +主光软阴影 / +两盏带阴影点光 /
    贴图特性全开 / 高光与反射关 / Specular 工作流 / AlphaTest / 透明+预乘 alpha）
    全部 `≤ 9.766E-006`，比对像素 16384/16384，哨兵 0。这个数是**尺子的地板**，
    报成上界而不是值。
    · 判据 1b（最坏像素归因，六档同组渲染）：1/2 档读回**逐位相同** `8.422852E-001`；
    3 档分子 `≤ 9.766E-010`、商 `≤ 1.159E-009` —— 比 Weber 1% 低七个数量级。
    结论口径是"两侧的差异小于可测下限"，不是"差异为 0"。
    · 判据 2（逐项故障注入 +2%）：`mainLightColor` 报出 1.895E-002、
    `additionalLightsColor` 1.982E-002、`giColor` 1.255E-002 —— 三项**有分辨力**；
    `vertexLightingColor` 报出地板值，判成**未覆盖**（本帧为 0 或占比 < 1%），
    并明写"它在判据 1 里的通过不可采信"。
  - 故障注入走 `_VistaDiffInject` 这个 **uniform 四元组**而不是四个关键字：
    正常输出恒 0，而"恒 0"有三种解释 —— 累加确实等价 / 被编译器折叠 /
    **根本没渲到被测像素**。只有注入已知量级的偏差并要求自检报出来，才能排除第三种。
    必须**逐项**注入，因为四项各由不同关键字组合决定是否活着；uniform 四元组
    一个变体就能分四次探到底。（顺带一条反直觉但正确的性质：若编译器把两条表达式
    折叠成一条、输出恒 0，那不是自检失效而是**最强的通过** —— 编译器只有在
    证明两者等价时才敢折叠。）
    声明进**具名** `CBUFFER_START(VistaDiffDebug)`：裸写在全局作用域会落进隐式
    `$Globals`，那会让整个 pass 被判成 SRP Batcher 不兼容；具名 cbuffer 里的全局量
    不影响兼容性，而 `Shader.SetGlobalVector` 照样写得进去。
  - 未覆盖清单（自检自己印出来，不当成通过）：UniversalGBuffer / 延迟（B 在延迟下
    不存在挂载点，那条路由 A 负责）；`USE_CLUSTER_LIGHT_LOOP`（要把 Rendering Path
    切到 Forward+ 再跑一遍）；`LIGHTMAP_ON` / `DIRLIGHTMAP_COMBINED` /
    `DYNAMICLIGHTMAP_ON` / `SHADOWS_SHADOWMASK`（布景是运行时搭的，拿不到烘焙数据，
    于是 `MixRealtimeAndBakedGI` 的 subtractive 分支未覆盖 ——
    **而 #12 要动的正是它前后的位置，那一步必须补一个带烘焙数据的场景**）；
    `_ADDITIONAL_LIGHTS_VERTEX` / `EVALUATE_SH_VERTEX` / 探针体 / `_LIGHT_COOKIES` /
    `_LIGHT_LAYERS` / `_DBUFFER` / `_SCREEN_SPACE_OCCLUSION`（由 URP asset 与其他
    RendererFeature 决定，自检不去改全局资产，改了会落盘）；`DEBUG_DISPLAY` 接管路径
    （两边都输出替身颜色，比较无意义，shader 里直接写哨兵）。

- **逐像素太阳透射率（Task #12，对应 UE5 平行光的 Per Pixel Atmosphere Transmittance）。**
  动机是 Task #7 留下的那张 F2 包线表：太阳 5° 时「单值参考海拔」的有效范围只有 **51 m**，
  而外景高差是数百到数千米 —— 也就是说谷底与山顶的阳光本该不同色，而单值 `Light.color`
  只能给出其中一个。做法是在不透明着色里把直射光乘上 `T(着色点) / T(参考点)`。
  - `ShaderLibrary/SunTransmittance.hlsl`：唯一一份 `VistaSunTransmittanceRatio`，
    走的是**已经存在的**那张 Transmittance LUT + `VistaSampleTransmittanceToSun`，
    没有新增第三份 T 实现。挂载点在 `VistaLighting.hlsl` 里
    `MixRealtimeAndBakedGI` **之后**、乘进 `lightingData.mainLightColor` 那一步。
  - **输出的是比值而不是 T。** `T_ref` 已经乘进 `Light.color` 了
    （那条接缝是 `Light.color × intensity = E·T_ref·exposure/π`，
    0.97119 那条端到端验收就建立在它上面），再乘一次 T 就是乘两遍。
  - **`T_ref` 从 CPU 侧上传成全局**（`_VistaSunTransmittanceRef`，
    `VistaTimeOfDay.lastLightParams.transmittance`，随 Sky-View pass 逐帧重发）。
    理由是一条算术恒等式：
    `Light.color·intensity × ratio = (E·T_ref^CPU·exp/π) × (T^LUT(px)/T_ref^CPU) = E·exp/π × T^LUT(px)`
    —— CPU 那份 T **在代数上被约掉了**。而它要被约掉，分母就必须是**同一个 float**，
    即当初乘进 `Light.color` 的那一个；在 GPU 上重新采一次参考点的 T 会引入一个
    「两份 T 不同源」的误差项，且那个误差项恰好是随时间漂移的。
    `w` 通道是语义闸门：美术手填裸颜色（灯里没有 T_ref）时 `w = 0`，比值恒返回 1。
  - **两层闸门，各挡一件事，不是同一条判断写两遍。** 语义层是上面那个 `w`；
    数值层是 `VISTA_T_REF_FLOOR = 1e-6` 与 `VISTA_T_RATIO_MAX = 1e4`。
    下限的依据是本项目自己的可见门限：`T_ref = 1e-6` 时那一通道的直射光是
    `120000 × 1e-6 × 2.5431e-5 / π ≈ 9.7e-7` render units，比 1e-3 低 1000 倍 ——
    被夹住的通道本来就在看不见的量级。上限挡的是 `mainLightColor` 是 `half3`
    这件事：1/1e-6 = 1e6 溢出成 inf，再乘上此时 ≈ 0 的 `Light.color` 就是 **NaN**，
    而 NaN 会顺着 BRDF 污染整个像素。这条在 PC 上量不出来（D3D11 的 `half` 是 float），
    只会在移动端表现为「日落瞬间零星黑点」—— 正是最难归因的一类。
  - **一条看得见的固有局限，写在函数头而不是留着。** 太阳落到**参考高度**的地平线以下后
    CPU 侧 `Evaluate` 返回 0，`Light.color` 整个是 0，乘任何比值还是 0 ——
    此时物理上还能看到太阳的山顶拿不到直射光。量级不小：参考高度切线处 T 的红通道
    ≈ 0.107，折算 0.104 render units，**高于** 1e-3 的绝对可见门限。
    它不是疏漏，是「T 已经乘进灯里」这个选择的代价，信息在 CPU 侧就被销毁了。
    另一条路（CPU 不乘 T、全交给逐像素）没有这条局限，但代价是所有**不走 Vista/Lit**
    的东西 —— URP 自带 Lit、粒子、透明物、任何第三方 shader —— 彻底失去太阳衰减，
    变成「太阳永不变色」。那是全画面的回归，比「日落最后一小段山顶偏暗」严重得多。
  - **`Window/Vista/Validate Per-Pixel Sun Transmittance`：七条判据。**
    布景是 layer 31 上一面 z = 1000 m、9000 m 见方的墙，正交相机半高 4000 m，
    于是一张 128×128 的 RT 把海拔 31.3 m → 7968.8 m 铺成 128 行（每行 62.50 m）——
    **一次渲染就是一条 T-vs-海拔 曲线**，不需要移动相机。
    自带太阳（三个仰角）、自带 `T_ref` 覆盖（`s_DebugTRefOverride`）。
  - **实测（RTX 3060 / D3D11，`groundLevelWorldY = 0.0`，`bottomRadius = 6360.0 km`）：**

    | 判据 | 高 60° | 中 20° | 低 6° | 门 |
    |---|---|---|---|---|
    | ① 残差/容差（加法形式） | 0.109 | 0.126 | 0.185 | < 1 |
    | ① 除法形式 | 1.594E-3 | 1.215E-3 | 4.193E-3 | 可读测量 |
    | 　全图 max M | 3.608E-1 | 6.782E-1 | 7.177E-1 | > 3.125E-2 才过门 |
    | ② 比值 vs CPU 闭式 | 0.149 % | 0.167 % | 0.189 % | 1 % |
    | ③ 信号/容差 | 15.2× | 76.1× | 158.2× | ≥ 5× |
    | ④ max&#124;R2a−R2b&#124; | 0 | 0 | 0 | ≤ 9.766E-4 |
    | ⑦ 单调性违反次数 | 0 | 0 | 0 | = 0 |
    | 比值包线 | [1.0000, 1.2236] | [1.0000, 1.6611] | [1.0020, 4.5898] | 张开 ≥ 0.05 |

    对应的 `T_ref`：60° `(9.292E-1, 8.463E-1, 7.278E-1)`、20° `(8.325E-1, 6.591E-1, 4.508E-1)`、
    6° `(5.846E-1, 2.960E-1, 9.158E-2)`。
    判据⑤（语义闸门，下发 `w = 0`）：覆盖 16384/16384、哨兵 0、
    **最大 &#124;比值 − 1&#124; = 0.000E+000** —— 精确 0，不是「小于地板」，
    这是这条判据可能给出的最强通过。
    判据⑥（钳位覆盖，太阳 20°）：c1 `T_ref = 1e-3`（不触闸门）读数 ∈ [4.5200E+2, 9.2450E+2]、
    最坏相对误差 0.167 %、CPU 判定未夹住 49152/49152；c2 `T_ref = 1e-5`（只触上限）
    与 c3 `T_ref = 0`（下限+上限，二者无法互相分离，如实记为不可分离）读数都精确
    钉在 1e4、全部夹住、NaN/inf 各 0。
  - **判据①有加法与除法两种形式，两者都报。** 加法形式量的是
    `|(G + M·ratio) − (G + M) − M·(ratio−1)|`，好处是分母恒定、不受 M 趋零影响；
    除法形式量的是 `((G+M·ratio) − G) / M / ratio − 1`，只在 `M > 32×地板` 的像素上算，
    好处是读数直接是相对误差、人能读。两者的最坏点都落在同一片区域。
  - **判据②的口径是诚实的：它测不到 `T_ref`。** GPU 与 CPU 两侧的 `T_ref` 是同一个
    上传值，在比值里约掉了 —— 所以这条判据实际验证的是
    `positionWS → (r, muSun)` 这一步的参数化、加上 Transmittance LUT 的双线性误差。
    0.149 %~0.189 % 正是后者的量级。
  - **`k_SentinelGate` 取 2e4，与姊妹自检的 1e4 **故意**不同**，而且这一轮**真的用到了**：
    6 档载荷是比值本身、被 `VISTA_T_RATIO_MAX` 钳在**恰好** 1e4，
    沿用 1e4 会把判据⑥的 c2/c3 整片读成「未覆盖」—— 而那两档存在的唯一意义
    正是验证这个钳位。
  - **未覆盖清单**（自检自己印，不当通过）：`VistaEarthShadow` 分支
    （三个配置的 `muSun` 跨度分别只有 [0.8658, 0.8661] / [0.3417, 0.3421] / [0.1042, 0.1046]，
    远离 0，那条分支一次都没进）；`MixRealtimeAndBakedGI` 的 subtractive 半边
    （布景是运行时搭的，墙没有 lightmap UV / index，这个夹具**结构上**覆盖不了，
    要另外补一个带烘焙数据的场景）；点光/聚光；延迟 / GBuffer 路径。

- **`ShaderLibrary/FogMedium.hlsl`：雾介质模型**（Task #17，Step 3 的共享层）。
  只回答"空间某一点的雾是什么"（σ_t、σ_s、HG 的 g、归一化密度），
  不回答"这一点被照亮了多少" —— 后者统一在 `AtmosphereScattering.hlsl` 里，
  与 Rayleigh / Mie / 臭氧在**同一个表达式**里合成。
  这条分界是刻意的：雾有两个消费者（档 A 近层 froxel、档 D 并进 AP LUT），
  它们的**循环结构不同**但物理必须逐位相同，所以循环各写一遍、物理只有一份 ——
  与 `AtmosphereScattering.hlsl` 开头「只复制循环，绝不复制物理」是同一条纪律。
  - `CBUFFER_START(VistaFogCB)` 三个 float4：`_VistaFogAlbedo`（xyz 反照率 / w HG 的 g）、
    `_VistaFogExtinct`（xyz σ_t 在雾层底 1/km / w 自遮蔽的掠射放大上限）、
    `_VistaFogHeight`（x 相机相对雾层底高度 m / y 1/标高 1/m / z 环境项强度 / w 自遮蔽开关）。
    具名 cbuffer，SRP Batcher 兼容。
  - `VistaFogHeightMeters(tKm, rayDirWorldY)`、`VistaSampleFog(heightMeters)`、
    `VistaFogSampleNone()`、`VistaFogTransmittanceToSun(fog, sunDirWorldY)`。
    HG 相位**没有**在这里重写 —— 用 `AtmosphereScattering.hlsl` 里已有的
    `VistaHenyeyGreensteinPhase`；环境项也没有重写 —— 用 `SphericalHarmonics.hlsl` 里
    `VistaShAmbientMean`（那个函数的注释本来就写着"体积雾的环境项用这个"）。
  - `VistaEvaluateScatterSample` 加了 `VistaFogSample fog` 参数，
    `VistaRaymarchSettings` 加了 `fogAmbientRadiance`。四个调用点全改；
    不需要雾的三个（Transmittance / MS / SkyView，都走 `VistaIntegrateScatteredLuminance`）
    与 AP kernel 现在都传 `VistaFogSampleNone()`。
  - **回归判据（#17 唯一能给的那条）**：`VistaFogSampleNone()` 是全零字面量结构体，
    内联后常量折叠会把雾的三项连同 `VistaFogTransmittanceToSun` 一起消掉，
    而 `+0.0` 对有限浮点是精确的 —— 所以本次改动对 AP LUT **逐位无影响**。
    这个不是"看起来没变"：`Validate AP Variant A-B Agreement` 重跑后
    ②b 最大 `|A − B| = 1.221E-004`、越界 0、2.00 fp16 ulp，
    与改动前记录的读数四位有效数字全部相同；②-0 的动态范围仍是 100.0×。

- **档 D：雾并进 AP LUT 的 march**（Task #18，移动端主线 / PC 的远景层）。
  雾成为大气之外**并列的第四个介质组分**，只进 AP LUT 与（将来的）近层 froxel 两条
  march，静态 Transmittance / MS LUT 与 SkyView LUT 一律传 `VistaFogSampleNone()`。
  理由三条，都写在 `FogMedium.hlsl` 的文件头里：尺度差 3 个数量级（Mie 标高 1.2 km
  vs 雾 10~200 m）、静态表是球对称烘一次的（把雾并进去等于让雾在 6360 km 的球壳上生效）、
  SkyView 的 uv 打包依赖方位对称（局部雾会破坏它）。
  - `Runtime/Atmosphere/VistaFogSettings.cs`：美术口径的参数 → shader 口径的换算。
    **平均自由程 L（m）** `σ_t[1/km] = 1000/L`（HDRP 的 Fog Attenuation Distance
    就是这个量，所以口径有先例）或 **气象能见度 V（m）** `σ_t = 3912/V`
    （Koschmieder，2% 对比阈）。UE 那个无单位的 Fog Density 不采用。
    σ_s 不单独存，由 `σ_t × 反照率` 给出 —— 各存一份的话 `σ_s > σ_t` 这种
    不物理的组合在数据上就可表示，而它的症状（雾越远越亮）会被读成"积分器写错了"。
  - `_VistaFogHeight.x` 存的是**相机相对雾层底的精确米制高度**，不是从 `posKm` 反算的。
    fp32 在 6360 上的 ulp 是 0.49 m，一个标高 20 m 的地面雾整条剖面会被量化成 ~41 级
    台阶，症状是水平条带**且条带位置随相机高度跳**。
  - `VistaApReference`（4096 步参考解）**也做成含雾的**。不这么做的话 #7 的切片判据
    会产生一个巨大的**假失败**，而它的形状与"切片分布装不下雾"一模一样。
  - **Editor 自检 `Window/Vista/Validate Fog (AP + Sky)`**：4 视角 × 9 配置
    （视角①跑全部 9 档，其余三个视角跑签名档 B/C/D/G）。
    终判 **`雾自检通过（AP + 天空）`**（#18b 把天空那半边并进同一个菜单项之后，
    终判串从 `AP LUT 雾自检通过` 改了名 —— 旧串在 Editor.log 里仍然存在于更早的
    运行记录里，按旧串 grep 会拿到过期的读数）。

    | 判据 | 实测 | 门 |
    |---|---|---|
    | errCenter（行进循环） | 0.07% ~ 0.49% | < 5% |
    | errMid「实现」恒等余量 | **−0.056% ~ +0.068%** | ≤ +0.30% |
    | (3,0,z) 通道打包残差 | 9.7E-5 ~ 4.9E-4 | < 3E-3 |
    | 雾注入度（雾真的改变了表） | 23% ~ 984% | > 10% |
    | 视锥有效性 &#124;注入① − 注入②&#124; | 103.07% | > 5% |
    | 关雾 vs 空对照 max&#124;Δ&#124; | 0.000E+000（对照 0.000E+000） | 逐位相同 |

    「恒等余量」跨越零、有正有负，这本身就是这条断言**真的在算**的证据：
    若它恒等于 `−(固有+端点)`（即实测那一路根本没写），读数会全是一个大负数。
    正的那几个 0.0x% 正是预测的 fp16 存储圆整残差。
  - **判据③（CPU 侧钳位）**：`g = 1.5 → 0.99`；标高 `∞ → (1/H = 0, 自遮蔽关)`
    （无限标高下自遮蔽的 τ 无界，会把雾在阳光下压成全黑 —— 那是个授权错误，
    所以 CPU 侧直接关掉这一项而不是静默钳一个标高上限）；`Off → 三表全零`。
  - **errMid 现在判两条各自可失败的断言**，而不是一个平门（结构见「取舍」）：
    「实现」＝ 实测 ≤ 固有 + 端点 + 0.30%（这是**恒等式**，只会被实现错误破）；
    「分布」＝ 固有 < 2%（`固有` 一个 LUT 值都不含，改被测代码骗不过它）。
    最有力的一组读数是视角③（相机 300 m 俯视 20°）配置 D 的中景段：
    **实测 19.13% / 固有 19.34%** —— `固有` 比 `实测` 还大，即
    **这一段的误差全部是 Log-32 分布在 877 m 雾拐点上的固有重建上限，
    march 一分没贡献**（它的端点误差还抵消掉了一点插值误差）。
    B/C/G 三档同形：8.18%/8.36%、3.39%/3.59%、8.03%/8.27%。
  - **AP LUT 现在存预曝光辐亮度**：`float4(luminance · VISTA_EXPOSURE, greyTransmittance)`，
    绝对单位的消费端乘 `rcp(VISTA_EXPOSURE)` 还原。浓雾把柱内累积量推到 fp16 上限
    65504 之外（雾把整条视线变成一个亮源），而 EV100 = 15 的曝光系数是 2.542e-5，
    预曝光后峰值回到 O(1)。HDRP 的 volumetric lighting buffer 就是这么存的。
  - **`Window/Vista/Validate Atmosphere LUTs` 重跑（晴空回归）**：终判
    **`大气 LUT 自检通过`**。出厂档 `★ d=32 Logarithmic near 20m，步/柱 168，
    显存 512 KB，合格 ✔ 含余量 ✔`；扫描 20 组全部 `✔◎`；
    最便宜的合格候选仍是 `d=16 Power k=2，步/柱 138，256 KB`。
    第二视角（海拔 1 km／太阳 10° 落日）复核 `errC 0.05%`、`固有最差 0.22%`、
    `恒等余量 0.021%`、跨视角 `Δ 0.06%`。
  - **实测性能（RTX 3060 / D3D11）**：整链含雾 **0.220 ms** vs 晴空 0.210 ms（阈 0.300 ms）。
    成本拆分见「取舍」里那条「用均匀雾把两件事分开」。
- **天空像素的雾：逐像素闭式解（#18b）**。档 D 之前只把雾并进了 AP LUT，
  而 AP 合成刻意排除天空像素（`VISTA_AP_IS_SKY_DEPTH`），于是浓雾场景里
  **地平线以上是干净的蓝天、以下的远山已经被雾糊住**，交界处一条硬边。
  现在天空盒里补一条两项式：`L = L_sky·T_fog + albedo·(J̄_direct·T̄_sun + J̄_ambient)·(1 − T_fog)`。
  - **为什么是逐像素解析式，而不是"再存一份仅雾的 AP 子表"或"把雾并进 SkyView LUT"。**
    雾沿视线的透射率是 `exp(−k/dir.y)`，它对 `dir.y` 的导数在地平线处**发散**。
    AP 表在屏幕上只有 32×32、SkyView 只有 192×108 —— 用任何一张表去重建这个梯度，
    台阶都恰好落在**地平线**上，也就是这个特性唯一重要的地方。
    再加一张 32³ fp16 表还要 +512 KB，而档 D 存在的理由本来就是移动端。
    解析式约 10 条 ALU、零新纹理、梯度精确。
  - **入散射项是恒等式而不是近似**：`σ_s = albedo·σ_t`、`u = σ_t∫ρ` 时
    `∫₀^∞ σ_s ρ J̄ e^{−u} dt = albedo·J̄·(1 − T_∞)` 逐通道**精确**（给定常数 J̄）。
    σ_s 在换元里约掉了 —— 这就是天空这条路要的是无量纲反照率、而不是 σ_s 的原因。
  - **自遮蔽的入散射加权均值 `VistaFogSunTransmittanceMean`（本次的主项修正）**。
    march 逐样本采局部密度，所以自遮蔽沿射线是变的；两项式只有一个 J̄。
    第一版把它冻在**相机处**的密度上，那是 τ → ∞ 的极限，中等厚度下系统性偏暗。
    换元 `x = e^{−t/L}` 后积分有闭式解：
    `T̄ = [(e^{−k} − e^{−τ})/(τ − k)] · τ/(1 − e^{−τ})`，
    三个极限都对：`k→0 ⇒ 1`、`τ→∞ ⇒ e^{−k}`（旧实现正是这个极限）、
    `τ→0 ⇒ (1−e^{−k})/k`。约 6 条 ALU，**没有任何经验常数**。
    实测：布景②（太阳 5°）档 G 的相对差 **34.82% → 1.99%**、布景① **6.11% → 1.32%**；
    而自遮蔽**关**的两档（①B 1.30% / ②B 1.72%）**逐位不动** ——
    这同时证明了 `tauSun = 0` 时该函数退化成精确的 1.0（`[(1−e^{−τ})/τ]·[τ/(1−e^{−τ})]`）。
  - **两条等效路径长度的上界来历不同，所以拆成两个函数**：
    太阳向 `VistaFogSunPathKm` 的上界是**观感参数** `_VistaFogExtinct.w`
    （无限大平板在太阳贴地时给出无穷光学深度，会把日出日落的雾压黑，
    而那恰好是雾最该好看的时刻）；天空向 `VistaFogSkyRayPathKm` 的上界是
    **星球曲率给出的物理量** `sqrt(2πRH)`（Chapman 掠射极限，标高 50 m → 44.7 km），
    没有美术旋钮。塞进同一个参数，调用点就看不出上界是哪来的了。
  - **失能态改成零态**：`VistaFogSunPathKm` 在开关关时返回**路径长度 0** 而不是
    提前返回透射率 1 —— 这样关态的 `exp(−0)` 逐位为 1，*而且*上面那个加权均值
    自动退化成 1（k = 0 的解析极限），不需要在两个地方各写一次开关判断。
  - **Editor 自检扩到 3 布景 × 5 档（`Window/Vista/Validate Fog (AP + Sky)` 同一个菜单项）**，
    终判 **`雾自检通过（AP + 天空）`**。天空那半边四条判据，各配**独立**的尺子：

    | 判据 | 内容 | 实测 | 门 |
    |---|---|---|---|
    | 4 | `T_fog` 闭式解 vs 按雾 e 折定步长的高步数积分 | 全格 ~6.7E−6 | < 1E−3 |
    | 5 | 整个两项式 vs 同一射线的高步数含雾 march | 见下 | 3%／实测基线 |
    | 6 | 雾关 ⇒ 两项式输出与无雾 march **逐位**相同 | 0.000E+000 | 逐位 |
    | 7 | 覆盖性：Chapman 上界 168 次／dir.y 地板 30 次／自遮蔽开 192 次／掠射上限 64 次 | 四支全非零 | > 0 |

    判据5 逐布景：①（雾内 cam2m 太阳60°）B 1.30% C 0.31% D 1.39% G 1.32%（门 3%）、
    H 2.44%（均匀雾基线 8%）；②（雾内 cam2m 太阳5°）B 1.72% C 0.46% D 1.80% G 1.99%、
    **H 5.65% @仰角 55.5°**（基线 8%，余量 2.348%）；③（雾上 cam300m）
    B 6.09% C 16.44% G 6.09% H 2.34%（掠射基线 20%）、**D 空判据**。
  - **AP 那半边逐行核对没动**：两次运行的 AP 行 **29/30 逐字节相同**，
    唯一的差别是通过/失败的表头 —— 天空侧的改动没有扰动 AP。
  - **`Window/Vista/Validate AP Variant A-B Agreement` 重跑**（选"存预曝光辐亮度"时
    承诺要重跑的那一条）：**通过**，覆盖 11368 像素，最大 |A − B| = 1.221E−4
    = **1.00 fp16 ulp**（阈 3 ulp = 变体 A 多出的舍入次数），比 Weber 1% 严 6.8 倍；
    ②-0 动态范围 62.9×（≥ 10× 才算有分辨力）。
    晴空下 `VistaFogSourceRadiance` 收的是 `VistaFogSampleNone()`，
    所以那次签名变更在这条判据上应当**零影响** —— 实测确认。

- **近层体积雾的 froxel 体：资源 + 切片分布 + 判据（#19）。**
  三张 3D RT（注入 / 注入历史 / 深度积分，均 `R16G16B16A16_SFloat` + UAV），
  生产档 1080p /8 × 64 片 = **240×135×64**。参考：UE5 的 `VolumetricFogGridPixelSize`
  默认 8、HDRP 的 V-Buffer 也是屏幕 /8；切片数 64 = HDRP Medium。
  **本条只交付「能被对账的骨架」**：逐 froxel 光照注入是 #20、深度积分是 #21、
  时间重投影是 #22。
  - **XY 用「屏幕比例」而不是固定 240×135。** 后者在 2560×1440 下变成 1/10.7，
    效果会随分辨率漂移 —— 同一套参数在两台机器上不是同一个画面。
  - **切片分布的存储约定，以及它与 HDRP 的一处刻意分歧。**
    编码坐标 `e(d) = ln(d/near)/ln(far/near)` **直接就是 3D 纹理的 w 坐标**，
    切片 i 存「相机到 `d_i = near·(far/near)^((i+0.5)/N)` 的累积」——
    即存储距离**正好落在纹素中心**，读端 `w = e(d)`，一个偏移都不用记。
    HDRP 存的是分段远平面 `t(i+1)`，于是 `e(t(i+1)) = (i+1)/N` 而纹素中心在
    `(i+0.5)/N`，读时必须显式回退半个纹素 —— 它那个已知的 half-slice bias 就是这么来的。
    实测：`Encode(Stored(i)) − (i+0.5)/N` 五档最差 **9.75e-8**；
    若采用 HDRP 的约定，这一格会读到 `0.5/N` = 7.8e-3 ~ 1.6e-2，**相差 5 个量级**。
  - **代价（必须显式说出来，不能悄悄带过）：体积实际的远端是
    `d_{N-1} = far·(far/near)^(−0.5/N)`，不是 `far`。**
    生产档实测 **61.3740 m**，比参数 64 m 短 **4.10%**。
    所以 **AP 的 `nearDistanceKm` 必须填 0.061374，不是 0.064** ——
    填后者会在 61.4~64 m 之间留一段两层都算过的雾，
    症状是那个距离上一圈很淡的亮环。五档实测短缺：
    A 4.10% / B（32 片）8.04% / C 5.77% / D（128 片）1.78% / E 4.95%
    —— 片数越少越短，这也是为什么它不能当常数硬编码。
  - **分段的介质求值点取两个存储距离的几何均值**，闭式解正好是 `e = i/N`，
    即 `near·(far/near)^(i/N)`；分段 0 退化成度量中点 `0.5·d_0`（几何均值在 0 处退化）。
    它比分段的**度量**中点早 `(√ρ−1)/(ρ−1) − 0.5`：五档实测
    A −0.010473 / B −0.020936 / C −0.014841 / D −0.004497 / E −0.012697。
    这个数**不设门、只报数**，它是 #21 深度积分的一阶偏置输入。
  - **分段 0 从相机（0）而不是从 `near` 开始。** 相机与近裁剪面之间那一段薄雾
    若被跳掉，症状是贴脸处少一点雾 —— 量小且没有报错。判据里单列一格：
    片 0 的 `segNear` 必须**精确为 0**（相对误差在 0 处无定义，只能判绝对）。
  - **远边界被阴影距离夹紧时显式报错，不静默夹。**
    美术把范围调到 500 m、画面却没变，这个问题在日志里查不到任何线索。
    夹紧源取 `UniversalCameraData.maxShadowDistance`
    （URP 里 = `min(asset.shadowDistance, camera.farClipPlane)`），
    因为远裁剪面也会砍掉阴影距离。**阴影全关时 URP 把它置 0，那时必须豁免** ——
    否则体积被压成 0，把「没有光柱」升级成「连雾都没有」。
    判据是 6 行纯 CPU 表，包括 `(500, 0) → 500 不夹` 这个豁免的真正测点，
    且**同时断言诊断串的有无**（只判数值的话，静默夹紧照样全绿）。
  - **占位 kernel 写的是四个「距离」而不是 froxel 中心的世界坐标。**
    世界坐标会把视锥四角插值那套**方向**映射折进来，而那套映射已由 AP 的
    round-trip 判据覆盖（两者共用同一个 `VistaApFroxelRayDirection`）——
    折进来之后一次失败无法归因到「距离映射」还是「方向映射」。
    四个通道故意各不相同且量级差一个 ρ：若 XY 与 Z 轴被写反、或索引差一片，
    读回来的比值就不是 ρ。写成 `(1,1,1,1)` 那种「能证明写过」的哨兵值就抓不到轴序错误。
    存在的理由是 `RWTexture3D` 绑定失败这个坑 —— `RTHandle` 的 2D 便捷重载会
    静默建成 Tex2D，症状只有 Editor.log 里一行 warning，画面上是整张表全零。
  - **`VistaFogSettings.Mode` 里仍然没有 `Froxel` 档，这是有意的。**
    在 #20 的注入光照存在之前，那个枚举值选了什么都不会发生 ——
    等于把一个哑档位摆给美术。RenderGraph 接线同样推到 #20；
    #19 的代码只经**立即派发器**（`VistaImmediateLutDispatcher`）被 Editor 自检消费。
  - **自检 `Window/Vista/Validate Froxel Volume (Slices)`：5 档 × 9 条判据全绿**，
    终判 `froxel 体自检通过（分布 + 分配 + 夹紧）`。五档覆盖生产档 / 32 片移动候选 /
    宽范围 0.1~200 m / 128 片密切片 / 被夹紧档；D 与 C 的 XY 相同而深度不同，
    顺带验证分配脏检查认得出深度变化。
    - 纹理往返（fp16，门 1e-3）：7.771e-4 / 7.212e-4 / 9.104e-4 / 9.572e-4 / 8.865e-4
      —— **只有 1.04~1.39 倍余量，这条读数无法在不换 RT 格式的前提下收紧**，
      因为 fp16 的相对半 ulp 就是 4.883e-4。
    - 解析路径（fp32，门 1e-6）：编解码 round-trip 最差 6.41e-7、
      纹素中心恒等式最差 9.75e-8、相邻比最差 5.29e-7。
    - **fp16 往返与 fp32 解析拆成两组读数、两个门。** 把恒等式建立在读回来的
      fp16 值上，判据的地板会被抬到 1e-3 —— 那时「纹素中心恒等式」（要求 1e-6）
      根本无法判定，而它恰好是这套分布约定唯一的立足点。
    - 接手点对 `far·ρ^(−0.5/N)`（一个**与实现里 `near·ρ^((N−0.5)/N)` 不同的表达式**）
      最差 1.6e-7；对 GPU 读回的最后一片最差 5.0e-4。

- **逐 froxel 光照注入：级联阴影 + 主光 + 天空环境（#20）。**
  近层体第一次装进真实的能量：`FroxelInjection` 核逐 froxel 求
  `(σ_s·J 预曝光, 灰度 σ_t)`，接进 `VistaAtmospherePass` 的
  `RenderPassEvent.BeforeRenderingPrePasses`。深度积分是 #21，时间重投影是 #22，
  所以 `VistaFogSettings.Mode` 里**仍然没有** `Froxel` 档 ——
  注入挂在 `VistaVolumetricFogSettings.enableInjection` 这个开发者开关上（默认关）。
  - **阴影查询直接调三个底层函数，不走 `MainLightRealtimeShadow`。**
    后者（`Shadows.hlsl:385`）在 `MAIN_LIGHT_CALCULATE_SHADOWS` 未定义时
    **直接 `return 1.0`** —— 关键字漏设的症状是「整个场景没有光柱、且不报错」。
    改调 `GetMainLightShadowSamplingData()` / `GetMainLightShadowParams()` /
    `SampleShadowmap()`（`Shadows.hlsl:151 / 186 / 306`，三个都没有关键字门），
    漏设的后果退化成「全按级联 0 查」= 远处光柱错位，一个**能被看见、能被归因**的症状。
  - **`#pragma multi_compile _ _MAIN_LIGHT_SHADOWS_CASCADE`，只声明这一个。**
    不声明 `_MAIN_LIGHT_SHADOWS_SCREEN`：`TransformWorldToShadowCoord`
    （`Shadows.hlsl:356`）对它是 `#if defined`，没声明就在**编译期**选中级联矩阵那一支，
    于是这个核永远不会去读屏幕空间阴影图（compute 里根本没有屏幕坐标）。
    不声明 `_SHADOWS_SOFT*`：tent 是 4/9 tap，逐 froxel（默认档 207 万个）付不起，
    而体积雾是低频的 —— HDRP 的 `VBufferLighting` 同样只取 1 tap。
  - **`isPerspectiveProjection` 必须传 `false`。** 走 `#else` 那一支时
    `TransformWorldToShadowCoord` 返回的 `w` 恒为 `0.0`（`Shadows.hlsl:361`），
    传 `true` 会让 `SampleShadowmap` 做 `xyz /= w` = 除以 0 ⇒ 整张表 NaN，
    而 NaN 会顺着三线性插值蔓延到全屏。判据⑦ 实测 0 / 16384 非有限。
  - **URP 的记录顺序 grep 过了，注入 pass 不需要改 event。**
    `UniversalRendererRenderGraph.cs`：`OnRecordRenderGraph`(:588) →
    `OnBeforeRendering`(:647，体在 :733) 里记录阴影 pass 并赋
    `resourceData.mainShadowsTexture`(:750)，而 `BeforeRenderingPrePasses`
    要到 `OnMainRendering`(:990) 里的 :1009 才被记录 —— 严格**在阴影之后**。
    `_MAIN_LIGHT_SHADOWS_CASCADE` 也是在阴影 pass 的 render func 内
    用 `cmd.SetKeyword`(:277) 设的 GlobalKeyword，全局关键字对 compute 生效。
    两条都由判据实测确认（flags 读回 `0x13` = RAN|SHADOWMAP|CASCADE，URP asset 4 级联）。
  - **`_MainLightShadowmapTexture` 不需要逐核绑**：阴影 pass 用
    `builder.SetGlobalTextureAfterPass(...)`（`MainLightShadowCasterPass.cs:413`）
    把它发布成全局纹理。两张静态大气表与 SH buffer 反而**逐核显式绑**，
    理由是 Task #7 那笔旧账（依赖「Sky-View 一定先跑」的那一版里，改相机高度与太阳角度
    一个读数都不动，而且不报错）。
  - **消光存灰度而不是 RGB，包络量过了。** 一个 `float4` 就够，分通道要三张表全部翻倍。
    默认档 `D = 61.374 m`：Rayleigh `τ_blue = 2.0e-3` vs 灰度 `1.07e-3`
    ⇒ 透射率差 **0.096%**；夹到阴影距离的极端档 `D = 500 m` ⇒ **0.78%**；
    雾自己的 `σ_t` 中性色时**精确**为 0 误差。都在 1% 的 Weber 门内。
    顺带一条**反直觉的结论**：近层体的 `σ_t` **必须含大气**（Rayleigh + Mie + 臭氧）——
    `D = 500 m` 时蓝光的大气 `τ` 是 1.66%，已经越过门，
    「近层只算雾、大气整段留给 AP」是不成立的。
  - **相位函数在注入时求值**（HDRP / UE5 路线），不留到 #21 积分时。
    代价是每个 froxel 只有一个相位方向、时间重投影时相位跟着历史一起被拖 ——
    #22 的 ghosting 读数因此**没有对照臂**（没做运行时 A/B 开关），
    这条是「接受 HDRP/UE5 的已知局限」，不是「量过之后选的」。
  - **`Window/Vista/Log Volumetric Fog State`：11 格覆盖性判据全绿。**
    它**不是**数值判据（能量对不对由 #19 的分布判据与 AP 那批误差判据兜着），
    要抓的是**跨模块接线**这一类失效。走 `Camera.Render()` 同步渲一帧
    而不是 `SceneView.RepaintAll()`：后者是排队重绘，菜单回调返回时还没画完，
    读回来的会是**上一次**请求的结果 —— 而 min/max 是跨帧单调的，
    那种错位会长得非常像一个合理的读数。
    - 原始槽位 `[199999, 1000000, 19, 16384, 1034, 0, 0, 381, 0, 998626, 800000, 2048, 2048, 1]`。
    - ⓿ 槽位容量 `buffer.count = 14 = k_ShadowProbeSlots = 本文件期望`。
      三处对不上的症状不是报错 —— D3D11 上越界 UAV 写是**静默丢弃**，
      判据会读到恒为初值的格子并把它当成「这一路没执行」。
    - ① flags `0x13`（含「跑过」位 RAN）。没有 RAN 位的话，一个从未派发的核留下的全零
      与「级联关 + 阴影图未绑 + 屏幕空间关 + 软阴影关」这个**完全合法**的组合
      在报表上长得一模一样。
    - ② 编译期关键字：CASCADE `true`（期望从 URP asset 的 `cascades 4` 推），
      SCREEN / SOFT 都必须 `false`。
    - ④ 探针网格 `16384 / 16384`（32×32×16，**固定值不跟体积分辨率走**：
      跟着变会让「换档后 min 变了」分不清是阴影变了还是采样点变了）。
    - ⑤a 阴影值域 `[0.199999, 1.000000]`；⑤b 被遮 `1034 / 16384`。
    - ⑥a atlas 尺寸 `GetDimensions ⇒ 2048×2048`。
    - ⑥b atlas 深度 `min 0.000000 / max 0.998626` ⇒ 跨度 `9.986e-1`（门 > 1e-3）。
    - ⑦ 非有限 froxel `0 / 16384`。
    - ⑧ fp16 余量：注入 rgb 最大 `3.810e-1`（预曝光后）⇒ 距 65504 天花板 **×1.719e5**。
      这个读数是从 RGBA16F 的 UAV **读回来的**，不是 CPU 侧重算 ——
      fp16 的饱和只有走一趟纹理往返才算量过。
    - ⑨ `|_WorldSpaceCameraPos − _VistaFroxelCameraWS| = 0 mm`（门 ≤ 1 mm）。
      `GetMainLightShadowFade`（`Shadows.hlsl:434`）用 URP 的 `_WorldSpaceCameraPos`
      算淡出距离，而 `_VistaFroxelCameraWS` 存在的理由恰恰是不信任那个全局。
      仍然调它、不自己重写那一行 `saturate` ——「同一个量的第二份实现连 8 行的
      辅助函数也算」。代价就是这条依赖，而这一格把它变成一个**能失败的数字**。
    - 实测分配口径 `138×74×64, near 0.30 m, far 50.0 m（从 64 夹下来）,
      handoff 48.041 m, ρ 1.083219`；夹紧诊断按设计只报一次。
      **（#21 时布景变了，这一档已不再被 shadow distance 夹住：
      `far 64.0 m, handoff 61.374 m, ρ 1.087406`。本节其余读数中
      `被遮 1034` 与 `槽位 14` 同样是当时的布景/版本，#21 一节里是现值。
      不改这里的数 —— 一份写下来的读数是当时那一次运行的证据，改它等于伪造。）**
    - **两条未覆盖路径按设计点名**：注入历史表（#22）与积分表（#21）的**写入**路径
      本节一次都没跑过，`VistaAtmospherePass` 里也刻意没有 `ImportTexture` 它们 ——
      顺手导入会让「没人写」这件事在代码里看不出来。
      **（两条都已结清：积分表在 #21，历史表在 #22a。而 #22a 顺带证明「历史表的写入路径」
      这个说法本身就是错的 —— 双缓冲只交换写下标，没有谁往「历史」里写。
      详见待办里那一条与「坑」里那条自我否证的文档。不改这里的原话 ——
      它是当时那次运行与当时的理解的证据。）**
  - **Vista 的 `VolumetricFog.compute` 是本项目第一个 `#include` URP `Shadows.hlsl`
    的 compute shader，编译干净、跑通。** `Shadows.hlsl` 自己 include 了 core 的
    `Common.hlsl` / `ShadowSamplingTent.hlsl` / `GlobalSamplers.hlsl`、URP 的 `Core.hlsl`、
    core 的 `CommonMaterial.hlsl` 与 `Shadows.deprecated.hlsl`（:4–9），
    所以只 include 它一个就够。此前标记的风险没有成立。

- **沿视线的深度积分 + 调试视图（#21）。**
  近层体第一次成为一张**可消费**的表：`FroxelIntegration` 核按柱（一个线程一根 froxel 柱）
  从近到远递推 `L += T_acc · ∫ₛₑ𝓰 S·e^(−σs) ds`、`T_acc *= e^(−σ·Δ)`，
  逐切片写出累积量。**消费它的合成仍然没写**（那是 #25），
  所以这一条同时交付了让它在画面上可见的手段：`Hidden/Vista/FroxelDebug` 调试视图。
  - **积分表的 alpha 存 `1 − T`，与 HDRP（`_VBufferLighting`）和 UE5
    （`IntegratedLightScattering`）都相反。** 两条理由，第二条是意外收获：
    ① **清空态安全**：这张 3D 表的清空值是 0，存 `1 − T` 时 0 ⇒ `T = 1` ⇒ 完全通透，
    也就是「没人写过」等于「没有雾」；存 `T` 时 0 ⇒ 全黑，
    一次漏派发会表现为**整屏黑**而不是「没有雾」。失能态 = 零态。
    ② 顺带买到一条**可判性**：`max(1 − T) > 0` 就是「有人写过这张表」，
    不需要额外的哨兵槽位。存 `T` 的话清空态 0 与「雾浓到不透明」是同一个合法读数。
  - **段内积分复用 `VistaSegmentIntegral`（`AtmosphereScattering.hlsl:492`），
    不为 froxel 另写一份。** AP LUT 核（`AtmosphereLut.compute:415/:624`）与积分核
    因此是同一份实现 ——「同一个量的第二份实现连 8 行的辅助函数也算」。
    代价是**一个已经存在的共享函数，其潜在 bug 会在新的调用点第一次被触发**：
    `σ → 0` 时那一支写成「两支都算、再用 `lerp` 选」，而
    **一个 NaN 闸如果只在一支分支里，`lerp` 会把 NaN 从被丢弃的那一支带回来**。
    修法是分母改 `max(σ, 1e-30)`，两支都无条件有限。AP 侧同时受益。
  - **望远镜恒等式：均匀介质下离散递推**精确**等于连续闭式解。**
    因为分段恰好平铺 `[0, dᵢ]`（`SegmentNear(0) = 0`、`SegmentFar(i) = SegmentNear(i+1)`,
    这三条 #19 判过），中间项全部抵消。判据不算任何误差、只搬原始读数，
    余量**带符号**打印。六档实测残差（double）：
    `5.895e-15 / 2.194e-15 / 6.793e-14 / 4.113e-15 / 4.917e-15 / 1.596e-11`。
    **让核多写一行就能把近似换成恒等式时，就多写那一行。**
  - **三种被拒的积分写法，逐段与累积偏差并排量过**（同一条近似的两个数量级不同，
    只印累积会把一个一阶误差读成可接受）：
    - 漏乘 `T_acc`（把每段的 in-scatter 直接相加）：累积
      `+196.91% / +180.12% / +189.66% / +207.34% / +420.99% / +0.05%`。
      最后那一档 0.05% 是**光学薄**档 —— 一道只看最亮布景的门会漏掉它。
    - 段内用中点透射率（`S·Δ·e^(−σ·Δ/2)`）：累积 `−0.04% … −0.14%`，
      逐最长段 `−x²/24 = −0.24% … −1.40%`。二阶，**只印不设门**。
    - 段内完全不衰减（`S·Δ`）：累积 `+3.61% / +7.37% / +5.15% / +1.58% / +5.17%`。
      最弱一档 D 的 1.58% = 2e-3 门的 **7.9 倍** —— 门要拿最弱档去摆。
  - **级数分支阈值的最优值算出来了：`x* = (24·2⁻²³)^¼ = 4.113e-2`，
    而在跑的是 `1e-4`。** 阈值太小的代价是「本该走级数的段去走了 `exp`，
    在抵消区里丢精度」，六档实测相对代价 `A 8.920e-5 / B 3.930e-5 /
    C 5.598e-4（最差）/ D 1.046e-4 / E 1.119e-4 / F 在级数支上（2.159e-14）`。
    **推迟到 #25**：这个常数由 AP 与 froxel 共用，换它要连 AP 的一整批误差判据一起重跑。
    今天的状态日志把**影响面**量出来了：真实帧的 `x ∈ [6.539e-5, 1.144e-2]`，
    `x_max < x*` ⇒ 换完之后本布景**每一段**都会转到级数支 ——
    那不是一个边角优化，它会整体改写这条积分路径。
    「一条从别人的论文里抄来的经验常数，它的适用区间可能正好被你的另一处优化推出去」。
  - **fp16 的典型-UAV 存储在 D3D11 / RTX 3060 上是**截断**（向零取整），不是最近舍入。**
    方法：对每个（请求值, 读回值）对算 `e = floor(log2(req))`、`ulp = 2^(e−10)`、
    `k = (req − 2^e)/ulp`，同时算 `trunc = 2^e + floor(k)·ulp` 与 `near = 2^e + round(k)·ulp`。
    12 个读回值**全部**等于 `trunc`，但**只有 3 个样本有区分力**
    （`frac > 0.5`：D 档 `S_r` 0.878、F 档 `σ_t` 0.814、F 档 `S_r` 0.787）——
    「一条结论若用 12 个样本支撑而其中只有 3 个有区分力，报表不标出来就等于用 9 个空样本充数」。
    **后果：fp16 的相对地板是整个 eps `2⁻¹⁰ = 9.766e-4`，不是半个 eps `2⁻¹¹`。**
    而且截断是**单边**的，误差会同向累积而不是相互抵消。
    「fp16 的地板是半 eps 还是整 eps，取决于取整模式；把门按半 eps 摆而硬件在截断，
    门就正好压在地板上」。三道门因此重摆：`k_RelGate` 1e-3 → 2e-3、
    #19 的 `k_TextureGate` 1e-3 → 2e-3（它此前的实测是 0.96×，**差一点就是假失败**）、
    以及新加的 `k_Fp16RelTrunc`（与 `k_Fp16Rel` **分开**：一个是往返存储的地板，
    另一个是解析计算的地板，混成一个常数会让两类误差互相掩护）。
    顺带修掉一处**静默解耦**：`k_SynthGate` 的值是手抄的、耦合只写在注释里，
    改 `k_RelGate` 时它会静默脱钩 —— 改成 `const float k_SynthGate = k_RelGate;`，
    **注释断言的耦合要由代码保证**。
  - **`dtKm` 的抵消地板律修正为 `k_PowUlpBudget · ulp · ρ/(ρ−1)`。**
    此前漏了 `ρ/(ρ−1)` 这个放大因子、又把超越函数当成只有半 ulp，
    于是在最容易触发抵消的 D 档判出了一次**假失败**。
    `k_PowUlpBudget = 8.0` 个整 fp32 ulp 的理由：GPU 的 `pow` 与 `Mathf.Pow`
    是同一个超越函数的**两份实现**，D3D11 只保证 `exp2`/`log2` 到 1 ulp。
    修完六档的实测/界比值全部 < 1（`A 0.46 / B 0.26 / C 0.71 / D 0.55 / E 0.66 / F 0.46`）。
    「一个尺子自己算错地板，会在最容易触发抵消的那一档上判出一个假失败」。
  - **`_VistaGround.w`（= `worldToAtmosphere` = 1e-3）是一处会静默归零整张表的陷阱。**
    注入的 alpha 是 `σ_t` in **1/km**，而 `VistaFroxelSegmentNear/Far` 返回**米**；
    这个缩放只在 `VistaAtmosphereParameters.Bind(...)`（:110-111）里上传，
    而那只经由 `PrepareLuts`（`VistaAtmosphereLuts.cs:444`）到达 ——
    一个跳过 `PrepareLuts` 的立即模式自检读到 0，整张表静默归零。
    解决手段是一条**归因行**：与被测核**逐字同一个表达式**打印实测的
    `_VistaGround.w`（六档全部 `1.000e-3`，相对差 `0.000e+0`）。
    **加一条归因行，再去理论化。**
  - **`Hidden/Vista/FroxelDebug`：四档调试视图，是**永久资产**而不是脚手架。**
    合成落地**之后**「画面上的雾不对」有两个来源 —— 表算错了、或者合成读错了
    （w 坐标差半个纹素、透射率乘反、分层接手点错位）。这两种失效在最终画面上
    长得一模一样，把表**单独**画出来是唯一能把它们分开的手段。
    HDRP 的 Volumetric Fog debug view 与 UE5 的 `VisualizeVolumetricFog`
    存在的理由完全相同，两者也都没有在功能完成后删掉。
    - **整屏替换，不叠加**：叠加会让「表是空的」与「表很淡」再次无法区分，
      而这个视图存在的全部目的就是消除这类混淆，它自己不能再引入一次。
    - **三个主档按像素自己的场景深度采样**，不是显示某个固定切片 ——
      看到的就是将来会被合成进去的那一个操作数。第四档「单片」回答的是另一个问题：
      表**自身**在某个深度上有没有洞、有没有条带（深度耦合的视图回答不了它，
      因为空中与天空方向上的切片永远不会被任何像素采到）。两档互补。
    - **越界不静默钳死**：`e > 1`（归 AP LUT）画品红、`e < 0`（比近端更近）画青，
      钳到端点会让「近层没覆盖到这里」与「这里雾很淡」在画面上一样，
      而前者恰好是分层接手点错位的症状。天空的深度是远平面，天然落进 `e > 1`，
      不需要单独分支。品红区的屏占比直接就是 #27 的近层覆盖率读数。
    - **不做色调映射，所以排在 `AfterRenderingPostProcessing`**：
      套了之后「表饱和了」与「tonemap 滚到顶了」在画面上无法区分。
      排在后处理之前会被别人的 tonemap + 分级改写一遍，那时屏幕亮度与表里的数
      就没有关系了 ——「一个看起来像在看数据、其实在看曲线的视图，比没有视图更坏」。
      口径也诚实划清：落到目标里的是 `raw × gain`，交换链自己的显示编码不在本 pass 管辖内。
    - **Off 档整趟 pass 不排入**（不是「排进来但 shader 里早退」）：
      它声明了 `ConfigureInput(Depth)`，光是排入就要一次全屏深度拷贝。
      失能态 = 零态，否则「我关掉了却还有开销」只能靠读 shader 才发现。
    - **切片下标在 C# 侧夹紧，不留给采样器的 `PointClamp`**：
      「一个夹紧规则若留给采样器的 CLAMP 去做，「我填了 200」与「我填了 63」在画面上一样」。
      抽成 `VistaVolumetricFogSettings.ResolveDebugSlice(...)` 让渲染路径与状态日志
      **共用同一份实现** —— 各写一份的症状是日志说「看的是第 63 片」而画面上是第 127 片。
    - **两张表走 `MaterialPropertyBlock` 而不是全局**，于是这一趟 pass 不声明
      `AllowGlobalStateModification` ——「本 pass 不改全局」是代码里读得出来的事实，
      不是一句注释。附件用 `AccessFlags.WriteAll`（整屏覆盖，旧内容一个都不要）。
    - **`VistaFroxelFrameData : ContextItem` 而不是再导入一次 RTHandle**：
      「同一个 RTHandle 导入两次，会在图里产生两条互不相识的资源记录指向同一块显存」——
      不但没换来顺序保证，还把 aliasing 藏了起来。
    - **相机类型的门与产出方**逐字一致**（`Game || SceneView`）。**
      只判 `debugView != Off` 的话，反射探针相机（大气 pass 刻意不为它分配 froxel 体）
      每帧走进「表不存在」那条警告分支，而主相机帧又把去重串清掉 ——
      症状是每秒 60 条警告，且内容指向一个根本没配错的开关。
      同步之后那条警告只剩**唯一**一个成因，可以直接点名。
    - **实测四档全部通过，且抓到了一个此前没被看见的东西**：
      档 3（注入 RGB）里胶囊体右下方有一块暗斑 —— **那是 #20 的逐 froxel 级联阴影
      第一次出现在画面上**（与 `enableSunSelfShadow` 无关，后者是 AP LUT 的解析自阴影）。
      档 2（积分 alpha）的灰↔品红边界是 50 m 径向等值面切过起伏地形，量级与解析值
      `1 − exp(−2.5·0.05) ≈ 0.1175` 一致。档 4（单片 63，≈48 m）无品红、无洞、无条带。
      「表不存在」那条警告分支实测**只报一条**（不是每帧一条），去重生效。
  - **`Window/Vista/Log Volumetric Fog State` 扩到 19 格，`k_ShadowProbeSlots` 14 → 19。**
    立即模式自检（`VistaFroxelIntegrationSelfTest`）用合成介质判数值正确性，
    但它**不覆盖** RenderGraph 那条积分写入路径、也不覆盖真实雾的 `x` 包络 ——
    这五个新槽位是那两件事唯一的覆盖手段。全绿，且逐格复核过「绿的理由对不对」。
    - 原始槽位 `[199999, 1000000, 19, 16384, 1312, 0, 0, 381, 0, 999984, 800000,
      2048, 2048, 1, 129272, 19, 0, 65390, 11439225]`。
    - ⑩ **执行性哨兵用 `SEG_X_MIN`（初值 `uint.MaxValue`，对应 `x = 4.29`，
      物理上不可能）**，不用任何 max 槽位 —— 那些的初值是 0，而 0 是合法读数。
      同时这一格**只**证明探针核跑过，不证明积分核跑过；混成一格的话，
      积分派发漏掉之后它照样全绿，而它绿的理由是错的。后者是独立的 ⑪c。
    - ⑪a 积分表非有限 `0 / 16384`；⑪b `max(1 − T) = 1.293e-1 ≤ 1`
      （**定义性不变量**，不是精度门：> 1 只能来自「乘反了」「递推漏乘」，
      而那类错误在画面上的症状是雾偏浓 ——「一个看起来物理上讲得通的漂移，
      是最容易被接受、因此最容易掩盖污染的形态」）。
    - ⑪c 积分表被写过：`1.293e-1 > 定点地板 1e-6`。
      **作用域点名**：这一格只在布景有介质时能失败。
    - ⑪d 均匀介质上界：`σ_t(地面) 2.5/km × d 6.137e-2 km ⇒ 1 − e^(−σd) = 1.422e-1`，
      实测 `1.293e-1`（比值 `9.088e-1`）。`σ_t` 直接取 `fog.extinctionPerKm`，不重算。
    - ⑪e **量纲门**（新）：`∫S·T dt ≤ max(S)·d`（因 `T ≤ 1`），
      `3.810e-1 × 6.137e-2 = 2.338e-2` vs 实测 `1.900e-2`，比值 `8.125e-1`。
      它要拒绝的最小错答案是**段长差 1000 倍**（米/千米混用，即 `_VistaGround.w` 那个坑），
      而那个错答案会把比值推到 1e±3。裕度 `1.875e-1` 对上定点相对分辨率 `5.263e-2`
      —— 裕度必须大于分辨率，否则这一格是「一个正好压在门上的读数」。
    - ⑫ 包络：实测 `x ∈ [6.539e-5, 1.144e-2]`，推导区间 `[8.1e-6, 0.289]`。
      **这道门宽 ×3.568e4，报表上必须说出来**，否则一个宽门会被读成一道紧门。
      定点缩放取 `1e9` 而不是 `1e6`：后者会把包络下端压到 8 个刻度上，
      「一个定点缩放选小了，包络的下端会落进自己尺子的地板里」。实测下端剩 65390 个刻度。
    - ⑫b **紧上界**（新）：`σ_t(地面) × 最长段`。最长段**扫一遍**取（切片 0 是 `[0, d₀]`，
      不服从后面的等比规律，「假设最后一段最长」是一条不需要的推导），
      实测 `4.933 m（切片 63）⇒ 上界 1.233e-2` vs `x_max 1.144e-2`，比值 `9.275e-1`，
      裕度 `7.248e-2`。段长复用 `desc.SegmentFar/SegmentNear` ——
      **这不是第二份实现**：那两个函数与 HLSL 的 `VistaFroxelSegmentLengthKm` 逐位一致
      这件事已经由 #19 的切片判据盯着，这里等于把那一格的结论接到这一格上。
    - ⑫ 顺带量出一条此前不知道的事实：**级数支在真实帧里是有覆盖的**
      （`x_min 6.539e-5 < 1e-4`），此前以为它只由合成介质自检覆盖。

- **froxel 体的时间重投影 + 采样抖动（#22a）。**
  近层体从「每帧从零算」变成「每帧只加一份新样本」：注入核在写出 `σ_s/σ_t` 之前，
  先把这一格的位置投回上一帧的 froxel 体里取历史值，按指数权重混合
  （下发的是**历史权重** `exp(−dt/τ)`，于是全零的 cbuffer 状态天然等于「纯本帧」）。
  抖动则把每个 froxel 的采样点在**深度 + 横向**两个方向上各推开不到一格，
  让相邻帧采到介质里不同的位置 —— 累积起来等于把有效采样数乘上历史长度。
  这一批是 #22 的前半：链路 + 抖动 + 判据；蓝噪声资产与 A/B 对照是 #22b。
  - **重投影排在积分**之前**，不是之后。** 注入表里存的是 `σ_s/σ_t` 与源项，
    都是**局部**量 —— 同一块空气不管从哪儿看都是那个密度，所以它可以跨帧复用。
    积分表存的是「从相机走到这一格」的累积量，换了相机位置就整条重算，
    重投影它等于把上一帧的相机位置混进这一帧的光路。
    HDRP（`VolumetricLighting.cs` 的 `_VBufferHistory`）走的也是这条分工。
  - **重投影的深度坐标用「格心到相机的欧氏距离」，不是 view-space z。**
    因为注入核自己构造世界坐标时用的是 `posWS = cameraWS + dMeters · rayDir`，
    `rayDir` 已归一化 ⇒ `dMeters` 是**径向**距离。
    用 z 的症状是画面四角的历史整体偏近、而中心完全正确 —— 一个只在边缘出现、
    看起来像「边缘插值精度不够」的偏移。
  - **`VistaFroxelReprojectDistance` 与 `VistaFroxelSampleDistance` 是两个量，
    不是同一个量的两份实现。** 前者答「这一格的值属于空间哪一点」，
    后者答「介质在哪儿被采样」。只有前者必须**精确落在纹素中心的 e 空间坐标上**，
    因为重投影是一次**反解**：先由世界坐标解出距离、再 encode 回 w。
    两者对 i ≥ 1 逐位相同，只在 i = 0 差 0.144 m —— 切片 0 的采样点是
    metric 中点 `0.5·d₀`（那是 `[0, d₀]` 上均匀采样的期望），
    而 `encode(0.5·d₀) + 0.5/N` 在实配下是**负数**（`0.0078 − 0.1293 = −0.114`），
    切片 0 的历史查询会**永久**被判成越界 ——
    那会让覆盖性判据因为一个结构性理由全绿。
  - **半个纹素。** 一格的值在 `decode(i/N)` 上求，但它的**纹素中心** w = `(i+0.5)/N`
    decode 出来是另一个距离。所以历史查询的 w 必须是 `encode(d_prev) + 0.5/N`。
    漏掉这半个纹素时，**相机静止的每一帧都逐位正确**，只有沿视线移动才露出
    向相机方向的拖影 —— 这正是 HDRP 那个已知的 half-slice bias 的形状。
    判据⑬把这个「要拒绝的最小错答案」（`0.5/N = 7.813e-3`）**印在报表上**。
  - **抖动在 e 空间里做，于是「它无偏」是一条恒等式而不是一个论证。**
    振幅 1 的深度抖动，`slice ± 0.5` 对应的区间**逐位**等于该段的 `[近, 远]`；
    而它的**期望正好等于不抖时的采样点**（几何均值 = e 空间中点）。
    在**米**上均匀抖动的话期望会落到 metric 中点上，比几何均值晚 0.0105 格 ——
    症状是「打开抖动之后雾稍微浓了一点」，一个看起来像调好了的系统性偏差。
  - **切片 0 的深度抖动是惰性的，而那不是偏差。** `slice = 0 + jitter.z ∈ [−0.5, 0.5)`
    永远走 `VistaFroxelSampleDistance` 里 `slice < 0.5` 那条退化分支、返回 `0.5·d₀` ——
    而那**就是** `[0, d₀]` 上均匀采样的期望。丢掉的只有那一格的方差缩减。
    横向抖动对切片 0 照常生效。
  - **横向抖动刻意不夹紧。** `VistaApFroxelRayDirection` 在 uv 离开 [0,1] 时
    线性外推四角方向，给出一条合法的、伸到视锥外半格的射线。
    夹紧会把边缘那一列的期望往里拉，症状是「画面四边一圈雾偏浓」。
  - **抖动序列用 R3 塑性常数的 Kronecker 序列**（`α = (1/φ₃, 1/φ₃², 1/φ₃³)`，
    `phase = frac(frameIndex · α)`），**而那次乘法必须在 CPU 上用 `double` 做。**
    在 float 上算，`frameIndex` 过了 2²⁴ 就丢整数位、相位**冻住** ——
    60 fps 下约 78 小时，一个只在长时间挂机后才现形、
    且表现为「抖动突然不起作用了」的问题。
    逐 froxel 的偏移用 PCG3D（Jarzynski & Olano 2020），
    不用 `sin(dot(p,k))*43758.5453`（低位在 uint→fp32 转换里丢掉，
    取整方向依编译器而定）。
  - **历史失效判定：边界检查 + 亮度变化率降权，按「最先命中的那一条」上报。**
    六条谓词（未初始化 / 相机后方 / 出屏 / 深度越界 / 亮度突变 / NaN）互斥，
    掩码里只置一位，于是判据⑮的守恒式（各分支计数之和 == 派发趟数 × 16384）
    比「逐条 > 0」严格：任何一格意外返回 OK、或掩码带了两位，和都会**小于**期望。
    出屏那条**先算完 `uvwPrev` 再判范围**：`w` 过零时先做除法，
    `xy/w` 飞到 ±inf 还能被范围判据挡住，但 `0/0` 的 NaN 会让
    `any(uv<0)` 与 `any(uv>1)` **同时为假** —— 一个相机后方的点被判成可用。
  - **亮度降权的死区下端必须摆在「抖动自己制造的散布」之上**，否则这条规则会把
    抖动噪声判成「场景变了」而降权 —— **亲手毁掉它本该保护的累积**。
    于是判据⑮b 量的是「同一个 froxel 两次**独立**抖动抽样」的相对亮度差之 max
    （刻意**不是**历史 vs 本帧 —— 那个数混着相机运动与场景变化）：
    实测 `1.412e-1`，而默认死区下端 0.25 只有 **×1.77** 的余量。
    报表因此直接印出可执行下限：本布景 `luminanceRejectStart` 必须 > `1.412e-1`，
    并声明这个下限随布景走（雾越浓、明暗对比越大，散布越大）。
  - **NaN 闸放在 `lerp` 之前而不是之后。** 一个 NaN 如果只在被丢弃的那一支里，
    `lerp` 会把它带回来。造 NaN 的位模式**从 uniform 派生**
    （`asfloat(asuint(_VistaFroxelReprojProbe.x) | 0x7FC00000u)`），编译器折不掉它。
    这一格证明的是「闸接对了」，**不是**「`AnyIsNaN` 在这台硬件上判得对」——
    后者是 URP 的位模式比较（`core/ShaderLibrary/Common.hlsl:609`，
    用 `(asuint(x) & 0x7FFFFFFF) > 0x7F800000` 而不是 `x != x`，
    所以 fast-math 折不掉它）。这条区分印在报表上。
  - **双缓冲用「数组 + 写下标」，历史有效性用**下标恒等比较**而不是计数。**
    用计数的话，「交换了两次却只派发了一次」（某帧 pass 被跳过）会被判成历史有效，
    读到的却是两帧前的表 —— 而那正是一个**看起来物理上讲得通的漂移**。
    交换点也刻意不藏进 `Prepare`：立即模式的自检每跑一次就会翻一次写下标，
    症状是判据**隔次失败**。
  - **判据⑬（静止恒等性）在 xy 上是**精确** fp32 恒等，不是近似。**
    `CalculateFrustumCorners(rect, 1f, ...)` 取的四角落在 **z = 1 平面**上且**未归一化**，
    平面上的双线性插值仍落在该平面上 ⇒ `(x/z, y/z)` 对 uv **线性** ⇒
    把 `cam + t·normalize(P)` 投回去精确复原原 uv（非对称/物理相机同样成立）。
    这条精确性依赖「重投影矩阵与四角同源于 `camera.projectionMatrix`」，
    所以矩阵构造被收进 `VistaFroxelReprojection.ViewProjOf` **唯一一个**静态函数，
    并在它的文档里指名判据⑬依赖于此。谁手工覆盖投影矩阵（或上 TAA 抖动）
    这一格就会红 —— **那正是想要的红**。实测 `max|Δuvw| = 6.962e-6`。
    那个矩阵也**不能**过 `GL.GetGPUProjectionMatrix`：
    `VistaApFroxelRayDirection` 的 uv.y 是自下而上的。
  - **六格新判据全绿**（两轮独立运行读数逐位重合）：
    ⑬ 静止恒等性 `6.962e-6`（门 `2.795e-4`，离最小错答案 ×1122）；
    ⑭ 上一帧视图的新鲜度；⑭b 在线掩码与 CPU 状态**双向**一致；
    ⑭c 帧后写槽一致；⑮ 六条失效路径全被驱动过 + 三条计数守恒；
    ⑮b 死区下端 vs 抖动散布。判据槽位 19 → 33。
  - **`VistaFroxelReprojection` 刻意不用 URP 的 `UniversalCameraHistory`。**
    三条理由写进了类文档：立即模式的自检没有相机；那套分配惯例是屏幕尺寸的 2D；
    以及自己写的能把**失效原因**打印出来（`lastInvalidReason`），
    而那个字符串是判据⑭b 的输入。
    per-camera 隔离的代价只有一次 `int` 比较。

- **抖动源：复用 URP 自带蓝噪声 + 两档运行时对照（#22b）。**
  `JitterMode` 补上 `BlueNoiseTexture` 这一档，把 #22a 那个「选了什么都不发生的
  枚举值」填实。资产**不自带**：拿 URP 自己那张
  `UniversalRenderPipelineRuntimeTextures.blueNoise64LTex`
  （`Textures/BlueNoise64/L/LDR_LLL1_0.png`，64×64、8 bit、Point、非 sRGB，
  Christoph Peters 那套环形 void-and-cluster 图）。三条理由：它能从公开 API 拿到
  且 `isAvailableInPlayerBuild`，所以进 Player 包；自烘只在「要 > 1 个独立通道」
  或「要 > 64 的瓦片」时才有意义，froxel 这个分辨率（屏幕/8）两条都用不到；
  一个生成器要能写回 package 目录，而 git URL 引用的 package 落在只读的
  `Library/PackageCache` 里 —— 那会变成「`file:` 引用下能跑、换成 URL 就炸」。
  **Vista 至今零二进制资产，这一条被保住了。**
  - **一个症状底下叠着两个独立缺陷**，这是本节最贵的一课。症状是核里
    `_VistaFroxelBlueNoise.Load` 逐像素读到 0，而 CPU 侧**所有**表面证据都显示接上了。
    - 缺陷一：`Shader.SetGlobalTexture` 写的是 Unity 的**立即态**全局属性表，
      它**不进命令流**，而一次 compute dispatch 的纹理绑定是在命令流里解析的。
      正确的那句话不是「compute 读不到全局」，而是**「全局纹理必须被写进命令流」**。
      修法：新增 `VistaLutSlot.BlueNoise` + `IVistaLutDispatcher.HasTexture(slot)`，
      把 `Texture2D` 包成非持有的 `RTHandle`（`RTHandles.Alloc(Texture)`）、
      `renderGraph.ImportTexture` 之后在**三趟**消费它的 pass 里 `UseTexture(Read)`，
      再由 `VistaFroxelVolume.BindBlueNoise` 逐核绑。
    - 缺陷二（**才是读到全 0 的真凶**）：那张资产的 `.meta` 里
      `textureType: 10`（SingleChannel）+ `singleChannelComponent: 0`（Alpha）
      + `alphaUsage: 2`（FromGrayScale）⇒ 导入成 `TextureFormat.Alpha8`
      （D3D11 侧 `DXGI_FORMAT_A8_UNORM`），数据只在 **`.a`**，而 Vista 读的是 `.r`。
      铁证是 URP 自己的消费点：`ShaderLibrary/LODCrossFade.hlsl:19` 拿**同一张资产**
      读的就是 `.a`。文件名里的「LLL1」说的是**源 PNG** 里 R=G=B=L，
      那不是导入后的样子 —— **通道是导入设置决定的，不是文件名决定的**。
      修法是一个字符。
    - 缺陷一修好之后读数**逐位未变**。按「第一个可疑对象被修掉、读数却没变时，
      必须回头重新归因」，没有去怀疑修法没生效，而是加了一把新尺子。
  - **判据⑰d 是把两者分开的那把尺子**：核里向**被测资源自己**问
    `_VistaFroxelBlueNoise.GetDimensions(w, h)`，走 `InterlockedMax` 写进两个新槽位。
    未绑定的 SRV 在 D3D11 上 `GetDimensions` 返回 `0×0`、`Load` 返回 0 ——
    于是「绑定没到达核」（0×0）与「绑上了但内容是 0」（64×64）不再同形。
    双向：资产取不到时派发处刻意不绑 ⇒ 期望 `0×0`，所以它在两条路径上都能失败。
    它**依赖另一格**才能给出正确因果（初值 0 让「没绑上」与「核压根没跑」读数相同，
    两者由 `BN_COUNT` 分开），这条依赖在报表里被点名。
    这一格与判据⑥a（`_MainLightShadowmapSize` 不可信 ⇒ 向 atlas 自己问尺寸）
    是**同一条**理由的第二次应用。
  - **⑰b/⑰c 是精确算术的门，不是统计门。** 64×64 = 4096 个像素、256 个灰阶，
    void-and-cluster 的排序是**秩均匀**的 ⇒ 每一阶正好 16 个像素。
    直方图分 8 桶、桶界 `min((uint)(v*8), 7)` 落在 k = 31.875 … 223.125，
    全是非整数且 fp32 余量 ~0.004 ≫ eps ⇒ **每桶精确 512**。
    均值精确 0.5 ⇒ 抖动偏移 `(v − 0.5)` 的期望精确为 0，也就是
    **开抖动不引入密度偏差** —— 这才是这两格的载荷，「图长得均匀」只是表象。
    实测 `[512×8]`、均值 `0.499995`（偏差 `−4.888e-6`，**负号**与
    「定点向零截断 ⇒ 只可能偏低」的预言一致），方差 `0.0839873`
    对解析值 `0.0839869`（`E[v²] = 511/(6·255)`）。
    这两格同时是**通道守卫**：读错通道时那张图恒 0 或恒 1，
    直方图会全落进第 0 桶或第 7 桶。
  - **精确算术的门只摆在它精确成立的那个量上。** 线上用的是动画后的
    `n' = frac(n + φ)`，而 `frac` 绕回**不是**平移：源图的灰阶是 `k/255`（含 0 与 1
    两端），绕回之后两端**撞成同一个值** ⇒ 多重集合变了 ⇒ 均值偏离 0.5 到
    `O(1/256)`。所以 ⑰ 量**源图 v 本身**，而 `n'` 的矩只印成 ⓘ、永不设门。
  - **判据⑱ 只判邻域相关的符号，不判幅度。** `frac` 绕回让 ρ₁ 随相位 φ 摆动
    （`k=l=1` 那一项里带 `Re(e^{4πiφ}·E[e^{2πi(v+v')}])`），那个摆幅没有紧的界；
    而 φ 无关的那一项 `−E[cos 2π(v−v')]/(2π²)` 在蓝噪声下**是负的**，符号不受影响。
    两轮不同 φ 的实测印证了这个设计：φ =`(0.4773,0.7070,0.3832)` 时
    蓝噪声 ρ =`(−0.1195, −0.1511)`，φ =`(0.3726,0.0394,0.9473)` 时
    `(−0.1251, −0.1143)` —— **幅度动、符号不动**。
    程序化档是**真正的空对照**：hash 之间独立，而独立性被任何逐点映射
    （包括 `frac`）保留 ⇒ 它的 ρ ≡ 0 与相位无关，门按抽样标准差 `1/√4096` 的 3σ 摆。
  - **判据⑳c 把一条我自己写错过的论证换成了读数。** 原先注释里写的是
    「CLT 把逐片独立的聚合场抹成白噪声」—— 那条对程序化 hash 成立，
    对蓝噪声**是错的**。固定的逐片瓦片步进 `s = (17,17)` 让聚合滤波器精确等于
    `K(f) = δ[f_x + f_y ≡ 0 (mod 64)]`，剩下的基函数只有 `e^{2πi f_x(x−y)/64}`
    ⇒ `Ā(x,y) = h((x−y) mod 64)`，也就是**对角条纹**。
    不用 Fourier 的初等版本更好核对：`17·49 = 833 = 13·64 + 1` ⇒ 17 在 mod 64 下可逆
    ⇒ 一列的 64 个抽样点正好是过 `(x,y)` 的**整条反对角线** `{(x+k, y+k)}`。
    这条对**任意**逐点函数成立，所以 `frac` 那次绕回救不了它。
    实测：逐片 DDIAG `3.300e-8` 对 DAXIS `1.607e-1`，差 **×4.9e6**。
  - **⑳ 量的是逐点恒等式的 max 差，不是相关系数。** 两条理由：归一化的 ρ₁
    判不出「变白了」（N 个解相关场平均后分子分母同比缩小，ρ₁ 不变）；
    而拿定点矩（scale `1e5`、地板 `1e-5`）去算 ρ 时，地板对上
    `Var(Ā_逐片) ≈ 1e-3`，与门只差个位数倍。换成逐点差之后地板降到
    fp32 求和序误差 —— 两个和是**同一个** 64 点多重集合，只是起点旋转了
    `17⁻¹ ≡ 49` 片。（**不是**「逐位相同」：fp32 加法不满足结合律，
    旋转次序留下一点非零残差，实测 `3.3e-8`，在那个 `3.8e-6` 的保守界下面 ×115。）
  - **⑳c 必须配对照格。** 只判 DDIAG ≈ 0 时，一个**恒为常数**的聚合场会假通过 ——
    这不是设想：修好通道**之前**那一格读到的就是全 0 常数场，
    是同时量的轴向差 DAXIS 把它判红的。
  - **⑳a 的区分力边界写进了报表。** 它抓得到 latZStride/编码/槽位下标的错，
    **抓不到「源接错了」**—— 蓝噪声与程序化 hash 的边缘分布几乎相同。
    这也不是设想：通道错着的那一轮，⑳a 印的是 ✔，两组均值都是 `0.915030`、
    方差都是 `0.000000`。源接错那一半由 ⑱ 的符号门覆盖，两格合起来才完整。
  - **判据㉑ 档位接线是双向的**：「恰好一个为 0，且是 `settings` 指名的那一个」。
    只判「选中的那个为 0」时，一个把两档实现成同一件事的 bug 会全绿。
  - **cbuffer 档位位与「这一趟真的绑上了没有」在同一处强制一致**
    （`VistaFroxelReprojection.Data.Bind`：`!HasTexture(BlueNoise)` 时把
    `effJitter.z` 清零）。两者各自独立时，一趟忘了 `UseTexture` 的 pass 会走蓝噪声
    分支读到 0，症状是抖动退化成一个**逐帧常数**的相位偏移 —— 查不出来。
  - **组合禁忌（现在是读数，不再是推导）**：源 = `BlueNoise` **且** 形态 =
    `PerSlice` 时聚合场退化成 45° 斜纹。报表在踩到这个组合时打 `⚠`。
    程序化 + `PerSlice` 不受影响。随机瓦片偏移（`K(f) → O(1/√N)`）留给 #27 定夺。
  - **判据槽位 33 → 82**，判据总数 37 格全绿（0 红 0 警）。
    `VistaBlueNoise.Invalidate()` 挂在 `VistaAtmosphereFeature.Dispose`——
    那一趟正好覆盖换 URP 资产 / 改全局设置 / shader 重编译三件事，
    于是不必再单独挂 `RenderPipelineManager.activeRenderPipelineDisposed`。

### 取舍

- **AP 出厂档 = d=32 / Logarithmic / near 20 m（PC）；移动档 = d=16 / Power k=3。**
  两档都由上面那两张表定，而不是由"看起来够了"定。
  - **为什么 PC 不省到 d=16。** 判据（1% 的 Weber 可见阈）只能判**合格**，不能判
    **该选谁** —— 20 组配置全部合格。所以选择必须落到成本上：16→32 在稳态五 pass
    整链上只多 **0.012 ms**（链的 6.6%、0.300 ms pass 预算的 4%、16.67 ms 一帧的
    0.07%）、显存多 256 KB，换来最差段误差约 **4 倍**余量
    （0.77%/T0.49% → 0.18%/T0.16%，视角 2 是 0.91%/T0.46% → 0.21%/T0.14%）
    和 2.5 倍的近场切片密度（①2 ②5 → ①4 ②10）。
    0.012 ms 小于这次测量自身的离散度（整链 ±3~7%），也就是说 d=16 省下来的东西
    **测不太准**，而被换掉的是四倍余量。这个方向的取舍在成本可忽略时是单向的。
  - **为什么 d=32 上选 Log 而不是 Power。** 纯晴空下 Power k=3 数值更好
    （最差段 0.04% vs 0.18%），但两者都远在阈值内，这个差额买不到画面。
    真正的依据是 Step 3 的雾**要合成进这张 AP LUT**（已定的 UE5 路线），
    而高度雾的密度梯度恰好落在晴空几乎空着的近场段：实测①段累积入散射只有参考白的
    0.26~0.31% —— 晴空近场切片"看起来免费"正是因为那里本来没有东西。
    Log 在 d=32 上给近两段 4/10 片，Power k=2 只给 1/3 片（同一档实测的分段片数）。
    ⚠ 这条是**前瞻性理由，尚未实测**：Step 3 接上后要用带雾的场景重新跑这套扫描，
    结论可以被推翻。写在这里是为了让它到时候被复核，而不是被当成已验证的事实。
  - **为什么移动档反过来选 Power k=3。** 它在 d=16 上给出与 PC 出厂档同级的晴空精度
    （最差段 0.18%/T0.12%，视角 2 是 0.19%/T0.10%），显存只有一半（256 KB），
    单测比 d=32 Log 便宜 18% —— 靠**重分布**切片而不是**增加**切片拿到的。
    d=16 上 Log 与 Power 的差距是 4~5 倍（0.77% vs 0.18%），代价是行进步数
    ×1.25~1.47，这是移动端唯一值得付的那一笔。
    它与上一条不矛盾：移动端的雾走**解析高度雾**那条实现，不骑 AP LUT，
    所以移动端不需要近场 AP 切片密度。
  - 诚实的保留：d=16 上数值最好的分布是 Power，且判据认为 16 片对**晴空**在两个
    测试视角上已经够。余量档没能区分出优劣，选择来自成本 + 分段内容需求，不来自门。

- **单位用 km 而不是 m**。地球半径 6360 km 写成 6.36e6 后，froxel raymarch 里的 `r*r`
  会到 4e13，逼近 fp32 有效位上限（~1.7e7 整数精度），远处步进会出现明显抖动。
  世界空间(m) 只在边界处乘 `_VistaGround.w = 0.001` 转换。
- **积分采样数 40 而非 Bruneton 的 500**。这张表只在参数变化时重算，采样数不进帧开销；
  40 段梯形法的误差已经压到 3e-3，再加采样是浪费烘焙时间。
- **曝光只在"交出引擎"的那一层乘，GPU 侧资源一律留绝对光度量。**
  另一种写法是让 SH 投影核与反射滤波核直接输出已曝光的值，出口就不用管了 ——
  少一处容易漏的乘法（而这次正是漏在那里）。不选它的理由是判据会全部失效：
  自检的参考解、Step 3 的雾、Step 4 的 PRT relight 都按绝对量对账，
  折进核里之后每一条阈值都会跟着 EV100 漂，改一次曝光就要重标一遍全部自检。
  代价是"出口"成了必须逐个点清的清单（当前两个：`ambientProbe`、
  `reflectionIntensity`），所以把它写进了「坑」里当成纪律，而不是靠记性。
- **接受"Vista 用绝对光度量、宿主工程用 Unity 常规单位"两制共存，不去改宿主的灯。**
  Project-ARPG 里现成的灯和材质都是常规单位。理论上更干净的做法是把整个工程
  换成物理单位（灯改 lux、相机走物理曝光），但那会动到所有已调好的场景，
  而收益只在"数字更自洽"。既然曝光已经在交接点补上，两制在**画面**上是一致的
  （实测环境光 ~0.13 对平行光 3.14，比例正常），就把边界留在交接点。
  这条待与 Task #8（TimeOfDay 平行光接入）一起复核 —— 那时大气的太阳要真的去驱动
  场景里的平行光，两制的接缝会正式落到一个具体的转换系数上。
- **Transmittance 用 fp16**。值域 [0,1]，且下游只做乘法不做累加，没有精度累积。
  256×64×8B = 128 KB。
- **多次散射用各向同性近似 + 等比级数，不做完整 Monte Carlo**。完整解要在每一阶重解
  辐射传输方程，烘焙从毫秒级涨到分钟级，而差别在 1% 量级、过 tonemap 后不可见。
  UE5 的 SkyAtmosphere 与 Unity HDRP 的 PhysicallyBasedSky 用的都是这套近似——
  这是"业内主流"而非省事。代价是二阶以上散射丢失方向性，逆光时太阳附近的多次散射
  略偏暗，但被一阶的 Mie 前向峰完全盖住。
- **MS LUT 尺寸 32×32 定为算法常量，不做质量分级项**。它对 (muSun, 高度) 是极平滑的
  二维函数，128×128 与 32×32 肉眼无差别。分级项留给真正吃性能的 SkyView / AP。
- **步段内用解析积分 `S·(1−e^(−σ·dt))/σ` 而非矩形法 `S·dt`**。低步数下矩形法在光学厚的
  步段会显著高估，是带状的主因之一。步段内取样位置取 0.3 而非中点 0.5，因为密度沿
  步段指数衰减、能量重心偏近端。
- **天空走 Skybox 材质，不自己画全屏 pass**。Unity 的环境光（`AmbientMode.Skybox`）
  与反射探针都是从 skybox 材质渲一张 cubemap 卷积出来的，走材质等于免费接上这两条链路；
  自己画全屏 pass 就要把这两套都重写。代价是天空只能在 `DrawSkyboxPass` 的时机画
  （不透明之后、透明之前）—— 对我们无影响，aerial perspective 是独立 pass。
- **太阳圆盘不烘进 SkyView LUT，单独解析画**。192×108 的表上圆盘（张角 0.53°）
  占不到一个纹素，烘进去只会得到一个被双线性糊开的方块。更硬的理由是**动态范围**：
  圆盘亮度在 1e9 量级，烘进去这张表必须从 fp16 升到 fp32，显存与带宽翻倍；
  排除圆盘后全表峰值 54304，安全落在 fp16 的 65504 以内（实测值见上）。
- **SkyView 尺寸 192×108 定为质量分级项**（与 MS LUT 的 32×32 相反）。
  它逐帧重算，且地平线附近的纹素密度直接决定日落时会不会看到横向台阶。
  移动端分级降到 128×72 + SPP 8/16（Task #7 落地）。
  **判据 B 给出了这条分级的量化依据**：192×108 自身的双线性插值误差
  E ≈ 1.885e-3，可见阈 1e-2，余量 5.3 倍。按 O(h²)（实测阶 1.69，中位 2.05），
  边长减半误差涨 4 倍 → 96×54 的 E ≈ 7.5e-3，仍在阈下但只剩 1.3 倍余量。
  所以 128×72 是有依据的下限，96×54 是极限而不是选项。
- **banding 判据不设阈值，改测收敛阶**。原计划是"扫一遍、看二阶差分有没有超过某个数"。
  问题是那个数只能从跑出来的结果附近挑，等于把结论写进判据 —— 面试时这是最先被问倒的地方。
  换成测阶之后，判定依据变成"幅度随步长怎么变"，这是被测对象的**性质**而不是我的选择：
  p≈2 是物理曲率、p≈1 是双线性在纹素边界的斜率跳变、p≈0 是真 C⁰ 跳变。
  代价是要多烘两档步长（靠对同一次扫描抽稀，实际零成本）和多写一条判据 C。
  换来的是唯一一个外部常数只剩感知阈（Weber 1%），其余全部实测。
- **判据 C（地板）不是可选项**。p≈0 有两种含义：真台阶、或者信号已经掉进 fp16 量化噪声里。
  一个要修一个不用修，而只看阶数分不开 —— 必须先实测地板。
  而且**污染方向是危险方向**：地板若被高估（窗口跨了纹素边界，量到的是斜率跳变而非噪声），
  判据 A 的"低于地板 → 通过"会**更容易**成立。所以 C 的实现不去赌窗口够不够小
  （v 轴被 sqrt warp 扭过，平均纹素 1.67° 根本不代表 8° 处的局部值），
  而是步长逐档减半到幅度自己停下来；未到平台时报告把 A 里所有"低于地板"的结论
  **降级为无判定**，不当成通过。
- **一次扫描抽稀出三个步长，而不是烘三遍**。判据 A 要 Δ = 0.5°/1°/2° 三条曲线定阶。
  按 0.5° 烘 181 次再按 stride 1/2/4 抽稀，比烘三遍省三分之二的时间，
  更重要的是三条曲线**严格同源** —— 分开烘的话，任何一处参数不一致都会伪装成
  "阶不对"，而那是这套判据唯一的输出。
- **判据 B 用分辨率对折 + Richardson 外推，而不是"跨纹素走一步看跳多少"**。
  后者量到的是"某一步的跳变"，回答不了实际要问的那个问题（192×108 够不够）；
  前者直接给出**这个分辨率自身的插值误差**，并且自带前提检验：`e_lo/e_hi ≈ 4`
  不成立就说明 O(h²) 不成立，1.333 这个外推系数随之失效，报告判不可引用。
  代价是要多烘两个分辨率档（各 1 次，可忽略）和一次 `SetSkyViewResolution` 的
  分配抖动（跑完在 `finally` 里恢复 192×108）。
- **`RenderPassEvent.BeforeRenderingPrePasses`**。LUT 不依赖任何屏幕空间资源，
  而下游消费者（天空盒、雾、不透明物的 aerial perspective、SH 投影）分布在整条管线上。
  排在最早处，所有下游都能无条件拿到当帧的表。
- **一个 RendererFeature 管整个大气模块，不是每个效果一个**。LUT 是共享资源，
  拆成多个 feature 会让用户配得出"开了雾但没开大气"这种必然黑屏的组合。
- **`IVistaLutDispatcher` 抹平两条录制路径，而不是把 dispatch 写两遍**。
  RenderGraph 的 `ComputeCommandBuffer.SetComputeTextureParam` 六个重载**全部只收
  `TextureHandle`**，而 Editor 自检/预览只有原生 `CommandBuffer`（只收 `RTHandle`），
  两者之间没有转换。写两遍的后果是：哪天只改了运行时那份采样数，自检还在验旧参数，
  而它照样全绿 —— 自检就从资产变成了负债。实现是 `readonly struct` + 泛型约束，
  JIT 去虚化，无装箱。
- **不走 `AddUnsafePass` + `GetNativeCommandBuffer` 这条逃生通道**（`GetNativeCommandBuffer`
  只接受 `UnsafeCommandBuffer`，所以想拿原生 cb 就必须放弃 `AddComputePass`）。
  放弃它会同时失去 `EnableAsyncCompute` 与图的依赖校验，而换来的"省一层抽象"并无收益：
  LUT 排在所有光栅工作之前，native render pass 合并对它没意义。
  这三张表本就是持久资源，所以保持 `ImportTexture` + 让图只负责 barrier / 性能剖析 / 异步调度。
- **AP 用 32×32×32 的 froxel 表，而不是在合成 shader 里逐像素 raymarch**。
  逐像素 raymarch 的画面上限更高（能吃阴影贴图、能做逐像素的深度截断），但代价是
  雾的开销随分辨率线性涨；froxel 表把它固定成 32768 个柱子，与屏幕分辨率解耦。
  这是 UE5 SkyAtmosphere 与 HDRP PhysicallyBasedSky 都采用的形态。
  代价是深度方向只有 32 片，靠非线性分布 + 三线性插值补 —— 补得够不够由 errMid 判定。
- **AP 的散射与透射分两张表，不打包成一张 RGBA**。合成式是 `color·T + S`，
  S 在 1e4 量级、T 在 [0,1]，塞进一张 RGBA 就必须牺牲其中一个的精度或色彩
  （灰度透射率）。分两张的实测代价是灰度近似在 32 km 处偏差 71.2%（见上）——
  这个数字本身就是"为什么 PC 档必须存彩色透射率"的论据。
  移动端档位仍可只采一张：散射表的 alpha 里备了灰度透射率。
- **errCenter 与 errMid 测的是两件不同的事，所以用两套不同的分母**。
  这一条是 AP 自检里最容易被写成"随便定个阈值"的地方，展开记一下：
  - errCenter = `|LUT[i] − truth(D(w_i))| / truth(D(w_i))`，**本地**相对误差。
    它比较的是"同一个位置上，AP 的一次行进循环"与"共享积分器跑 256 步"，
    测的是**代码等价性**（步数够不够、重写的循环有没有写歧）。
    代码等价性必须处处成立，所以分母用本地真值、阈值定在 5%。
  - errMid = `|(LUT[i]+LUT[i+1])/2 − truth(D(w_mid))| / LUT[depth−1]`。
    分子是三线性插值在两片中点给出的值与真值之差，测的是**切片分布够不够密**。
    分母**不能**用本地真值：Power(k=2) 的 `d ∝ w²` 在 w=0 处导数为 0，
    第 0 片在 d=0、第 1 片在 d=33 m，而中点 `w=0.5/31` 对应 d=8.3 m，
    插值给 `0.5·L(33m)`、真值约 `0.25·L(33m)` —— 本地误差恒为 **100%**，
    加多少切片都不会变，这是参数化**算术上强制**的，不是欠采样。
    而那里的绝对量只有 1.35 cd/m²，整根柱子要累积到几千，画面上并不存在这个误差。
    改用**柱子在最远片上的累积总量**作分母后，这个数直接读作
    "贴到画面上的雾量错了百分之几"，阈值也就能从"平滑渐变上多少对比度会看出带状"
    （约 1%）定成 2%，而不是拍一个比值。换分母后 argmax 从退化的第一对切片
    移到了 13.9 km / 23.4 km —— 也就是雾真正有量级的地方，这正是该被监控的位置。
  - 顺带得到 Task #6 的结论输入：等切片数下 Power(k=2) 的插值保真度优于 Log
    （0.15% vs 0.31%），但 Log 的第 0 片能落在 `nearDistanceKm`（20 m）而非 0，
    对"相机贴着地形时近处不要有雾"更友好。两者都远在阈值内，最终取哪个按画面定。
- **环境光用 L2（SH9）而不是 L1（SH4）**。判据是日落：那时天空是"天顶暗 + 地平线一圈
  橙红 + 背侧蓝紫"，L1 只有一个方向矢量，会把这一圈塌成单侧色偏 —— 朝太阳与背对太阳
  的物体拿到的环境色几乎一样，日落的方向感全丢。L2 至少能表达"水平方向亮、上下暗"
  这个二阶形状。另外 `RenderSettings.ambientProbe` 本身就是 `SphericalHarmonicsL2`，
  用 L1 是主动丢信息而不省任何东西；UE 的 SkyLight 与 HDRP 的 ambient probe 也都是 L2。
- **buffer 里存原始辐射亮度矩 `L_i`，不存已与余弦瓣卷积过的辐照度 SH**。
  消费端不止一个：漫反射要 `Â_l` 卷积（`VistaShIrradiance`），Step 3 的体积雾要的是
  **各向的入射亮度**（`VistaShRadiance`，不该乘余弦），Step 4 的 PRT relight 要与
  烘出来的可见性 SH 做逐系数点乘（也不该预卷积）。存卷积后的版本等于把三个消费者中的
  两个逼去反解 `Â_l`，而 `Â_2 = π/4` 一旦被漏乘就是 4 倍误差、且只在二阶上体现 ——
  症状是"环境光的方向对比过强"，几乎不可能被认成 bug。
- **太阳圆盘排除在 SH 之外**。这也是不用 `AmbientMode.Skybox` 的**唯一**动机
  （见「坑」：圆盘会让环境光偏亮约 8 倍）。排除是能量守恒的：圆盘的直射由 URP 的
  平行光负责，卷进环境光就是把同一份能量记两遍。
- **投影时采均匀 Fibonacci 方向，不遍历 SkyView LUT 的纹素**。逐纹素累加看着更"省"
  （表就在那儿），但 SkyView 的 uv 参数化在地平线附近是**故意加密**的（双段平方 warp），
  按纹素等权累加等于给地平线一圈几倍的权重 —— 日落时那一圈正是最亮的地方，
  结果是环境光整体偏橙，且**偏多少取决于 LUT 分辨率**：Task #7 把移动端降到 128×72
  时环境色会跟着变，而这种耦合在改分辨率的那一刻不会有任何报错。
  均匀采样把"表的存储密度"与"积分的测度"解耦。UE5 SkyAtmosphere 的 SkyLight capture
  同样是独立方向集，不复用 SkyView 的参数化。
  实现上 z 在 (−1,1) 等距（Archimedes 投影保证面积均匀）+ 黄金角 `π(3−√5)`，
  方位角先 `frac` 再乘 2π，否则高 index 处 fp32 的有效位全被整圈数吃掉。
- **一份系数、两个出口；GPU 消费者绝不走 CPU 那条路**。雾与 PRT relight 如果读
  CPU 读回的结果，环境光就会比天空慢 2~3 帧 —— 快速过渡的日落里表现为"雾的颜色追不上
  天空"，而这种延迟错位在静态截图里完全看不出来，只在动起来时暴露。
  所以 GPU 侧直接绑 buffer（同帧可用），CPU 侧那条只服务 Unity 内建的漫反射环境光
  （`RenderSettings.ambientProbe`），那里本来就有一帧以上的滞后，无所谓。
- **读回请求在记录期发起，不在 SH pass 的 execute 里**。`AsyncGPUReadback.Request`
  是 CPU 侧 API，与图无关；放在记录期最前面，它拿到的必然是"上一帧已经写完"的内容，
  时序不依赖图什么时候提交。放进 execute 反而要处理"同一帧内请求自己刚写的 buffer"，
  多一次隐式同步。代价是延迟从 2 帧变 3 帧 —— 对漫反射环境光无感。
- **`VistaLutBufferSlot` 让 dispatcher 也抹平 buffer 绑定，尽管这次两条路径的类型是兼容的**。
  与纹理那条的处境不同，值得写下来：纹理侧 `SetComputeTextureParam` 的六个重载**只收
  `TextureHandle`**，原生 `CommandBuffer` 只收 `RTHandle`，不抽象就编译不过 —— 抽象是被迫的。
  buffer 侧不是：`ComputeCommandBuffer.SetComputeBufferParam` 有直收 `GraphicsBuffer` 的
  重载，而 `BufferHandle` 又有到 `GraphicsBuffer` 的隐式转换，所以"直接把持久
  `GraphicsBuffer` 递给 dispatch"**两条路径都能编译、都能跑出正确画面**。
  它悄悄丢掉的是：图不知道这个 pass 碰了这块 buffer，于是不插 barrier、不做依赖排序、
  也不会在剖析里显示这条边。这类错误没有任何编译期或运行期信号，只在换厂商驱动或
  开异步 compute 时才炸 —— 正是必须靠约定拦住的那一类。所以 buffer 也走
  `ImportBuffer` + `UseBuffer` + slot 枚举，与纹理保持同一种写法。
- **自检的判据设计：截断误差不能当判据，均值恒等式才能**。
  这是 #5a 里唯一真正需要想的东西，展开记：
  - 表面上最自然的判据是"SH 重建 vs 高精度参考解，逐法线比"。它不行 ——
    SH9 是二阶带限的，日落天空含大量高阶成分，**必然**对不上。实测正午 ≤ 2.17%，
    日落最暗那个法线（朝下、只吃地面反弹）达 **31.25%**。把阈值定在能容下 31% 的地方，
    等于放过任何小于 31% 的缩放错误；定紧了则"日落必红"，而必红的自检会被习惯性忽略，
    等于没有。**这一项测的是 L2 的表达力，不是代码的正确性，两者不该共用一个阈值。**
  - 真正判定的三条，都与截断无关：
    ① **均值恒等式**（唯一的*精确*判据）：
       `mean_n[(1/π)∫L·max(0,n·ω)dω] = (1/π)∫L·mean_n[max(0,n·ω)]dω = (1/4π)∫L dω`，
       而 SH 侧同一个量恰好是 `L_00·Y00`（一阶以上在整球上均值为 0）。
       于是它与"L2 够不够"完全无关，只取决于 `4π/N` 权重、`Ŷ_0`、`Â_0` ——
       也就是唯一会真出 bug 的那部分。实测 **1.36e-7 / 8.29e-8**。
    ② **GPU 重建 == CPU 重建**：两条"矩 → 辐照度"的实现互不相干（GPU 走 `Â_l` 与
       归一化基；CPU 走 `k_RadianceToUnitySh` + Unity 的未归一化多项式基），
       失败模式不重叠。实测 **8.15e-8 / 1.23e-7**。
    ③ 正午档的逐法线偏差判 5%：正午天空足够平滑，截断本就只有 2.17%，
       这一档的紧阈值能抓"某一阶的 `Â_l` 缩放错了"（至少 30% 量级），
       而那种错误会**同时**污染两个太阳高度，截断误差却只在日落放大 —— 可区分。
  - 顺带给出一个设计结论的实测依据：**31% 的截断误差就是镜面反射必须另走 cubemap 的
    理由**（#5b）。这个量级在漫反射上过 tonemap 后可接受，在镜面上就是把高光糊成一团。
- **`VISTA_SKY_SH_SAMPLES` 取 1024**，依据是自检里那条"1024 vs 4096 均值偏差"：
  正午 0.01%、日落 **2.75%**。日落那 2.75% 落在 DC 项上（≈0.04 EV），且方向集是**固定的**
  （不做逐帧抖动），所以误差是太阳角度的平滑函数 —— 不会有时域闪烁，只是一个恒定的
  微小偏色。用不着为它加采样。这个数留在自检里打印，Task #7 分级时直接读。
  当前整趟只 dispatch 一个线程组（64 线程 × 16 次采样），加到 4096 就是 64 次 ——
  单 SM 上的纹理延迟会真实体现在耗时里，而收益是看不见的 0.04 EV。

- **反射走 cubemap 而不是直接用 #5a 的那份 SH**。依据是 #5a 自检里那个 31.25% ——
  L2 在朝下法线上的截断误差。这个量级在**漫反射**上过完 tonemap 可以接受
  （地面反弹本来就是低频的），在**镜面**上就是把日落地平线那圈橙红糊成一团均匀的橙，
  而反射恰恰是观众唯一能逐像素对照"这个方向的天空到底什么颜色"的地方。
  移动端也还是 cubemap（只是内容由 SH 重建），理由见下一条。
- **反射不做「渐进预滤波」（读 mip N−1 写 mip N），每级都直接从 SkyView LUT 积分。**
  渐进预滤波是这件事的标准做法（HDRP 的 `IBLFilterGGX`、Karis 2013 的实现都是），
  但它要求同一张资源在同一趟里既当 SRV 又当 UAV，而 RenderGraph 跟踪的是**整张资源**
  的状态 —— 与三张静态大气表必须拆 pass 是同一类 UB（见「坑」）。绕开只有三条路：
  ① 源 mip 也按 UAV 读（丢掉硬件双线性与跨面无缝滤波，cube 接缝处出硬边）；
  ② 两张纹理 ping-pong（HDRP 的形态，正确但显存翻倍且要在图里 `GenerateMips`）；
  ③ 拆 7 个 pass 串行（正确，但每个 pass 只有几百纹素的工作量，全是调度开销）。
  直接从 LUT 积分把这一整类问题消掉：全程纯写、只有 UAV 一个状态、零 barrier、
  七趟 dispatch 挤在一个 pass 里。而且**质量严格更好** —— 没有 box filter 的近似、
  没有跨级累积误差、mip0 是精确镜面，接缝在结构上不可能出现（每个纹素积的都是
  真实世界方向）。
  能这么做的前提是开销够低：需要滤波的纹素只有 mip1~6 共
  `6 × (1024+256+64+16+4+1) = 8190` 个，配上随 mip 上升的采样数总计约 376k 次
  LUT 双线性**取样**（不是 raymarch），加上 mip0 的 24.6k 次约 0.4M。
  （这个估算是纠正过一次的：第一版把 mip0 也算进滤波、还用了一个平的采样数，
  得出 2.1M，据此差点选了渐进预滤波 —— 改对之后架构结论就翻了。）
- **不做逐帧摊销（HDRP 会把面/mip 分帧）。** HDRP 摊销是因为它在 256² 上滤波、
  且走 42 趟光栅 pass；那个约束不迁移过来 —— 这里 mip0 只有 24576 个纹素，
  全部七级一趟做完。摊销这件事留给 Task #7 当分级旋钮（与采样数、辐射来源一起）。
- **cubemap 尺寸 64² 是被 URP 定死的，不是拍的。** `ImageBasedLighting.hlsl:16` 的
  `UNITY_SPECCUBE_LOD_STEPS = 6`，而 URP 的 `GlobalIllumination.hlsl` 用的是
  **单参**重载 `PerceptualRoughnessToMipmapLevel(pr)` —— 也就是它假设 maxMip 恰好是 6，
  即 mip 0..6 共 7 级，即边长 `1 << 6 = 64`。所以这两个常量都从
  `UNITY_SPECCUBE_LOD_STEPS` 推导（`SkyReflection.hlsl:41-42`），
  而不是各写一个字面量：URP 哪天改了那个 6，我们跟着变，而不是安静地错位一档。
  自检判据 ③ 把 HLSL 侧的实际取值报回 C# 比对，所以"两边不一致"是一条会红的断言。
- **判据 ② 的阈值按太阳高度分两档（正午 0.5% / 日落 6%），不是统一放宽到 6%。**
  实测日落 4.315%、正午 0.078%，差了 55 倍，而这个差是**物理**：
  cube 三通道分别偏暗 4.3% / 1.4% / 0.2% 且一律偏暗 —— 线性插值在凸的亮带上
  系统性下冲的签名。日落地平线那圈橙红是全天角频率最高的结构（64² 下每纹素约 1.4°），
  几乎全在红通道；蓝通道在日落时整片天空近乎平坦，所以几乎不错。
  统一取 6% 的代价是正午档从"回归探测器"退化成"形式上绿着"：真出现 1% 级别的整体
  缩放错（比如曝光被乘了两次），只有正午那一档能抓住它，日落档会被离散化误差淹掉。
  这与 #5a 日落档取 `PositiveInfinity` 的做法**故意不同** —— 那边的截断误差在特定
  法线上无界，给不出有意义的上限；这边的离散化误差是有界的，留一个真阈值它才还是断言。
  另外这 4.3% 只影响 mip0：任何有粗糙度的表面读 mip≥1，那里 GGX lobe 的张角远大于
  1.4°，离散化被滤波宽度盖掉了。
- **太阳圆盘不进反射图。** 两个理由，任一个单独就够：① URP 的平行光已经给了解析
  GGX 高光，圆盘再进反射就是重复计一次；② 圆盘亮度在 1e9 量级而 fp16 上限 65504，
  进来直接饱和成 Inf，而 Inf 会经预滤波的归一化污染整片纹素（不是一个点，是一片）。
- **GGX 预积分用各向同性近似（N = V = R），不做"更物理"的 NdotV 相关 lobe。**
  真实 GGX lobe 掠射时会拉长成各向异性，但采样端（URP 的
  `GlossyEnvironmentReflection`）用的是不带 NdotR 的单参重载 —— 也就是说
  **它本来就假设这张图是各向同性预滤波的**。跟着采样端的假设走，
  而不是把写入端做得更物理然后与它错开；后者的症状是掠射角反射整体偏暗，
  而且查起来会怀疑到积分本身。
- **wide 核的形状分界定在 `K ≥ 64`，而且这个 64 是量出来的、不是推出来的。**
  第一版写 `mip > 0`（"除了 mip0 全走 wide"），结果 mip1 从 0.021 **退化**到 0.041 ms，
  反而成了单项最大 —— 它的 K=32 只喂饱一半 lane，另一半空转，还白付一轮 6 步 barrier
  归约；而 mip1 在 narrow 下本来就是全场吞吐最高的一级（96 组 × 64 线程 × 每条 32 深，
  延迟早被别的 warp 藏住了）。同一次测量里 mip2 从 0.032 改善到 0.017，
  分水岭正好落在 K=32 与 K=64 之间。门槛的物理含义因此是"每条 lane 至少领一个样本"。
  也没有为 K<64 再切一层「一组多纹素 + 组内分段归约」：按 mip2 达到的 5.89 G样本/s
  折算，mip1 那 196.6k 个样本要 0.033 ms，比 narrow 的 0.021 还慢 —— 多一套索引换更差
  的结果。**SH 模式（K 恒为 16）完全没有实测**，它走 narrow 只靠这条占用率论证支撑。
- **分界写成 mip 序号（`!useSh && mip >= 2`），不在 C# 里重算一遍 K。**
  K 的定义在 HLSL 的 `VistaSkyReflectionSampleCount`，运行时抄第二份就多一处会走歧的
  真源。代价是这个 `2` 与那个函数隐式绑着，所以自检判据 ④a 把 HLSL 侧的 K **逐 mip
  导出**、C# 拿它跟门槛和 `mip >= 2` 对账 —— 必须让它会红，因为 K 改了而分界没跟上的
  症状是某一级性能悄悄退化，不是错图，画面上永远看不出来。
- **合并粗糙几级的 dispatch：已识别，主动搁置。** 改完形状之后瓶颈换成了 per-dispatch
  的固定开销（证据 3 给出地板占反射 pass 的 59%），下一个杠杆是把 mip3~6 那 510 个纹素
  合进一趟。但那需要同时绑 4 个 `RWTexture2DArray` UAV 加一个级别 switch，
  换回约 0.03 ms —— 而整链已经是 0.170 / 0.300 ms。**在已达标的链路上为 0.03 ms 引入
  一处形状复杂度不划算**，把它记在这里比做掉它更有价值：留着当"下次预算收紧时先动谁"的
  已定位杠杆。
- **计时用「模型 B 为主 + 模型 A 交叉验证」；逐 pass 绝对值只从模型 A 引，模型 B 只读占比。**
  模型 B = Edit 模式立即提交、N 次背靠背摊销；它测的是**吞吐**，相邻 dispatch 允许重叠，
  所以逐 pass 值是下界。模型 A（Play 模式 `ProfilerRecorder`）测**帧内延迟**，跑在真正的
  RenderGraph 图上，是唯一把 barrier / pass 边界 / 资源状态转换算进去的口径。两个模型都不是
  "随手加个 Stopwatch"：立即模式的状态转换由图形层插，**不是** RenderGraph 那批 barrier。
  这条分工原本写成"只对外引用整链数字"，跑完模型 A 之后改了 —— 理由见下一条。
- **同一个 pass，两套口径的可引用性可以反过来。** 反射 pass 在模型 B 里离散度 ±5%~±132%
  （0.076 / 0.095 / 0.101 三次复测），判成不可引用；在模型 A 里是 0.093 ms，
  min 0.186 / max 0.202（±9%），完全可引用。原因不是"模型 A 更准"，而是这个量级上
  **两个模型的误差来源不同**：模型 B 要从摊销值里扣一个 0.161 ms 的固定开销，被测量
  （0.09 ms）比扣减项还小，扣减自身的误差就主导了结果；模型 A 直接读 300 个真实帧的
  marker，不做任何扣减。所以"哪个数字能引用"是**按口径和量级判的**，不是按工具判的。
  推论：模型 B 的逐 pass 数在 0.01~0.02 ms 档一律只读占比，绝对值去模型 A 拿。
- **静态表的判定方向要掉过来：稳态里"没有样本"才是对的。** 第一版把七个 pass 一视同仁，
  凡是取不到样本就报"pass 名走歧 / pass 被剪"，于是两张静态表（Transmittance /
  Multi-Scattering）在 300 帧里 0 样本被判成失败 —— 而那恰恰是脏标记正常工作的证据。
  真正该报警的是反向：这两行**有**样本 = 静态表每帧重算，白烧约 0.044 ms/帧，
  **画面完全正常，只有计时器能看见**。这类"期望缺席"必须显式建模成正判据；
  否则工具每次都对着正确行为报警，人很快就学会忽略它的警告 —— 那时它就不再是判据了。
- **多相机求和不能悄悄除掉。** Editor 里 Scene View 与 Game View 各渲染一次，
  开了 `SumAllSamplesInFrame` 之后一行的值是**两次之和**。除以出现次数是合法的，
  但合法性有前提：LUT 尺寸全是定值（256×64 / 32×32 / 192×108 / 64²×7 / 32³）、
  与相机分辨率无关，每帧只有一份大气参数，所以两次渲染做同样的工作量。
  报告把出现次数（min~max）、原始和、单次值、以及这条前提**一起打出来**：
  出现次数在帧间变动（min≠max）时连"稳态"都不成立，那时除法直接不做。
  先试过用 `ExecuteMenuItem("Window/General/Game")` 把 Game 置前来消掉第二个相机，
  这台机器的停靠布局下无效（Scene 与 Game 不在同一区域），于是保留除法分支 —— 
  能消掉假设当然更好，消不掉就把假设写在数字旁边，而不是让它隐含在结论里。
- **每项取 M 轮的最小值，不取平均。** 噪声在这里是单向的 —— Editor 重绘、驱动争用只会
  **加**时间，不会减。平均会把偶发的重绘平摊进结果，min 是对"无争用真实耗时"的一致估计。
  min 与 max 同时上报，离散度 > 25% 的行标 ⚠ 并**不参与判定、不对外引用**：
  这台机器上单个 pass 到 0.01~0.02 ms 档就已经在噪声地板下（复测见过 ±132%）。
  正因为有这条纪律，才能诚实地说出"mip1 那一级确实快了 2×，而 pass 总数在噪声内没变"。
- **CPU 上重算一份 T，而不是异步读回 GPU 的 Transmittance LUT。** 读回是"零物理重复"的
  写法，但异步读回有 1~2 帧延迟，而延迟的那一项是**直射光色** —— 拖时间滑竿时天空已经
  红了、物体还是白的，是全场最显眼的滞后，而文档 Step 2 的验收标准明写"无一项滞后"；
  同步读回则 stall。另有三个消费者根本不在渲染上下文里：Step 4 的 PRTGI 烘焙（要对每个
  时刻 relight）、自检、Editor 时间轴预览，它们连"上一帧的读回结果"都没有。
  代价是第二份实现，靠 CPU/GPU 逐通道对账自检控住 —— 那条自检是**双向**的，
  两条完全不同的路径对上才说明两边都对。多一把独立的尺子是资产不是负债（#7 里两次
  "尺子自己坏了"都是因为只有一把尺子）。
  也没有用 Chapman 函数那类解析近似：它只覆盖**指数**密度剖面，而臭氧是帐篷剖面，
  正是黄昏蓝紫对侧色的来源，这个项目里不能丢。
- **由场景组件写灯，不由 RenderPass 写。** 从渲染代码里改场景状态本身是反模式（标脏、
  与 Undo 打架、多相机时后渲染的覆盖前一个），但更要紧的是**光色会变成相机相关的**：
  同一盏灯在主相机与反射探针相机下拿到不同的 T，画面与反射对不上。太阳照的是**几何**
  而不是相机，所以求值点必须是场景里一个与相机无关的固定参考点。
- **色度放 `color`、幅度放 `intensity`，不把整个矢量塞进 `color`。**
  `Light.color` 在 Inspector 上是非 HDR 的颜色选择器：塞 >1 的值代码里能生效，
  但一被面板碰到就夹掉，症状是"手点一下灯颜色，画面突然变暗"。UE 与 HDRP 同样是
  色度/幅度分离。归一化取**最大通道**而不是亮度 —— 取亮度会让日落 T=(0.3,0.1,0.03)
  归一出一个 >1 的红通道，同样被夹掉。
- **单值参考海拔的近似**保留，但把它的失效范围**算出来放到面板上**。
  用一个高度的 T 照亮整个场景在正午是够的（包线 271 m），问题是包线随太阳角剧烈收窄
  （5° 只剩 51 m），而魂系/开放世界外景的地形起伏是数百到数千米量级 ——
  也就是说**这个近似恰好在日出日落这段最关键的时间失效**。
  三条路：① 就这么用并在文档里写一句"注意高差"（等于没写）；② 每个高度层各摆一盏灯
  （灯数爆炸，且过渡处有硬边）；③ 逐像素透射率。选 ③，但它是独立一步（Task #12），
  因为它要改**不透明着色**而不只是写灯 —— 对应 UE5 平行光上的
  Per Pixel Atmosphere Transmittance 开关，也就是说这是有生产先例的分级项，
  而不是我发明的东西。当下先把包线做成面板读数：美术能当场看到"我这个场景高差 800 m、
  现在太阳 5°、包线只有 51 m"，这比一句文档提示有用得多，也是 #12 的立项依据。
- **不做大气折射。** 真实地平线附近折射把太阳抬高约 0.57°（约等于一个日面直径），
  所以"日出时刻"会差一两分钟。不做的理由不是难，是**它会污染判据**：
  加了折射之后高度角就不再是纯天文量，A/B/C/D 四条对闭式解的判据全部要跟着改口径，
  而画面上的收益是日出日落各早/晚一两分钟。真需要时它是一个后处理式的单调映射，
  可以在不动天文核心的前提下叠上去。
- **`k_MinMuSun` 取 0，不加人为的地平线淡出。** 太阳在地平线以下时直射光物理上就是 0，
  而 Transmittance LUT 的参数化在 mu < 0 且视线穿地时给的是"穿过地心"的无意义值。
  取 0 而不是某个负的软化值：地平线附近的软化应该由**大气自己**给出（T 在那里已经掉到
  1e-2 量级，是连续的）；人为再叠一层淡出会让"日落时刻的光色"变成一个凑出来的东西，
  那就不能拿物理去辩护了 —— 而这份作品集里每个选择都要能被追问。
- **朝向用 `Quaternion.Euler(alt, az+180, 0)`，不用 `LookRotation(-dir, up)`。**
  太阳升到天顶时 forward 与 up 共线，`LookRotation` 退化（结果未定义），
  症状是赤道地区正午光向突然乱跳。Euler 形式没有这个退化，附带好处是 Inspector 上的
  Rotation.X 可以直接读作"太阳仰角"。
- **一个量只允许有一份实现。** 这一步为此做了三次提取：包线求解器从自检搬进
  `VistaSunTransmittance`（面板与 F2 项共用）、灯值一致性判据 `LightMatchesComputed`
  留在面板里但改成 public static（诊断自检调它）、`ApplyLightParams` 从私有改成 public
  （接缝自检驱动它渲一帧）。理由都一样：**若自检自己抄一遍被测逻辑，改一边忘另一边时
  自检会替错的那份背书**，等于自检失效。这类"两份真相"在 #7 里吃过教训。
  代价是三个本可以是私有的 API 变成了公开面，接受。
- **诊断面板 `RequiresConstantRepaint() => true`。** 时间滑竿可能被 Timeline / 演示脚本 /
  别的面板驱动，不重绘的话面板会显示一个过期状态，而**过期的诊断比没有诊断更坏**
  （它让人去查一个不存在的问题）。代价量化过：包线求解是 40 次二分 × 41 段积分，
  几十微秒，且只在面板可见时发生。
- **面板绝不显示过期的 `lastLightParams`。** 三个前提（有灯 / 开着光色驱动 / 有 feature）
  任一不满足，那个结构体就是上一次成功求值留下的旧值 —— 尤其无灯这一支，`Apply` 提前
  返回，连大气那段都没跑。所以改成显式给出**不是活值的原因**那一行，而不是照着旧值画。
  这个洞是我重读自己刚写的面板代码时发现的，不是测出来的：第一版只判了缺 feature 与
  关驱动两支。

- **全屏合成不采样颜色目标，把两个乘子交给混合器，代价是两趟。**
  直觉写法是"采 cameraColor → 算 → 写回"，但同一张 RT 不能同时采样和写入：
  要么申请临时 RT 再拷回（双倍带宽），要么把 `cameraColor` 换成新贴图
  （那会强制 `requiresIntermediateTexture = true`，多一次最终 blit）。
  两条都不走，因为 `dst·T + S` 的两个乘子混合器都能做：
  `Blend Zero SrcColor` 得 `dst·T`，再 `Blend One One` 得 `dst·T + S`。
  换来的不只是省一次拷贝：不读颜色目标意味着**直接渲后台缓冲也能用**、
  **MSAA 下逐样本混合天然正确**、移动端两趟画在同一个 RenderPass 里不打断 tile。
  两趟的**顺序是公式的一部分**：反过来先加后乘是 `(dst + S)·T`，散射项被多衰减一次，
  症状是远景雾偏暗且越远越暗（T 越小错得越多）。
- **`AccessFlags.Write` 而不是 `ReadWrite`，尽管这个 pass 在做混合。**
  RenderGraph 把"Write 且未附加 Discard"当作 partial write，load action 仍然是
  `Load`（core 包 `NativePassCompiler.cs` 的 `partialWrite` 那一段），
  混合要的正是这个。URP 自己的 `DrawSkyboxPass` / `DrawObjectsPass` 也是这么声明的
  （全仓只有 `StopNanPostProcessPass` 用 `ReadWrite`）。
  改成 `ReadWrite` 不会让画面变对，只会多一条读依赖 —— 所以代码里写了注释挡住这个"修复"。
- **`_VistaApConsumer` 由 Sky-View pass 下发，不跟着 AP pass 走。**
  变体 B 的开关必须**每帧无条件写**，包括 AP 整个关掉的那一帧 —— 那时它得是 0。
  跟着 AP pass 走的话，AP 关掉的帧里那个 pass 根本不排入（记录期就 return），
  uniform 留着上一帧的 1，材质就会去采一张已经释放的 3D 表。
  「关掉某功能后画面才坏」是最难反查的一类失效，所以它挂在唯一一个"一定存在"的
  逐帧 pass 上。同理它不塞进 `_VistaApFlags` 的空位：那一组只在 AP 启用时才被写。
  也没有给 `VistaAtmosphereViewData.Bind` 加参数 —— 那会波及约十个不关心它的 dispatch 调用点。
- **排入与否在 `AddRenderPasses` 里判完，不在 `RecordRenderGraph` 里 return。**
  这个 pass 声明了 `ConfigureInput(Depth)`，**光是排入**就会让 URP 安排一次全屏深度拷贝。
  为一个什么都不做的 pass 拷一张深度是实打实的浪费。
  三个条件（`compositeMode == Fullscreen`、`m_Luts.isAerialPerspectiveValid`、材质有效）
  与大气 pass 里的 `apEnabled` 必须同真同假；这条耦合写成了
  `PrepareAerialPerspective` 上的**契约注释**，而不是在运行时再复制一份 guard ——
  复制出来的 guard 会变成"永不触发的保护性代码"，这个项目已经把那条教训记在上面了。

- **逐像素 T 的乘法插在 `MixRealtimeAndBakedGI` 之后，不是之前。**
  这不是"顺手放哪儿"：`MixRealtimeAndBakedGI` 在 subtractive lightmap 模式下会
  **读** `mainLightColor` 去从烘焙 GI 里减掉实时直射的那一份。
  插在它之前，减掉的就是修正后的直射光 —— 而烘焙那一侧记的是**参考高度**的太阳，
  两边不同源，症状是「山顶/谷底的 GI 出现与海拔相关的过减或欠减」，
  这种偏差是低频的、铺满整面墙的，反而最不像 bug。
  插在之后，subtractive 那一步吃到的仍是 URP 原本的那个值，
  修正只作用于最终累加进画面的直射项。
  代价是承认一件事：**烘焙 GI 本身不参与逐像素修正** ——
  它是在参考高度的太阳下烘的。这条在大高差场景里是可见的，但要解决它得动烘焙器，
  属于 Step 4 的范围，不该在这里用一个"看起来对"的乘法糊过去。

- **只覆盖主平行光；天光、bakedGI、点光/聚光都不参与。**
  天光已经把透射率积在 Sky-View LUT 里了，在这里再乘一遍就是乘两遍
  —— 与「输出比值而不是 T」是同一条错误的另一种形态。
  点光/聚光不是天体，没有"穿过整层大气到达着色点"这回事；真要做也是另一套体积衰减，
  不能借这条比值。

- **验收夹具用正交相机 + 一面竖墙，换来「一次渲染就是一条 T-vs-海拔 曲线」。**
  透视相机下海拔与像素行不是线性关系，要么得逐档移动相机（N 次渲染、N 份状态），
  要么得在 CPU 侧反解视锥（等于给夹具自己写一份投影，又是"第二份实现"）。
  正交 + 竖墙把这两样都消掉：行号 → 海拔是一个乘加，
  而被测的 `VistaSunTransmittanceRatio` 只吃 `positionWS`，不关心相机是什么投影。
  这个选择有一条代价，而且**已经咬过一次**：AP 合成的距离重建是按透视视锥四角插值的，
  正交相机下它给出的距离是错的 —— 于是 AP 的污染变成逐像素乱跳的样子（见「坑」）。
  夹具因此必须把 `compositeMode` 临时按到 `Off`。

- **累加自检（`VistaLitAccumulationSelfTest`）里把 #12 强制关掉，范围是整条而不只是判据 1。**
  那条自检比的是「我的累加」与 `UniversalFragmentPBR`，而 #12 **只乘我这一侧** ——
  它一生效，两侧就天然差 `mainLightColor·(ratio−1)`，与拷贝抄得对不对完全无关，
  一条本该长期绿着的回归自检会永久变红。判据 2（逐项故障注入）跑在同一套布景上，
  四项注入会一起带上这个共同基线偏差，所以范围必须是整条。
  关的方式是下发 `w = 0` 走那条**语义闸门**，而不是新加一个 `#if` ——
  后者会让被测的代码路径与出货的不是同一条。
  并且必须在报告里**点名**：一条被强制关掉的东西若不点名，
  「累加 ≡ `UniversalFragmentPBR`」会被读成「#12 不改变光」，恰好是反的。

- **`giColor` 在累加自检里灵敏度不足（占比 50.00%，恰在门下），选择承认而不是改布景。**
  判据 1 的门是 Weber 1%，注入量是 2%，于是能被判据 1 看见的项要求占比 > 50%。
  实测 `giColor` 占比 50.00%（×10 复量交叉验证过，见「坑」），刚好在门下 ——
  意思是判据 1 会放过 `giColor` 里一个 2% 量级的抄错。
  两条修法都试过在纸上：把布景的环境光调亮、或把注入量调大。
  第一条要动 `RenderSettings.ambient*`，那是**会落盘的全局资产状态**，
  而本自检的一条既有约束就是不改会落盘的东西（改了就污染用户工程）；
  第二条会让「这条自检看得见 2% 的偏差」这句话失效，而 2% 是踩着 Weber 1% 定的，
  换成 20% 之后自检只能担保「看得见一个肉眼早就看得见的错」，
  那正是判据 2 存在的意义被抽掉。
  所以选择是**如实报出占比与灵敏度不足**，把 `giColor` 的正确性交给别处：
  它的两个来源（SH 环境 + 反射探针）分别由 #3 的环境 SH 自检和 #10 的反射自检负责，
  那两条不经过 `UniversalFragmentPBR` 这把尺子，不受占比限制。

- **Step 3 的体积雾走「两个 froxel 体 + 一个采样函数」，推翻早先「雾合成进 AP LUT」的字面写法。**
  `ShaderLibrary/AerialPerspective.hlsl` 原本写着「近层的输出按 transmittance 加权写进本表的
  近端切片」。这句话在**目标**上是对的（下游只认一个采样函数，交界处不会露缝），
  但字面照做拿不到光轴，而理由是一个可以直接算出来的数：AP LUT 是 `(32, 32, 32)`，
  1080p 下每个 XY 纹素覆盖 **60 × 33.75 像素**，而一根光柱的边缘在屏幕上是 2~5 像素宽 ——
  差 15~30 倍，三线性插值只会把它抹成一团亮斑。
  AP 自己能用 32×32 恰恰因为它在屏幕空间是低频的（同文件开头那句「AP 在屏幕空间和深度上
  都极其平滑」），而**体积阴影是这句话的反例**。所以要保的不是「同一张表」，是「同一个采样函数」。

  排除掉的第二条路：把整张 AP LUT 的 XY 抬到 160×90，一个体解决。代价是 froxel 数
  32k → 460k（14×），而远端 32 km 的切片**完全不需要**这个 XY 分辨率 ——
  等于让远景为近段 300 m 的光轴买单。分层的理由在原注释里已经写清楚了
  （近层能采级联阴影、远层采了也是常量），这里只是把「分两层」推进到「分两张纹理」。

  定案：新增近层 froxel 体（1080p 8px 格 → 240×135×64，带级联阴影与局部灯），
  远端保持现有 32³ AP LUT，`VistaSampleAerialPerspective` 内部按距离挑，
  消费者（变体 A 全屏 pass、变体 B 的 `Vista/Lit`）签名与调用点一行不动。
  显存代价：240×135×64 RGBA16F = 16.6 MB，加历史与积分体约 50 MB —— PC 可接受，
  移动端不可接受，所以移动端档不是「同一条路降分辨率」而是另一条路（见下条）。

- **PC 主线用 froxel 体积雾，移动端档用「雾介质并进 AP LUT + 屏幕空间光轴」，两者互斥。**
  四条路排过：A froxel 体积雾 / B 解析高度雾 + 屏幕空间径向模糊光轴 /
  C 屏幕空间逐像素 raymarch / D 雾介质并进现有 AP LUT（无阴影查询）。
  选 A 做主线的三条理由全部落在这个作品集要出的画面上，没有一条是「更能体现能力」：
  ① 晨昏穿过树林/拱门的体积光是签名镜头，而这类镜头里太阳常常在画面外 ——
     B 的屏幕空间光轴在太阳出画时**完全没有输出**，直接出局；
  ② 雾里的篝火/火把只有 A 做得到，而那是夜景的主要光源形态；
  ③ 沼泽与洞穴的局部雾体只有 A 有干净的表达。
  业内先例：UE5 Volumetric Fog（8px 格 / 128 片 / 时间重投影）、
  HDRP Volumetric Fog（240×135×64）、Frostbite（Hillaire 2015，这套结构的源头）。

  C 被排除的理由反直觉，值得记下来：半分辨率 960×540 × 32 步 = **1660 万次阴影采样**，
  而 A 在 240×135×64 只有 **207 万次** —— froxel 把阴影采样在深度上摊给了整个 tile
  里的所有像素，逐像素 raymarch 每个像素各算一遍。**C 在同画质下比 A 更贵且更差**，
  所以它连「实现一遍做对照」的资格都没有，不进清单。

  D 不是额外工作量，这一点决定了「每个效果实现两遍」与「A/B 交叉验证」在 Step 3
  **重合成同一份工作**：D 就是现有 AP kernel 里加一层雾介质（密度剖面 + HG 相位 + 反照率），
  太阳照度复用 #12 的 `VistaSampleTransmittanceToSun`，环境项复用 Step 1 的
  `VistaSkyAmbientProbe`。于是 A 是 PC 档兼被测对象，D+B 是移动档兼对照组。

- **光轴从 froxel 里出来，不做单独的径向模糊 pass；PC 上两者禁止叠加。**
  逐 froxel 采样级联阴影的自然结果就是光轴，且自带正确的 3D 遮挡与「太阳在屏幕外也有」。
  一条要点名的限制：8px 格意味着光柱边缘是**软**的（UE5 的光轴就是这个观感），
  想要「石缝里一道锐利的细光」得抬 XY 分辨率，而不是叠一遍屏幕空间 pass ——
  叠了就是同一份能量算两遍，与 `VistaLitForwardPass.hlsl` 文件头那条
  「不能同时上 `MixFog` 和 AP」是同一个错误形状。屏幕空间那一版只作为移动端替身存在。

- **近层雾体的最远距离绑定 URP 的 shadow distance，切片分布不与 AP 一致。**
  超出级联覆盖之后阴影查询返回常量，光轴**按构造**不可能存在，多算的 froxel 是纯浪费。
  所以雾体范围跟着 shadow distance 走（URP 默认 50 m，大世界通常抬到 150~500 m），
  之外交回 AP LUT。切片分布则要和阴影级联边界对齐（级联边界处 froxel 边界对上，
  避免跨界抖动），而远层 AP 保持 Logarithmic —— 原注释里那条推论
  「远层可以用任意切片分布，不受必须和阴影级联对齐的约束」在这里第一次被真正用上。

- **雾是并列的第四个介质组分，只进两处 march；静态 LUT 与 SkyView LUT 一律传 `VistaFogSampleNone()`。**
  雾没有做成 `VistaSampleMedium` 的一个参数，理由是三条可以直接指出来的差别：
  ① 尺度差三个数量级（Mie 标高 1.2 km，雾标高 10~200 m）；
  ② Rayleigh/Mie/臭氧进了**静态**的 Transmittance / MS LUT（烘一次、球对称参数化），
  而雾逐帧变、可局部、要给美术 K 曲线 —— 把雾并进那两张表等于让雾在 6360 km 的球壳上生效；
  ③ SkyView LUT 的 uv 打包依赖方位对称，局部雾会破坏它。
  于是雾只进 AP LUT 的 march（档 D）与近层 froxel 的 march（档 A）。
  推论：**天空像素的雾不是靠 SkyView LUT 里含雾拿到的**，而是靠雾体远端的
  transmittance / inScatter 覆盖上去 —— 与 UE5 一致，也正是「分层」这个结构本来就该有的行为。
  代价是 `VistaEvaluateScatterSample` 多一个参数、四个调用点全改；
  换来的是"能量计算只有一份"这条纪律没有破口。

- **雾的高度必须从 `t·rayDir.y` 推，绝不能从 `posKm` 反算。**
  这不是风格问题，是一个能算出来的数：march 的位置 `posKm.y` 在 6360 km 附近，
  fp32 在那里的 ulp 是 2¹²·2⁻²³ = 4.883E-004 km ≈ **0.49 m**。
  于是 `r - bottomRadius` 或 `VistaAtmosphereToWorld(posKm).y` 拿到的高度分辨率只有半米，
  对一个标高 20 m 的地面雾，整条密度剖面被量化成 ~41 级台阶，
  症状是雾里出现水平条带、且**条带位置随相机高度跳**（量化格点是绝对的）。
  正确形式：`_VistaFogHeight.x`（CPU 用精确世界 Y 算的米制标量）+ `t·dir.y·1000`，
  全程不出现 6360 这个大数。`VistaFogHeightMeters` 看起来只是一个 mad，
  但换成"从 p 反算"就会坏 —— 这是本项目第二次踩到同一个 6360，
  第一次是 `VistaEarthShadow` 的 10 m 偏移（见「坑」）。

- **雾按平地板处理（高度 = 世界 Y），不跟随星球曲率。**
  曲率带来的高度偏差是弓高 d²/(2R)，R = 6360 km：
  d = 500 m → **1.97 cm**；d = 2 km → **31.4 cm**；d = 5 km → **1.97 m**。
  近层雾体的范围绑定 shadow distance（150~500 m），那里偏差比标高小两个数量级，
  平地近似在这个范围内是精确的。档 D 的 AP LUT 铺到 32 km，但雾在那个距离上
  早已指数衰减掉；只有"标高上千米的准均匀雾"才会让曲率可见，而那种设置本身
  就该用大气的 Mie 去表达。另外平地板正好是「高度雾」作为**美术概念**的定义
  （一层平的雾），跟随曲率反而让远处雾层看起来往下掉，不符合预期。

- **cbuffer 里存 σ_t + 单次散射反照率，而不是 σ_s 与 σ_t 各存一份。**
  各存一份的话 `σ_s > σ_t` 这种不物理的组合在数据上就是可表示的，
  而它的症状（能量放大、雾越远越亮）会被误读成"积分器写错了"。
  `saturate(albedo)` 放在 shader 里做，让这个非法态**不可表示**；
  albedo 是 uniform，saturate 会被提到循环外，逐样本零开销。
  同一条思路的另一面：失能态 = 全零态（σ_t = 0 时逐位等于没有雾），
  所以"忘了推这份 cbuffer"的后果只能是**没有雾**，不可能是"雾长错了"——
  这一点与 `_VistaApConsumer` 相反（它失能态 0 使能态 1，残留会反过来，
  所以那个必须每帧无条件重写），雾这里不需要那道保险。

- **美术口径用平均自由程 / 能见度，不用 UE 的 Fog Density。**
  shader 侧收 σ_t（1/km），CPU 侧按两个换算暴露：
  平均自由程 L（m）：`σ_t = 1000 / L`（走 L 米透射率降到 1/e，即 HDRP 的
  Fog Attenuation Distance，有先例）；气象能见度 V（m）：`σ_t = 3912 / V`
  （Koschmieder，2% 对比阈）。L = 100 m ⇒ σ_t = 10 /km ⇒ 能见度 391 m，
  两个数字都能直接对着场景验。UE 的 Fog Density 是个没有单位的调参数字，不采用。

- **雾的环境项入射亮度放在逐视线的 `VistaRaymarchSettings` 里，而不是逐样本采 SH。**
  两个理由，第二个才是决定性的：① 天光探针是相机位置的一个 SH，沿视线不变，
  逐样本重算是纯浪费；② 抬到 settings 里让 `AtmosphereScattering.hlsl` **不依赖**
  `_VistaSkyAmbientSh` 这个 StructuredBuffer —— 否则每个 include 它的片元着色器
  （Vista/Lit、天空、水面）都要白占一个 t 槽位，而移动端 GLES3 上片元着色器里的
  SSBO 是已知雷区。需要它的只有两个 compute kernel，各自在 setup 处调一次
  `VistaShAmbientMean`（SphericalHarmonics.hlsl 里那一份，注释里本来就写了"体积雾用这个"）。
  已知近似：这一项**不含遮挡**，洞穴里的雾拿到的天光和空地一样多。
  另有一个容易写错的系数：各向同性相位下 ∫p·L dω 就是平均入射亮度本身，
  所以源项是 σ_s·L̄ 而**不是** σ_s·L̄·(1/4π) —— 那个 1/4π 已经在
  `VistaShAmbientMean` 的推导里约掉了。写成后者的症状是阴影里的雾暗 12.57 倍，
  看起来像"环境项强度调小了"，很难反查。

- **雾对阳光的自遮蔽做了，但默认关 —— 因为 UE5 与 HDRP 都不做这一项。**
  它是可选增强而不是欠缺的功能：`τ = σ_t·(H/sy)·exp(-h/H)` 是指数剖面的闭式解，
  一个 rcp 加一次 exp，不需要沿太阳方向再 march；有了它浓雾才会"雾顶亮、雾底暗"，
  那是最能读出厚度的层次。不默认开的理由是模型是**无限大平板**：
  掠射太阳下 1/sy → ∞，而真实雾带是有限宽的（阳光从侧面出来了），
  无限板会把日出日落时的雾压黑 —— 那恰好是雾最该好看的时刻。
  所以放大倍数必须有上限（`_VistaFogExtinct.w`），且那是观感参数不是物理常数，
  写死在代码里会变成一个说不清来历的魔数。
  一个保留的坏配置：标高 → ∞（均匀雾）时 τ 无界，雾在阳光下变全黑。
  物理上是对的，是**授权错误**不是代码错误；保留这个行为是因为"全黑"能一眼看见，
  静默钳一个标高上限会让症状退化成"浓雾偏暗一点"，反而查不出来。

- **不透明物表面收到的直射光不吃雾的衰减 —— 这是预期行为，不是 bug。**
  表面的太阳光走 #12 那条链路（大气透射率折进 `Light.color × intensity`，
  CPU 侧 `VistaSunTransmittance`），那里没有、也不会有雾的自遮蔽项。
  于是浓雾里地面收到的直射光不被雾衰减，只有相机到地面这一段被 AP / 雾体的
  transmittance 衰减。这是 UE5 与 HDRP 同样的取舍（它们的雾对直射光的遮挡
  只有阴影贴图那一份）。写下来是因为它长得很像 bug：#18 接上雾之后
  "浓雾里地面偏亮"不要去 `VistaSunTransmittance` 里找错。
  真要补，正确位置是给平行光加一份雾的垂直柱密度衰减，
  但那会让同一个量出现第二份实现（CPU 一份、shader 一份），
  必须像 #12 那样用 ratio 形式让 CPU 项代数上约掉才行 —— 那是独立的一件事。

- **采样点放在段中心（`VISTA_SAMPLE_SEGMENT_T = 0.5`），而不是 Hillaire 的 0.3。**
  这一条是 #18 最贵的一次纠错，而且它是**可以算准的**，不需要经验常数。
  `VistaSegmentIntegral` 已经把段内的指数衰减**解析**处理掉了，所以最优采样点
  不是几何中点、而是**透射率加权质心**：
  `s*/dt = [1 − (1+x)e^(−x)] / [x(1 − e^(−x))]`，`x = σ_t·dt`。

  | x = σ_t·dt | 0（极限） | 1 | 2 | 2.8 | 3 |
  |---|---|---|---|---|---|
  | 质心 s*/dt | 0.5 | 0.418 | 0.3435 | **≈0.30** | 0.281 |

  也就是说 **0.3 是 x ≈ 2.8 时的质心**。Hillaire 的 march 没有段内解析积分、
  且步长按大气标高定，工作点确实落在那一带；本项目的段内是解析的、
  而且雾感知步长上限把 x 压到远小于 1，**那个常数的适用区间已经被自己的另一处
  优化推出去了**。抄过来的后果是整条 march 从二阶退化成一阶（见「坑」）。
  - **推论：τ 自适应的偏移是死代码**，不要去写。两个坏区间**由构造互斥**：
    x 大的地方介质是均匀的，`VistaSegmentIntegral` 本来就精确；
    介质陡变的地方雾的步长上限强制 `dt ≪ e折`，于是 x ≪ 1 而质心**就是** 0.5。

- **雾感知的步长上限：系数 0.4，无条件生效，不降级成画质档。**
  要控制的**不是**"中点法积分整条被积函数"的误差（段内指数已解析），
  而是它的前提：`VistaSegmentIntegral` 拿单点的 σ 当整段常数。
  用中点密度 ρ\* 代替整段，对源项积分的相对偏差有闭式解（`x = dt/L`）：
  `∫ρ ds / (ρ*·dt) = sinh(x/2)/(x/2) = 1 + x²/24 + O(x⁴)`。
  要 < 1% ⇒ `x < 0.49`，取 **0.4** 留余量（0.4²/24 = 0.67%）。
  `L` 是雾沿射线的 e 折 `H/|dir.y|`。
  - **它和 `VISTA_SAMPLE_SEGMENT_T` 是耦合的**：`x²/24` 之所以是首阶，
    靠的是 ρ 的一阶偏差在中点左右对称抵消。改任一个必须重核另一个。
  - **闸用光学深度而不是密度**：`σ_t·ρ·segLen < 1e-3` 的段无论怎么积分都改不了
    千分之一的透射率。不加这个闸，一条 32 km 的视线会在整条路上按雾的 e 折收紧步长，
    而雾在 5 个标高外密度已 < 0.7%。段内密度沿 t 单调（高度线性 + exp 单调），
    两端点取 max 即段内最大值，不需额外采样。
  - **它是承重的（实测）**：关掉上限（只留大气的 0.25 km）后视角③配置 D 的切片误差
    **6.90%**，超 5% 门；开着时同组最差 **0.49%**。而视角①/④（真水平视线）
    关闭前后**逐位相同** —— 证实 `dir.y → 0` 上它确实不生效，那两个视角不能用来判它。
  - **成本（实测，用均匀雾把两件事分开）**：同 σ_t、标高 ∞ 的均匀雾走**同样的
    逐样本数学**，但 `efold → ∞` 让上限永不生效，于是
    `均匀 − 关雾 = 雾数学`、`指数 − 均匀 = 步长上限`。
    512×288×32 的放大档（离开派发地板）：步长上限 **+2.6%**（A-A 空测 0.4%，可判；
    门 50%）；雾数学 +0.4%，落在 2× 空测之下 —— **未量到**，如实记为未量到。
  - **已知包络（写下来而不是等以后发现）**：`segLen/stepMax ≤ 0.673·(camH/H) + 4.7`，
    所以 `STEPS_MAX = 64` 覆盖 `camH/H ≤ 88`（标高 20 m 的雾从 1.76 km 俯视）。
    再高会被静默截断 —— 那时该走 #19 的近层 froxel 体，而不是继续加 AP 的步数。

- **errMid 从一个平门拆成两条各自可失败的断言：「实现有没有错」与「分布够不够用」。**
  errMid 有两个完全不同的来源：切片分布在这一段太疏（**表达能力上限**，
  只有换分布或换近层 froxel 体才能改）、以及行进循环本身积错了。
  一个 2% 的平门把两者判成同一个失败，而它们导向的行动截然不同。
  实测的对比就是这条判据存在的理由：**同一份代码在视角① 上读 0.4%，
  在视角③ 上读 19.25%**。
  - **分解是恒等式，不是模型。** 记 `L` = LUT 在切片中心的值、`R` = 4096 步参考解在
    切片中心的值、`R_mid` = 参考解在 w 中点的值，三角不等式给出
    `|½(L_i+L_{i+1}) − R_mid| ≤ |½(R_i+R_{i+1}) − R_mid| + ½(|L_i−R_i| + |L_{i+1}−R_{i+1}|)`，
    左边是**实测 errMid**，右边第一项 `固有` 是**切片分布自己的中点重建能力**
    （一个 LUT 值都不含 ⇒ 改被测代码骗不过它），第二项 `端点` 已由 errCenter 单独设门。
    于是：**实现** ＝ `实测 ≤ 固有 + 端点 + 0.30%`（fp16 存储圆整允差）；
    **分布** ＝ `固有 < 2%`（可被显式声明放宽，代价印在同一行）。
    errMid 的每一部分现在都归属于恰好一个负责人。
  - **为什么弃掉了泰勒曲率预测（#18 走过的弯路）。** 第一版用
    `|g_{i−1} − 2g_i + g_{i+1}|/8` 去**估**同一个量，于是需要一个 2× 经验裕度。
    两处失效：视角③配置 D 读到 `实测/预测 = 2.6×` 被判成"模型不成立"，
    而真实成因只是**泰勒展开在近处的陡步长上失效**；更糟的是两规则结构
    **在 `pred = gate` 处不连续** —— pred 1.9% 拿零裕度、2.1% 拿 2× 裕度。
    教训：**能算准的量不要去估**。让核多写一行（新增 `(4,0,z)` 那一行存
    切片**中心**的参考/LUT 透射率）就能把近似换成恒等式，那就多写那一行。
    副产品：恒等式判据**没有边缘切片**（只需端点 z 与 z+1），
    而曲率版会因 NaN 回退丢掉切片 0 与 d−2。
  - **视角③ 的声明上限是「实测基线」而不是推导值，这一点必须诚实标出。**
    该视角的雾拐点在 `300/(sin20°·1000) = 0.877 km`（相机降到雾层顶），
    Log 32 片在那里片长 ≈ `0.269·877 = 236 m`，而雾沿射线的 e 折是
    `50/0.342 = 146 m`（标高 50 m）或 `20/0.342 = 58 m`（标高 20 m）——
    片长是 e 折的 1.6~4.1 倍，中点线性插值在这一片上必然差得离谱。
    这里**拿不出更紧的解析上界**：纯指数的中点插值误差是 `cosh(Δ/2L) − 1`，
    `Δ/L = 4.07` 时算出 **290%**，而实测只有 **19%** —— 因为累积入散射
    在打到地面处**饱和**了，指数律在这一段是个远远不紧的上界。
    **把 290% 写成门等于没有门**（不紧到 15 倍的解析上界写成门 = 没门）。
    所以 `capacityCeilingS = 25%` / `capacityCeilingT = 15%` 只承担
    「回归基线」这一个职责，且它们只放宽「分布」那一条断言，
    「实现」那条恒等式一分不放；档 D 的雾**画质**门在 #27（档 A vs 档 D 的逐像素差）。
    ~~#19 落地后这两个字段必须删掉。~~ —— **该待办已撤销，前提是错的**，见 #19 那一节。

- **参考解的步数从 256 提到 4096。**
  256 步的参考解自己就带着与被测对象同量级的误差 —— 尺子的地板与被测量的期望值
  同量级时，尺子会替被测对象伪造一个结论。这一条改动同时把 #7 记录的那张
  errCenter / errMid 表整体抬高，所以旧表的数字与新表不可横向比较。

- **两张 32 片的 AP 表都装不下带雾的视角③，这是结构性的，不是调参能解决的。**
  Log 32 片能覆盖的两个包络条件：① `σ_t·ρ(t)·t < 1.05`；② `t·|dir.y| < 1.05·H`。
  视角③（拐点 877 m）**两条都不满足**，而且**雾感知的切片分布在结构上不可能存在**
  （切片分布是全屏共享的一张 3D 表，而雾的拐点位置随视线方向变）。
  这正是 Step 3 要同时做**解析高度雾闭式解**与 **#19 近层 froxel 体**的理由，
  也是这两条路不是"同一件事的两种精度"的理由。

- **天空雾这条判据的门，一半是推导出来的、一半是标成「实测基线」的 —— 刻意的。**
  两项式剩下的最大近似是「整条 `L_sky` 都被乘上 `T_fog`」，
  而真实情况是雾外那一段大气的入散射不该被雾衰减。它的量级模型是
  「（雾的**沿射线**跨度 / Rayleigh 的沿射线跨度）×（1 − T_fog）」，
  两个跨度都要按 Chapman 封顶，于是有**三个区间**：
  - 非掠射、有限标高、相机在雾内 ⇒ `H/H_R = 50 m / 8 km ≈ 0.6%`，门取 **3%**（推导 × 5）；
  - 掠射（两个跨度都被曲率封在 `sqrt(2πRH)`）⇒ `sqrt(H/H_R) = 7.9%`，
    是比值的**平方根**；实测最差 16.44%，基线 **20%**；
  - **均匀雾（H → ∞）** ⇒ 跨度由 `1/σ_t` 而不是 `H/dy` 定，
    `min(H, 1/σ_t)/H_R` = 天顶 12.5%／55° 约 10%；实测 5.65%，基线 **8%**。

  后两个区间**不写成推导门而写成实测基线**，理由是那条份额公式是**量级估计、
  不是紧的上界**：它在布景②掠射档偏低十倍（预测 0.13%、实测 1.30%），
  在均匀雾档又偏高两倍（预测 10~12.5%、实测 5.65%）。
  「一个不紧到 15 倍的解析上界写成门，等于没有门」——
  所以那两格在报表上印的是「**未判达标**」，而不是「达标」，
  它们的画质门被显式推到 #27 的 A-vs-D 逐像素对比里去兑现。
  值得记的是这个模型的**结构**在均匀雾档得到了一次独立印证：
  它预测均匀雾的最差方向在**天顶附近**、而**掠射反而更好**
  （曲率把 Rayleigh 跨度封在 316 km，份额掉到 0.3%）—— 与有限标高档的最差方向
  **恰好相反**。实测的 55.5° 落在预测的这一侧。

- **判据4 的参考解只能自己重写一份，不能复用 `VistaApReference`。**
  那个 4096 步参考解是按**大气**的介质定步长的（Rayleigh 标高 8 km），
  而这条判据要判的是标高 50 m 的雾 —— 尺子自己就跨掉一个 e 折，
  它会先于被测对象失效，然后把自己的误差报成被测对象的误差。
  所以判据4 的参考积分按**雾的 e 折**定步长，且判据5 的尺子地板
  （`x²/24`，x = dt/e折）**逐方向打印**出来，低于地板的读数只报成上界。
  这是「尺子的地板与被测量的期望值同量级时，尺子会自己伪造一个结论」的直接应用。

- **覆盖性判据（判据7）用的是被测函数自己造出来的见证值，不是在 C# 里重算一遍。**
  kernel 把 `VistaFogSunPathKm(sunDir.y)` 与 `VistaFogSunPathKm(0.0)` 一起写进
  slot 6 —— 后者是「必然走掠射上限那一支」的输入，于是「上限有没有生效」
  这个判断只需要比两个读数，不需要在 CPU 侧重写 `min(rcp(sy), _VistaFogExtinct.w)`。
  「同一个量的第二份实现连 8 行的辅助函数也算」。

### 坑

- **`_MainLightShadowmapSize` 只在软阴影档才被下发，硬阴影档下读到的是别人留下的脏值 ——
  而那个脏值是一个看起来完全合法的 `1×1`。**
  #20 的 ⑥ 号判据（阴影 atlas 有没有内容）第一版按这个全局算采样坐标，
  读回来 `atlas 深度 min 0.000000 / max 0.000000 ⇒ 跨度 0`，**红**。
  - **归因的入口是同一趟里另一格读数与它矛盾**：判据⑤b 同时报 `1034 / 16384` 个探针点被遮。
    atlas 是常数、却有一千多个点查出了遮挡 —— 这两件事不可能同时为真，
    所以「场景里没有投影物」这个最自然的解释当场被排除，问题在尺子自己身上。
  - 去 URP 源码 grep 而不是猜：`MainLightShadowCasterPass.cs:334` 把
    `_ShadowOffset0`（:336）、`_ShadowOffset1`（:339）、`_ShadowmapSize`（:343）
    **三行一起**包在 `if (shadowData.supportsSoftShadows)` 里。本项目是硬阴影档，
    这一帧根本不下发，读到的是上一个走过别的路径的相机留下的值。
  - 实测那个值是 **1**，出处是同文件 `:34` 的
    `s_EmptyShadowmapSize = new Vector4(1, 1, 1, 1)`（由 `:29` 的
    `k_EmptyShadowMapDimensions = 1` 来），在 `SetShadowParamsForEmptyShadowmap`（:245，
    从 :426 调）里设 —— 也就是某个走过 empty-shadowmap 路径的相机
    （SceneView / 材质预览 / 反射探针）留下的。
  - **`1` 比 `0` 更坏。** `1×1` 是一个完全合法的 atlas 尺寸，拿它算格心
    `(2i+1)·smW / (2·32)` 全部得 0，1024 个探针点塌到同一个 texel，
    不越界、不报错、不产生 NaN，症状与「场景里没有投影物」逐字相同。
    读到 0 至少还能被一句 `smW > 0` 抓住。
  - URP 自己不受影响：那个尺寸只被 tent 软阴影的 offset 消耗
    （`Shadows.hlsl:160` 把它塞进 `shadowSamplingData`），1 tap 的硬阴影路径根本不读它。
    所以这是一条**只有 Vista 会踩**的坑 —— 复用引擎全局时，"URP 自己用得没问题"
    不等于"它在所有画质档下都有效"。
  - **修法：向被测资源自己问，而不是问引擎全局。**
    核里改成 `_MainLightShadowmapTexture.GetDimensions(smW, smH)`，实测 2048×2048。
    少一个跨模块依赖，而且它的失效有唯一归因（返回 0 就是纹理没绑）。
    同时把两个尺寸都记进探针槽位（11 / 12），并拆出独立的一格 ⑥a 判它 > 0，
    让 ⑥a 失败时 ⑥b 印「未覆盖」而不是「失败」—— 与核内那句 `if (smW > 0u && smH > 0u)`
    的跳过**逐字对应**。
  - 那个不可靠的全局也留在探针里（槽 13）当**对照读数**，判据把它和真实尺寸并排打印。
    理由：「它为什么曾经是个常数」应该在报表上有出处，而不是留在注释里 ——
    注释会被后来的人当成过时的担忧删掉，一个打印出来的 `1（真实 atlas 宽是 2048）` 不会。

- **reversed-Z 让「阴影 atlas 是不是空的」这条判据产生了一次假通过，
  而它坏在自己唯一的职责上。**
  第一版的规则是「`min < 0.999` ⇒ atlas 有内容」，隐含假设「清空态是 1.0」。
  - D3D11 / Vulkan / Metal 上 URP 的阴影投影矩阵带 reversed-Z 翻转
    （`MainLightShadowCasterPass.cs:296-299` 那个 no-op 矩阵的 `m22 = usesReversedZBuffer ? 1 : 0`
    是同一件事的另一半），**清空后的 atlas 深度读回来是 0.0**。
    于是 atlas 全空时 `min = 0 < 0.999`，判据宣布「有内容」——
    一个假通过，而这一格存在的**唯一**理由就是把「atlas 是空的」与
    「atlas 有内容但我们查出来恒 1」分开归因。
  - 「尺子和被测对象用了不同的物理，产生的假失败比真失败更贵」这条的**更坏的镜像**：
    它产生的可以是一个假通过，而假通过恰好坏在该判据唯一的职责上。
  - **修法：改判「atlas 是不是一个常数」，也就是 `max − min`。**
    「空 atlas 是一个常数」不依赖任何深度约定，reversed-Z 与正向 Z 都成立。
    实测 `min 0.000000 / max 0.998626 ⇒ 跨度 9.986e-1`（门 > 1e-3）。
    顺带这两个数把约定本身也读出来了：`min ≈ 0 且 max > 0` ⇒ reversed-Z。
  - 残余风险点名：全部 1024 个采样点在光空间共面时 atlas 也会是常数 ——
    那要求整个 atlas 只有一个正对光的平面，可忽略，但要写在报表里。
  - **这一条同时反过来印证了上一条。** 旧规则（`min < 0.999 ⇒ 有内容`）
    在 `1×1` 那个 bug 下会读到 `min = 0` 并**全绿**。两个缺陷各自都能藏住对方，
    只有先把假通过修成真判据，那个 `1×1` 才有机会红出来。
  - 一般化：**判据的规则若要拿读数去跟「清空值 / 默认值」比，先问那个值是否依赖平台约定。**
    改判「是不是一个常数」就与约定无关了。

- **`SampleShadowmap` 的返回下界是 `1 − shadowStrength` 而不是 0，
  而那个没有归因的 `min = 0.2` 编码着一条缺失的判据成因。**
  URP `Shadows.hlsl:306` 的 `SampleShadowmap` 最后一步是
  `return lerp(1.0, s, _MainLightShadowParams.x)`，`x` 就是主光的 shadowStrength。
  - 判据⑤a 读到 `阴影值域 min 0.199999`。这个数当时**无法归因** ——
    既不是 0（完全遮挡）也不是别的什么已知量，很容易被当成噪声或者 fp 误差接受。
    「一个没有归因输入的整数读数会被当成噪声接受，而它其实编码着一个判据缺失的成因」。
  - 把 `GetMainLightShadowParams().x` 也记进探针（槽 10），读回 `0.800000`，
    地板 `1 − 0.8 = 0.2`，与实测 `0.199999` 差 `-0.000001`（定点编码的 1e-6 量化）。
    归因闭合。
  - **真正的收获不是解释了那个 min，而是补上了「一个点都没被遮」的第三种成因**：
    `strength == 0` 时 `SampleShadowmap` 恒返回 1，而那与「阴影矩阵算错了」
    在报表上长得一模一样。在记下 strength 之前，这条成因根本不在候选列表里。
  - **这一格印成 ⓘ 而不是门。** 值域 `[1−strength, 1]` 是 `lerp` 的构造性保证，
    判它等于「一个本轮无法失败的守卫」。但它的**输入**（strength）要打印出来，
    因为那才是能变、能出错、能解释别的读数的东西。

- **一次全绿的运行，必须逐格复核「绿的理由对不对」，而不是只看颜色 ——
  本段的第二次复核抓到了第一次复核漏掉的缺陷。**
  #20 的判据一共全绿过两次，两次都有问题：
  - 第一次全绿：⑥ 的规则拿 `min` 去比一个**假设的**清空值 1.0，是假通过（见上）。
  - 第二次（把 ⑥ 改成判跨度之后）：⑥ 红了，真因是 `_MainLightShadowmapSize`（见上）。
  - 第三次全绿之后再复核，发现**我自己写的 ⓘ 文案与读数矛盾** ——
    注释里断言「硬阴影档 ⇒ 这个全局读到 0 是预期的」，而实测是 1。
    于是又去 grep 出 `s_EmptyShadowmapSize`，才拿到那条「1×1 看起来完全合法」的结论。
  - 规则：**颜色只说明「按当前规则通过」，复核要问的是「当前规则要拒绝的最小错答案是什么，
    这次的读数真的能拒绝它吗」**。把那个最小错答案的数值印在报表上，
    是让这件事下次不必靠记性的唯一办法。

- **判据自己的抵消误差伪造了一个「失败」—— 4 档全红，被测代码是对的。**
  #19 的求值点判据第一版写成「求值点落在分段里的度量位置」这个**减法**形式：
  `frac = (sample − segNear)/(segFar − segNear)`，期望 ≈ 0.5，门 1e-6。
  五档读到 0.95e-6 ~ 7.5e-6，**4 档越门**。
  - **归因在动手之前做完，这是这一条能被正确修掉的全部原因。**
    读数严格按 `1/(ρ−1)` 排序：D 档 `ρ−1 = 0.0366` 最坏 7.5e-6、A 档 0.0874 → 2.89e-6、
    E 档 0.107 → 3.52e-6、C 档 0.126 → 3.55e-6、B 档 0.182 最好 0.95e-6，
    与 `3e-7/(ρ−1)` 吻合到 2 倍以内。三个操作数都 ≈ d，两两相减只剩 `(ρ−1)·d`，
    fp32 的 `3e-7·d` 绝对误差于是被放大成 `3e-7/(ρ−1)` 的相对误差。
    **这是尺子的地板，不是被测代码的缺陷。** 若当时先去动被测函数，
    改任何一处都不会让读数变好，而"改了没用"很容易被读成"分布公式错得更深"。
  - **一般结论（新增的一条定律）：由两个近乎相等的指数相减构成的残差，
    其地板是 `ulp/(ρ−1)` 而不是 `ulp`。** 推论：**当操作数近乎相等时，
    优先写恒等式的乘法／比值形式，而不是减法形式。**
  - 修法是换成代数等价的乘法形式 `sample²/(segNear·segFar) − 1`
    （片 0 走 `sample/(0.5·segFar) − 1`，对应被测函数自己的退化分支）。
    **诊断由此得到独立确认**：新读数 3.785e-7 ~ 9.098e-7，不但低了 4~16 倍，
    而且 **`ρ` 趋势整个消失**（D 档 4.7e-7 反而好于 C 档 9.1e-7）。
    一个"变小了"只能说明改动有效，一个"趋势消失了"才能证明成因就是那个放大项。
  - **分支只开在片 0，与被测函数里的分支位置逐字对应。** 判据的分支若比被测函数多
    或少一处，一次失败就无法归因到「公式错」还是「分支错」。
  - **门是按「floor + 最小错答案」重新推的，不是把 1e-6 松成一个圆整数。**
    地板 ~1e-6；这道门必须拒绝的**最小错答案**是「把求值点写成度量中点」，
    在比值形式里的偏差 `2·|(√ρ−1)/(ρ−1) − 0.5|·(ρ−1)`，五档里最小的 D 档是 3.295e-4。
    门取 **1e-5**：地板之上 10 倍、最小错答案之下 33 倍。
    **那个倍数每档都印在报表上（33×/183×/272×/374×/764×）**，
    所以「门紧不紧」是一个能读的数，而不是一句注释里的形容词。

- **「不可判定」被判定行读成了「通过」。**
  模型 A 接上合成后的第一版：合成那一行印着「不可判定：出现次数与点名对不上」，
  而最后的判定行照样打「模型 A 交叉验证完成」。
  成因是那道门只看 `compositeMissing / compositeCpuOnly / compositeOccUnstable`，
  而 `compositeOccUnstable` 只数「帧间在变」—— 一个稳定的 2 不算缺口，
  于是"一次判定都没跑"这件事没有任何变量记录。
  修法是加一个 `compositeJudged` 进那道门。
  **未覆盖的路径必须是缺口，不能是通过**，这是本项目已经记过的反模式又犯一次
  （上一次是自检里的"没进这个 renderer"分支）。

- **下界判定失败之后还在描述落点，同一份报告里出现两句互相矛盾的话。**
  故障注入（把 `floorMs *= 10`）时方向告警正确响了 ——
  「**低于全天空下界 0.349 ms**」、判定行「合成包线**方向反了**」——
  但紧接着的下一行照样打「落在包线内（占上界 66%）」。
  成因是落点段落只由 `minMs > ceilMs` 选择，没有挂在下界判定上。
  真出故障时这句话会**替故障背书**：读的人看到"落在包线内"就不会再往上看。
  修法是把 `bool aboveFloor` 提到前面，没过就印
  「下界未过，落点位置不再解读：包线本身已经不可信」，一个位置数字都不给。
  这条是"故障注入不只是验告警响不响，还会顺手挖出没人想过的自相矛盾"的实例 ——
  两次注入（点名数 +1、下界 ×10）都是这么用的，第二次的收获比第一次大。

- **`runInBackground` 关着 + Editor 失焦 = 静默挂死，而且连挂在哪都看不见。**
  Project-ARPG 的 `ProjectSettings.asset:86` 是 `runInBackground: 0`。
  Editor 一失焦 play 循环就停走，`Time.frameCount` 冻住，
  于是按帧数推进的采样永远等不到 —— 表现是**菜单执行完什么都不打印，一直挂着**，
  而控制台还被 Clear on Play 清空了，连"武装成功"那条都看不见。
  静默挂死是最坏的失败模式：它和"还在跑"、"pass 名走歧"长得一模一样。
  实测那次挂了 50 s 才靠 `manage_editor(stop)` 逼出
  「交叉验证被手动中断，采样帧数不足，未出报告」，才确认 `s_Running` 是设上了的。
  两条修法：
  - 进 play 时打开**运行期的** `Application.runInBackground`、退出还原，
    **不是** `PlayerSettings.runInBackground` —— 后者会写进 `ProjectSettings.asset`，
    那就成了"量性能的工具改了工程配置"。运行期属性退出即还原，磁盘上不留痕
    （已核对：跑完后那一行仍是 0，`git status` 干净）。报告头上把这件事声明出来。
  - 15 s 无**帧号**进展就中止并说出成因。看帧号而不是看墙钟，
    否则"机器慢"会被误判成"卡死"。

- **双源混合在 Unity 里不存在 —— 试过了，记下来免得再试一遍。**
  硬件本来有一条一趟写法：双源混合（`SV_Target0 = S`、`SV_Target1 = T`，
  `dst = S·One + dst·Src1Color`）。但 `UnityEngine.Rendering.BlendMode` 里
  **根本没有** `Src1Color` / `Src1Alpha`（反射实测只有 `Zero/One` 与 `Src|Dst` 的
  `Color|Alpha` 那十一个），ShaderLab 的 `Blend` 解析器也直接拒收：
  `Parse error: syntax error, unexpected TVAL_ID, expecting TVAL_VARREF or TVAL_BMODE`。
  发现路径值得记：这个 Pass 是**先写进 shader 的**，是刚写好的接线自检在第一次运行时
  报出编译错误才查清的 —— 而它同时暴露了一个更坏的现象：整个 shader 编译失败时，
  `Material.passCount` 仍然返回 3，pass 名却是替身 shader 的
  `<Unnamed Pass 0>` / `DepthNormalsOnly` / `DepthOnly`。
  也就是说**只判 passCount 不判 pass 名的自检会在这种情况下通过**。
  其余混合方程也表达不出 `dst·T + S`：`Blend One SrcAlpha` 只能给出灰度 T，
  `Blend One OneMinusSrcColor` 的 dst 系数是 `1−S` 而不是 T。
  所以两趟不是"先图省事"，而是 Unity 里唯一可表达的形式。
  **这条同时推翻了我上一轮自己的计划**：原本打算保留双源那一趟做带宽 A/B，
  #15 的性能项里那个对比必须删掉。

- **绝对光度量的管线里，"少乘一次曝光"的症状是"阴影不见了"，而不是"画面太亮"。**
  `VISTA_EXPOSURE` 全工程只在 `VistaSky.shader` 里乘了一处；两条**引擎出口**
  （SH → `RenderSettings.ambientProbe`、cubemap → `customReflectionTexture`）
  发的是裸的绝对量。它们的消费者是 URP Lit 的 `unity_SHAr` 与
  `GlossyEnvironmentReflection` —— 不是 Vista 的 shader，身上没有曝光级。
  于是环境光重建值 1.4e3~8.2e3，而工程里的平行光是 Unity 常规单位
  （`m_Intensity: 3.14159`），比值 1600~2600 倍：直接光只占最终亮度的约 0.04%，
  低于 1% 的 Weber 可见阈（判据来自 Task #6）。shadowmap 全程正确、
  Frame Debugger 里一切正常，但阴影**在数值上被淹没了**。
  这类 bug 的排查代价来自它的伪装：报出来的现象（"没有阴影"）与病因
  （"环境光单位不对"）在概念上毫无关联，很容易直接去查阴影距离、级联、
  `m_Shadows.m_Type`。教训：**混合单位制的边界必须显式列出来**，
  凡是把值交给引擎/第三方 shader 的地方都是一个边界，一个都不能漏。
  同理，只在自家 shader 里乘曝光的做法只有在"所有消费者都是自家 shader"时才成立 ——
  一旦要接引擎的现成路径，这个前提就不再成立。
- **"实测这个属性不会置脏"这类断言，会在代码里放很久然后被发现是错的。**
  `BindReflectionToRenderSettings` 原先带一句注释："实测环境光那几个属性不会置脏"。
  实测（`Log Ambient Probe State` 报 `isDirty`）是 **True**。这是本项目记过的
  *stale-assertion* 模式的又一个实例（上一个是计时器里硬编码的判定文本）。
  处置不是把注释改对就完了 —— 是让代码**不再依赖**任何"不会置脏"的前提，
  一律走保存/还原。凡是靠"某个引擎行为恰好如此"来省掉清理逻辑的设计，
  都欠着一次这样的返工。
- **`RenderSettings` 是"逐场景序列化"+"域重载不丢"的组合，这两条加起来会伪造出
  "改动没生效"。** 修完曝光后重编译，`Log Ambient Probe State` 仍印 5119 ——
  看起来像改动没编进去。两个原因叠在一起：(a) `read_console` 的 `count`
  返回的是**最旧**的匹配（已记过的坑，这次又踩了），读到的是上一次的报告；
  (b) `RenderSettings` 是原生状态，域重载不会清，而 Edit 模式下 Game view
  不重绘就没有新的回读完成，于是内存里那份陈旧的绝对量探针一直挂着。
  确认手段：先 grep 场景 YAML —— `ambientProbe` **不在**序列化字段里
  （只有 `m_UseRadianceAmbientProbe: 0`），所以磁盘上找不到它；
  然后进 Play 模式强制出真帧才拿到 0.1302。
  顺带一条：Edit 模式下**反射出口是同步写、环境光出口是异步回读**，
  完全可能出现"`defaultReflectionMode` 已 Custom 而 `ambientMode` 还是 Skybox"
  的中间态；诊断只报环境光那一条会把这种偏态误判成"链路没通"。
  所以 `Log Ambient Probe State` 现在两条出口一起报。
  进 Play 模式后也要**等几帧**：回读本身 2~3 帧，刚进去就读会拿到全零。
- **"保存前还原"挡不住"基线本身是脏的"。** 守卫还原的是"本次运行首次写入前扣下的
  原值"。如果某个场景在守卫存在之前就已经把 `m_AmbientMode: 4` /
  `m_DefaultReflectionMode: 1` 写进了磁盘，那么这次打开场景扣下来的"原值"
  就是被污染的那一份 —— 守卫会忠实地把 Custom 还原回去，**越保存越干净不会发生**。
  这种状态**无法自动分辨**（Custom 也可能是用户有意设的），只能由人显式复位。
  于是加了 `ForgetBaselines()`（只丢基线、不写回）与
  `Window/Vista/Reset Scene Ambient & Reflection`。顺序是硬性的：
  丢基线必须在保存**之前**，否则守卫会在写盘前把脏基线还原回去，
  表现为"复位了也保存了，磁盘上还是 Custom"。
- **验证一次"保存不会写入 Custom"，先得确认那次保存真的写盘了。**
  第一版验证是点 `File/Save` 然后 grep 磁盘 —— 看到 `0` 就以为守卫生效。
  但当时 `isDirty = False`（前一步的复位刚保存过），`File/Save` 是空操作，
  这个"通过"什么都没证明。这是本项目一直在追的 *false-pass* 模式。
  改成显式 `EditorSceneManager.SaveScene`（无条件写盘），并在保存前先把
  实时状态打出来确认它确实是 `Custom` + `2.54e-5` + cubemap 已绑定，
  测试才有内容。**判据的前提条件本身也需要被测量**，不能假定它成立。
- **改了口径就要回头查所有按旧口径标定过的阈值。** 探针改成"交接时乘曝光"之后，
  `Log Ambient Probe State` 里那句"链路已接通"的门限 `c_0 > 1e-3` 是照**绝对量**
  定的，直接沿用会在低太阳角/夜间把正常链路判成未接通
  （1 cd/m² × 2.54e-5 = 2.5e-5 < 1e-3）—— 一个**自己造出来的假失败**。
  修法是把门限按曝光归一，且曝光从 `_VistaSunDirection.w` 读（与运行时同一个来源），
  不在自检里重算一遍 EV100：重算就是第二份真值，迟早走歧。
- **`Scene.handle` 在 Unity 6000.4 已换成 `SceneHandle`**，到 `int` 的隐式转换
  标记为废弃（CS0618）。存 `Scene` 结构本身即可 —— 它自带 `==` 且比的就是句柄，
  既躲开废弃 API，也不用跟着 `SceneHandle` 的表示形式改。
- **`MenuItem` 路径里的 `&` 要写成 `&&`**，而通过 MCP 的 `execute_menu_item`
  调用时传的也必须是带双写的那份（`Reset Scene Ambient && Reflection`），
  传单个 `&` 会报"菜单项无效"。
- **本次的既定残留：装了大气模块的场景在 Editor 里会长期挂 `*`。**
  写 `ambientProbe` 会置脏，太阳一动就再脏一次。**不**用
  `EditorSceneManager.ClearSceneDirtiness` 去消它 —— 那会连带清掉用户自己未保存
  改动的脏标记，关闭时不再提示保存，是拿数据丢失换一个星号。
  受影响面已实测：扫过的约 29 个场景里只有 `Assets/Scenes/Demo.unity` 被污染过
  （已复位为 `0 | 0`）；`World_02` 的 Trilight、
  `PolygonFantasyHeroCharacters/Demo_RandomCharacter` 的 Flat 都是原有设置，与 Vista 无关。
- `Categorization.CategoryInfo`（Graphics Settings 面板分组用的特性）是**引擎内部 API**，
  只对 Unity 官方 SRP 包开放（`InternalsVisibleTo` 白名单）。第三方 package 实现
  `IRenderPipelineResources` 时不能加这个特性，否则 CS0246。纯显示层，去掉无影响。
- LUT 的单位区间端点必须落在**纹素中心**（Bruneton 的 `0.5/size + x*(1-1/size)` 内缩），
  不做这层映射时双线性采样会把区间外的值混进来，症状是地平线附近出现台阶状色带。
  这是整套 LUT 最容易漏的精度坑，且症状延后到最终天空颜色才暴露 —— 所以先写自检工具
  比先写 RenderGraph pass 更划算。
- **HG 相位函数的 cosθ 符号**。约定 `cosTheta = dot(rayDir, sunDir)`（看向太阳为 +1）时，
  分母必须是 `1 + g² − 2g·cosθ`。写成 `+2g·cosθ` 会把前向散射峰搬到太阳的**反侧**，
  症状是光晕出现在背光方向。推导写在 `AtmosphereScattering.hlsl` 的函数注释里。
- **朝太阳的透射率不能只靠 Transmittance LUT**。太阳在地平线以下时，Bruneton 参数化会
  给出一条穿过星球本体的无意义路径，必须另做 ray-sphere 判定（`VistaEarthShadow`）归零。
- 建 MS LUT 时积分器必须关掉 `useMultiScattering`，否则自引用（读一张正在写的表）。
- `execute_code` 在本工程不可用：Roslyn 未安装，CodeDom 因引用程序集过多导致
  mono 命令行超长。验证逻辑要做成 `[MenuItem]` + `Debug.Log` 才能自动化调用。
  且多行日志经 MCP 转发只保留首行，报告需压成单行。
- **RenderGraph 只在 pass 边界插入资源状态转换**，一个 pass 内同一资源只有一个状态。
  Transmittance 以 UAV 写出 → MultiScattering 当 SRV 读 → SkyView 读前两张，
  因此**必须拆三个 pass**。挤在一个 pass 里在 D3D12 / Vulkan 上就是缺 barrier 的 UB，
  在 NV 驱动上经常"看起来是对的"，换台 AMD 机器才炸。
  立即模式（Editor 预览）没有这个问题：原生 `CommandBuffer` 的状态转换由图形层自动插入
  —— 这也是为什么先写好的立即模式自检是可信的。
- **每个 Vista pass 都要 `AllowPassCulling(false)`**。URP 自带的 `DrawSkyboxPass` 无法声明
  对我们 LUT 的读取（它不认识这些资源），图里于是没有任何消费者，整个大气模块被静默剪掉。
  症状极具误导性：天空一片黑，而 Frame Debugger 里连 pass 都找不到。
- **全局纹理统一在 SkyView pass 里发布**，不是各自在产出 pass 里发布。静态表的 pass 在
  参数不变的帧里根本不存在，那些帧也必须有全局绑定；而 `SetGlobalTextureAfterPass`
  允许一个 pass 为它**读**的资源设全局，正好用上。
- **`SetGlobalTextureAfterPass` 的第一个参数是 `in` 而不是 `ref`**。反射把 `in` 显示成
  `ref`，照着反射结果加 `ref` 会 CS1615。
- **`view.Bind` 在 execute 里写全局 cbuffer，所以 SkyView pass 必须
  `AllowGlobalStateModification(true)`**。
- **太阳方向取 `visibleLights[i].localToWorldMatrix.GetColumn(2)` 而不是 `light.transform`**：
  `VisibleLight.light` 在某些剔除路径下为 null。
- **环境光/反射若设为 `AmbientMode.Skybox`，会把太阳圆盘一起卷积进去**。
  EV100=15 下圆盘约 41000 渲染单位、占球面 1.8e-5，平均贡献 ~0.74，而天空平均只有 ~0.1
  —— 环境光会偏亮约 8 倍。所以 `VistaSkySetup` **故意不动** `ambientMode`，
  只在日志里警告。漫反射那条链路已由 Task #5a 的 SH 投影（不含圆盘）接管
  （`AmbientMode.Custom` + `RenderSettings.ambientProbe`）；反射链路见 #5b。
- **`RTHandles.Alloc` 的 3D 重载必须显式给 `dimension: TextureDimension.Tex3D`**。
  少写这个参数会命中 2D 的便捷重载，静默分配出一张 Tex2D，于是 `RWTexture3D` 绑定失败，
  而**唯一的提示是 Editor.log 里一行 warning**，Console 里什么都没有。
  症状是 AP 表读回来全 0 —— 看起来像 kernel 没跑，其实是绑定就没成。
- **3D 纹理不能用 `RenderTexture.active` + `ReadPixels` 读回**：那套 API 是 2D-only，
  在 3D RT 上**静默只读到 slice 0**，不报错。自检里所有 3D 读回都走
  `Graphics.CopyTexture(src, z, 0, tmp2D, 0, 0)` 逐片拷。
  这个坑的恶劣之处在于"读到的数据看着完全合理"，只是每一片都一样。
- **往 fp16 表里塞诊断数值要先换单位**。相机海拔以 km 存是 9.766e-6，落进 fp16 的
  次正规区，读回来是 0，与"真的为 0"分不开。诊断通道统一换成 m 再写。
- 装 RendererFeature 要同步 `m_RendererFeatureMap`（URP 用其中的 `localFileId` 在引用丢失后
  重建），少写它的症状是重启 Editor 后 feature 变 `None`。`localId` 在
  `AssetDatabase.AddObjectToAsset` 之后立刻可取，不用等 `SaveAssets`。
- **`VistaEarthShadow` 的偏置方向反了，代价是全场亮度低 6~9%，而症状伪装成"求积不准"。**
  这是这个模块最贵的一个坑，完整记一下，因为它示范了"看起来像精度问题的东西其实是
  参数化问题"，以及为什么该停止推理、去加观测。
  - 症状：AP 的一次行进循环与共享积分器的 256 步参考解，在最近的切片上差 **24%**。
    远处切片全部对得上（< 1%）。
  - 三个**猜错**的方向（都很像，都不是）：① AP 的行进循环与共享积分器不等价 ——
    实测差 0.30%，等价；② `c = dot(oc,oc) − Rb²` 在量级 4e7 上算，fp32 那里的间隔是 4，
    所以 c 的符号是噪声、`VistaRaySphereIntersectNearest` 返回一个 ~0.01 km 的伪根
    把 `tMax` 钳掉 —— 实测 `tBottom = −1`，没有伪根；③ `(S − S·T)/σ` 在极小 dt 下
    灾难性抵消 —— 量级上界约 1%，远不够。
  - 真正解决问题的一步不是继续推理，而是注意到**参考解自己就不是线性的**：
    `3.553/0.020 = 177.6` 而 `7.234/0.0333 = 217`。在 20~33 m 的路径上
    `VistaEvaluateScatterSample` 只随 r、muSun 变化，实质是常数，被积函数**必须**线性。
    这把问题从"哪个积分器错了"改写成"为什么被积函数不是常数"，于是加了三个观测：
    区间（`tBottom` / `tTop` / 海拔 / `up·ray`）、`earthShadow` 在阶跃两侧的取值、
    以及线性探针 `ref(0.04)/ref(0.02)`。
  - 读数直接指出原因：相机海拔 **9.766 m**、`up·ray 0.01802`、
    **`earthShadow @3 m = 0` 而 `@1 km = 1`**、线性比值 2.553。
  - 根因：`VistaEarthShadow` 把遮挡球的球心沿 `+up` 推了 10 m，即**朝采样点推近**，
    于是 `c = (r − offset)² − Rb² < 0` —— 海拔不足 10 m 的采样点一律被判成
    "在星球内部"，直射项被整体归零。UE 的 SkyAtmosphere 就是这么写的，在那里不显眼
    （近处切片本就贡献极小），但本项目的自检把它照出来了：相机在 9.766 m、
    中心视线每 km 爬 18 m，阶跃落在 `t = (10 − 9.766)/18.02 = 0.0130 km`，
    而 AP 循环的第 0 片节点正好在 `t = 0.003` 和 `t = 0.013`。
    **两套求积在阶跃两侧取节点，差出几十个百分点是必然的 —— 两个积分器都没错。**
  - 修法：球心改推 `−up`，即**推远**，于是 `c = (r + offset)² − Rb² > 0` 恒成立，
    判定退化成物理上正确的"太阳是否在当地地平线以下"。10 m 的容差同时还兜住 fp32：
    r 在 6360 km 上的 ulp 是 0.49 m，海拔算出负值也不会误判
    （顺便解释了为什么实测海拔读到 9.766 而不是 10 —— 差 20 个 ulp）。
  - 修完每一项预测都对上了：`earthShadow @3 m` 0→1、线性比值 2.553→2.022、
    Log errCenter 23.99%→1.19%、Log errMid 19.96%→1.13%、Power errCenter 5.89%→0.30%。
    天空基线整体上移（正午地平线 11316→12053，日落峰值 49728→54304），
    而**天顶值一个都没动** —— 天顶视线不经过贴地那 10 m，正是该有的签名。
- **共享积分器的均匀分支只覆盖了 `tMax·(N−0.7)/N` 的路径**。
  `tNew = tMax·(i + 0.3)/N` 配 `dt = tNew − t`，最后一段止于 `tMax·(N−0.7)/N`。
  N=256 时短 0.27%（无所谓），N=2 时只覆盖 65%（严重）。
  目前所有走这条分支的调用都用高步数，所以没有实际影响，
  但**低步数分级（Task #7 的移动端档）不能直接复用这个分支**，需要改成
  末段补齐或换成 variableSampleCount 那条（它的 t1 有 `> 1.0 → tMax` 的兜底）。
- **`SphericalHarmonicsL2` 的归一化约定文档没写全，必须实测。**
  两个未知：这个类型存的是"辐射亮度 SH"还是"已与余弦瓣卷积过的辐照度 SH"
  （相差逐阶的 `Â_l` = π、2π/3、π/4），以及基函数常数 `Ŷ_i`（`Y00 = 0.2820948` 等）
  有没有折进去。猜错的症状是环境光整体亮/暗约 3 倍 —— **在任何单一场景里都像
  "美术没调好"**，不会被当成 bug。
  - 第一次尝试是从 `AddDirectionalLight` 的打印结果反解，错了：那样有两个未知量、
    一个方程，而且我当时以为 +y 方向光的 L2 项为 0（并不是）。
    **正确做法是停止推理、去构造观测**：一次只把一个系数置 1，在一个 9 个基全非零的
    通用方向上求值，权重就直接量出来了 —— 完全不需要知道 Unity 内部折了哪些常数。
  - 实测：`k_i` 恰好等于 `1/Ŷ_i`，即 `Evaluate` 用的是**未归一化多项式基**
    `{1, y, z, x, xy, yz, 3z²−1, xz, x²−y²}`（就是 `ShadeSH9` / `unity_SHAr` 那套形态）；
    且 `AddAmbientLight(white)` 后 `Evaluate` 各向恒为 1，说明它返回的是
    **albedo=1 的出射亮度**（辐照度/π）。
  - 于是写入公式是 `c_i = (Â_l/π)·Ŷ_i·L_i`。这三项实测都写成了断言而不是打印：
    Unity 哪天改约定必须在自检里炸，而不是等到看图。
- **RenderGraph 里没有 `SetGlobalBufferAfterPass`。** 纹理有 `SetGlobalTextureAfterPass`，
  buffer 没有对应物（`Runtime/RenderGraph` 整个目录里 "SetGlobalBuffer" 零命中，
  它只存在于 CommandBuffer 包装层：`ComputeCommandBuffer` 上四个重载）。
  所以 buffer 全局只能在 render func 里手动绑，且该 pass 必须
  `AllowGlobalStateModification(true)` 才能过 `ThrowIfGlobalStateNotAllowed()`。
  `BufferHandle → GraphicsBuffer` 的隐式转换要查 `RenderGraphResourceRegistry.current`，
  只有在 execute 阶段才有效 —— 正好也只能在那里绑。
- **compute pass 里绑 `RWStructuredBuffer` 用 `UseBuffer`，不是 `UseBufferRandomAccess`。**
  读实现才分得清：`UseBuffer` 就是 `UseResource(handle, flags)`；
  `UseBufferRandomAccess` 在此之上多调一次 `SetRandomWriteResourceRaw(h, index, ...)`，
  那是给"**光栅** pass 里用 u# 寄存器写 UAV"准备的（对应旧的
  `Graphics.SetRandomWriteTarget`）。compute kernel 是按名字绑的，多绑一次随机写目标
  只会白占一个 UAV 槽。两个都能跑，所以只能靠读源码区分。
- **`RequestAsyncReadback` 不在 `ComputeCommandBuffer` 上**，只声明在
  `IUnsafeCommandBuffer`（四个重载）。想在 compute pass 里发读回是走不通的 ——
  这正好推向"读回在记录期用 `AsyncGPUReadback.Request` 发"那条更简单的路（见「取舍」）。
- **均值恒等式只在两侧用*同一个方向集*时才精确成立。**
  第一版参考解用 4096 样本、投影用 1024，恒等式在日落红通道差 **2.75%** ——
  那不是 bug，是 1024 与 4096 在"地平线几度宽的高对比亮带"上的求积差异。
  一个只在 N→∞ 才成立的等式没法卡 1e-3 的阈值。
  修法是给参考核加**第二个**均值组，用与投影完全相同的 1024 方向集：
  于是两边逐项对应、只差归约顺序，实测降到 1e-7。
  被丢掉的那 2.75% 反而成了有用的东西 —— 它就是采样数的收敛性度量（见「取舍」末条）。
  参考核的输出布局做成**自描述**的（法线也一并写出、均值组的法线写零向量做标记），
  C# 侧就不必镜像一份法线定义；镜像的那份迟早与 shader 走歧，届时自检会拿错法线去比，
  报出的偏差既不是 0 也不是明显错误 —— 最难查的那种失败。
- **Dispose 顺序：先清读回、再放 buffer。** `VistaSkyAmbientProbe.Dispose` 里会
  `WaitForCompletion`，反过来就是让在飞的读回请求从已释放的显存里搬数据。
- **白天环境光的 `c_1`（y 的系数）是**负**的，这不是符号错。** 第一眼一定会怀疑
  ——"天上亮、地上暗，y 矩怎么会是负的"。实测 Demo 场景 `c_1 = (−3359, −2331, −417)`，
  而按法线重建出来是 上 (1444, 2721, 5400) / 下 (8163, 7383, 6234)：
  **朝下确实比朝上亮 5.7 倍（红通道）**。原因是天顶恰好是全天最暗的地方
  （正午 R 仅 ~943 cd/m²），而天底吃的是被太阳直射的地面反弹
  （≈ `albedo·E☉·sin(elev)·T/π`，正午在数千量级）。
  通道比例同时印证：`|R| > |G| >> |B|` —— 蓝天（上）与暖地面（下）在 y 矩上几乎抵消，
  所以蓝通道的 y 矩只剩 −417。把这三向的重建值固定打进诊断里，
  就是为了不必每次重新推一遍。
- **逐帧写 `RenderSettings.ambientProbe` / `ambientMode` 不会把场景标脏**（实测 Demo
  场景连续渲染后 `isDirty` 仍为 `False`）。所以不需要给导出加"仅在值变化时写"的门控。
  这条是写之前担心过、实测证伪的 —— 记下来免得以后又去加没用的门控。

- **Unity 不允许把 Cube RT 绑到 compute 的 `RWTexture2DArray`。** 报错：
  `Property (_VistaSkyReflectionRW) at kernel index (0) has mismatching output texture
  dimension (expected 5, got 4)`（5 = Tex2DArray = HLSL 声明，4 = Cube = 绑上来的 RT）。
  硬件层面 cube 的 UAV view **就是** 2D array view，所以这纯粹是引擎侧的校验，
  不是能力限制。落地方案：compute 写一张 6 层 × 7 级 mip 的 `Tex2DArray` 中转纹理，
  dispatch 完逐面 `CopyTexture` 搬进 cube，代价 6×64²×7级 fp16 ≈ **0.4 MB**。
  另一条路是照 HDRP `IBLFilterGGX` 改成光栅（`SetRenderAttachment` 带
  `mipLevel`/`depthSlice`），那要重写 GGX 积分核；中转纹理让 GGX 积分、
  mip↔粗糙度反函数、逐面方向约定这三处最贵最容易错的东西一个字都不用动。
  顺带**加强**了自检：判据 ① 现在同时验证 `element → CubemapFace` 的映射，
  搬错面的症状是那一面单独炸红，而不是"六面都对但整体转了 90°"这种要看图才发现的错。
  - 先前我写下过一条"core / URP 里没有 compute 写 cubemap 的先例"，
    当时被自己纠正为"分配有先例、绑定没有" —— 这次的报错正好证实了那个拆分：
    `Runtime/PathTracing/Environment/CubemapRender.cs:152-160` 确实
    `dimension = Cube` + `enableRandomWrite = true`，但它自己是用
    `SetRenderTarget(new RenderTargetIdentifier(tex, 0, (CubemapFace)i))` 逐面**光栅**写的，
    从没把它当 compute UAV 绑过。**`enableRandomWrite` 分配得下来 ≠ 绑得上去。**
- **`CopyTexture` 只存在于 `IUnsafeCommandBuffer`**（`IUnsafeCommandBuffer.cs:430-464`），
  `ComputeCommandBuffer` / `RasterCommandBuffer` 上都没有。所以 RenderGraph 侧的
  拷贝必须是 `AddUnsafePass` + `CommandBufferHelpers.GetNativeCommandBuffer(ctx.cmd)`。
  这与 `RequestAsyncReadback`、`SetGlobalBuffer` 是同一类不对称 —— 判断一个 API
  在 RenderGraph 里能不能用，看的是它挂在哪个 command buffer 接口上，不是它在
  原生 `CommandBuffer` 上存不存在。
  - 要用的重载是 `CopyTexture(src, srcElement, dst, dstElement)`（:440），
    它一次搬一个 element 的**全部 mip** —— 所以是 **6 次调用，不是 42 次**。
  - 立即模式（Editor 自检 / 预览）不需要拆：原生 `CommandBuffer` 的状态转换由图形层
    自动插，dispatch 与 copy 录在同一条 cmd 里是安全的。所以一个
    `CopySkyReflectionToCube(CommandBuffer)` 同时服务两条路径。
- **`RTHandles.Alloc` 的 `slices` 对 Cube 必须是 1，对 Tex2DArray 才是层数。**
  `RTHandleSystem.cs:881` 把 `slices` 直接转成 `RenderTextureDescriptor.volumeDepth`；
  cube 的六面来自 `dimension`，再给 `slices: 6` 就是要一个 6×6 面的东西。
  这次一张 cube（`slices: 1`）+ 一张 array（`slices: 6`）并排放着，正好是对照。
- **FXC：`GroupMemoryBarrierWithGroupSync` 不能位于任何线程相关 `return` 的下游 ——
  即使那个条件事实上是组内常量。** 自检核最自然的写法是
  `if (group == MEAN_GROUP) { ...; return; }` 三段并列，FXC 直接拒绝：
  `thread sync operation must be in non-varying flow control`。`SV_GroupID` 在组内明明
  是常量，但 FXC 的判据是**语法上**的。而且这不算保守过度：DXIL 层面 barrier 要求
  整组到齐，提前退出的线程永远到不了。
  修法是各段判据顺序排开、barrier 一律留在顶层无条件流里，让**九个组**全部走完全部
  barrier —— 出结果的组由 `if (group == ...) && tid == 0u` 决定**写不写**，
  而不是决定**走不走**。代价是每组多跑两趟归约，自检核不值一提。
  （备选是拆三个 kernel，但那样 C# 侧要维护三次 dispatch 与三套绑定，
  而这三条判据是互相印证的，拆开反而弱化了它。）
  这条规则在 wide 滤波核上有个反过来的推论：**它的 dispatch 必须精确覆盖纹素数**，
  所以核内一个越界检查都不能有 —— 有 `if (id >= count) return;` 的话下游的归约 barrier
  就非法了。也就是说"派发规模刚好等于工作量"在这里不是省一点开销的优化，
  而是 barrier 合法性的前提。反过来，barrier 出现在**字面常量**上界的循环里是合法的，
  出现在线程相关的 `if`（不带 `return`）之后也是合法的 —— FXC 只卡 `return`。
  另外两个核的线程数用 `#error` 绑在一起：改了 wide 核却没改自检，
  判据 ④a 会退化成"用 64 段去验 32 段"，仍然全绿，但验的不是同一件事。
- **HLSL 的 `out` 数组 copy-out 语义会打乱 FXC 在 `if` 里的初始化跟踪。**
  共享积分函数第一版用 `out float3 sum[N]`，在条件分支里赋值后 FXC 报
  "use of potentially uninitialized variable"。换成返回一个 `struct` 就干净了。
  （`SkyReflection.compute:157` 那条同名警告是**另一回事**、且是刻意留的：
  `VistaSkyReflectionRadiance` 的两个来源分支 FXC 判不出必有其一，两个滤波核各报一次；
  自检核不报，因为它的来源是字面常量。）
- **`#pragma only_renderers` 没有 `d3d12` 这个 token。** D3D12 走的就是 `d3d11` 的
  编译目标，写上只换来一条 `Unrecognized renderer` 警告。三个文件都清掉了
  （`SkyReflection.compute`、`AtmosphereLut.compute:23`、`VistaSky.shader:40`）。
- **自检输出缓冲的「行基址」不要用组号做算术。** 第一版写的是 `MEAN_GROUP + i` /
  `MIP_GROUP + 3 + m`，看着简洁，实际在第 9 行留了个洞，而 C# 侧按"紧凑排列"去读
  就整段错位。**行基址与组号是两件不同的事，别复用同一个数** ——
  现在 HLSL 与 C# 两侧各有一份显式的 `ROW_*` / `k_ReflVerifyRow*` 常量。
- **`unity_SpecCube0_HDR` 的残余风险已实测排除：`(1, 1, 0, 0)`**，
  也就是 `DecodeHDREnvironment` 是恒等 —— float Cube RenderTexture 经
  `customReflectionTexture` 挂上去时引擎不会塞一个解码系数。
  这条原本列为"待 Task #6 用一个 roughness-0 的球对着背后天空验"的开放风险
  （症状是"反射整体亮度差一个常数倍"），现在由 `Log Sky Reflection State` 直接读出
  全局量解决了 —— 比看图判断可靠，也省掉了 Task #6 的一项。
- **`filterMode` 给中转纹理留 `Trilinear` 而不是 `Point`。** 它不会被采样，
  滤波模式在功能上无关紧要；但它是 RenderDoc / Frame Debugger 里排查反射问题的第一站，
  预览图跟着 cube 的滤波模式走比较不容易看错。
- **cube 上取掉了 `enableRandomWrite`。** 它现在只是 `CopyTexture` 的目标 + SRV，
  开着也能跑。取掉是为了不让"这张图是 compute 直接写的"这个**已经被验伪**的假设
  留在代码里 —— 下一个人（包括三个月后的我）会照它去 debug。
- **在 `file:` 引用的本地 package 里新建文件，Unity 不会自动发现。**
  `refresh_unity(mode: if_dirty)` 返回 `refresh_triggered: false` 就过去了，
  编译看着成功、控制台干净，但新的 `[MenuItem]` 根本没注册（菜单执行报
  "might be invalid, disabled, or context-dependent"）。要 `mode: force` + `scope: all`。
  这个失败形态很坑，因为它**看起来像代码写错了**而不是像资产没导入。
- **`GraphicsFence` 在 Edit 模式 + D3D11 下不可用，而能力位说它可用。**
  `SystemInfo.supportsGraphicsFence` 返回 `True`，但 `fence.passed` 永远不翻 ——
  CPU 侧轮询 fence 要靠 Editor 推动提交，自旋把主线程占满就互相锁死。
  计时器因此改成开跑前**探测**（≤250 ms），探测不过就退回 `GraphicsBuffer.GetData()`
  的硬同步，并把这件事印在报告头上。教训是一句可以直接复用的话：
  **能力查询 ≠ 该原语在当前线程模型下可用。**
- **`ProfilerRecorder` 抓 `ProfilerMarker` 必须给 `SumAllSamplesInFrame`，
  而"逐样本"这个默认口径根本不支持。** 不给的话 `StartNew` 直接抛
  `NotSupportedException: ... only with SumAllSamplesInFrame or CollectOnlyOnCurrentThread`。
  另一个选项在这里没用：RenderGraph 的 pass marker 记在**渲染线程**上，
  从主线程按"当前线程"收集会是空的。于是口径被 API 强制成"一个样本 = 一帧的总和"——
  这不是我选的，是唯一能跑的那条。附带后果是多相机会被求和（见「取舍」）。
  两个次级坑：① 我凭印象写的 `WrapAroundBuffer` 不存在，真名是
  `WrapAroundWhenCapacityReached`（枚举成员表用 `unity_reflect` 查出来的，
  不查就是三处 CS0117）；② 第一版没 fail-fast，异常在 `s_Started = true` 之前抛，
  于是每个 Editor tick 重试一次，控制台被同一条填满，真正有用的信息
  （第一条的类型与位置）被埋在几十条重复里 —— 起 recorder 失败是**配置错**，
  重试不会好，必须立刻收摊并退出 play。
- **`ProfilerRecorderSample` 除了 `Value` 还有 `Count`。** 我原来为了知道"这一帧同名
  marker 出现了几次"，打算再起一组不带 `SumAllSamplesInFrame` 的 recorder 去做
  sum/single 的比值 —— 而 `Count` 就是被求和的样本个数，一个字段解决的事不该用两倍
  采样开销去换。查一遍结构体成员比设计一个绕路的机制便宜得多。
- **"期望缺席"被判成失败，比漏判更糟。** 第一版报告对七个 pass 一视同仁，
  两张静态表在 300 帧里 0 样本被打成"pass 名走歧 / pass 被剪"。工具对着正确行为报警，
  代价是人学会忽略它的警告；判据的价值全在"红了一定有事"。修法见「取舍」里
  静态表那条：方向掉过来判，缺席=通过，有样本=报警。
- **"改了一条没有任何判据覆盖的路径，而自检仍然全绿"——最贵的一种假通过。**
  mip1~6 从 narrow 核换到 wide 核之后，自检四条判据（当时是三条）全绿、
  而且数字**逐位不变**。原因是判据 ① 与 ② 都采 `LOD 0.0`，也就是全落在 narrow 核上；
  我当时以为"判据 ② 会立刻抓住归约写错"，错了 —— 整球均值也只采 LOD 0。
  补的两条刻意分工：④a 纯算术（同方向同 K，分摊切法 vs 顺序算完，不碰纹理，
  红了锅一定在求和）、④b 纯布线（wide 产出的 mip2 走硬件采样 round-trip，
  覆盖 groupId → (纹素, 面) 映射、UAV 写入、逐面 `CopyTexture`）。
  一条判据只该有一个失败方向，否则红了还要再查一轮"是算错还是写错"。
- **判定文本不能硬编在证据行里 —— 修法生效之后它会继续断言旧病因。**
  计时器的证据 2 原先无条件打印"一次纹理取样的延迟，完全没被隐藏"。归因阶段这句是对的
  （295 ns/迭代），wide 核落地之后是 32 ns/迭代，这句就成了**假断言**，而报告照旧绿着。
  三条证据全部改成从实测值算三分判定，并留一条显式的**"这一条不做判定"**带 ——
  没有那条中间带，阈值附近的抖动会被强行归到某一侧，比不判定更糟。
  同一批还清掉了一个更早的同类问题：预先注册分类（"若 A 主导则修 X，若 B 主导则修 Y"）
  然后把它当结论打印，等于让工具去确认作者的先验。
- **`read_console` 的 `count: 1` 返回的是**最旧**的匹配项，不是最新的。**
  只要一条报告的时候会拿到上一次运行的结果，而两次报告长得几乎一样，
  非常容易当成"改动没生效"。规矩：一律多要几条，并核对匹配数量。
- **收敛阶只分"光滑 vs 台阶"两态，会把每一次普通的过滤纹理采样判成缺陷。**
  banding 判据最初的设计是两态：p≈2 = 光滑（通过）、p≈0 = 台阶（失败）。
  漏掉的是中间那一档 —— 双线性重建在**纹素边界**上斜率不连续，跨界的三点二阶差分
  ≈ |Δf'|·Δ，阶恰好是 1。这是硬件双线性的固有行为，任何过滤纹理都有；
  两态判据会把它归到"台阶"那一侧。实测最坏方向的阶是 0.88，正落在这一档，
  两态版会把一个不需要修的东西报成失败。三态之后它的处置是**外推**（幅度 ∝Δ，
  按生产步长缩小后再比可见阈），而不是修。
- **两点算不出"平台"。** 判据 C 第一版只跑两个步长（0.005° / 0.0025°），
  得到 1.241e-3 / 8.577e-4、阶 0.53 —— 卡在 0 和 1 正中间，两种解释都说得通，
  下不了结论。原因是二阶差分里同时有 ∝Δ⁰（噪声）和 ∝Δ²（函数曲率）两项，
  两点只能读出**混合物的表观指数**。改成四档逐级减半后
  （1.241e-3 → 8.577e-4 → 1.068e-3 → 1.067e-3，末两档阶 0.00）平台自己出来了。
  教训：要判"某一项是否已经消失"，就得缩到它消失、看剩下的那个值停住，
  而不是拿两点去拟合一个自己假设的模型。
- **粗档抽稀会把极值整个跨过去，表现为"幅度反而变小"。** 判据 A 的 Δ=2° 档
  幅度 6.057e-2 < Δ=1° 的 6.573e-2 —— 粗档只有 46 个采样点，最坏那个太阳角
  根本没被采到。于是三点不成幂律。处置不是换数据，而是**说出来**：
  阶只取 1°→0.5°，2° 档降级为旁证，报告打 ⚠ 注明"三点幂律不成立不代表信号异常"。
  若不注明，下一个读报告的人（包括三个月后的我）会以为信号有问题。
- **步段解析积分照公式直写，在短步段上会灾难性相消 —— 而"短步段"是 AP 的常态。**
  `∫₀^dt S·e^{-σt} dt = S·(1-e^{-σ·dt})/σ`。σ·dt 掉到 1e-7 量级时，
  `exp(-σ·dt)` 与 1.0 之间只剩一两个 fp32 台阶，相减出来的几乎全是舍入噪声。
  地表 σ ≈ 1e-2 /km，步长短于 ~1 cm 就会踩到 —— 而 AP 首片天生只有几米长，
  Step 1 把 `tMax` 设成近处几何深度时（1 m 外的墙）也正是这个量级。
  #7 的 256 步参考解跑 1 m 路径时 σ·dt ≈ 4e-8，实测把 `errCenter` 抬到 **126%**。
  当时差点被判成"切片布得太近导致行进不准" —— 其实是**尺子自己坏了**。
  修法：σ·dt < 1e-4 时改用 `1 - x/2 + x²/6` 的截断展开（误差 < 4e-14，且整个
  不做除法），逐通道 `step` 选择（RGB 三向 σ 差 6 倍，同一步长可能一个通道相消
  一个不相消）。
- **同类的第二个"尺子偏置"：均匀步长分支把段长写成"相邻取样点之差"。**
  取样点在段内 30% 处（`VISTA_SAMPLE_SEGMENT_T`），段边界应当是 `[i·dt, (i+1)·dt]`。
  写成相邻取样点之差后，第一段只剩 0.3·dt、末段整个丢掉，总覆盖变成
  `tMax·(N−0.7)/N` —— N=256 时少积 0.27%。症状是 `errCenter` 在**全部 20 组**
  配置上都读到 0.25~0.29%，一个几乎不随配置变化的常数偏置，
  把切片布局本该显出的差别整个盖住。
  两次都是同一个反面模式：**把参考解自身的偏置当成被测对象的缺陷**。
  规矩：一个误差指标在所有配置上都给出同一个非零值时，先怀疑参考解。
- **AP LUT 的 pass 漏了 `view.Bind()`，只在换视角时才显形。**
  核读的是全局 view 常量，而上一个 pass 恰好绑的是同一组值，所以单视角自检永远绿。
  #7 加了第二个视角（海拔 1 km / 太阳 10°）之后立刻暴露：AP 用的是**第一个视角**的
  参数。这类 bug 的可发现性完全取决于自检有没有第二个状态 —— 单状态测不出
  "状态没被写进去"。
- **判定行的口径里藏着一条没有依据的偏好。** `ValidateApSliceBudget` 原先打印
  `定档 → d=16 Log 20m`，取的是"通过的候选里最省的那个"。但那套判据
  （1% Weber 可见阈）只能判**合格**；20 组全过时，"取最省的"就是一条藏在代码里的
  偏好，而当时并没有成本数字支撑它 —— 补测之后恰恰相反（省 0.012 ms 换掉 4 倍余量）。
  改成 `最省候选 →`（它回答的是"合格线能压到多低"），并另起一行判**出厂档**。
  同一批修掉的伪通过：判定原先挂在"扫描里有没有配置能过"上，那永远会绿
  （总有一档够精细），改坏默认值也照样绿。现在出厂档在扫描行里标 ★、单独判
  "合格 + 含余量"，且**不在网格里就直接失败**（改了默认值不同步网格会响）。
  具体毫秒数**不在自检代码里抄一份**：抄了就会在核变化后静默过期，
  自检只留口径与理由，数字留在成本侧的报告里。

- **URP 完全不消费 `Light.lightUnit`，物理灯单位是 HDRP 独有的。** URP 包里搜不到任何
  `LightUnit` 引用，所以 lux → `color × intensity` 的那一步只能由我们自己乘。
  这条决定了整个接缝的存在必要性；先假设"填 lux 就行"会得到一个暗 π 倍的画面。
- **URP 无条件打开 `lightsUseColorTemperature`，代价是 85.22% 的光色偏移。**
  `UniversalRenderPipeline.cs:449-450` 里 `lightsUseLinearIntensity` 还看色彩空间，
  但 `lightsUseColorTemperature = true` 是**无条件**的。于是引擎算
  `VisibleLight.finalColor` 时会在我们已经算好的物理色度上再乘一遍
  `CorrelatedColorTemperatureToRGB(colorTemperature)`。反例实测：手动开 3000K，
  端到端像素偏离 **85.22%** —— 而面板上完全看不出是这个开关干的。
  所以 `ApplyLightParams` 每次写灯都强制 `useColorTemperature = false`。
- **`Light.color` 按 Gamma 语义存，写线性值进去会暗 89.04%。** 引擎在
  `lightsUseLinearIntensity`（URP 在线性色彩空间下会打开）时算的是
  `finalColor = Light.color.linear × intensity`。我们手里的 `lp.color` 已经是线性色度，
  所以写进去之前必须先 `.gamma`，否则被 `.linear` 多转一次。反例实测偏离 **89.04%**。
  这两条（色温 + gamma）都是**先写了保护性代码、再各配一条反例测量**才敢留的：
  一行防御若在反例下也通过，说明它没有实质后果，应当删掉。
- **C# 的 `Color.gamma` 与引擎的 `.linear` 是精确互逆，不是 pow(2.2) 近似。**
  这一条是我**预先登记为风险**然后被实测推翻的：担心 C# 用分段 sRGB 而引擎用
  幂近似，往返会留下几个百分点的残差。实测往返误差 0.03%，两边用的是同一条分段曲线。
  把被推翻的假设也记下来，因为下一个读代码的人会有同样的担心。
- **逐通道判据必须是"同通道的相对 AND 绝对"，跨通道 OR 是一个假通过的洞。**
  低太阳时三通道的 T 差两三个数量级（0.5° 实测 0.168 / 0.022 / 0.0003）。
  第一版判据写成"最大相对误差 或 最大绝对误差"，于是出现**红通道的绝对量小去替蓝通道的
  相对量大担保** —— 两个数根本不来自同一个通道。这个坑在透射率对账那条自检里踩过一次，
  现在判据收进 `IsTransmittanceDifferenceVisible`，逐通道循环、同通道 AND。
- **尺子的条件数：同一个物理量，"求极值"和"求根"差三个数量级。**
  子午线对称性判据（D）要先定位太阳时正午。第一版用"最大化高度角"去找 ——
  那是个**平坦的二次极值**：float 的高度角分辨率 7.6e-6°、正午附近曲率 k ≈ 3.8 °/h²，
  代入 `Δt = sqrt(eps/k)` 只有约 5 s 精度；而方位角在正午附近以 ~38°/h 变化，
  5 s 的轴偏移直接变成 0.05° 的残差 —— 与判据阈值同量级。改成对时角 H = 0 二分：
  H 是单调的、15°/h，一次二分就到 float 底。**归因验证**：故意保留峰值搜索版本当尺子，
  它给出的轴偏差 8.6 s，按 15°/h 换算应引入 0.1808° 残差，实测残差 0.1811° —— 对上了。
  这是本项目第三次踩「把尺子自己的偏置当成被测对象的缺陷」，所以这次先量尺子。
- **"参考海拔差几百米应该没事"是错的，而且错在最关键的时段。**
  这是动手前的直觉，被 F2 那张表推翻：正午 271 m 尚可，太阳 5° 时只剩 51 m、1° 时 41 m。
  也就是说单值参考海拔**恰好在日出日落失效**，而那正是这套大气最想展示的时段。
  没有这条测量，Task #12 会被排在很后面甚至不做。
- **每条报警都要双向验：既要有让它亮的场景，也要有让它灭的场景。**
  只验亮会漏掉误报（在正常场景里常亮，用户学会忽略所有报警 —— 那时它就不再是判据了），
  只验灭会漏掉漏报（永不触发的 HelpBox 等于一行注释）。
  `Window/Vista/Validate TimeOfDay Diagnostics` 四组：A `sunMissing`、
  B `atmosphereMissing`、C 灯值一致性、D `useColorTemperature`。
  C 组的三步是"写灯后一致 → 只污染 intensity → 只污染 color"，
  两个通道**分开**污染是为了验出 AND 没被写成"一项通过就整体通过"；
  实测两次都在"抓到不一致"的同时确认另一项仍判一致。
- **覆盖不到的路径要说是覆盖不到，不能让它读起来像通过了一条强判据。**
  上面四组里有两组给不出反例：B 组的 `VistaAtmosphereFeature.current` 是 feature 在
  `Create()` / `Dispose()` 里设的静态量，Editor 侧没有合法途径把它翻成 null 再翻回来；
  A 组的正向格更是**物理上不可达**（下一条）。两组的标题和报告行都明写了这一点。
  这是 #7 记下的「未覆盖路径的假通过」的反面 —— 那次是自检全绿而一整条路径没有判据。
- **`RenderSettings.sun` 的 getter 有回落语义：字段为空时它返回场景里最亮的平行光。**
  A 组本来打算"清掉 `RenderSettings.sun` → `sunMissing` 应为 true"，稳定失败。
  第一反应是竞态 —— 场景那盏灯上也挂着 `VistaTimeOfDay` 且开着
  `AssignRenderSettingsSun`，而 `new GameObject` / `AddComponent` 会触发编辑器同步重绘、
  跑一遍 `ExecuteAlways` 的 `Update()`，把自己写回这个全局。于是把清空挪到 `Apply` 的
  **前一行**，中间不留任何会触发重绘的调用 —— **仍然失败**。中间只剩一次 `Apply`，
  不可能有别人插进来，所以竞态假设被自己的修法排除了。
  真正的原因是 getter 本身：写 null 后**立刻**读回，拿到的仍是场景那盏灯
  （procedural skybox 就是这样取太阳的）。产品结论：`ResolveSun` 的第三级在任何有平行光
  的场景里恒非空，`sunMissing == true` 只在"一盏平行光都没有"时出现 —— 而那也正是它该
  报警的时候。所以 A 组改成把 getter 的回落**测成观测值**，作为"正向格为什么不可达"的
  依据，而不是留一句断言；也没有为凑一格通过去硬造条件（禁用场景里的灯、开临时场景），
  那些手段本身就是风险源。
  两次失败都是靠**先加归因行再理论**定位的：第一版报告只说"期望 true 实得 false"，
  下一步只能靠猜；加上"测量后 `RenderSettings.sun` = ? / 自身 Light = ? / m_Sun = ?"
  之后，一次运行就把原因缩到一个具体对象上。
- **诊断旗子的时序错误比功能 bug 更坏。** `m_AtmosphereMissing` 原先算在
  `sun == null` 的提前返回**之后**，于是"既没灯又没 feature"时它会停在上一帧的值，
  面板照着过期状态报警 —— **它让人去查一个不存在的问题**。改成在任何提前返回之前算。
  这条修法给不出反例测量（理由同 B 组），所以自检里明写它只是一致性核对；
  能挡住的唯一失败模式是"走过无灯路径后旗子仍与 `current` 一致"。
- **fp16 的相对精度是尺度无关的，所以接缝自检不需要绝对可见性豁免，而透射率自检需要。**
  fp16 相对精度 2^-11 = 4.88e-4 不随数值大小变（实测地板 5.047e-4）。
  接缝测的是一个 O(1) 的比值，量级固定，纯相对判据就够；透射率在低太阳角下 T → 0，
  分母自己都快没了，纯相对判据会在那里永远判"不一致"，所以必须配一条绝对门 ——
  且必须是**同通道**的绝对门（见前面那条跨通道 OR 的坑）。
  同一个 Weber 1% 阈在两条自检里用法不同，不是不一致，是量级条件不同。

- **往 shader 里声明为 `Color` 的属性写值，`SetVector` 和 `SetColor` 一样会做 gamma → linear。**
  我在 AP 数值验收里写了注释断言反面（"`SetColor` 会转换，`SetVector` 原样写入"），
  第一次运行就被打脸：写 0.18 渲出来是 **0.0272 = `sRGBToLinear(0.18)`**。
  URP `Unlit.shader` 的 `_BaseColor` 带 `[MainColor]`、类型是 Color，转换发生在属性层，
  与用哪个 setter 无关。修法是显式写 `new Color(v,v,v,1f).gamma`。
  值得记的是**为什么能被发现**：黑和白是这个变换的两个不动点，
  只有中灰会暴露它 —— 而中灰那一列本来是为"独立预测靶"加的，抓到这个坑是白赚的。
  两列黑白的方案会一路全绿地把错误带下去。
- **一个错的注释可以造出一个"通过一切"的地板。** 上一条那个 0.849 的中灰偏差被喂进
  `measuredRel`，算出 `floor = 1.698E+000` —— 比后面每一个被判量都大，
  于是判据③④⑤⑥**全部空过**，报告还是绿的。
  修法是让 `measuredRel` 只由白和黑（sRGB 的两个不动点）算出，中灰那一列改成
  `greyUsable ∈ (0.02, 0.98)` 的合理性**报告**而不是断言 —— 它是被预测的量，
  给一条产出数据设通过门会让判据自我实现。修完地板落到 **9.760E-004**。
  这是「把尺子自己的偏置当成被测对象的缺陷」的镜像：这次是**尺子自己的偏置把被测对象洗白了**。
- **上游判据 `return false` 会把唯一能抓某个故障的下游判据挡在门外。**（新的反模式）
  "信号非空"那条原先写 `20 × k_RelTol = 0.2`（一个发明出来的倍数）然后 `return false`。
  注入"两趟混合顺序调换"时远端散射从 0.2375 掉到 0.0756，于是这条把
  **"有信号但被算错"误判成"没有信号"**并提前返回，而判据④存在的**唯一理由**就是抓这个故障。
  两处都改了：门槛回到全项目那把尺子（Weber 1%），提前返回换成一个 `vacuous` 旗子 +
  一行横幅 —— 信号太弱确实会让下游的**通过**没有意义，但下游的**失败**仍然是真信息，
  所以判据照跑完，只声明其中的 "OK" 不可采信。
- **URP 的 Unlit 也调 `MixFog`。** 布景想要一个"已知入射值"，就必须在测量期间强制
  `RenderSettings.fog = false` 并在 `finally` 里还原 —— 否则场景里随手开的雾会直接
  记进 T 的账上，而 Unlit 材质看起来"不该受雾影响"。
- **探针自带一盏平行光、挂在探针专属 layer 上，就不必去动全局 `RenderSettings.sun`。**
  `GetSunDirection` 优先取 URP 的主平行光；探针相机的 `cullingMask` 只留自己那一层，
  场景那盏灯于是被剔掉，探针这盏自动成为主光。
  这比"临时改全局再还原"稳得多 —— 上面那条 `RenderSettings.sun` getter 回落语义
  已经证明了那条路会失败。layer 是运行时扫全场 `Renderer` / `Light` 的占用位、
  从 31 往下找空位拿到的，不写死。
- **`ViewportPointToRay` 的 origin 在近裁剪面上，不在相机位置上。**
  按半径摆四边形时用它会在近端引入系统误差：近裁 0.1 m、目标距离 5 m 时是 2%，
  刚好落在 Weber 1% 阈的两倍上，看起来像"近端合成有偏差"。
  改成 `ViewportToWorldPoint(z=1)` 减相机位置再归一化。
- **前两个自检的隔离手法在这里都不能复用。** `VistaLightSeamSelfTest` 用 0.09 单位的
  薄近远裁剪面夹出一片平板，装不下 5 m → 45 km 的视锥；把布景挪到 `y = 1e5` 会让探针
  跑到 100 km 海拔、直接出大气层顶。所以这次用的是**层剔除 + 专属光**这条隔离，
  代价是要自己扫空闲 layer。手法不通用这件事本身写进了工具的类注释，
  免得下一个自检照抄一个装不下的方案。
- **拿坏点自己的离散度当判据的宽容度上界，判据就永远通过。** 反模式家族又添一例。
  性能 harness 第一版里，判据 ②（斜率折叠）的上界取的是**全部**点里最差的相对离散度。
  而 720p 那一档有个 ±520% 的坏点，于是上界被撑到 519.8% —— 任何极差都能过。
  一条不可能失败的判据不是判据。
  修法是先按 `k_MaxSpread = 0.25`（与 LUT profiler 那条"±25% 以上不引用"同一把尺子）
  把坏点判成不可用，再让上界只从**入选档位的可用点**里取。
  改完上界落到 16.0%，实测极差 9.6% —— 这才是一次真的通过。
  推论也一并落实了：被排除的档位既不进折叠、**也不给上界**，否则判据又松一层。
- **全局预热不能靠每项测量自己那 20 次。** 同一版里，720p 是全场第一次测量，
  于是它付了两个 pass 的 PSO 创建、RT 首次分配、以及 GPU 从低功耗档爬频。
  表现是它的「覆盖 0%」测出 **0.181 ms**（另外三档同位置折算是 0.026 ms/Mpx 量级），
  把最小二乘拽成 **−0.11 ms/Mpx 的负斜率**、"天空省下的比例"打印成 −601.5%。
  `VistaGpuTimer` 里每项各自的 20 次预热只够把**这一项**的命令流跑热，付不掉
  整场一次性成本。加了 1000 次的全局预热，并且把"第一趟 vs 第二趟"两个数都印出来 ——
  实测 **265.9 ms vs 112.6 ms（2.36×）**，那个比值就是这一层存在的实测理由；
  不印出来它看起来像多余的防御。预热之后 720p 的「覆盖 0%」降到 0.025 ms、
  斜率 0.0247，与另外三档折叠。
- **测量窗口不等长，相对离散度就不可横向比较 —— 该加长窗口，不是放宽容差。**
  预热修好之后 720p 仍被判不可用：它的「覆盖 25%」离散度 36% > 25%。
  但那一档的五个点本身是漂亮的直线（斜率 0.0249，与另外三档折叠得很好）——
  被判掉的不是被测对象，是**测量窗口太短**：720p 用 200 次时总窗口只有 ~6 ms，
  而另外三档是 21~26 ms，于是 Editor 自己的重绘抖动在它身上占比大得多。
  修法是把 720p 的 reps 提到 800（窗口 ~24 ms，与其余档位拉齐），
  而**不是**放宽 `k_MaxSpread` —— 放宽容差去让一档通过，就是上面那条反模式的翻版。
  同一条道理让判据 ④ 的两个 N 从 200/50 改成 400/100：本判据必须让 N 变，
  没法把窗口拉齐，但可以让两个窗口都够长。200/50 那一版的小 N 只有 ~5 ms 窗口，
  相对差 15.2% 擦着 15.7% 的上界过 —— 擦线通过等于没判；改成 400/100 后是 3.1% vs 8.4%。
- **渲染 → 读回路径有一条幅度 ±1/1024 的**加性**扰动场。**（本项目最贵的一次测量）
  `Vista/Lit` 累加自检连续报出最大相对误差 `6.595E-004`，八个关键字配置一模一样。
  那个量级与 fp16 相对精度（`2^-11 = 4.88E-004`）几乎相等，于是我把它当成
  "求值次序不同导致的 fp16 舍入"找了很久 —— 而它根本不在 shader 里。
  抓出来的办法是加一个 5 档：往 RT 里写死 `(0.25, 0.5, 0.75)` 这三个**精确的 half**，
  读回来逐像素低 1~2 个 ulp，最大偏离恰好 `9.766E-004 = 1/1024`，且**随像素位置变化**
  （`(0,0)` 与 `(64,64)` 不同）。写死的常量不可能"算错"，所以偏差只能来自路径。
  证明它是**加性**而不是相对的：把 0 档的 relError 在 shader 里 ×100 之后重跑，
  报数变成 `6.595E-006` —— 恰好是原来的 1/100。若那 6.6e-4 是被测对象的性质，
  它会随载荷一起放大；只有加性污染才会这样被除掉。
  **教训：尺子的地板与被测量的期望值同量级时，尺子会自己伪造一个结论。**
  而 relError 的期望值恰恰是 0，正踩在这条地板上。
  修法**不是**给判据放宽容差到地板（那会把 1e-3 量级的真实偏差一起放过），
  而是在**写出之前**把被测量抬到地板之上：×100 后地板折算回相对误差是 1e-5，
  比判定门（Weber 1%）与故障注入的 2% 都低三个数量级。代价只有一次乘法。
  机制未定（`FinalBlit` / dither / 格式转换 / `ReadPixels` 路径都有可能），
  但地板的**幅度**已经被量出来、并且每轮都被交叉验证，所以修法与机制无关。
- **拿两个上界做相对比较，判据必然报"尺子坏了"。**（新的反模式，这一轮踩了两次）
  地板量出来之后，归因链上那条恒等式判据
  `|relCpu − rel0| > 5%·rel0` 立刻开始报警：relCpu = `2.899E-010`、rel0 = `6.595E-006`,
  两个数**都是上界**而不是测量值，它们的差必然接近地板本身 —— 于是一条本该
  验证"3/4 档量的是不是同一件事"的判据，把"两侧完全一致"印成了"归因链断裂"。
  修法三条，缺一不可：① 在所有"拿 rel0 去比"的判据**之前**插入
  "两侧同时落在各自地板以下"这条**结论**分支（那是这台尺子能给出的最强结果，
  不是故障）；② 紧跟一条"只有一侧落地板"的**矛盾**分支（用 `&&` 与 `||` 把两者分开）；
  ③ 剩下那条相对容差里把两条地板作为**加性**项带上
  （`5%·rel0 + relFloor + NumFloor/den`）。
  一般规则：**地板一旦量出来，就必须作为一个具名量（`relFloor` / `NumFloor`）
  穿过每一条比较；任何两个操作数都可能落在地板以下的比较，必须先门控、不能靠放容差。**
  同一轮里还顺手把分母判据（`den ≥ 分母下限`）**上移**到所有数值解释之前 ——
  否则下游会去解释一个分母本身就错的商。
- **容差取 0，隐含了一条已被实测推翻的前提。**（同一族的第三例）
  5 档那条"读回值必须逐位等于 `(0.25,0.5,0.75)`"的守卫，前提是写→读回无损。
  地板量出来之后那个前提没了，于是它报 16384/16384 偏离 —— 一条**恒报**的守卫
  与一条恒不报的守卫一样，都不是守卫。
  但修法**也不能**顺手把容差放宽到实测地板 —— 那是拿被测现象给自己的门限背书。
  修法是**把一条守卫拆成硬判据 + 测量**：
  ① `misclassified` —— 最近邻分类，读数归到 `{0.25, 0.5, 0.75, 预清屏值, 0}`
  里最近的那个候选，门限由**候选之间的间距**（0.125，即地板的 128 倍）决定，
  与地板幅度无关，没有任何发明出来的容差；
  ② `maxDev` —— 一个**测量**，它反过来交叉验证 `k_ReadbackFloor` 这个常量。
  失败条件是"地板**变大**了"（变小不算失败，上界仍然成立）。
  于是 `k_ReadbackFloor` 从一个假设的常量变成一个每轮被验证的常量，
  判据 1 那个上界也因此站得住。实测：误判 0/16384、地板 1.00×。
- **读数接近 0 的档位，无法自证自己执行过。** 上一版 3 档写 `num×1e6`、5 档写死 0，
  两者在真实情形下都接近 0 —— 于是它们读回一个与 0 档相同的小数时，
  "else 分支没进"和"值确实很小"在数值上**完全不可分辨**，归因链在这里断掉，
  而我却先去怀疑读回路径。修法是给每一档加上"我是几档"的印记：3 档加偏置 0.25
  （读回 < 偏置一半 → 这一档根本没执行，别去解释那个数），
  5 档输出**三个互异**的常量（顺带还能查出通道错位）。
  1/2/4 档不加印记：它们的量级本身与 0 档差三个数量级，已经是印记了。
- **印记的偏置取大了会把分辨力吃掉。** 3 档偏置第一版取 8，`ulp(8) = 7.8e-3`，
  分子只能分辨到 `7.8e-9`；而读回路径那 2 个 ulp 的亏损换算成分子是 `−1.6e-8`,
  于是解码出一个**负的绝对差** —— 一个不可能的数，它本身就是分辨力不足的证据。
  改成 0.25（`ulp = 2.44e-4`，分子分辨到 `2.4e-10`），同时 0.25 仍远高于那条
  1e-3 的加性地板，"有没有执行"这个判断不受影响。
- **哨兵值与放大后的被测量撞车，方向恰好是最危险的那一侧。** 哨兵原取 100，
  是在 relError 未放大时定的；×100 之后 100 只相当于"100% 的偏差"，
  于是一次真实的严重不一致会被读成"这个像素没参与比对"——
  假通过，而不是假报警。改成 30000（远低于 half 上限 65504），
  RT 清屏色同步改掉。判定用 `!(m < gate)` 而不是 `m >= gate`，`NaN`/`inf` 同样落进哨兵一侧。
- **本轮无法失败的守卫，要在报告里点名说它无法失败。** `worstAll` 被钉在地板上之后，
  "两次渲染是不是同一幅画"那条守卫（`rel0 <= worstAll * 0.5`）本轮不可能触发 ——
  rel0 只能等于地板，不可能低于地板的一半。报告现在明写它是**惰性判据**、本轮
  不提供保障，并点明真正排除"换了画面"的是 1/2 档读回的 `8.422852E-001`：
  那是一个与地板无关、有结构的量，换了画面它会跟着变。
  一条读起来像保障、实际不可能失败的判据，比没有这条判据更坏。

- **拿一条落在尺子地板之下的预测去分类，会在读数落进地板内时产生假失败。**（新的反模式）
  A-B 一致性自检要判"A 的深度反投影路径引入了多大的距离误差"，
  第一版的门限直接写成 `excessA <= 2 × D32 预测`。但 D32 在 34 km 处的预测是
  6.57E-004 m，而读出地板是 7.813E-003 m —— 预测比地板小 **12 倍**。
  于是一个完全正常的 0.005 m 读数（well inside 地板）会被判成"?"并**红**。
  实测 `excessA` 恰好是 0，所以这个缺陷这一轮没有发作 —— 是我看着报告里
  "D32 预测 6.57E-004 / 地板 7.813E-003"这两个数并排放着才发现的。
  这与 #6 记下的「容差取 0 隐含了一条已被实测推翻的前提」是同一个病：
  **门限必须以尺子的分辨力为底**。修法是 `max(2 × 预测, 地板)`，
  并且在表里给每个落在地板下的预测打上「(地板下)」的标记。
- **结论的语气必须跟着分辨力走：能排除的说排除，不能确认的说上界。**
  同一处的第一版结论写的是「判定 = D32」。数据支持的只有一半：
  D24 的 689 m 是地板的 88195 倍，**确实被排除**；但 D32 的预测在地板之下，
  所以自检**无法区分**"深度量化确实只有 D32 那么大"与"深度路径根本不引入量化"。
  改成「已排除 24 位定点深度，读数与 32 位浮点深度**相容**」，后面跟一句
  显式的上界声明。这条与 #7 那条「覆盖不到的路径要说是覆盖不到」是一件事的两面：
  那次是路径没被覆盖，这次是**量级低于分辨力**，两种情况下报告都不许把
  "我没看见"写成"它不存在"。
- **两个都压在地板上的统计量，它们之间的大小关系是噪声。** ②a 的报告里有一句
  "斜面的 Δd 应当大于正对相机的板子（深度梯度更大）"的机制预期。
  但当两者都被地板顶住时（本轮：板子 0.013 / 斜面 0.009，地板 0.008），
  那句话会引导读者从噪声里读出一个结论 —— 而且方向还是反的。
  报告里于是加了一支：两个数都 ≤ 地板时，改印「这个比较**没有分辨力**」
  而不是机制预期。本轮板子 0.013 > 地板，这一支正确地没有触发。
- **让不经过被测路径的那一侧去当尺子。** 要量"深度反投影带来多少距离误差"，
  就不能用 A 自己的读数当基准。变体 B 的 `positionWS` 是**插值**来的、
  完全不碰深度缓冲，所以 B 对一组已知距离（直接从 transform 读出来的）
  的相对误差就是"读出路径 + 插值"的地板，与深度无关。
  A 相对同一组已知距离**超出** B 的那一部分，才是深度那条路的账。
  这一步之后 D24 / D32 的区分才有意义 —— 否则两个候选都被同一个未标定的
  误差裹着，又落回「拿两个上界做相对比较」。
- **同一个量的第二份实现会以"两条判据门限不一样"的形式发作，连 8 行的辅助函数也算。**
  这轮写完之后有一个 `MeasureDistanceFloor`，而 `JudgeDistanceCalibration`
  内部已经算过同一个地板。两份实现今天给同一个数，改一处忘另一处时
  症状是"表里的地板和门限里的地板对不上"—— 而报告里两处都印着"地板"。
  删掉前者，地板改成 `out` 返回：**一份实现，一处调用**。
  同一轮里 `VistaApCompositeAcceptance` 里那份手写的 `k_Fp16Rel = 4.88e-4f`
  也一并改成引用 `VistaSelfTestNumerics`（另外两条自检早就是引用的，
  就它还是字面量 —— 正是"改一边忘另一边"已经发生了一半的样子）。
- **读数为 0 的档位要能自证自己执行过 —— 但用论证而不是再加一次测量。**
  `excessA` 全 8 列都是 0.00E+000，形式上与"两个距离出口都没生效、
  两边读到的是同一份数据"无法区分。这里没有再加一条判据，而是在报告里
  写清两条：① 若任一出口没生效，那个通道读到的是**合成后的颜色**（约 0.2）
  而不是 km 量级的距离，相对误差会是 ~99% 而不是 1E-006；
  ② 判据②a 的逐像素 Δd 本身非零，证明这是两份不同的数据。
  能用现有读数论证的事情不该再花一次渲染 —— 但**必须写出来**，
  否则下一个人看到一列 0 只能重新怀疑一遍。

- **调试载荷被 AP 全屏 pass 当颜色合成了一遍。**
  #12 的自检第一次跑，归因档读回的不是我下发的值：`override = (1,1,1,·)` 读到
  `(0.9971, 0.9937, 0.9863)`。逐通道解一条仿射 `payload·T + L`：
  绿 `T = 0.99147 / L = 0.00223`，蓝 `T = 0.9798 / L = 0.0065` ——
  **蓝比绿衰减更多、加得更多，那是大气透视的指纹**。
  成因：`VISTA_LIT_DIFF_DEBUG` 分支只绕开了变体 B（那条路径根本不调
  `VistaApplyApTail`），而变体 A 是一个**独立的全屏 pass**，它不认识调试载荷，
  照样把 payload 当颜色乘透射率、加散射。
  于是「`w = 0` 时比值恒 1」这条判据读到 0.0913 的偏差。
  更糟的是本夹具用正交相机，而 AP 的距离重建是按透视视锥四角插值的 ——
  contamination 因此**逐像素不同**，看起来就像「比值随位置乱跳」，
  完全不像「有一层东西盖在上面」。
  修法是整条自检期间把 `compositeMode` 按到 `Off` 并在 `finally` 里还原。
  取 `Off` 而不是 `InShader`：两者在 DIFF_DEBUG 下都给出干净载荷，但 `Off` 连 AP 的
  两次 dispatch 都省了，少一个活动部件。
  修完之后 `override = (1,1,1,·)` 读回**精确** `(1.0000, 1.0000, 1.0000)`，
  `(0.5, 0.25, 0.125)` 读回精确 `(0.5000, 0.2500, 0.1250)`。
  **一条通则：DIFF_DEBUG 一类的载荷输出，必须同时按住所有会写同一张 RT 的后续 pass，
  而不只是自己所在的那条路径。**「我这条路径没调它」不等于「它没跑」。

- **探针灯挂在孤立 layer 上并不足以让它成为主光 —— 决定权在 `RenderSettings.sun`。**
  夹具原来的注释写着「场景灯不在 `cullingMask` 里会被剔掉，所以不必动全局」。
  那句话是错的，代价是三条判据一起失真。
  URP 的 `GetMainLightIndex` 第一条规则是「`visibleLights` 里等于 `RenderSettings.sun`
  的那盏**直接返回**」，而**平行光并不因为 layer 不在 `cullingMask` 里就从
  `visibleLights` 里消失**。于是主光是场景那盏：
  · Vista 发布的 `_VistaSunDirection` 是它的方向 —— 判据②拿一个错的 `muSun` 去对账，
  相对误差 13 % / 42.6 % / 584 %，而我一度怀疑那是 LUT 精度；
  · 它相对夹具那面墙几乎掠射（NdotL ≈ 0.04），主光贡献 M 只有 ~0.025，
  低于除法测量的分母下限 —— 判据①的除法形式**一个像素都没量到**。
  两个症状、一个成因。修法是 `RenderSettings.sun = 探针灯` + `finally` 还原，
  还原顺序必须在**销毁布景之前**：反过来的话中间有一小段「sun 指向已销毁对象」的窗口，
  那条窗口里任何一次编辑器重绘都会把它当成「没有太阳」。
  修完实测：`_MainLightColor` 从掠射的 M ≈ 0.025 变成 `(1.0000, 1.0000, 1.0000)`，
  `_MainLightPosition = (-0.3218, 0.3418, -0.8831)` 与 CPU 期望
  `(-0.3214, 0.3420, -0.8830)` 吻合到 4e-4，判据②从 13 %/42.6 %/584 %
  收到 **0.149 %/0.167 %/0.189 %**。
  **归因是靠新加的 10/11 两档（`_MainLightPosition` / `_MainLightColor`）读出来的，
  不是猜出来的** —— 9 档只有「Vista 发布的太阳」，它单独无法分辨
  「URP 压根没选中我要的那盏灯」与「URP 选对了、Vista 解析错了」，
  而这两种成因的修法在不同的文件里。11 档还顺带回答一个判据级的问题：
  主光色若是黑的，任何「乘在主光上」的判据都乘在零上，全部退化成 `0 == 0` 的空判。
  顺带一条**待查**：姊妹自检（`VistaLitAccumulationSelfTest`、
  `VistaApVariantAgreementSelfTest`）用的是同一套自建布景，可能有同一个盲点。
  —— 已查，盲点是活的，见下面「那条『待查』查完了」。

- **一条读数为 0 的测量，不能不说自己没执行过。**
  除法形式那一段只报「过门像素子集内的 max M」，读数是 `0.000E+000` ——
  而那个 0 既可以是「没有任何像素过门」，也可以是「过了门但 M 恰好为 0」，
  两者的含义完全不同（前者是夹具坏了，后者是被测量为零）。
  加了一个**全图** `max M`（与门限无关地统计）和一句显式的
  「**没有任何像素过门**，除法形式这一轮没有执行」。
  修完读数是 `3.608E-1 / 6.782E-1 / 7.177E-1`，那句话再没出现过。
  这是本项目第三次撞上同一个形状：**本轮无法失败的守卫/测量，要在报告里点名。**

- **判据④那个「随配置漂移」的画面量级，漂的不是 GI，是污染。**
  上一版三个太阳配置读到 `max R2 = 0.49 / 0.80 / 0.84`，我当时准备把它解释成
  「环境光随太阳高度变化」。AP 按掉之后三档变成**完全一致**的 `2.995E-002` ——
  那三个不同的数是 AP 散射项混进载荷造成的，与 GI 无关。
  记下来的原因不是这个数本身，而是：**一个「看起来物理上讲得通」的漂移，
  是最容易被接受、因此最容易掩盖污染的形态。**
  2.995E-2 是读回地板的 30 倍，判据④非空，就按这个数报。

- **那条「待查」查完了：两条姊妹自检的盲点是活的，且症状恰好不是失败。**
  上一条待查说姊妹自检可能有同一个盲点。按**归因先行**先量再改：给
  `VistaLitAccumulationSelfTest` 加了一段常驻的 10/11 档读出，实测
  `_MainLightPosition = (-0.6538, 0.7539, -0.0444)`，而布景那盏是
  `(-0.2531, 0.5736, -0.7791)`，最大分量差 `7.346E-001`；
  `_MainLightColor = (0.8926, 0.8018, 0.6748)`，也不是布景那盏的纯白 ——
  URP 选中的是**当前打开的那个场景**里的太阳。`VistaApVariantAgreementSelfTest`
  带着同一句被 #12 推翻的注释（「场景那盏不在 cullingMask 里，会被剔掉，
  于是主光就是这一盏」），同样没设 `RenderSettings.sun`。

  爆炸半径是**读代码**定下来的，不是推测的：`Apply` 里切阴影用的是
  `camData.renderShadows`，与是哪盏灯无关，所以配置①/②仍然有差别，
  判据 1/1b/2 的等价性结论不受影响（两侧读的是同一个 `_MainLightColor`）。
  真正坏掉的是两件事 ——
  ① `Build` 里那句「太阳从相机后上方打过来，好让球体的影子落在背板上」是一条
  **覆盖性断言**，它的前提不成立；
  ② 两条自检的读数依赖「当前打开的是哪个场景」，而报告里看不出这个前提。

  这就是那个形状值得单独记下来的原因：**一条覆盖性断言若靠隔离机制间接保证，
  它失效的症状不是失败，而是判据在一条更窄的路径上照样全绿。**
  带着这个盲点跑，累加自检 8 个配置全部 `≤ 9.766E-006`、比对像素 16384/16384、
  哨兵 0，报告上一个字的异常都没有。

  改法两条：两条自检都临时接管 `RenderSettings.sun`（在 `finally` 里
  **先还原全局、再销毁布景** —— 反过来会留下一小段「sun 指向已销毁对象」的窗口，
  那条窗口里任何一次编辑器重绘都会把它当成没有太阳），并且把接管结果
  报成**实测读回值**而不是「我设过了」这句声称。改完累加自检的分量差降到
  `1.311E-003`，低于门 `1.953E-003`（10 档解码把读回地板放大 2 倍），
  `_MainLightColor = (1.3984, 1.3984, 1.3984)` = 布景那盏 intensity 1.4 的纯白；
  AP 一致性自检两条判据数字不变（②a 全部落在读出地板 0.008 m 以下，
  ②b 最大 `|A − B| = 1.221E-004`、越界 0、2.00 fp16 ulp），
  与预期一致 —— 两个变体本来就读同一个主光。

- **把「阴影分支被覆盖」从断言改成读数，顺手发现「未覆盖」这个词一直在说错话。**
  上面那条断言修不了「它到底覆盖没覆盖」，因为配置①/②的关键字表都是空的，
  唯一差别是 `camData.renderShadows`。于是「②覆盖了阴影分支」有两层含义，
  强度差很远：弱的一层是「那段代码编进去、跑了」，强的一层是
  「算出的 `shadowAttenuation` 真的 < 1，即真有像素落在阴影里」。
  只有强的那层让「阴影分支被比对过」有意义 —— 若一个像素都没被遮住，
  那段代码每次都返回 1，与没有阴影的路径在数值上完全相同，
  判据 1 在②上的通过就只是在①上的通过又跑了一遍。
  加了一条测量（①/②两次渲染的逐像素最大差）：`3.601E-001`，
  超读回地板的像素 `151/16384` ⇒ **强含义成立**。

  改这一段时才看清判据 2 的三态里藏着一句错话。注入 δ 在第 t 项上，
  报出的相对误差就是 `δ·X/total`，所以 **`报出/δ` 就是该项在最佳像素上的占比**。
  上一版把所有报不出来的项都写成「这一项在本帧为 0 或占比低于 1%」——
  本轮 `giColor` 报出 `1.000E-002`，占比 **50.00%**，被那句话说成 < 1%，
  差 50 倍；更糟的是它指向的修法也是错的（去查这一项为什么没参与，
  而实际该做的是承认判据 1 对它的灵敏度不够）。现在拆成两种成因并给出占比：
  占比落在尺子地板以下 = **真的没参与**（`vertexLightingColor`，`≤ 0.03%`，
  判据 1 在它上面是 `0 == 0` 的空判）；占比不小、只是 `δ×占比` 落在判据 1
  自己的 1% 门之下 = **参与了但灵敏度不足**（`giColor`，50.00%；
  门/注入 = 50%，所以占比不足 50% 的项必然如此）。

  `giColor` 那个 50.00% 恰好等于「门/注入」，**一个正好压在门上的读数，
  判定由打印出来的最后一位小数决定** —— 本项目被这个形状咬过（尺子地板与
  期望值同量级时，尺子会替被测对象伪造一个结论），所以没有去解释这个巧合，
  而是换一把刻度不同的尺子复量：按代数 `报出 = δ·X/max(|参考|, 1e-3)` 对 δ
  严格线性，占比应当与 δ 无关；它若随 δ 变，只可能是分母下限被触发或
  最大值像素换了地方，而两者都会让「占比」不再是占比。
  ×10 注入复量：`99.45% / 98.83% / 50.00% / 0.00%`，与 ×1 的差
  `0.04 / 0.00 / 0.00 / 0.03 pp`，全部在容差 `0.0488pp`（= 地板/δ）之内 ⇒
  **占比这个读数可信**，50.00% 是本帧的真实占比。
  报的是两次占比之**差**而不是两次读数之**比是不是 10**：后者在读数落到地板上时
  会自动成立（地板/地板 = 1），看起来只是「比值不对」却不指向原因。

- **抄来的 0.3 让整条 march 从二阶退化成一阶，而症状被我读成了别的东西。**（#18 最贵的一次）
  `VISTA_SAMPLE_SEGMENT_T` 第一版抄 Hillaire 的 0.3。段内偏离中心时 ρ 的一阶偏差
  不再左右抵消，误差退化成 `|0.5 − segT|·x` 的**一阶量**。
  症状是**4 倍步数只把误差降到 1/3.7**（一阶该降 4 倍、二阶该降 16 倍），
  而当时我把剩下的误差读成了"三线性重建误差"，于是去查了错的地方。
  这条的一般形式：**一阶误差在 4 倍步数下只降 3.7 倍，会被误读成"剩下的都是插值误差"**。
  以及：**一条从别人论文里抄来的经验常数，它的适用区间可能正好被你的另一处优化推出去**
  （这里是段内解析积分 + 雾感知步长上限把工作点从 x ≈ 2.8 推到了 x ≪ 1）。

- **把负数压成 0 的格式化函数，让三种完全不同的状态在报表上长得一样。**
  「恒等余量」（`实测 − 固有 − 端点`）的**正常态是负数**，但它复用了为非负量写的
  `Pct()`，而后者把一切 ≤ 0 的值都打成字面量 `"0%"`。于是
  「余量 −5%（充足）」「余量恰好 0（压线）」「−∞（这一段一片都没判）」
  三者的报表输出完全相同 —— 尺子自己造了个结论。
  更早的一层同病：`worstSlack` 初值写的是 `0f`，于是那个量**在数学上就不可能为负**。
  两处叠起来的后果是：即使「实测」那一路根本没写（`meas ≡ 0`），报表也会打
  `恒等余量 0%`，与「余量充足」不可区分 —— 正是本项目点过名的
  **「读数接近 0 的档位无法自证自己执行过」**。
  修法：初值改 `float.NegativeInfinity`，并**单独写一个 `SlackPct()`**
  （带符号、F3、`−∞` 印成 `"无判定片"`），而不是去改 `Pct` ——
  后者有一大堆消费端的量本来就非负，对它们印负号是噪声。
  修完后的读数是 **−0.056% ~ +0.068%**，跨越零 —— 这才是这条断言真跑过的证据。
  新反模式：**当一个量的正常态是负数时，给它单独一个格式化函数，
  而不是复用为非负量写的那个。**

- **出货档 32³ 上量不出雾的成本，不是因为它不花钱，是因为派发地板压着。**
  `numthreads(8,8,1)`、32×32 根柱 ⇒ **16 个线程组**，而 RTX 3060 有 28 个 SM。
  实测每柱代价：出货档 ~48–49 ns vs 放大档（512×288）~4.4 ns ⇒ **10.8–11.2 倍**
  ⇒ **出货档至少 91% 的时间不在做逐样本的工作**。所以雾的成本只能在放大档上量，
  出货档上的"看不出差别"是空判据。
  - 顺带一个反直觉读数：**512×288 是 32×32 的 144 倍柱数、约 36 倍总工作，
    时间只涨 4.4 倍。**
  - 第一版想用 `reps = 30` 摊销派发地板，被它自己的运行推翻了：
    重复派发同一个 dispatch **不改变每次派发的地板**。
    **要离开地板得用分辨率，不是摊销次数。**

- **A-A 空测差值才是尺子的分辨力，跨视角的差值不是。**
  第一版拿"两个不同视角的读数之差"当地板 —— 但**一个跨布景稳定复现的差值，
  只有在两个布景确实做同样工作时才是尺子噪声**，而两个视角的 march 工作量本来就不同
  （见下一条）。改成 A-A 空测：按 `offA, uni, exp, offB` 的顺序测四次，
  `basis = ½(offA+offB)`、`nul = |offB − offA| / basis`，读到 0.4%。
  并且**任何差值必须超过 `2 × A-A 空测`才算"量到"** —— 上一轮"雾数学"恰好压在
  1× 上，按 2× 裕度如实记为**未量到**。

- **不同视角的 AP 成本差别不是噪声，可以预测：march 打到地面就终止，
  而终止掉的切片一分钱不花。**
  `tLimit = min(max(tTop,0), tBottom>0 ? tBottom : ∞)`；一旦 `tNext` 饱和，
  `segLen == 0`，`if (segLen > 0.0)` 把内层循环整个跳过。
  于是**每柱步数由"打到地面的距离分布"决定**：
  视角①（2 m 平视）~85 步/柱、视角③（300 m 俯视 20°）~59 步/柱，
  **预测 85/59 = 1.44×，实测 1.37–1.42×**。
  这个模型顺手给了 #19 一条结论：**近层 froxel 体的成本也将由地面命中分布主导，
  不是由切片数主导** —— 所以"把切片数从 64 降到 32"这种省法可能省不到东西。
  同一个机制还让累积入散射曲线在打到地面处**饱和**，这正是 `cosh(Δ/2L) − 1` 那条
  指数律在视角③ 上不紧到 15 倍的原因。

- **0.25% 与 0.29% 这两个差值我没能归因，就如实写成没归因。**
  改 `dt` 定义那一轮留下两个量级相同、方向相反的小差值，
  候选成因至少两个（段内质心的重新分布 / 步数 clamp 的边界跳变），
  而现有的夹具分不开它们。**要分开得故意把 `dt` 定义回退一次**，
  那是 #27 的一项，不是这里能顺手做掉的。
  写在这里是因为「一个看起来物理上讲得通的漂移，是最容易被接受、
  因此最容易掩盖污染的形态」。

- **参考解不含雾会产生一个巨大的假失败，而它的形状与真失败一模一样。**
  `VistaApReference` 一开始是晴空的。接上雾之后 #7 的切片判据会把
  "参考解没有雾、被测对象有雾"这个差直接算成 errMid，量级和
  "切片分布装不下雾的拐点"完全一致 —— 两者都表现为中景段读到十几个百分点。
  改成含雾之后才有了 19.13% vs 固有 19.34% 那组能定性的读数。

- **`k_ApVisibleDeltaT` 从 1e-3 提到 3e-3：门有两条互相独立的下界，取大的那条。**
  ① 可见性：Weber 1% 允许到 1e-2；
  ② **这个量自己的测量地板**：`deltaT` 由三个 fp16 通道反算，
  `T ≈ 1` 处 1 ulp = 2⁻¹¹ = 4.9e-4，三次圆整 ⇒ ~1.5e-3 的圆整噪声。
  1e-3 落在②之下 —— 那就是**拿一个低于自己测量地板的门去判**，
  读数落进地板时会产生假失败。3e-3 是地板的 2 倍、可见性上界的 1/3。
  这条豁免的代价印在报告里：晴空两个视角都是 `31/31 片豁免`，
  其中最大 `|ΔT|` 为 **1.16E-3 / 5.97E-4**，而同一份数据的通道打包残差是
  **4.41E-4 / 4.60E-4** —— 豁免掉的量只有存储噪声的 2.6 倍，
  对它设**相对**门等于在量自己的圆整噪声。含雾时这一栏才真判（视角③ 读到 11.05%）。
  **把「未量到」与「量到且达标」印成两句不同的话**，所以 `判0片` 是诚实的输出，
  不是绿灯。

- **注入度量取 `max(dS, dT)` 等于只承认同一个像素上两项可见变化的一半。**
  改成**逐片求 `dS + dT` 再取 max**。同一轮还把 `k_FogAmbientMin` 从 1e3 降到 1e2：
  原值是按正午天顶推的，而判据要判的是布景里**最暗**的那个视角 ——
  **门是拿布景里最亮的一个视角推的，却要去判布景里最暗的那个。**

- **HLSL 的三元 `?:` 不能作用在 struct 值上。** `cond ? VistaSampleFog(h) : VistaFogSampleNone()`
  编译不过，必须写成先声明再 `if` 赋值。记一下是因为报错信息不指向这个原因。

- **一个已经判失败的格子，后面照样印着「达标」。**
  #18b 天空判据第一次跑出来，布景② 的均匀雾档读 `5.65% ✘`，
  但同一行末尾的结论串写着「（达标）」—— 因为那个串只按「这一档是不是走实测基线」
  分了两支，忘了再看一眼 `okModel`。
  这是本项目已经点过名的「一个把『未判达标』印成『达标』的判据，比一个平门更危险」
  的**镜像**，而且更糟：那次是把未判的印成达标，这次是把**已经失败的**印成达标。
  修法是把结论串拆成四支（空判据／未判达标／达标／未达标），
  让「达标」这两个字只能挂在真的过了质量门的格子上。

- **把门从 8% 收紧到 3%，才让一个此前被静默通过的配置现形。**
  `k_SkyFogModelMax` 第一版的 8% 是拿**高度**比（而不是**沿射线跨度**比）推的，
  推导本身有误。改成正确的 3% 之后，布景② 的均匀雾档立刻从「✔ 5.65%」
  变成「✘ 5.65%」—— 读数一个字没变，是门变了。
  归因也很干脆：那条推导有一条没写出来的前提「雾的沿射线跨度 = H/dy」，
  对均匀雾（H → ∞）不成立，那里跨度由 `1/σ_t` 定。
  于是补了第三个区间的基线，键控在**物理属性** `infiniteH` 上而不是档位标签上。
  记这一笔是因为它的价值容易被读反：**收紧门找出了一个没被覆盖的配置，
  而不是制造了一个新问题**。

- **一个读数恒为 0 的格子，可能根本不是「通过」，而是判据是空的。**
  布景③（相机 300 m）配置 D（标高 20 m）的判据4 印出
  `max|ΔT| = 0.00E+000 @无判定方向　判 62/64`、判据5 = 2.0E−004%。
  原因是 `ρ(300 m)` 在标高 20 m 下是 `e^{−15} = 3e−7`：
  雾在整条视线上最多吃掉透射率的 1.5E−003%，两项式与参考解**都退化成"无雾"**，
  读数 0 不构成任何证据。
  修法不是删掉这一格，而是加一个 `worstFogLoss < 1e-3`（Weber 阈的十分之一）
  的空判据探测器，报表上印「（空判据）」并说明：
  **这一格的证据力在「它本该是空的」这件事上** ——
  如果实现里漏了 `ρ(h_cam)` 这个因子，它会读出一个荒谬的亮度，而不是 0。
  这条与已点名的「读数接近 0 的档位无法自证自己执行过」是同一族，
  区别是这次连"执行过"都不需要 —— 需要的是"它应当什么都不做"。

- **一个从没执行过的守卫，要在报表上点名说它没执行过。**
  判据5 有一道「这个方向的尺子地板高过门，就把它排除」的守卫，
  全部 3 布景 × 5 档 = 15 格里**一次都没触发**。
  报表现在显式印「**本轮一次都没触发** —— 那段排除逻辑本轮未被执行，
  不构成『它写对了』的证据」，并补一句解释（全部布景的尺子地板都在门以下，
  说明参考步数足够，不是判据放宽了）。
  依据是「本轮无法失败的守卫要在报告里点名」，
  以及「一个默认关闭、又没有判据覆盖的开关，等于一段永远不会被发现写错的代码」。

- **`Validate AP Variant A-B Agreement` 报 `VistaAtmosphereFeature.current 为 null`
  是会话前置条件，不是回归。** 那个静态字段只在相机渲帧时被赋值，
  而反复 `refresh_unity` 触发的 domain reload 会清掉它 —— 如果之后没有任何相机渲过帧，
  这个菜单项就必然失败。查法是先在 Editor.log 里找同一条日志前缀更早的**成功**记录；
  修法是先强制渲一帧（截个图就够）再调菜单项。
  记一下是因为它的报错文本长得像"feature 没装进 Renderer"，会把人引到 Renderer 配置上去。

- **fp16 的地板是**整个** eps，不是半个 —— 因为 D3D11 的典型-UAV 存储在截断，不在舍入。**
  #21 的往返判据第一版把门按半 eps（`2⁻¹¹ = 4.88e-4`）摆，于是**门正好压在地板上**。
  - 查法是拿读数自己去反解取整模式，而不是查文档：对每个（请求值, 读回值）对算
    `e = floor(log2(req))`、`ulp = 2^(e−10)`、`k = (req − 2^e)/ulp`，
    同时算 `trunc = 2^e + floor(k)·ulp` 与 `near = 2^e + round(k)·ulp`，看读回值等于哪个。
    12 对**全部**等于 `trunc`。
  - 但**只有 3 对有区分力**（`frac(k) > 0.5` 时两个候选才不同：D 档 `S_r` 0.878、
    F 档 `σ_t` 0.814、F 档 `S_r` 0.787）。报表必须把这个标出来 ——
    「一条结论若用 12 个样本支撑而其中只有 3 个有区分力，报表不标出来
    就等于用 9 个空样本充数」。
  - 后果不止是门要放宽一倍：截断是**单边**的，误差同向累积而不是相互抵消，
    所以多段递推里的累积误差比按舍入估的要大。
  - 连带修了三道门：`k_RelGate` 1e-3 → 2e-3；#19 的 `k_TextureGate` 1e-3 → 2e-3
    （它此前的实测是门的 **0.96 倍** —— 差一点就是一次假失败，而当时没人知道为什么它这么紧）；
    新增 `k_Fp16RelTrunc` 与 `k_Fp16Rel` **分开**（一个是走 fp16 纹理往返的地板、
    一个是 fp32 解析计算的地板，混成一个常数会让两类误差互相掩护）。
  - 顺带暴露了一处**静默解耦**：`k_SynthGate` 的值是手抄的，「它必须等于 `k_RelGate`」
    只写在注释里。改成 `const float k_SynthGate = k_RelGate;` ——
    **注释断言的耦合要由代码保证**。
  - 一条要点名的边界：`VistaSkyBandingSelfTest.k_Fp16SecondDiffFloor = 1.95e-3f`（4·2⁻¹¹）
    **还没有按这条结论复核过**。它那条路径是 render target 写入而不是 compute UAV 写入，
    可能不受影响，但「可能」不是结论 —— 已记进待办。

- **抵消区的误差地板漏了放大因子，会在最容易触发抵消的那一档判出假失败。**
  `dtKm = far·ρ^a − far·ρ^b` 这种差分的相对误差不等于操作数的相对误差，
  要乘 `ρ/(ρ−1)`（`ρ ≈ 1.087` 时是 **12.5 倍**）。第一版地板既漏了这个因子、
  又把 `pow` 当成只有半 ulp，于是 D 档（`ρ` 最接近 1）红了一格。
  - `pow` 要按 **8 个整 fp32 ulp** 算：GPU 的 `pow` 与 `Mathf.Pow` 是同一个超越函数的
    **两份实现**，D3D11 只保证 `exp2`/`log2` 各自到 1 ulp，复合之后没有任何保证。
  - 修完六档的实测/界比值全部 < 1（`A 0.46 / B 0.26 / C 0.71 / D 0.55 / E 0.66 / F 0.46`）。
  - 「一个尺子自己算错地板（漏了 `ρ` 这个因子、又把超越函数当成只有半 ulp），
    会在最容易触发抵消的那一档上判出一个假失败」。

- **`_VistaGround.w` 只在 `Bind(...)` 里上传，跳过 `PrepareLuts` 的自检读到 0，
  整张表静默归零。** 注入的 alpha 是 `σ_t` in **1/km**、而 `VistaFroxelSegmentNear/Far`
  返回**米**，两者之间的换算就是这个 `worldToAtmosphere = 1e-3`
  （`VistaAtmosphereParameters.cs:110-111`，只经由 `VistaAtmosphereLuts.cs:444` 到达）。
  立即模式自检不走 `PrepareLuts`，于是每一段的 `x = σ_t·Δ` 都是 0，
  而读数 0 在这张表上是**合法**的（雾很淡）—— 症状是判据全绿。
  修法是一条**归因行**：与被测核**逐字同一个表达式**把实测的 `_VistaGround.w` 印出来
  （六档全部 `1.000e-3`，相对差 `0.000e+0`）。**把隐藏依赖变成一个能失败的打印数字。**

- **执行性哨兵与被证明的对象不是同一个核，症状是那一格照样全绿、而绿的理由是错的。**
  #21 的判据⑩ 初版叫「积分核执行性」，用 `SEG_X_MIN` 槽位当哨兵。
  但那个槽位是**探针**核从**注入**表的 alpha 现算出来写的 ——
  探针核跑了它就有值，与积分表写没写完全无关。
  真要漏了积分派发，⑩ 会绿，而它绿的理由与它的标题无关。
  拆成两格：⑩「探针核执行性」+ ⑪c「积分表被写过」。后者能成立是因为
  **积分表的 alpha 存 `1 − T` 而不是 `T`** —— 清空态 0 意味着「通透」，
  所以 `max > 0` 就是「有人写过」，不需要额外的哨兵槽位。
  （存 `T` 的话清空态 0 与「浓到不透明」是同一个合法读数，这一格就不存在。）
  顺带一条：哨兵初值要挑一个**物理上不可能**的值。这里 `uint.MaxValue` 对应 `x = 4.29`，
  一段 froxel 的光学厚度不可能到那个量级；换成任何 max 槽位就不行，
  那些的初值是 0，而 0 是合法读数。

- **一道宽到 3.6e4 倍的推导区间门，挡不住一个 1000 倍的单位错。**
  #21 的判据⑫ 判「实测 `x = σ_t·Δ` 落在推导出来的配置区间 `[8.1e-6, 0.289]` 内」，
  第一轮全绿。但这张表暴露的头号风险恰好是米/千米混用（上一条那个 `_VistaGround.w`），
  而那类错误差 **1000 倍** —— 一道宽 **35680 倍**的门根本挡不住它。
  修法不是收紧那道包容性门（它的宽度是配置区间本身的宽度，收不了），
  而是在旁边**另加两道紧门**，并把裕度与定点相对分辨率**并排印出来**证明门没压在自己的地板上：
  - ⑪e 量纲门：`∫S·T dt ≤ max(S)·d`（因 `T ≤ 1`），实测比值 `8.125e-1`，
    裕度 `1.875e-1` vs 定点分辨率 `5.263e-2`（3.6 倍）。
  - ⑫b 紧上界：`σ_t(地面) × 最长段`，实测比值 `9.275e-1`。
    最长段**扫一遍**取，不假设是最后一段 —— 切片 0 是 `[0, d₀]`，
    它不服从后面那个等比规律。
  两道紧门的段长都复用 `desc.SegmentFar/SegmentNear`，**这不算第二份实现**：
  那两个函数与 HLSL 的 `VistaFroxelSegmentLengthKm` 逐位一致这件事
  已经由 #19 的切片判据盯着，这里等于把那一格的结论接到这一格上。
  另外那道宽门的宽度必须**印在报表上**，否则一个宽门会被读成一道紧门。

- **一条陈旧的「这条路径未被覆盖」注释，在路径落地之后比没有注释更贵。**（#22a）
  `VistaVolumetricFogState` 里那条 ⓘ 一直写着「注入历史表的写入路径本节一次都没跑过，
  `VistaAtmospherePass` 里也刻意没有 `ImportTexture` 它」。#22a 落地之后这句话
  **两半都成了假的**：历史表被 import 了，而且它压根就**没有**「写入路径」这回事 ——
  双缓冲的交换只改写下标，本帧写的永远是 `FroxelInjection` 那张，
  下一帧交换后它就成了历史。一条**自我否证**的文档比没有文档贵：
  读者会据此认为这块还没被覆盖，从而不去看真正覆盖它的判据⑬⑭b⑮。
  改法是把「要覆盖的是什么」重新写清楚（「读到的是不是上一帧那张」），
  并逐格点名哪三条判据盯着它。

- **报表里印「帧后」的值，会让一格全绿旁边站着一个看起来矛盾的 `False`。**（#22a）
  状态行印 `historyContentValid = False`，而紧挨着的判据⑭说「历史在用」。
  两者都对：`historyContentValid ≡ isAllocated && lastWritten == 1 − writeIndex`，
  一帧的顺序是「交换 → 判历史 → 注入 → NoteInjectionDispatched」，
  注入把 `lastWritten` 推成 `writeIndex`，于是**帧后**这个式子必然为假。
  但报表没说自己印的是帧后的值，读者会以为它就是被测代码帧内吃进去的那一份。
  修法不只是加个标签 —— 顺手把它做成了一格**能失败**的判据（⑭c）：
  帧后 `lastWritten == writeIndex` 恰好等价于「本帧的注入核真的派发到了当前写槽上」，
  它为假就是「交换了两次却只派发了一次」那个错配，
  而那个错配在画面上只表现为「累积没生效」，与「历史权重被填成 0」无法区分。

- **一条推导出来的误差地板被第一次实测证伪，而它当时正被写成门的一半。**（#22a）
  判据⑬的地板原本按「相机在原点 1 km 量级内 ⇒ fp32 投影几个 ulp（~1e-7），
  log/exp 各再放一次 ⇒ 取 1e-6 作保守上界」推。实测 `6.962e-6`，
  是那个「上界」的 **7 倍** —— 读数落在自己声明的天花板**之上**。
  原因也清楚：格心世界坐标到 64 m 量级，`decode`/`encode` 各带一次 `pow`/`log`，
  超越函数不是半 ulp。按「当推导给不出紧的上限时，诚实地把门标成实测基线」办：
  取与实测同量级的 `1e-5` 当地板，门仍摆在它与最小错答案的几何中点
  （`2.795e-4`），并把**被证伪的那段推导留在注释里** ——
  它记着「这个量不该被当成一次 fp32 往返」。

- **两个独立的缺陷可以叠在同一个症状上。**（#22b，本节最贵的一课）
  症状：`_VistaFroxelBlueNoise.Load` 在核里逐像素读到 0，而 CPU 侧
  `available == true`、绑定函数确实被调过、HLSL 声明在、shader ID 对得上 ——
  **所有表面证据都显示接上了**。修掉第一个可疑对象（下一条）之后读数**逐位未变**。
  按「第一个可疑对象被修掉、读数却没变时，必须回头重新归因」办：
  没有去怀疑修法没生效（那是最自然、也最费时间的方向），而是**加一把新尺子**
  把「绑定到不到核」与「绑上了但内容是 0」分开 —— 于是第二个缺陷一下就暴露了。
  两个缺陷都是真的、都必须修，只是只有一个能解释那个 0。

- **`Shader.SetGlobalTexture` 写的是立即态全局属性表，它不进命令流；
  而一次 compute dispatch 的纹理绑定是在命令流里解析的。**（#22b，缺陷一）
  正确的那句话不是「compute 读不到全局纹理」，而是
  **「全局纹理必须被写进命令流」**。
  - **一个看起来现成的反例**：URP 的 `_MainLightShadowmapTexture` 确实能被 Vista 的
    注入核读到（判据⑤a/⑤b 量到 1312 个被遮挡点）。拿它去论证
    `Shader.SetGlobalTexture` 也行**是错的** —— 那条能工作的原因是 RenderGraph 的
    `SetGlobalTextureAfterPass` 会在**每一趟 pass 之前**用 `cmd.SetGlobalTexture`
    把它**重新下发进命令流**（`MainLightShadowCasterPass.cs:413`）。
    「一个『引擎的某张全局纹理能被我的 compute 读到』的先例，其成立原因
    可能是一条你没看的机制。」教训：先去 grep 引擎里那个能工作的先例走的哪条 API。
  - `ComputeCommandBuffer.SetGlobalTexture` 的四个重载**只吃 `TextureHandle`**，
    `SetComputeTextureParam` 的六个重载也一样 —— 没有任何入口接受裸 `Texture`。
    唯一的路是 `RenderGraph.ImportTexture(RTHandle)`，所以要先把 `Texture2D` 包成
    RTHandle（`RenderGraphResourceRegistry.ImportTexture` :462-465 显式允许
    「包着一张不能往里渲的普通 2D 纹理」的 RTHandle）。
  - **一个 null 的 RTHandle 绑上去不会炸，那更糟。** `RTHandle` 的隐式转换
    （`RTHandle.cs:148-150`）是 null 安全的，但给出 `default(RenderTargetIdentifier)`
    = **CameraTarget** —— 一次**静默绑错**。所以绑定必须由
    `IVistaLutDispatcher.HasTexture(slot)` 守住，而不是靠「null 应该没事」。
    也**不能**拿 `VistaBlueNoise.available` 去守 graph 侧的绑定：那是两份真相
    （资产在、但这一趟 pass 忘了 `UseTexture`），得问 dispatcher 本趟解析得到没有。
  - 包装用 `RTHandles.Alloc(Texture)`，它**不设** `m_RTHasOwnership`
    （`RTHandleSystem.Alloc(Texture)` :1473-1483），所以 `Release()`
    （`RTHandle.cs:237-246` 只在持有时销毁）**不会**销毁 URP 那张资产 ——
    这条理由必须写在 `Invalidate` 的注释里，否则下一个读者不敢还它。

- **一个引擎资产的「数据在哪个通道」是导入设置决定的，不是文件名决定的。**（#22b，真凶）
  `Textures/BlueNoise64/L/LDR_LLL1_0.png` 的名字说「LLL1」= R=G=B=L、A=1，
  那说的是**源 PNG**。而它的 `.meta` 里
  `textureType: 10`（SingleChannel）+ `singleChannelComponent: 0`（Alpha）
  + `alphaUsage: 2`（FromGrayScale）⇒ 导入成 `TextureFormat.Alpha8`
  （D3D11 侧 `DXGI_FORMAT_A8_UNORM`），**数据只在 `.a`**。
  在 D3D11 上读 `.r` 逐像素得 **0** —— 与「压根没绑上」在报表上**完全同形**。
  铁证是 URP 自己的消费点：`ShaderLibrary/LODCrossFade.hlsl:19` 拿**同一张资产**
  读的就是 `.a`（资产在 `UniversalRenderPipelineRuntimeTextures.cs:55` 声明，
  在 `UniversalRenderPipeline.cs:2112` 下发）。
  一个字符的修法，找它花的时间是那个字符的四位数倍。
  教训：**去引擎的 `.meta` 里读导入设置，并去 grep 引擎自己的消费点确认通道约定** ——
  两步都不能省，`.meta` 说的是格式，消费点说的是约定。

- **本项目一条旧的注记要缩小适用范围：「绑一个 null 纹理时 shader 读到引擎默认白图
  （全 1）」只对 material shader 成立。**（#22b）
  **compute 里未绑定的 SRV 在 D3D11 上读到全 0，`GetDimensions` 也返回 `0×0`。**
  这条差别正是上一条那个「完全同形」的根源：错通道读到 0、未绑定也读到 0。
  如果当时按旧注记预期「未绑定 ⇒ 全 1」，第一轮就会把读到的 0 归因成
  「绑上了、内容是黑的」，方向反了。

- **当「没绑上」与「绑上了但内容是 0」在报表上同形时，唯一的出路是向被测资源
  自己问尺寸。**（#22b，判据⑰d）
  核里 `_VistaFroxelBlueNoise.GetDimensions(w, h)` 走 `InterlockedMax` 写两个槽位：
  `0×0` = 绑定没到达核，`64×64` = 到达了 ⇒ 剩下的只能是内容/通道。
  这一格与判据⑥a（`_MainLightShadowmapSize` 不可信 ⇒ 向 atlas 自己问尺寸）
  是**同一条**理由的第二次应用：「一个从引擎全局读来的尺寸／范围，
  宁可向被测资源自己问」。
  两点配套：它做成**双向**的（资产取不到时派发处刻意不绑 ⇒ 期望 `0×0`），
  以及它**依赖另一格**才能给出正确因果（`InterlockedMax` 初值 0 让
  「没绑上」与「核压根没跑」读数相同，两者由 `BN_COUNT` 分开）——
  这条依赖要在报表里**点名**。

- **一条陈旧的归因文字，在新判据落地之后会变成错的。**（#22b）
  判据⑰b 的 `⚠` 原本写着「全落进第 7 桶 = 绑定失败」。⑰d 落地之后
  「绑定到达性」有了独立判据，⑰b 剩下的只有内容/通道 ——
  而且方向还反了：**读错通道（`.r`）落进第 0 桶**，落第 7 桶反而是
  「URP 哪天改回 Red 导入 ⇒ `.a` 恒为 1」。
  归因文字与判据分工必须同步改，否则报表会把人指向一个已经被别的格子排除掉的方向。

- **一个报表里的魔数等于没有印。**（#22b）
  给⑰d 加格式 ⓘ 时写的是 `graphicsFormat.ToString()`，印出来是一个裸的 **`54`**，
  而旁边的散文断言它是 `A8_UNORM`。反射查过：
  `UnityEngine.Experimental.Rendering.GraphicsFormat` **没有任何 alpha-only 成员**
  （`A8_UNORM` 压根不在枚举里），所以那个值没有名字。
  有名字的尺子是 `UnityEngine.TextureFormat.Alpha8`（常量值 1）。
  改成印 `Texture.format`，并在旁边写清「为什么这个尺子而不是那个」。
  这一行**只印不判**：换平台时 Unity 可能给 SingleChannel 选别的等价格式，
  把它摆成门会造出一个纯粹因为平台不同而红的假失败
  （「一条只在这台机器成立的实测行为要做成 ⓘ 而不是门」）。
  「通道选对了」真正由⑰b/⑰c 判 —— 读错通道时那张图恒 0 或恒 1。

- **`frac(v + φ)` 不是平移，所以精确算术的门不能摆在动画后的场上。**（#22b）
  源图的灰阶是 `k/255`，含 0 与 1 **两端**；绕回把这两个灰阶**撞成同一个值**
  ⇒ 多重集合变了 ⇒ 均值偏离精确的 0.5 到 `O(1/256)`、直方图不再每桶精确 512。
  所以⑰（秩均匀 512×8、均值精确 0.5）只量**源图 v 本身**，
  而⑱⑲的矩只印成 ⓘ、**永不设门**。
  「把精确算术的门只摆在它精确成立的那个量上」——
  摆错的后果不是漏检，是一个每次改相位就换一次颜色的假失败。

- **动画后场的一阶相关 ρ₁ 是相位相关的，把它的幅值写成门等于把一个自己没有上界的量
  当成常数。**（#22b，判据⑱只判符号）
  展开里 `k=l=1` 那一项带 `Re(e^{4πiφ}·E[e^{2πi(v+v')}])`，随 φ 摆动且没有紧的界；
  而 φ 无关的那一项 `−E[cos 2π(v−v')]/(2π²)` 在蓝噪声下**是负的**。
  所以门只判**符号**。两轮不同 φ 的实测印证了这个设计：
  φ = `(0.4773, 0.7070, 0.3832)` ⇒ 蓝噪声 ρ = `(−0.1195, −0.1511)`；
  φ = `(0.3726, 0.0394, 0.9473)` ⇒ `(−0.1251, −0.1143)` —— **幅度动、符号不动**。
  程序化档是**真正的空对照**：hash 之间独立，而独立性被任何逐点映射（含 `frac`）
  保留 ⇒ 它的 ρ ≡ 0 与相位无关，门按抽样标准差 `1/√4096` 的 3σ 摆。

- **「逐片独立的横向抖动会被 CLT 抹成白噪声」这条对程序化 hash 成立，对蓝噪声是错的。**
  （#22b，一条我自己写进注释的错论证）
  固定的逐片瓦片步进 `s = (17, 17)` 让聚合滤波器精确等于
  `K(f) = δ[f_x + f_y ≡ 0 (mod 64)]`，剩下的基函数只有 `e^{2πi f_x(x−y)/64}`
  ⇒ `Ā(x, y) = h((x − y) mod 64)`，也就是**沿反对角的对角条纹**，不是白噪声。
  **不用 Fourier 的初等版本更好核对**：`17·49 = 833 = 13·64 + 1` ⇒ 17 在 mod 64 下
  可逆 ⇒ 一列的 64 个抽样点正好是过 `(x, y)` 的**整条反对角线** `{(x+k, y+k)}`。
  这条对**任意**逐点函数成立，所以 `frac` 那次绕回救不了它。
  实测：逐片 DDIAG `3.300e-8` 对 DAXIS `1.607e-1`，差 **×4.9e6**。
  教训：「在把一条 Fourier 论证写进注释时，再补一句不用 Fourier 的初等版本」——
  那句初等版本是这条错论证唯一能被自己发现的入口。

- **一个恒为常数的聚合场会让对角恒等式假通过 —— 必须同时量轴向差作对照。**（#22b）
  这不是设想：修好通道**之前**⑳c 读到的就是全 0 常数场，
  `Ā(p+(1,1)) − Ā(p) ≡ 0` 完美满足，是同时量的
  `max|Ā(p+(1,0)) − Ā(p)|` 把它判红的。
  「给恒等式判据配一个『同一个场在另一个 lag 上必须不等』的对照格」。

- **用归一化的一阶相关 ρ₁ 去判「聚合场变白了」是错的。**（#22b）
  N 个解相关场平均之后分子分母**同比缩小**，ρ₁ 不变 —— 它判不出「幅度塌了」。
  而拿定点矩（scale `1e5` ⇒ 地板 `1e-5`）去算 ρ 时，地板对上
  `Var(Ā_逐片) ≈ 1e-3`，与门只差个位数倍：
  「尺子的地板与被测量的期望值同量级时，尺子会自己伪造一个结论」。
  改成量**逐点最大差**之后地板降到 fp32 求和序误差量级（实测 `3.3e-8`）。

- **同一个多重集合、旋转求和次序，不等于逐位相同。**（#22b）
  ⑳c 的 ⓘ 原先写着两个和「连量化误差都逐位相同」—— 被**它自己的读数**证伪：
  fp32 加法不满足结合律，旋转起点（`17⁻¹ ≡ 49` 片）会留下一点非零残差，
  实测 `3.3e-8`。那个 `3.8e-6` 的保守界因此不紧（实测在它下面 ×115）。
  两条修法：把过强的论证改成「同一多重集合 / 旋转起点 / 非零 fp32 残差」，
  以及**把报表里的比值算出来而不是手抄**（`k_AggDiagFloor / slcDDiag`
  而不是硬编码「×115」）—— 手抄的那个数在下次改地板时不会跟着改。

- **一格「无区分力」的判据会在源已经死掉的时候印 ✔。**（#22b，判据⑳a）
  ⑳a 抓得到 latZStride / 编码 / 槽位下标的错，**抓不到「源接错了」**——
  蓝噪声与程序化 hash 的**边缘分布几乎相同**，任何只看均值/方差的对账都抓不到它。
  这不是设想：通道错着的那一轮它印的是 ✔，两组均值都是 `0.915030`、
  方差都是 `0.000000`（**两个都是 0** 正是它自己那条「区分力边界」ⓘ 预言的形态）。
  源接错那一半由⑱的符号门覆盖，两格合起来才完整 ——
  「一个『必要不充分』的判据要在报表上自己说出来，并配一个能补上那一半的第二格」。

- **双向判据若在 slice 0 上做，两个 z 步进档逐位相同 ⇒「恰好有一个为 0」会以
  两个都为 0 的方式假通过。**（#22b）
  slice 0 的 `z·stride mod 64` 对任何 stride 都是 0。旋钮检查必须挪到 slice 1。
  同一族的另一个坑：四个「恰好有一个为 0」的槽位若走 `InterlockedMin`，
  初值 0 会让**四个都读 0** —— 必须走 `InterlockedMax`。
  两条合起来是同一句话：「一个读数正好等于清空值的判据，全绿的理由是错的」。

- **一个用「把 hash 的 z 输入挪 977 片」去造第二次抽样的判据，在逐列一致档下
  `zLat` 恒为 0 ⇒ 读数只剩深度那一项，是一个偏低的下界。**（#22b）
  它仍然有用（下界超门就是真超门），但**必须在报表上标成下界**，
  否则「抖动散布 `1.377e-1` vs 死区起点 `0.2500`」这个裕度会被读成一个已经量准的量。

- **一个走 64 次 fp32 累加再除 N 的量，与走一次求值的同一个量对账时，
  误差传到方差上带 `×2×(E[v²]/Var)` 的杠杆。**（#22b）
  本例 `E[v²]/Var ≈ 3.98` ⇒ 杠杆 ×7.96。门按「一次求值的误差」摆会正好压在地板上。
  「把『64 次累加 vs 一次求值』的对账门按传到方差上的杠杆重新推」。

- **cbuffer 里的「档位位」与「这一趟到底绑上了没有」必须在同一处强制一致。**（#22b）
  两者各自独立时，一趟忘了 `UseTexture` 的 pass 会走进蓝噪声分支、读到 0，
  症状是抖动退化成一个**逐帧常数的相位偏移** —— 画面上像「抖动开了但不起作用」，
  查不出来。修法：`VistaFroxelReprojection.Data.Bind` 里
  `!HasTexture(BlueNoise)` 时把 `effJitter.z` 清零 ——
  「把档位位与能不能真的绑上在同一处（`Data.Bind`）强制一致」。

- **一个「我是第一趟派发所以继承线上 uniform」的顺序性论证，在前一趟已经覆写过
  同一组 cbuffer 时是假的。**（#22b）
  探针 pass 原本靠顺序去继承注入核吃进去的那份 `Data`。
  改成**显式重绑**「注入核实际吃进去的那一个 `Data` 实例」（`online.Bind(d)`），
  并且这次绑定**无条件调** —— 「一个只服务判据的资产绑定要无条件调」，
  条件化会让它在某些帧静默变成另一份输入。
  「参考解要用被测核实际吃进去的那份输入」，顺序不是输入。

- **一个通道选错的缺陷被修好之后，一条纸上的推导会突然变成一个美术可以选中、
  且选中就出结构性瑕疵的组合。**（#22b，`(BlueNoise, PerSlice)` 组合禁忌）
  蓝噪声这一路第一次真的活了，于是「固定步进 ⇒ 对角条纹」从预言变成**读数**
  （DDIAG `4.0e-8` 对 DAXIS `1.067e-1`）。而 `settings` 允许把这两个枚举同时选上 ——
  这是「一个默认关闭、又没有判据覆盖的开关」的近亲，**更坏**：它允许被选中。
  画面症状是切片台阶不再是台阶、而是一组 45° 斜纹 ——
  **比台阶更难被当成「雾本来就这样」**。
  修法是把推导做成一条**会在踩中时变 `⚠` 的报表行**，
  把它从注释里的警告变成一条可执行的配置约束。
  程序化 + `PerSlice` 不受影响（hash 之间独立 ⇒ CLT 那条论证在**那一档**上是对的）。
  随机瓦片偏移（能把 `K(f)` 压到 `O(1/√N)`、让两档兼容）留给 #27，
  现在它有了一个**实测的**动机。

### 待办（Task #6 验收时补）

- ~~**#19 留下两条自己点名的未覆盖路径。**~~ **两条都已结清。** 注入历史 RT 的写入路径
  ~~要等 #22（时间重投影的双缓冲 swap）~~、~~深度积分 RT 的写入路径要等 #21~~。
  #19 时自检只验证这两张 RT **被正确分配**（Tex3D / 尺寸 / RGBA16F / UAV / 三个句柄互不相同），
  内容一律为零，而**零内容与"从来没被写过"在报表上长得一样** ——
  所以那两格在自检输出里有一条显式的 ⓘ 点名，不靠读者自己发现。
  - **积分表这一半在 #21 结清**：写入路径由状态日志的判据⑩⑪⑫覆盖
    （⑪c 直接判「这张表被写过」，靠的是 `1 − T` 存储约定让清空态可判），
    画面侧由 `Hidden/Vista/FroxelDebug` 的四个档位覆盖。
  - **历史表这一半在 #22a 结清，而结清的方式说明当初那句话本身就是错的**：
    历史表**没有**「写入路径」这回事 —— 双缓冲的交换（`SwapInjectionBuffers`）
    只改写下标，本帧写的永远是 `FroxelInjection` 指向的那张，下一帧交换后它就成了历史。
    所以要覆盖的从来不是「写」，而是「**读到的是不是上一帧那张**」，
    盯它的是判据⑬（静止恒等性）、⑭b（在线掩码与 CPU 状态双向一致）、
    ⑮（六条失效路径 + 计数守恒）。那条陈旧 ⓘ 已经改掉，
    并作为一条坑记在上面（自我否证的文档比没有文档贵）。
- **AP 的积分起点还没有接上 `desc.handoffMeters`。**（#19 原本写的是「#20 的活」，
  **这句是错的**，见下。）在接上之前，近层体与 AP LUT 都从 t = 0 开始积分，
  两层同开会把近段的雾算两遍；这也是 `Mode.Froxel` 现在不存在的第二个理由。
  - **只把 `nearDistanceKm` 填成 `handoffMeters` 是不够的。** `AerialPerspectiveLut`
    在切片循环前把 `tPrev` 初始化成 `0.0`（`AtmosphereLut.compute:375`），
    所以第 0 片的行进区间是 `[0, near]` —— 把 near 推远只会让第 0 片**变长**，
    近段的雾照样被算进去，双计一点没少。要真去掉双计，`tPrev` 的初值必须一起移到
    handoff 上，而那会改变 AP LUT 的语义（从「相机到 t 的累积」变成
    「handoff 到 t 的累积」），读端的合成也要跟着改。
  - 所以这件事的归属是 **#21 / #25**，不是 #20：#21 拥有近层积分表的语义定义，
    #25 拥有统一采样函数（近层与 AP 在哪个距离上交接、谁负责哪一段）。
    **#20 因此刻意一个字都不碰 AP。**
  - 顺带：Task #7 的 AP 档位扫描是在**无雾**且 `near = 20 m` 下做的，
    handoff 接上之后 `near` 会变成 48~500 m 量级，Log vs Power 的结论可能翻转 ——
    那条重扫已经在 Step 3 的待办里点名了。

- ~~LUT 七个 pass 的实测耗时（目标合计 < 0.3 ms）~~ —— **已完成**，数据见 Added 的
  计时器一节。当时对反射的估算是"0.4M 次取样 ≈ 0.03 ms"，实测第一版是 0.391 ms，
  差一个数量级：估算按取样率算，忽略了粗糙那几级每组只有一条线程、延迟完全裸露。
  拷贝那一条的担心（多一个 pass 边界的 barrier）没有成立，实测 0.030 ms。
- **模型 A 交叉验证：Play 模式 `ProfilerRecorder`（`ProfilerCategory.Render` +
  `GpuRecorder`）对七个 pass 名逐个取数** —— **已完成**，见 Added 的
  `VistaLutGpuRecorderCrossCheck` 一节。结论与预期一致（A/B = 1.21），
  并且意外地把"反射单 pass 不可引用"这条限制**解除**了。
- 合并 mip3~6 的 dispatch（per-dispatch 地板已占反射 pass 的 59%）。
  已定位、已估价（约 0.03 ms）、主动搁置，理由见「取舍」。
- SH 模式（移动端路径）的反射耗时**完全没测**。计时器目前只跑 LUT 模式，
  形状分界里 `!useSh` 那一半只有占用率论证、没有实测支撑。
- 太阳 0°→90° 扫描的 banding 数值判定 —— **已完成**，见 Added 的
  `VistaSkyBandingSelfTest` 一节。当时的截图判断（"是量化而非分辨率不足"）
  被数值证实了，但理由和当时想的不一样：分辨率那一项（判据 B，1.885e-3）本身也
  只在地板的 1.8 倍上，两者是同一个量级，说"是量化不是分辨率"其实分不开 ——
  真正的结论是**两项都已经小到看不见**（各差可见阈 5 倍以上）。
- ~~AP 的分辨率与切片分布定档~~ —— **已完成**（Task #7），依据见 Added 与「取舍」。
  当时以为"32³ 够不够、Power 还是 Log"必须等 Step 1 的合成才能判，这一点只对了一半：
  **合格与否**不用等 —— 把"看得见吗"换成 Weber 1% 可见阈、分母取日照下 albedo 0.3
  漫反射面（`0.3·120000/π ≈ 1.1e4 cd/m²`）之后，"远山有没有可见雾量台阶"
  就是一个可以现在算的数（分段 errMid / errMidT），不需要眼睛。
  真正等不了的是**该选谁**：判据只能判合格，20 组全过，所以选择落到了实测毫秒 +
  显存上。仍然挂着的两件事：
  - Log 优于 Power 的理由是"雾的密度梯度落在近场"，这条是前瞻性的，**未实测**。
    Step 3 接上后要用带雾场景重跑这套扫描，结论可以被推翻。
  - 移动档（d=16 Power k=3）的精度只在**晴空**下核过；它配的是解析高度雾那条实现，
    等那条落地后要单独核一遍。
- ~~**逐像素大气透射率**（Task #12）~~ —— **已完成**，见 Added 与「取舍」「坑」三节。
  三条约束都落地了（复用同一张 Transmittance LUT / `T_ref` 从 CPU 上传因此在代数上约掉 /
  与变体 B 同一个 pass）。七条判据的实测数据在 Added 里。仍然挂着的三件事：
  - **F2 那张包线表还没在开启后重测。** 原计划的验收方式是「包线应当整体撑开到搜索上界」，
    实际先做成了一套七判据的夹具（它能量到 F2 表量不到的东西：CPU 闭式对账、
    钳位覆盖、单调性）。但 F2 表是**面向画面**的那一半 ——
    它回答「现在一个单值参考海拔能覆盖多大高差」，重测才能给出「这一项买到了多少」。
    要等 Step 3 的雾接上再一起跑，否则得跑两遍。
  - **`MixRealtimeAndBakedGI` 的 subtractive 半边仍未覆盖**，而这一项动的正是它前后的位置。
    现在的夹具**结构上**覆盖不了（布景是运行时搭的，墙没有 lightmap UV / index）——
    要补一个带烘焙数据的场景，这条在累加自检的未覆盖清单里已经挂了两轮。
  - **`VistaEarthShadow` 分支未覆盖**：三个太阳配置的 `muSun` 跨度都远离 0
    （最低那档 [0.1042, 0.1046]），那条分支一次都没进。它挡的是 LUT 的垃圾值而不是
    物理遮挡，要覆盖得把太阳压到参考高度地平线附近 —— 而那一带 `Light.color` 已经是 0，
    与上面那条「固有局限」是同一片区域，需要单独设计判据。
- ~~**`VistaFogTransmittanceToSun`（雾对阳光的自遮蔽）默认关，#27 必须有一条判据点名跑过它。**~~
  —— **已完成（#18b）**。当时列的三件事现在都有了判据，但落点跟当时想的不一样：
  ① 开关生效 → 判据5 在档 B（关）与档 G（开）上各判一次，两档只差这一个 bool，
  而档 G 的读数在修正前是 34.82%、修正后 1.99%，分辨力远超地板；
  ② 闭式解对得上 → **不需要单独判**，因为它与天空视线用的是同一个
  `VistaFogOpticalDepth`，判据4 拿高步数数值积分核的就是那个函数体；
  ③ 掠射钳位真的在钳 → 判据7 逐方向计数，`自遮蔽开 192 次 / 掠射上限 64 次`，
  见证值由 kernel 调 `VistaFogSunPathKm(0.0)` 自己造（不在 CPU 侧重写那条 min）。
  所以「跑不出就删掉这个函数」这条作废 —— 它现在是有判据的代码。
- ~~**天空像素现在拿不到雾（#18b）。**~~ —— **已完成（#18b）**，见 Added 与「取舍」。
  当时写的修法（把 `VistaFogSourceRadiance` 从 `VistaEvaluateScatterSample` 里提出来，
  天空路径套 `L_sky·T_fog + albedo·S̄·(1 − T_fog)`）方向是对的，
  但漏了一项：`S̄` 里的自遮蔽沿射线是变的，冻在相机处会偏暗 34.8%。
  真正的解是那个入散射加权均值恒等式。
- **两条声明为「实测基线」的门必须在 #27 兑现成画面差。** 布景③ 的 20%（掠射/曲率区间）
  与均匀雾档的 8%，现在报表上印的都是「未判达标」——
  它们的画质结论要落到 #27 的「档 A vs 档 D 逐像素差」上，而不是留在这两个数字上。
- **判据5 的「尺子地板过高就排除该方向」这道守卫从未执行过**（15 格里 0 次触发）。
  它的代码路径**未被验证**。要么造一个尺子确实不足的布景（把参考步数压低、
  或用一个极小标高把 e 折推到步长以下）让它必须触发一次，要么删掉它 ——
  与上面那条「默认关闭又没判据的开关」同一族。
- ~~**`capacityCeilingS/T` 两个字段在 #19 落地后必须删掉。**~~ —— **该待办撤销（#19 收尾时）：
  前提是错的。** 原话是"存在的前提是 AP 表装不下 877 m 的雾拐点，而 #19 的近层 froxel 体
  正是来解决这个的"。实际上近层体覆盖 **[0, 61.374 m]**（默认档接手点），
  877 m 的拐点比它远 **14 倍** —— 拐点在任何档位下都落在 AP LUT 的区间里，接不走。
  且档 D（`Mode.AerialPerspective`，移动端主线）**按定义**没有近层体，
  所以只要档 D 还发布，这两个基线就是它表达能力上限的常驻陈述。
  当时把「近层体接走近段的雾」读成了「近层体接走视角③ 的雾拐点」。
  档 D 的雾画质门仍在 #27 的「档 A vs 档 D 逐像素差」——**那一条没变**。
- **Power k=3 的 errCenter 随深度**增长**，是唯一一条朝 5% 门走的分布。**
  实测 `d=32 → 0.44%`、`d=48 → 1.25%`、`d=64 → 3.58%`（都还在门内）。
  其余分布都是"越深越准"。这与直觉相反（切片越多、行进步数越多），
  说明 k=3 在远端把切片挤得太开、`clamp(ceil(segLen/stepMax), 2, 64)` 的 2 步下限
  在那些片上生效了 —— 但**这只是假说，没测**。
  移动档候选里有 `d=16 Power k=3`，所以这条要么给它一条判据，要么换候选。
- **SkyView LUT 没有高步数参考解判据。** 于是 `VISTA_SAMPLE_SEGMENT_T`
  从 0.3 改到 0.5 对 SkyView 的影响**完全没有判据覆盖**（AP 那边有 4096 步参考解，
  SkyView 那边只有绝对亮度的几个抽点与单调性）。这个共享积分器的改动波及三张表，
  却只有一张表能量到 —— 要补一条同形的高步数参考判据。
- **`dt` 定义回退一次，把那 0.25%/0.29% 的归因分开**（#27）。见「坑」里那一条。
- **级数分支阈值换成最优值 `x* = (24·2⁻²³)^¼ = 4.113e-2`**（#25）。
  现在跑的是 `1e-4`，六档实测代价 `C 档最差 5.598e-4`。
  推迟的理由是这个常数由 AP 与 froxel **共用**，换它要把 AP 的整批误差判据一起重跑。
  #21 顺带量出了影响面：真实帧的 `x ∈ [6.539e-5, 1.144e-2]`，
  `x_max < x*` ⇒ 换完之后本布景**每一段**都会转到级数支。
  **这不是边角优化，它会整体改写这条积分路径** —— AP 的重跑不是"以防万一"，是必需项。
- **`VistaSkyBandingSelfTest.k_Fp16SecondDiffFloor = 1.95e-3f`（4·2⁻¹¹）
  还没按 fp16 截断这条结论复核过。** 那条路径是 render target 写入而不是 compute UAV 写入，
  可能不受影响 —— 但「可能」不是结论。若它也在截断，这个地板要翻倍到 3.9e-3。
- **`k_ReadbackFloor` 与截断结论的归因还没分开。** 那个常数的实测值恰好与整个 eps
  同量级，所以「它是读回路径自己的地板」与「它就是截断地板」这两种解释目前无法区分。
  分法是造一个请求值 `frac(k)` 明确大于 0.5 的样本，看读回值落在哪一侧。
- **#21 的判据⑪d 需要一次跨布景对照才完整**（#27）。
  它现在判「实测 `max(1 − T) ≤ 1 − e^(−σ_t(地面)·d)`」，实测比值 `9.088e-1`。
  但**相机远高于雾层时比值仍然 ≈ 1 才是可疑的**（说明高度衰减没生效），
  而这一格在贴地布景下无法区分这两种情况 —— 要一个高空相机的对照布景让它掉下来。
- **近层 froxel 体的切片数够不够，#21 没测**（→ #27）。
  现在的 64 片在 `far 64 m / ρ 1.087` 下最长段 4.933 m，`x_max 1.144e-2`
  ——「一阶误差足够小」这件事有推导、没有变步数的实测对照。
  做法与 AP 的档位扫描同形：把切片数减半/加倍，量最终画面的逐像素差。

- **判据⑯ 收敛性从 #22a 推迟到 #27**：「累积真的降了噪、且没有引入偏差」。  三条理由（在动手之前就写进了自检报表，不是事后补的）：
  ① #27 本来就持有残影/收敛这一项，且带一个**跨布景对照** —— 本节没有对照，
  而「一个跨布景稳定复现的差值，只有在两个布景确实做同样工作时才是尺子噪声」；
  ② 今天唯一便宜的形式是 max-vs-max 的统计门，**摆不紧**，
  而「一个把『未判达标』印成『达标』的判据，比一条平门更危险」；
  ③ **无偏这一半已经有解析恒等式兜着** —— e 空间里振幅 1 的深度抖动，
  其期望**正好**等于不抖时的采样点（几何均值 = e 空间中点），
  而它要打败的噪声由判据⑮b 那个 `JITTER_SPREAD`（实测 `1.412e-1`）量出来了。
  - **代价点名**（推迟的真实成本，不是「反正不重要」）：现在实现它要么加**第九个核**，
    要么让重投影探针那一趟去读注入表 —— 而后者会推翻那一趟
    「刻意不声明 `froxelInjection`」的理由。
  - **#27 要盯的失效症状**：「打开抖动之后雾稍微浓了一点」——
    一个看起来像调好了的系统性偏差，也就是最容易被接受、
    因此最容易掩盖污染的那种形态。

- **#22b：蓝噪声抖动资产 + A/B 对照。** #22a 只交付了程序化序列
  （R3 Kronecker + PCG3D），`JitterMode` 里刻意**还没有** `BlueNoiseTexture` 这一档 ——
  「一个选了什么都不发生的枚举值等于把哑档位摆给美术」。#22b 要补：
  - **128×128 的 2D void-and-cluster 蓝噪声 + 金门时间轴**，不是 3D 蓝噪声纹理。
    金比例在 1D 时间轴上是**可证最优**的，而给每个像素加**同一个** `t·φ` 增量是
    平移不变的 ⇒ **每一片仍然是蓝噪声**。所以「2D void-and-cluster + 金比例时间」
    在**两条边际轴上都最优**；而一张 3D 纹理的 z = 64 还意味着抖动序列
    **每 64 帧循环一次**，2D + 金比例**永不循环**，资产还从 1 MB 降到 16 KB。
  - 生成器（Editor 工具）+ **频谱/差异度判据**（低频能量占比、星形差异度），
    否则「这张图到底是不是蓝噪声」只能靠肉眼看。
  - **与程序化档的 A/B 对照**：同一布景、同一累积长度下的收敛速度，
    用一个「A 档受某旋钮影响、B 档不受影响」的对照区分
    「模式真的换了」与「资产是陈旧的」——
    因为「一个不重新导入资产就换档的验证流程，会让两个档位的画面看起来一模一样」。
  - **【括注：这条已结清，但落地方式与上面这份计划不同。】** 见本文
    「抖动源：复用 URP 自带蓝噪声 + 两档运行时对照（#22b）」那一条。三处差异：
    1. **没有 128×128 自烘资产，没有生成器，也没有频谱判据。** 改成复用 URP 自己那张
       `blueNoise64LTex`（64×64）—— Vista 至今零二进制资产因此被保住，
       代价是瓦片小一半、只有一个通道。理由（用户选的那一档）写在
       `VistaBlueNoise` 的类头注里：froxel 分辨率是屏幕/8，用不到更大的瓦片；
       而一个生成器要能写回 package 目录，git URL 引用下那目录是只读的。
       「这张图到底是不是蓝噪声」由**精确算术**的判据⑰b/⑰c 覆盖
       （秩均匀 ⇒ 每桶精确 512、均值精确 0.5），比频谱判据更紧，也不需要经验常数。
    2. **「哑档位」的顾虑已消除**：`JitterMode.BlueNoiseTexture` 现在真的存在，
       且被判据⑱⑲⑳㉑ 四格覆盖（含一格双向的档位接线判据）。
    3. **上面那句「给每个像素加同一个 `t·φ` 增量是平移不变的 ⇒ 每一片仍然是蓝噪声」
       是错的**，留在这里不改，因为它是当时的原话。`frac(v + φ)` **不是**平移 ——
       它把 `v = 0` 与 `v = 1` 两个灰阶撞成同一个值，多重集合变了、谱也变了。
       后果不是「不能用」（符号性质仍然保住，⑱ 两轮不同 φ 的实测印证了），
       而是**精确算术的门不能摆在动画后的场上** —— 这条现在写进了 ⑰ 与 ⑱ 的分工。
    金比例时间轴本身仍然在用（#22a 的 R3 Kronecker 相位），只是它的正当性
    换成了「{frac(k·α)} 在 [0,1)³ 上稠密」，不再是那句错的平移不变性。

## [0.1.0] - 2026-08-14

初始化。目标平台 PC（D3D12 / Vulkan），Unity 6000.4 + URP 17.4 RenderGraph。
